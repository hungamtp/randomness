const number = ['○', '○', '○', '○'];const pointer = "○";
const color = [ 'red', 'blue', 'brown' , 'black', 'black']; const location1 = new Map();
const text0 = document.getElementById('text0');
text0.innerHTML = number[0];
function textF0() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text0.setAttribute('fill', newColor);
  const id = text0.x['animVal'][0].value+""+text0.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text0.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text0.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text0.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text0.setAttribute('x', valueXIncrease);
      text0.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text0.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text0.setAttribute('x', valueXIncrease);
      text0.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text0.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text0.setAttribute('y', valueXIncrease);
      text0.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text0.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text0.setAttribute('y', valueXIncrease);
      text0.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF0, 0.001);
const text1 = document.getElementById('text1');
text1.innerHTML = number[0];
function textF1() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text1.setAttribute('fill', newColor);
  const id = text1.x['animVal'][0].value+""+text1.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text1.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text1.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text1.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text1.setAttribute('x', valueXIncrease);
      text1.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text1.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text1.setAttribute('x', valueXIncrease);
      text1.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text1.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text1.setAttribute('y', valueXIncrease);
      text1.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text1.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text1.setAttribute('y', valueXIncrease);
      text1.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF1, 0.001);
const text2 = document.getElementById('text2');
text2.innerHTML = number[0];
function textF2() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text2.setAttribute('fill', newColor);
  const id = text2.x['animVal'][0].value+""+text2.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text2.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text2.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text2.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text2.setAttribute('x', valueXIncrease);
      text2.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text2.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text2.setAttribute('x', valueXIncrease);
      text2.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text2.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text2.setAttribute('y', valueXIncrease);
      text2.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text2.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text2.setAttribute('y', valueXIncrease);
      text2.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF2, 0.001);
const text3 = document.getElementById('text3');
text3.innerHTML = number[0];
function textF3() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text3.setAttribute('fill', newColor);
  const id = text3.x['animVal'][0].value+""+text3.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text3.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text3.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text3.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text3.setAttribute('x', valueXIncrease);
      text3.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text3.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text3.setAttribute('x', valueXIncrease);
      text3.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text3.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text3.setAttribute('y', valueXIncrease);
      text3.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text3.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text3.setAttribute('y', valueXIncrease);
      text3.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF3, 0.001);
const text4 = document.getElementById('text4');
text4.innerHTML = number[0];
function textF4() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text4.setAttribute('fill', newColor);
  const id = text4.x['animVal'][0].value+""+text4.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text4.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text4.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text4.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text4.setAttribute('x', valueXIncrease);
      text4.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text4.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text4.setAttribute('x', valueXIncrease);
      text4.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text4.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text4.setAttribute('y', valueXIncrease);
      text4.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text4.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text4.setAttribute('y', valueXIncrease);
      text4.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF4, 0.001);
const text5 = document.getElementById('text5');
text5.innerHTML = number[0];
function textF5() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text5.setAttribute('fill', newColor);
  const id = text5.x['animVal'][0].value+""+text5.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text5.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text5.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text5.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text5.setAttribute('x', valueXIncrease);
      text5.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text5.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text5.setAttribute('x', valueXIncrease);
      text5.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text5.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text5.setAttribute('y', valueXIncrease);
      text5.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text5.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text5.setAttribute('y', valueXIncrease);
      text5.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF5, 0.001);
const text6 = document.getElementById('text6');
text6.innerHTML = number[0];
function textF6() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text6.setAttribute('fill', newColor);
  const id = text6.x['animVal'][0].value+""+text6.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text6.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text6.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text6.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text6.setAttribute('x', valueXIncrease);
      text6.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text6.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text6.setAttribute('x', valueXIncrease);
      text6.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text6.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text6.setAttribute('y', valueXIncrease);
      text6.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text6.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text6.setAttribute('y', valueXIncrease);
      text6.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF6, 0.001);
const text7 = document.getElementById('text7');
text7.innerHTML = number[0];
function textF7() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text7.setAttribute('fill', newColor);
  const id = text7.x['animVal'][0].value+""+text7.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text7.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text7.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text7.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text7.setAttribute('x', valueXIncrease);
      text7.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text7.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text7.setAttribute('x', valueXIncrease);
      text7.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text7.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text7.setAttribute('y', valueXIncrease);
      text7.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text7.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text7.setAttribute('y', valueXIncrease);
      text7.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF7, 0.001);
const text8 = document.getElementById('text8');
text8.innerHTML = number[0];
function textF8() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text8.setAttribute('fill', newColor);
  const id = text8.x['animVal'][0].value+""+text8.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text8.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text8.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text8.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text8.setAttribute('x', valueXIncrease);
      text8.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text8.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text8.setAttribute('x', valueXIncrease);
      text8.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text8.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text8.setAttribute('y', valueXIncrease);
      text8.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text8.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text8.setAttribute('y', valueXIncrease);
      text8.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF8, 0.001);
const text9 = document.getElementById('text9');
text9.innerHTML = number[0];
function textF9() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text9.setAttribute('fill', newColor);
  const id = text9.x['animVal'][0].value+""+text9.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text9.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text9.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text9.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text9.setAttribute('x', valueXIncrease);
      text9.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text9.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text9.setAttribute('x', valueXIncrease);
      text9.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text9.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text9.setAttribute('y', valueXIncrease);
      text9.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text9.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text9.setAttribute('y', valueXIncrease);
      text9.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF9, 0.001);
const text10 = document.getElementById('text10');
text10.innerHTML = number[0];
function textF10() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text10.setAttribute('fill', newColor);
  const id = text10.x['animVal'][0].value+""+text10.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text10.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text10.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text10.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text10.setAttribute('x', valueXIncrease);
      text10.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text10.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text10.setAttribute('x', valueXIncrease);
      text10.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text10.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text10.setAttribute('y', valueXIncrease);
      text10.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text10.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text10.setAttribute('y', valueXIncrease);
      text10.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF10, 0.001);
const text11 = document.getElementById('text11');
text11.innerHTML = number[0];
function textF11() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text11.setAttribute('fill', newColor);
  const id = text11.x['animVal'][0].value+""+text11.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text11.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text11.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text11.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text11.setAttribute('x', valueXIncrease);
      text11.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text11.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text11.setAttribute('x', valueXIncrease);
      text11.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text11.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text11.setAttribute('y', valueXIncrease);
      text11.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text11.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text11.setAttribute('y', valueXIncrease);
      text11.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF11, 0.001);
const text12 = document.getElementById('text12');
text12.innerHTML = number[0];
function textF12() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text12.setAttribute('fill', newColor);
  const id = text12.x['animVal'][0].value+""+text12.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text12.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text12.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text12.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text12.setAttribute('x', valueXIncrease);
      text12.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text12.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text12.setAttribute('x', valueXIncrease);
      text12.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text12.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text12.setAttribute('y', valueXIncrease);
      text12.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text12.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text12.setAttribute('y', valueXIncrease);
      text12.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF12, 0.001);
const text13 = document.getElementById('text13');
text13.innerHTML = number[0];
function textF13() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text13.setAttribute('fill', newColor);
  const id = text13.x['animVal'][0].value+""+text13.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text13.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text13.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text13.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text13.setAttribute('x', valueXIncrease);
      text13.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text13.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text13.setAttribute('x', valueXIncrease);
      text13.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text13.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text13.setAttribute('y', valueXIncrease);
      text13.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text13.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text13.setAttribute('y', valueXIncrease);
      text13.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF13, 0.001);
const text14 = document.getElementById('text14');
text14.innerHTML = number[0];
function textF14() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text14.setAttribute('fill', newColor);
  const id = text14.x['animVal'][0].value+""+text14.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text14.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text14.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text14.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text14.setAttribute('x', valueXIncrease);
      text14.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text14.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text14.setAttribute('x', valueXIncrease);
      text14.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text14.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text14.setAttribute('y', valueXIncrease);
      text14.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text14.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text14.setAttribute('y', valueXIncrease);
      text14.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF14, 0.001);
const text15 = document.getElementById('text15');
text15.innerHTML = number[0];
function textF15() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text15.setAttribute('fill', newColor);
  const id = text15.x['animVal'][0].value+""+text15.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text15.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text15.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text15.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text15.setAttribute('x', valueXIncrease);
      text15.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text15.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text15.setAttribute('x', valueXIncrease);
      text15.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text15.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text15.setAttribute('y', valueXIncrease);
      text15.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text15.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text15.setAttribute('y', valueXIncrease);
      text15.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF15, 0.001);
const text16 = document.getElementById('text16');
text16.innerHTML = number[0];
function textF16() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text16.setAttribute('fill', newColor);
  const id = text16.x['animVal'][0].value+""+text16.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text16.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text16.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text16.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text16.setAttribute('x', valueXIncrease);
      text16.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text16.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text16.setAttribute('x', valueXIncrease);
      text16.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text16.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text16.setAttribute('y', valueXIncrease);
      text16.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text16.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text16.setAttribute('y', valueXIncrease);
      text16.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF16, 0.001);
const text17 = document.getElementById('text17');
text17.innerHTML = number[0];
function textF17() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text17.setAttribute('fill', newColor);
  const id = text17.x['animVal'][0].value+""+text17.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text17.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text17.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text17.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text17.setAttribute('x', valueXIncrease);
      text17.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text17.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text17.setAttribute('x', valueXIncrease);
      text17.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text17.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text17.setAttribute('y', valueXIncrease);
      text17.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text17.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text17.setAttribute('y', valueXIncrease);
      text17.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF17, 0.001);
const text18 = document.getElementById('text18');
text18.innerHTML = number[0];
function textF18() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text18.setAttribute('fill', newColor);
  const id = text18.x['animVal'][0].value+""+text18.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text18.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text18.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text18.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text18.setAttribute('x', valueXIncrease);
      text18.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text18.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text18.setAttribute('x', valueXIncrease);
      text18.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text18.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text18.setAttribute('y', valueXIncrease);
      text18.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text18.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text18.setAttribute('y', valueXIncrease);
      text18.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF18, 0.001);
const text19 = document.getElementById('text19');
text19.innerHTML = number[0];
function textF19() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text19.setAttribute('fill', newColor);
  const id = text19.x['animVal'][0].value+""+text19.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text19.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text19.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text19.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text19.setAttribute('x', valueXIncrease);
      text19.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text19.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text19.setAttribute('x', valueXIncrease);
      text19.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text19.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text19.setAttribute('y', valueXIncrease);
      text19.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text19.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text19.setAttribute('y', valueXIncrease);
      text19.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF19, 0.001);
const text20 = document.getElementById('text20');
text20.innerHTML = number[0];
function textF20() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text20.setAttribute('fill', newColor);
  const id = text20.x['animVal'][0].value+""+text20.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text20.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text20.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text20.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text20.setAttribute('x', valueXIncrease);
      text20.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text20.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text20.setAttribute('x', valueXIncrease);
      text20.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text20.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text20.setAttribute('y', valueXIncrease);
      text20.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text20.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text20.setAttribute('y', valueXIncrease);
      text20.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF20, 0.001);
const text21 = document.getElementById('text21');
text21.innerHTML = number[0];
function textF21() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text21.setAttribute('fill', newColor);
  const id = text21.x['animVal'][0].value+""+text21.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text21.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text21.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text21.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text21.setAttribute('x', valueXIncrease);
      text21.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text21.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text21.setAttribute('x', valueXIncrease);
      text21.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text21.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text21.setAttribute('y', valueXIncrease);
      text21.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text21.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text21.setAttribute('y', valueXIncrease);
      text21.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF21, 0.001);
const text22 = document.getElementById('text22');
text22.innerHTML = number[0];
function textF22() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text22.setAttribute('fill', newColor);
  const id = text22.x['animVal'][0].value+""+text22.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text22.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text22.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text22.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text22.setAttribute('x', valueXIncrease);
      text22.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text22.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text22.setAttribute('x', valueXIncrease);
      text22.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text22.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text22.setAttribute('y', valueXIncrease);
      text22.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text22.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text22.setAttribute('y', valueXIncrease);
      text22.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF22, 0.001);
const text23 = document.getElementById('text23');
text23.innerHTML = number[0];
function textF23() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text23.setAttribute('fill', newColor);
  const id = text23.x['animVal'][0].value+""+text23.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text23.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text23.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text23.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text23.setAttribute('x', valueXIncrease);
      text23.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text23.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text23.setAttribute('x', valueXIncrease);
      text23.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text23.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text23.setAttribute('y', valueXIncrease);
      text23.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text23.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text23.setAttribute('y', valueXIncrease);
      text23.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF23, 0.001);
const text24 = document.getElementById('text24');
text24.innerHTML = number[0];
function textF24() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text24.setAttribute('fill', newColor);
  const id = text24.x['animVal'][0].value+""+text24.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text24.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text24.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text24.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text24.setAttribute('x', valueXIncrease);
      text24.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text24.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text24.setAttribute('x', valueXIncrease);
      text24.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text24.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text24.setAttribute('y', valueXIncrease);
      text24.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text24.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text24.setAttribute('y', valueXIncrease);
      text24.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF24, 0.001);
const text25 = document.getElementById('text25');
text25.innerHTML = number[0];
function textF25() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text25.setAttribute('fill', newColor);
  const id = text25.x['animVal'][0].value+""+text25.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text25.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text25.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text25.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text25.setAttribute('x', valueXIncrease);
      text25.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text25.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text25.setAttribute('x', valueXIncrease);
      text25.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text25.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text25.setAttribute('y', valueXIncrease);
      text25.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text25.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text25.setAttribute('y', valueXIncrease);
      text25.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF25, 0.001);
const text26 = document.getElementById('text26');
text26.innerHTML = number[0];
function textF26() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text26.setAttribute('fill', newColor);
  const id = text26.x['animVal'][0].value+""+text26.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text26.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text26.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text26.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text26.setAttribute('x', valueXIncrease);
      text26.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text26.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text26.setAttribute('x', valueXIncrease);
      text26.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text26.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text26.setAttribute('y', valueXIncrease);
      text26.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text26.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text26.setAttribute('y', valueXIncrease);
      text26.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF26, 0.001);
const text27 = document.getElementById('text27');
text27.innerHTML = number[0];
function textF27() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text27.setAttribute('fill', newColor);
  const id = text27.x['animVal'][0].value+""+text27.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text27.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text27.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text27.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text27.setAttribute('x', valueXIncrease);
      text27.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text27.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text27.setAttribute('x', valueXIncrease);
      text27.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text27.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text27.setAttribute('y', valueXIncrease);
      text27.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text27.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text27.setAttribute('y', valueXIncrease);
      text27.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF27, 0.001);
const text28 = document.getElementById('text28');
text28.innerHTML = number[0];
function textF28() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text28.setAttribute('fill', newColor);
  const id = text28.x['animVal'][0].value+""+text28.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text28.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text28.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text28.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text28.setAttribute('x', valueXIncrease);
      text28.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text28.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text28.setAttribute('x', valueXIncrease);
      text28.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text28.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text28.setAttribute('y', valueXIncrease);
      text28.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text28.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text28.setAttribute('y', valueXIncrease);
      text28.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF28, 0.001);
const text29 = document.getElementById('text29');
text29.innerHTML = number[0];
function textF29() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text29.setAttribute('fill', newColor);
  const id = text29.x['animVal'][0].value+""+text29.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text29.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text29.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text29.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text29.setAttribute('x', valueXIncrease);
      text29.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text29.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text29.setAttribute('x', valueXIncrease);
      text29.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text29.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text29.setAttribute('y', valueXIncrease);
      text29.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text29.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text29.setAttribute('y', valueXIncrease);
      text29.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF29, 0.001);
const text30 = document.getElementById('text30');
text30.innerHTML = number[0];
function textF30() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text30.setAttribute('fill', newColor);
  const id = text30.x['animVal'][0].value+""+text30.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text30.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text30.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text30.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text30.setAttribute('x', valueXIncrease);
      text30.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text30.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text30.setAttribute('x', valueXIncrease);
      text30.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text30.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text30.setAttribute('y', valueXIncrease);
      text30.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text30.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text30.setAttribute('y', valueXIncrease);
      text30.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF30, 0.001);
const text31 = document.getElementById('text31');
text31.innerHTML = number[0];
function textF31() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text31.setAttribute('fill', newColor);
  const id = text31.x['animVal'][0].value+""+text31.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text31.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text31.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text31.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text31.setAttribute('x', valueXIncrease);
      text31.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text31.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text31.setAttribute('x', valueXIncrease);
      text31.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text31.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text31.setAttribute('y', valueXIncrease);
      text31.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text31.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text31.setAttribute('y', valueXIncrease);
      text31.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF31, 0.001);
const text32 = document.getElementById('text32');
text32.innerHTML = number[0];
function textF32() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text32.setAttribute('fill', newColor);
  const id = text32.x['animVal'][0].value+""+text32.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text32.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text32.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text32.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text32.setAttribute('x', valueXIncrease);
      text32.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text32.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text32.setAttribute('x', valueXIncrease);
      text32.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text32.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text32.setAttribute('y', valueXIncrease);
      text32.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text32.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text32.setAttribute('y', valueXIncrease);
      text32.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF32, 0.001);
const text33 = document.getElementById('text33');
text33.innerHTML = number[0];
function textF33() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text33.setAttribute('fill', newColor);
  const id = text33.x['animVal'][0].value+""+text33.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text33.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text33.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text33.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text33.setAttribute('x', valueXIncrease);
      text33.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text33.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text33.setAttribute('x', valueXIncrease);
      text33.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text33.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text33.setAttribute('y', valueXIncrease);
      text33.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text33.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text33.setAttribute('y', valueXIncrease);
      text33.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF33, 0.001);
const text34 = document.getElementById('text34');
text34.innerHTML = number[0];
function textF34() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text34.setAttribute('fill', newColor);
  const id = text34.x['animVal'][0].value+""+text34.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text34.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text34.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text34.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text34.setAttribute('x', valueXIncrease);
      text34.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text34.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text34.setAttribute('x', valueXIncrease);
      text34.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text34.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text34.setAttribute('y', valueXIncrease);
      text34.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text34.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text34.setAttribute('y', valueXIncrease);
      text34.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF34, 0.001);
const text35 = document.getElementById('text35');
text35.innerHTML = number[0];
function textF35() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text35.setAttribute('fill', newColor);
  const id = text35.x['animVal'][0].value+""+text35.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text35.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text35.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text35.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text35.setAttribute('x', valueXIncrease);
      text35.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text35.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text35.setAttribute('x', valueXIncrease);
      text35.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text35.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text35.setAttribute('y', valueXIncrease);
      text35.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text35.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text35.setAttribute('y', valueXIncrease);
      text35.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF35, 0.001);
const text36 = document.getElementById('text36');
text36.innerHTML = number[0];
function textF36() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text36.setAttribute('fill', newColor);
  const id = text36.x['animVal'][0].value+""+text36.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text36.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text36.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text36.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text36.setAttribute('x', valueXIncrease);
      text36.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text36.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text36.setAttribute('x', valueXIncrease);
      text36.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text36.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text36.setAttribute('y', valueXIncrease);
      text36.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text36.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text36.setAttribute('y', valueXIncrease);
      text36.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF36, 0.001);
const text37 = document.getElementById('text37');
text37.innerHTML = number[0];
function textF37() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text37.setAttribute('fill', newColor);
  const id = text37.x['animVal'][0].value+""+text37.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text37.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text37.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text37.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text37.setAttribute('x', valueXIncrease);
      text37.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text37.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text37.setAttribute('x', valueXIncrease);
      text37.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text37.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text37.setAttribute('y', valueXIncrease);
      text37.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text37.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text37.setAttribute('y', valueXIncrease);
      text37.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF37, 0.001);
const text38 = document.getElementById('text38');
text38.innerHTML = number[0];
function textF38() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text38.setAttribute('fill', newColor);
  const id = text38.x['animVal'][0].value+""+text38.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text38.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text38.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text38.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text38.setAttribute('x', valueXIncrease);
      text38.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text38.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text38.setAttribute('x', valueXIncrease);
      text38.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text38.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text38.setAttribute('y', valueXIncrease);
      text38.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text38.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text38.setAttribute('y', valueXIncrease);
      text38.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF38, 0.001);
const text39 = document.getElementById('text39');
text39.innerHTML = number[0];
function textF39() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text39.setAttribute('fill', newColor);
  const id = text39.x['animVal'][0].value+""+text39.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text39.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text39.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text39.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text39.setAttribute('x', valueXIncrease);
      text39.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text39.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text39.setAttribute('x', valueXIncrease);
      text39.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text39.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text39.setAttribute('y', valueXIncrease);
      text39.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text39.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text39.setAttribute('y', valueXIncrease);
      text39.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF39, 0.001);
const text40 = document.getElementById('text40');
text40.innerHTML = number[0];
function textF40() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text40.setAttribute('fill', newColor);
  const id = text40.x['animVal'][0].value+""+text40.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text40.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text40.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text40.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text40.setAttribute('x', valueXIncrease);
      text40.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text40.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text40.setAttribute('x', valueXIncrease);
      text40.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text40.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text40.setAttribute('y', valueXIncrease);
      text40.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text40.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text40.setAttribute('y', valueXIncrease);
      text40.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF40, 0.001);
const text41 = document.getElementById('text41');
text41.innerHTML = number[0];
function textF41() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text41.setAttribute('fill', newColor);
  const id = text41.x['animVal'][0].value+""+text41.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text41.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text41.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text41.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text41.setAttribute('x', valueXIncrease);
      text41.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text41.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text41.setAttribute('x', valueXIncrease);
      text41.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text41.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text41.setAttribute('y', valueXIncrease);
      text41.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text41.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text41.setAttribute('y', valueXIncrease);
      text41.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF41, 0.001);
const text42 = document.getElementById('text42');
text42.innerHTML = number[0];
function textF42() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text42.setAttribute('fill', newColor);
  const id = text42.x['animVal'][0].value+""+text42.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text42.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text42.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text42.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text42.setAttribute('x', valueXIncrease);
      text42.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text42.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text42.setAttribute('x', valueXIncrease);
      text42.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text42.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text42.setAttribute('y', valueXIncrease);
      text42.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text42.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text42.setAttribute('y', valueXIncrease);
      text42.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF42, 0.001);
const text43 = document.getElementById('text43');
text43.innerHTML = number[0];
function textF43() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text43.setAttribute('fill', newColor);
  const id = text43.x['animVal'][0].value+""+text43.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text43.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text43.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text43.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text43.setAttribute('x', valueXIncrease);
      text43.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text43.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text43.setAttribute('x', valueXIncrease);
      text43.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text43.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text43.setAttribute('y', valueXIncrease);
      text43.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text43.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text43.setAttribute('y', valueXIncrease);
      text43.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF43, 0.001);
const text44 = document.getElementById('text44');
text44.innerHTML = number[0];
function textF44() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text44.setAttribute('fill', newColor);
  const id = text44.x['animVal'][0].value+""+text44.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text44.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text44.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text44.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text44.setAttribute('x', valueXIncrease);
      text44.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text44.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text44.setAttribute('x', valueXIncrease);
      text44.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text44.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text44.setAttribute('y', valueXIncrease);
      text44.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text44.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text44.setAttribute('y', valueXIncrease);
      text44.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF44, 0.001);
const text45 = document.getElementById('text45');
text45.innerHTML = number[0];
function textF45() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text45.setAttribute('fill', newColor);
  const id = text45.x['animVal'][0].value+""+text45.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text45.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text45.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text45.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text45.setAttribute('x', valueXIncrease);
      text45.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text45.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text45.setAttribute('x', valueXIncrease);
      text45.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text45.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text45.setAttribute('y', valueXIncrease);
      text45.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text45.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text45.setAttribute('y', valueXIncrease);
      text45.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF45, 0.001);
const text46 = document.getElementById('text46');
text46.innerHTML = number[0];
function textF46() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text46.setAttribute('fill', newColor);
  const id = text46.x['animVal'][0].value+""+text46.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text46.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text46.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text46.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text46.setAttribute('x', valueXIncrease);
      text46.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text46.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text46.setAttribute('x', valueXIncrease);
      text46.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text46.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text46.setAttribute('y', valueXIncrease);
      text46.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text46.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text46.setAttribute('y', valueXIncrease);
      text46.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF46, 0.001);
const text47 = document.getElementById('text47');
text47.innerHTML = number[0];
function textF47() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text47.setAttribute('fill', newColor);
  const id = text47.x['animVal'][0].value+""+text47.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text47.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text47.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text47.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text47.setAttribute('x', valueXIncrease);
      text47.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text47.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text47.setAttribute('x', valueXIncrease);
      text47.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text47.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text47.setAttribute('y', valueXIncrease);
      text47.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text47.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text47.setAttribute('y', valueXIncrease);
      text47.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF47, 0.001);
const text48 = document.getElementById('text48');
text48.innerHTML = number[0];
function textF48() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text48.setAttribute('fill', newColor);
  const id = text48.x['animVal'][0].value+""+text48.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text48.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text48.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text48.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text48.setAttribute('x', valueXIncrease);
      text48.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text48.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text48.setAttribute('x', valueXIncrease);
      text48.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text48.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text48.setAttribute('y', valueXIncrease);
      text48.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text48.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text48.setAttribute('y', valueXIncrease);
      text48.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF48, 0.001);
const text49 = document.getElementById('text49');
text49.innerHTML = number[0];
function textF49() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text49.setAttribute('fill', newColor);
  const id = text49.x['animVal'][0].value+""+text49.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text49.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text49.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text49.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text49.setAttribute('x', valueXIncrease);
      text49.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text49.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text49.setAttribute('x', valueXIncrease);
      text49.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text49.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text49.setAttribute('y', valueXIncrease);
      text49.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text49.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text49.setAttribute('y', valueXIncrease);
      text49.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF49, 0.001);
const text50 = document.getElementById('text50');
text50.innerHTML = number[0];
function textF50() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text50.setAttribute('fill', newColor);
  const id = text50.x['animVal'][0].value+""+text50.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text50.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text50.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text50.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text50.setAttribute('x', valueXIncrease);
      text50.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text50.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text50.setAttribute('x', valueXIncrease);
      text50.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text50.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text50.setAttribute('y', valueXIncrease);
      text50.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text50.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text50.setAttribute('y', valueXIncrease);
      text50.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF50, 0.001);
const text51 = document.getElementById('text51');
text51.innerHTML = number[0];
function textF51() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text51.setAttribute('fill', newColor);
  const id = text51.x['animVal'][0].value+""+text51.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text51.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text51.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text51.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text51.setAttribute('x', valueXIncrease);
      text51.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text51.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text51.setAttribute('x', valueXIncrease);
      text51.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text51.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text51.setAttribute('y', valueXIncrease);
      text51.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text51.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text51.setAttribute('y', valueXIncrease);
      text51.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF51, 0.001);
const text52 = document.getElementById('text52');
text52.innerHTML = number[0];
function textF52() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text52.setAttribute('fill', newColor);
  const id = text52.x['animVal'][0].value+""+text52.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text52.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text52.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text52.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text52.setAttribute('x', valueXIncrease);
      text52.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text52.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text52.setAttribute('x', valueXIncrease);
      text52.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text52.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text52.setAttribute('y', valueXIncrease);
      text52.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text52.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text52.setAttribute('y', valueXIncrease);
      text52.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF52, 0.001);
const text53 = document.getElementById('text53');
text53.innerHTML = number[0];
function textF53() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text53.setAttribute('fill', newColor);
  const id = text53.x['animVal'][0].value+""+text53.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text53.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text53.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text53.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text53.setAttribute('x', valueXIncrease);
      text53.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text53.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text53.setAttribute('x', valueXIncrease);
      text53.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text53.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text53.setAttribute('y', valueXIncrease);
      text53.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text53.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text53.setAttribute('y', valueXIncrease);
      text53.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF53, 0.001);
const text54 = document.getElementById('text54');
text54.innerHTML = number[0];
function textF54() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text54.setAttribute('fill', newColor);
  const id = text54.x['animVal'][0].value+""+text54.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text54.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text54.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text54.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text54.setAttribute('x', valueXIncrease);
      text54.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text54.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text54.setAttribute('x', valueXIncrease);
      text54.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text54.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text54.setAttribute('y', valueXIncrease);
      text54.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text54.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text54.setAttribute('y', valueXIncrease);
      text54.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF54, 0.001);
const text55 = document.getElementById('text55');
text55.innerHTML = number[0];
function textF55() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text55.setAttribute('fill', newColor);
  const id = text55.x['animVal'][0].value+""+text55.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text55.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text55.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text55.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text55.setAttribute('x', valueXIncrease);
      text55.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text55.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text55.setAttribute('x', valueXIncrease);
      text55.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text55.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text55.setAttribute('y', valueXIncrease);
      text55.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text55.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text55.setAttribute('y', valueXIncrease);
      text55.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF55, 0.001);
const text56 = document.getElementById('text56');
text56.innerHTML = number[0];
function textF56() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text56.setAttribute('fill', newColor);
  const id = text56.x['animVal'][0].value+""+text56.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text56.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text56.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text56.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text56.setAttribute('x', valueXIncrease);
      text56.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text56.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text56.setAttribute('x', valueXIncrease);
      text56.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text56.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text56.setAttribute('y', valueXIncrease);
      text56.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text56.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text56.setAttribute('y', valueXIncrease);
      text56.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF56, 0.001);
const text57 = document.getElementById('text57');
text57.innerHTML = number[0];
function textF57() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text57.setAttribute('fill', newColor);
  const id = text57.x['animVal'][0].value+""+text57.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text57.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text57.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text57.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text57.setAttribute('x', valueXIncrease);
      text57.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text57.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text57.setAttribute('x', valueXIncrease);
      text57.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text57.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text57.setAttribute('y', valueXIncrease);
      text57.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text57.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text57.setAttribute('y', valueXIncrease);
      text57.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF57, 0.001);
const text58 = document.getElementById('text58');
text58.innerHTML = number[0];
function textF58() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text58.setAttribute('fill', newColor);
  const id = text58.x['animVal'][0].value+""+text58.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text58.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text58.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text58.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text58.setAttribute('x', valueXIncrease);
      text58.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text58.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text58.setAttribute('x', valueXIncrease);
      text58.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text58.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text58.setAttribute('y', valueXIncrease);
      text58.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text58.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text58.setAttribute('y', valueXIncrease);
      text58.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF58, 0.001);
const text59 = document.getElementById('text59');
text59.innerHTML = number[0];
function textF59() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text59.setAttribute('fill', newColor);
  const id = text59.x['animVal'][0].value+""+text59.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text59.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text59.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text59.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text59.setAttribute('x', valueXIncrease);
      text59.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text59.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text59.setAttribute('x', valueXIncrease);
      text59.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text59.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text59.setAttribute('y', valueXIncrease);
      text59.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text59.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text59.setAttribute('y', valueXIncrease);
      text59.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF59, 0.001);
const text60 = document.getElementById('text60');
text60.innerHTML = number[0];
function textF60() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text60.setAttribute('fill', newColor);
  const id = text60.x['animVal'][0].value+""+text60.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text60.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text60.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text60.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text60.setAttribute('x', valueXIncrease);
      text60.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text60.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text60.setAttribute('x', valueXIncrease);
      text60.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text60.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text60.setAttribute('y', valueXIncrease);
      text60.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text60.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text60.setAttribute('y', valueXIncrease);
      text60.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF60, 0.001);
const text61 = document.getElementById('text61');
text61.innerHTML = number[0];
function textF61() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text61.setAttribute('fill', newColor);
  const id = text61.x['animVal'][0].value+""+text61.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text61.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text61.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text61.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text61.setAttribute('x', valueXIncrease);
      text61.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text61.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text61.setAttribute('x', valueXIncrease);
      text61.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text61.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text61.setAttribute('y', valueXIncrease);
      text61.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text61.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text61.setAttribute('y', valueXIncrease);
      text61.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF61, 0.001);
const text62 = document.getElementById('text62');
text62.innerHTML = number[0];
function textF62() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text62.setAttribute('fill', newColor);
  const id = text62.x['animVal'][0].value+""+text62.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text62.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text62.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text62.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text62.setAttribute('x', valueXIncrease);
      text62.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text62.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text62.setAttribute('x', valueXIncrease);
      text62.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text62.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text62.setAttribute('y', valueXIncrease);
      text62.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text62.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text62.setAttribute('y', valueXIncrease);
      text62.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF62, 0.001);
const text63 = document.getElementById('text63');
text63.innerHTML = number[0];
function textF63() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text63.setAttribute('fill', newColor);
  const id = text63.x['animVal'][0].value+""+text63.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text63.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text63.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text63.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text63.setAttribute('x', valueXIncrease);
      text63.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text63.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text63.setAttribute('x', valueXIncrease);
      text63.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text63.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text63.setAttribute('y', valueXIncrease);
      text63.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text63.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text63.setAttribute('y', valueXIncrease);
      text63.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF63, 0.001);
const text64 = document.getElementById('text64');
text64.innerHTML = number[0];
function textF64() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text64.setAttribute('fill', newColor);
  const id = text64.x['animVal'][0].value+""+text64.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text64.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text64.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text64.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text64.setAttribute('x', valueXIncrease);
      text64.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text64.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text64.setAttribute('x', valueXIncrease);
      text64.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text64.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text64.setAttribute('y', valueXIncrease);
      text64.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text64.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text64.setAttribute('y', valueXIncrease);
      text64.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF64, 0.001);
const text65 = document.getElementById('text65');
text65.innerHTML = number[0];
function textF65() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text65.setAttribute('fill', newColor);
  const id = text65.x['animVal'][0].value+""+text65.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text65.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text65.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text65.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text65.setAttribute('x', valueXIncrease);
      text65.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text65.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text65.setAttribute('x', valueXIncrease);
      text65.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text65.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text65.setAttribute('y', valueXIncrease);
      text65.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text65.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text65.setAttribute('y', valueXIncrease);
      text65.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF65, 0.001);
const text66 = document.getElementById('text66');
text66.innerHTML = number[0];
function textF66() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text66.setAttribute('fill', newColor);
  const id = text66.x['animVal'][0].value+""+text66.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text66.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text66.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text66.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text66.setAttribute('x', valueXIncrease);
      text66.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text66.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text66.setAttribute('x', valueXIncrease);
      text66.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text66.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text66.setAttribute('y', valueXIncrease);
      text66.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text66.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text66.setAttribute('y', valueXIncrease);
      text66.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF66, 0.001);
const text67 = document.getElementById('text67');
text67.innerHTML = number[0];
function textF67() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text67.setAttribute('fill', newColor);
  const id = text67.x['animVal'][0].value+""+text67.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text67.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text67.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text67.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text67.setAttribute('x', valueXIncrease);
      text67.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text67.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text67.setAttribute('x', valueXIncrease);
      text67.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text67.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text67.setAttribute('y', valueXIncrease);
      text67.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text67.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text67.setAttribute('y', valueXIncrease);
      text67.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF67, 0.001);
const text68 = document.getElementById('text68');
text68.innerHTML = number[0];
function textF68() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text68.setAttribute('fill', newColor);
  const id = text68.x['animVal'][0].value+""+text68.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text68.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text68.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text68.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text68.setAttribute('x', valueXIncrease);
      text68.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text68.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text68.setAttribute('x', valueXIncrease);
      text68.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text68.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text68.setAttribute('y', valueXIncrease);
      text68.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text68.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text68.setAttribute('y', valueXIncrease);
      text68.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF68, 0.001);
const text69 = document.getElementById('text69');
text69.innerHTML = number[0];
function textF69() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text69.setAttribute('fill', newColor);
  const id = text69.x['animVal'][0].value+""+text69.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text69.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text69.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text69.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text69.setAttribute('x', valueXIncrease);
      text69.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text69.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text69.setAttribute('x', valueXIncrease);
      text69.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text69.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text69.setAttribute('y', valueXIncrease);
      text69.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text69.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text69.setAttribute('y', valueXIncrease);
      text69.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF69, 0.001);
const text70 = document.getElementById('text70');
text70.innerHTML = number[0];
function textF70() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text70.setAttribute('fill', newColor);
  const id = text70.x['animVal'][0].value+""+text70.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text70.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text70.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text70.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text70.setAttribute('x', valueXIncrease);
      text70.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text70.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text70.setAttribute('x', valueXIncrease);
      text70.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text70.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text70.setAttribute('y', valueXIncrease);
      text70.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text70.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text70.setAttribute('y', valueXIncrease);
      text70.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF70, 0.001);
const text71 = document.getElementById('text71');
text71.innerHTML = number[0];
function textF71() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text71.setAttribute('fill', newColor);
  const id = text71.x['animVal'][0].value+""+text71.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text71.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text71.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text71.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text71.setAttribute('x', valueXIncrease);
      text71.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text71.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text71.setAttribute('x', valueXIncrease);
      text71.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text71.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text71.setAttribute('y', valueXIncrease);
      text71.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text71.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text71.setAttribute('y', valueXIncrease);
      text71.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF71, 0.001);
const text72 = document.getElementById('text72');
text72.innerHTML = number[0];
function textF72() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text72.setAttribute('fill', newColor);
  const id = text72.x['animVal'][0].value+""+text72.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text72.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text72.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text72.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text72.setAttribute('x', valueXIncrease);
      text72.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text72.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text72.setAttribute('x', valueXIncrease);
      text72.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text72.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text72.setAttribute('y', valueXIncrease);
      text72.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text72.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text72.setAttribute('y', valueXIncrease);
      text72.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF72, 0.001);
const text73 = document.getElementById('text73');
text73.innerHTML = number[0];
function textF73() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text73.setAttribute('fill', newColor);
  const id = text73.x['animVal'][0].value+""+text73.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text73.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text73.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text73.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text73.setAttribute('x', valueXIncrease);
      text73.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text73.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text73.setAttribute('x', valueXIncrease);
      text73.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text73.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text73.setAttribute('y', valueXIncrease);
      text73.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text73.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text73.setAttribute('y', valueXIncrease);
      text73.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF73, 0.001);
const text74 = document.getElementById('text74');
text74.innerHTML = number[0];
function textF74() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text74.setAttribute('fill', newColor);
  const id = text74.x['animVal'][0].value+""+text74.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text74.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text74.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text74.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text74.setAttribute('x', valueXIncrease);
      text74.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text74.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text74.setAttribute('x', valueXIncrease);
      text74.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text74.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text74.setAttribute('y', valueXIncrease);
      text74.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text74.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text74.setAttribute('y', valueXIncrease);
      text74.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF74, 0.001);
const text75 = document.getElementById('text75');
text75.innerHTML = number[0];
function textF75() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text75.setAttribute('fill', newColor);
  const id = text75.x['animVal'][0].value+""+text75.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text75.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text75.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text75.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text75.setAttribute('x', valueXIncrease);
      text75.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text75.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text75.setAttribute('x', valueXIncrease);
      text75.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text75.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text75.setAttribute('y', valueXIncrease);
      text75.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text75.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text75.setAttribute('y', valueXIncrease);
      text75.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF75, 0.001);
const text76 = document.getElementById('text76');
text76.innerHTML = number[0];
function textF76() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text76.setAttribute('fill', newColor);
  const id = text76.x['animVal'][0].value+""+text76.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text76.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text76.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text76.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text76.setAttribute('x', valueXIncrease);
      text76.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text76.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text76.setAttribute('x', valueXIncrease);
      text76.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text76.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text76.setAttribute('y', valueXIncrease);
      text76.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text76.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text76.setAttribute('y', valueXIncrease);
      text76.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF76, 0.001);
const text77 = document.getElementById('text77');
text77.innerHTML = number[0];
function textF77() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text77.setAttribute('fill', newColor);
  const id = text77.x['animVal'][0].value+""+text77.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text77.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text77.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text77.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text77.setAttribute('x', valueXIncrease);
      text77.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text77.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text77.setAttribute('x', valueXIncrease);
      text77.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text77.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text77.setAttribute('y', valueXIncrease);
      text77.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text77.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text77.setAttribute('y', valueXIncrease);
      text77.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF77, 0.001);
const text78 = document.getElementById('text78');
text78.innerHTML = number[0];
function textF78() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text78.setAttribute('fill', newColor);
  const id = text78.x['animVal'][0].value+""+text78.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text78.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text78.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text78.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text78.setAttribute('x', valueXIncrease);
      text78.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text78.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text78.setAttribute('x', valueXIncrease);
      text78.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text78.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text78.setAttribute('y', valueXIncrease);
      text78.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text78.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text78.setAttribute('y', valueXIncrease);
      text78.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF78, 0.001);
const text79 = document.getElementById('text79');
text79.innerHTML = number[0];
function textF79() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text79.setAttribute('fill', newColor);
  const id = text79.x['animVal'][0].value+""+text79.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text79.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text79.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text79.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text79.setAttribute('x', valueXIncrease);
      text79.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text79.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text79.setAttribute('x', valueXIncrease);
      text79.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text79.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text79.setAttribute('y', valueXIncrease);
      text79.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text79.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text79.setAttribute('y', valueXIncrease);
      text79.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF79, 0.001);
const text80 = document.getElementById('text80');
text80.innerHTML = number[0];
function textF80() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text80.setAttribute('fill', newColor);
  const id = text80.x['animVal'][0].value+""+text80.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text80.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text80.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text80.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text80.setAttribute('x', valueXIncrease);
      text80.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text80.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text80.setAttribute('x', valueXIncrease);
      text80.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text80.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text80.setAttribute('y', valueXIncrease);
      text80.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text80.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text80.setAttribute('y', valueXIncrease);
      text80.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF80, 0.001);
const text81 = document.getElementById('text81');
text81.innerHTML = number[0];
function textF81() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text81.setAttribute('fill', newColor);
  const id = text81.x['animVal'][0].value+""+text81.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text81.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text81.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text81.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text81.setAttribute('x', valueXIncrease);
      text81.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text81.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text81.setAttribute('x', valueXIncrease);
      text81.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text81.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text81.setAttribute('y', valueXIncrease);
      text81.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text81.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text81.setAttribute('y', valueXIncrease);
      text81.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF81, 0.001);
const text82 = document.getElementById('text82');
text82.innerHTML = number[0];
function textF82() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text82.setAttribute('fill', newColor);
  const id = text82.x['animVal'][0].value+""+text82.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text82.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text82.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text82.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text82.setAttribute('x', valueXIncrease);
      text82.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text82.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text82.setAttribute('x', valueXIncrease);
      text82.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text82.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text82.setAttribute('y', valueXIncrease);
      text82.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text82.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text82.setAttribute('y', valueXIncrease);
      text82.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF82, 0.001);
const text83 = document.getElementById('text83');
text83.innerHTML = number[0];
function textF83() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text83.setAttribute('fill', newColor);
  const id = text83.x['animVal'][0].value+""+text83.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text83.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text83.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text83.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text83.setAttribute('x', valueXIncrease);
      text83.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text83.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text83.setAttribute('x', valueXIncrease);
      text83.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text83.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text83.setAttribute('y', valueXIncrease);
      text83.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text83.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text83.setAttribute('y', valueXIncrease);
      text83.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF83, 0.001);
const text84 = document.getElementById('text84');
text84.innerHTML = number[0];
function textF84() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text84.setAttribute('fill', newColor);
  const id = text84.x['animVal'][0].value+""+text84.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text84.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text84.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text84.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text84.setAttribute('x', valueXIncrease);
      text84.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text84.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text84.setAttribute('x', valueXIncrease);
      text84.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text84.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text84.setAttribute('y', valueXIncrease);
      text84.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text84.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text84.setAttribute('y', valueXIncrease);
      text84.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF84, 0.001);
const text85 = document.getElementById('text85');
text85.innerHTML = number[0];
function textF85() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text85.setAttribute('fill', newColor);
  const id = text85.x['animVal'][0].value+""+text85.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text85.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text85.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text85.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text85.setAttribute('x', valueXIncrease);
      text85.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text85.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text85.setAttribute('x', valueXIncrease);
      text85.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text85.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text85.setAttribute('y', valueXIncrease);
      text85.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text85.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text85.setAttribute('y', valueXIncrease);
      text85.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF85, 0.001);
const text86 = document.getElementById('text86');
text86.innerHTML = number[0];
function textF86() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text86.setAttribute('fill', newColor);
  const id = text86.x['animVal'][0].value+""+text86.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text86.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text86.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text86.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text86.setAttribute('x', valueXIncrease);
      text86.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text86.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text86.setAttribute('x', valueXIncrease);
      text86.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text86.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text86.setAttribute('y', valueXIncrease);
      text86.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text86.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text86.setAttribute('y', valueXIncrease);
      text86.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF86, 0.001);
const text87 = document.getElementById('text87');
text87.innerHTML = number[0];
function textF87() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text87.setAttribute('fill', newColor);
  const id = text87.x['animVal'][0].value+""+text87.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text87.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text87.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text87.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text87.setAttribute('x', valueXIncrease);
      text87.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text87.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text87.setAttribute('x', valueXIncrease);
      text87.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text87.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text87.setAttribute('y', valueXIncrease);
      text87.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text87.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text87.setAttribute('y', valueXIncrease);
      text87.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF87, 0.001);
const text88 = document.getElementById('text88');
text88.innerHTML = number[0];
function textF88() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text88.setAttribute('fill', newColor);
  const id = text88.x['animVal'][0].value+""+text88.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text88.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text88.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text88.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text88.setAttribute('x', valueXIncrease);
      text88.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text88.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text88.setAttribute('x', valueXIncrease);
      text88.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text88.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text88.setAttribute('y', valueXIncrease);
      text88.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text88.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text88.setAttribute('y', valueXIncrease);
      text88.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF88, 0.001);
const text89 = document.getElementById('text89');
text89.innerHTML = number[0];
function textF89() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text89.setAttribute('fill', newColor);
  const id = text89.x['animVal'][0].value+""+text89.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text89.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text89.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text89.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text89.setAttribute('x', valueXIncrease);
      text89.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text89.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text89.setAttribute('x', valueXIncrease);
      text89.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text89.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text89.setAttribute('y', valueXIncrease);
      text89.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text89.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text89.setAttribute('y', valueXIncrease);
      text89.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF89, 0.001);
const text90 = document.getElementById('text90');
text90.innerHTML = number[0];
function textF90() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text90.setAttribute('fill', newColor);
  const id = text90.x['animVal'][0].value+""+text90.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text90.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text90.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text90.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text90.setAttribute('x', valueXIncrease);
      text90.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text90.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text90.setAttribute('x', valueXIncrease);
      text90.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text90.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text90.setAttribute('y', valueXIncrease);
      text90.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text90.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text90.setAttribute('y', valueXIncrease);
      text90.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF90, 0.001);
const text91 = document.getElementById('text91');
text91.innerHTML = number[0];
function textF91() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text91.setAttribute('fill', newColor);
  const id = text91.x['animVal'][0].value+""+text91.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text91.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text91.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text91.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text91.setAttribute('x', valueXIncrease);
      text91.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text91.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text91.setAttribute('x', valueXIncrease);
      text91.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text91.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text91.setAttribute('y', valueXIncrease);
      text91.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text91.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text91.setAttribute('y', valueXIncrease);
      text91.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF91, 0.001);
const text92 = document.getElementById('text92');
text92.innerHTML = number[0];
function textF92() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text92.setAttribute('fill', newColor);
  const id = text92.x['animVal'][0].value+""+text92.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text92.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text92.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text92.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text92.setAttribute('x', valueXIncrease);
      text92.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text92.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text92.setAttribute('x', valueXIncrease);
      text92.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text92.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text92.setAttribute('y', valueXIncrease);
      text92.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text92.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text92.setAttribute('y', valueXIncrease);
      text92.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF92, 0.001);
const text93 = document.getElementById('text93');
text93.innerHTML = number[0];
function textF93() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text93.setAttribute('fill', newColor);
  const id = text93.x['animVal'][0].value+""+text93.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text93.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text93.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text93.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text93.setAttribute('x', valueXIncrease);
      text93.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text93.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text93.setAttribute('x', valueXIncrease);
      text93.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text93.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text93.setAttribute('y', valueXIncrease);
      text93.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text93.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text93.setAttribute('y', valueXIncrease);
      text93.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF93, 0.001);
const text94 = document.getElementById('text94');
text94.innerHTML = number[0];
function textF94() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text94.setAttribute('fill', newColor);
  const id = text94.x['animVal'][0].value+""+text94.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text94.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text94.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text94.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text94.setAttribute('x', valueXIncrease);
      text94.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text94.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text94.setAttribute('x', valueXIncrease);
      text94.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text94.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text94.setAttribute('y', valueXIncrease);
      text94.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text94.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text94.setAttribute('y', valueXIncrease);
      text94.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF94, 0.001);
const text95 = document.getElementById('text95');
text95.innerHTML = number[0];
function textF95() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text95.setAttribute('fill', newColor);
  const id = text95.x['animVal'][0].value+""+text95.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text95.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text95.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text95.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text95.setAttribute('x', valueXIncrease);
      text95.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text95.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text95.setAttribute('x', valueXIncrease);
      text95.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text95.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text95.setAttribute('y', valueXIncrease);
      text95.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text95.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text95.setAttribute('y', valueXIncrease);
      text95.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF95, 0.001);
const text96 = document.getElementById('text96');
text96.innerHTML = number[0];
function textF96() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text96.setAttribute('fill', newColor);
  const id = text96.x['animVal'][0].value+""+text96.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text96.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text96.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text96.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text96.setAttribute('x', valueXIncrease);
      text96.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text96.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text96.setAttribute('x', valueXIncrease);
      text96.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text96.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text96.setAttribute('y', valueXIncrease);
      text96.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text96.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text96.setAttribute('y', valueXIncrease);
      text96.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF96, 0.001);
const text97 = document.getElementById('text97');
text97.innerHTML = number[0];
function textF97() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text97.setAttribute('fill', newColor);
  const id = text97.x['animVal'][0].value+""+text97.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text97.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text97.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text97.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text97.setAttribute('x', valueXIncrease);
      text97.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text97.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text97.setAttribute('x', valueXIncrease);
      text97.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text97.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text97.setAttribute('y', valueXIncrease);
      text97.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text97.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text97.setAttribute('y', valueXIncrease);
      text97.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF97, 0.001);
const text98 = document.getElementById('text98');
text98.innerHTML = number[0];
function textF98() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text98.setAttribute('fill', newColor);
  const id = text98.x['animVal'][0].value+""+text98.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text98.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text98.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text98.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text98.setAttribute('x', valueXIncrease);
      text98.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text98.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text98.setAttribute('x', valueXIncrease);
      text98.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text98.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text98.setAttribute('y', valueXIncrease);
      text98.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text98.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text98.setAttribute('y', valueXIncrease);
      text98.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF98, 0.001);
const text99 = document.getElementById('text99');
text99.innerHTML = number[0];
function textF99() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text99.setAttribute('fill', newColor);
  const id = text99.x['animVal'][0].value+""+text99.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text99.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text99.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text99.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text99.setAttribute('x', valueXIncrease);
      text99.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text99.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text99.setAttribute('x', valueXIncrease);
      text99.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text99.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text99.setAttribute('y', valueXIncrease);
      text99.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text99.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text99.setAttribute('y', valueXIncrease);
      text99.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF99, 0.001);
const text100 = document.getElementById('text100');
text100.innerHTML = number[0];
function textF100() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text100.setAttribute('fill', newColor);
  const id = text100.x['animVal'][0].value+""+text100.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text100.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text100.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text100.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text100.setAttribute('x', valueXIncrease);
      text100.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text100.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text100.setAttribute('x', valueXIncrease);
      text100.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text100.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text100.setAttribute('y', valueXIncrease);
      text100.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text100.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text100.setAttribute('y', valueXIncrease);
      text100.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF100, 0.001);
const text101 = document.getElementById('text101');
text101.innerHTML = number[0];
function textF101() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text101.setAttribute('fill', newColor);
  const id = text101.x['animVal'][0].value+""+text101.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text101.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text101.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text101.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text101.setAttribute('x', valueXIncrease);
      text101.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text101.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text101.setAttribute('x', valueXIncrease);
      text101.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text101.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text101.setAttribute('y', valueXIncrease);
      text101.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text101.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text101.setAttribute('y', valueXIncrease);
      text101.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF101, 0.001);
const text102 = document.getElementById('text102');
text102.innerHTML = number[0];
function textF102() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text102.setAttribute('fill', newColor);
  const id = text102.x['animVal'][0].value+""+text102.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text102.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text102.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text102.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text102.setAttribute('x', valueXIncrease);
      text102.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text102.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text102.setAttribute('x', valueXIncrease);
      text102.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text102.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text102.setAttribute('y', valueXIncrease);
      text102.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text102.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text102.setAttribute('y', valueXIncrease);
      text102.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF102, 0.001);
const text103 = document.getElementById('text103');
text103.innerHTML = number[0];
function textF103() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text103.setAttribute('fill', newColor);
  const id = text103.x['animVal'][0].value+""+text103.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text103.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text103.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text103.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text103.setAttribute('x', valueXIncrease);
      text103.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text103.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text103.setAttribute('x', valueXIncrease);
      text103.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text103.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text103.setAttribute('y', valueXIncrease);
      text103.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text103.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text103.setAttribute('y', valueXIncrease);
      text103.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF103, 0.001);
const text104 = document.getElementById('text104');
text104.innerHTML = number[0];
function textF104() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text104.setAttribute('fill', newColor);
  const id = text104.x['animVal'][0].value+""+text104.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text104.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text104.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text104.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text104.setAttribute('x', valueXIncrease);
      text104.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text104.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text104.setAttribute('x', valueXIncrease);
      text104.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text104.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text104.setAttribute('y', valueXIncrease);
      text104.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text104.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text104.setAttribute('y', valueXIncrease);
      text104.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF104, 0.001);
const text105 = document.getElementById('text105');
text105.innerHTML = number[0];
function textF105() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text105.setAttribute('fill', newColor);
  const id = text105.x['animVal'][0].value+""+text105.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text105.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text105.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text105.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text105.setAttribute('x', valueXIncrease);
      text105.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text105.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text105.setAttribute('x', valueXIncrease);
      text105.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text105.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text105.setAttribute('y', valueXIncrease);
      text105.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text105.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text105.setAttribute('y', valueXIncrease);
      text105.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF105, 0.001);
const text106 = document.getElementById('text106');
text106.innerHTML = number[0];
function textF106() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text106.setAttribute('fill', newColor);
  const id = text106.x['animVal'][0].value+""+text106.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text106.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text106.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text106.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text106.setAttribute('x', valueXIncrease);
      text106.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text106.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text106.setAttribute('x', valueXIncrease);
      text106.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text106.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text106.setAttribute('y', valueXIncrease);
      text106.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text106.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text106.setAttribute('y', valueXIncrease);
      text106.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF106, 0.001);
const text107 = document.getElementById('text107');
text107.innerHTML = number[0];
function textF107() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text107.setAttribute('fill', newColor);
  const id = text107.x['animVal'][0].value+""+text107.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text107.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text107.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text107.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text107.setAttribute('x', valueXIncrease);
      text107.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text107.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text107.setAttribute('x', valueXIncrease);
      text107.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text107.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text107.setAttribute('y', valueXIncrease);
      text107.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text107.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text107.setAttribute('y', valueXIncrease);
      text107.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF107, 0.001);
const text108 = document.getElementById('text108');
text108.innerHTML = number[0];
function textF108() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text108.setAttribute('fill', newColor);
  const id = text108.x['animVal'][0].value+""+text108.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text108.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text108.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text108.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text108.setAttribute('x', valueXIncrease);
      text108.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text108.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text108.setAttribute('x', valueXIncrease);
      text108.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text108.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text108.setAttribute('y', valueXIncrease);
      text108.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text108.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text108.setAttribute('y', valueXIncrease);
      text108.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF108, 0.001);
const text109 = document.getElementById('text109');
text109.innerHTML = number[0];
function textF109() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text109.setAttribute('fill', newColor);
  const id = text109.x['animVal'][0].value+""+text109.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text109.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text109.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text109.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text109.setAttribute('x', valueXIncrease);
      text109.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text109.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text109.setAttribute('x', valueXIncrease);
      text109.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text109.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text109.setAttribute('y', valueXIncrease);
      text109.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text109.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text109.setAttribute('y', valueXIncrease);
      text109.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF109, 0.001);
const text110 = document.getElementById('text110');
text110.innerHTML = number[0];
function textF110() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text110.setAttribute('fill', newColor);
  const id = text110.x['animVal'][0].value+""+text110.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text110.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text110.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text110.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text110.setAttribute('x', valueXIncrease);
      text110.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text110.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text110.setAttribute('x', valueXIncrease);
      text110.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text110.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text110.setAttribute('y', valueXIncrease);
      text110.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text110.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text110.setAttribute('y', valueXIncrease);
      text110.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF110, 0.001);
const text111 = document.getElementById('text111');
text111.innerHTML = number[0];
function textF111() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text111.setAttribute('fill', newColor);
  const id = text111.x['animVal'][0].value+""+text111.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text111.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text111.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text111.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text111.setAttribute('x', valueXIncrease);
      text111.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text111.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text111.setAttribute('x', valueXIncrease);
      text111.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text111.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text111.setAttribute('y', valueXIncrease);
      text111.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text111.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text111.setAttribute('y', valueXIncrease);
      text111.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF111, 0.001);
const text112 = document.getElementById('text112');
text112.innerHTML = number[0];
function textF112() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text112.setAttribute('fill', newColor);
  const id = text112.x['animVal'][0].value+""+text112.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text112.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text112.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text112.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text112.setAttribute('x', valueXIncrease);
      text112.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text112.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text112.setAttribute('x', valueXIncrease);
      text112.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text112.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text112.setAttribute('y', valueXIncrease);
      text112.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text112.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text112.setAttribute('y', valueXIncrease);
      text112.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF112, 0.001);
const text113 = document.getElementById('text113');
text113.innerHTML = number[0];
function textF113() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text113.setAttribute('fill', newColor);
  const id = text113.x['animVal'][0].value+""+text113.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text113.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text113.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text113.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text113.setAttribute('x', valueXIncrease);
      text113.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text113.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text113.setAttribute('x', valueXIncrease);
      text113.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text113.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text113.setAttribute('y', valueXIncrease);
      text113.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text113.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text113.setAttribute('y', valueXIncrease);
      text113.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF113, 0.001);
const text114 = document.getElementById('text114');
text114.innerHTML = number[0];
function textF114() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text114.setAttribute('fill', newColor);
  const id = text114.x['animVal'][0].value+""+text114.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text114.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text114.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text114.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text114.setAttribute('x', valueXIncrease);
      text114.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text114.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text114.setAttribute('x', valueXIncrease);
      text114.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text114.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text114.setAttribute('y', valueXIncrease);
      text114.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text114.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text114.setAttribute('y', valueXIncrease);
      text114.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF114, 0.001);
const text115 = document.getElementById('text115');
text115.innerHTML = number[0];
function textF115() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text115.setAttribute('fill', newColor);
  const id = text115.x['animVal'][0].value+""+text115.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text115.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text115.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text115.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text115.setAttribute('x', valueXIncrease);
      text115.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text115.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text115.setAttribute('x', valueXIncrease);
      text115.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text115.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text115.setAttribute('y', valueXIncrease);
      text115.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text115.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text115.setAttribute('y', valueXIncrease);
      text115.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF115, 0.001);
const text116 = document.getElementById('text116');
text116.innerHTML = number[0];
function textF116() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text116.setAttribute('fill', newColor);
  const id = text116.x['animVal'][0].value+""+text116.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text116.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text116.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text116.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text116.setAttribute('x', valueXIncrease);
      text116.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text116.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text116.setAttribute('x', valueXIncrease);
      text116.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text116.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text116.setAttribute('y', valueXIncrease);
      text116.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text116.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text116.setAttribute('y', valueXIncrease);
      text116.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF116, 0.001);
const text117 = document.getElementById('text117');
text117.innerHTML = number[0];
function textF117() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text117.setAttribute('fill', newColor);
  const id = text117.x['animVal'][0].value+""+text117.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text117.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text117.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text117.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text117.setAttribute('x', valueXIncrease);
      text117.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text117.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text117.setAttribute('x', valueXIncrease);
      text117.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text117.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text117.setAttribute('y', valueXIncrease);
      text117.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text117.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text117.setAttribute('y', valueXIncrease);
      text117.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF117, 0.001);
const text118 = document.getElementById('text118');
text118.innerHTML = number[0];
function textF118() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text118.setAttribute('fill', newColor);
  const id = text118.x['animVal'][0].value+""+text118.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text118.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text118.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text118.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text118.setAttribute('x', valueXIncrease);
      text118.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text118.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text118.setAttribute('x', valueXIncrease);
      text118.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text118.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text118.setAttribute('y', valueXIncrease);
      text118.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text118.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text118.setAttribute('y', valueXIncrease);
      text118.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF118, 0.001);
const text119 = document.getElementById('text119');
text119.innerHTML = number[0];
function textF119() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text119.setAttribute('fill', newColor);
  const id = text119.x['animVal'][0].value+""+text119.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text119.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text119.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text119.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text119.setAttribute('x', valueXIncrease);
      text119.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text119.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text119.setAttribute('x', valueXIncrease);
      text119.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text119.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text119.setAttribute('y', valueXIncrease);
      text119.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text119.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text119.setAttribute('y', valueXIncrease);
      text119.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF119, 0.001);
const text120 = document.getElementById('text120');
text120.innerHTML = number[0];
function textF120() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text120.setAttribute('fill', newColor);
  const id = text120.x['animVal'][0].value+""+text120.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text120.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text120.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text120.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text120.setAttribute('x', valueXIncrease);
      text120.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text120.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text120.setAttribute('x', valueXIncrease);
      text120.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text120.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text120.setAttribute('y', valueXIncrease);
      text120.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text120.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text120.setAttribute('y', valueXIncrease);
      text120.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF120, 0.001);
const text121 = document.getElementById('text121');
text121.innerHTML = number[0];
function textF121() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text121.setAttribute('fill', newColor);
  const id = text121.x['animVal'][0].value+""+text121.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text121.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text121.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text121.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text121.setAttribute('x', valueXIncrease);
      text121.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text121.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text121.setAttribute('x', valueXIncrease);
      text121.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text121.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text121.setAttribute('y', valueXIncrease);
      text121.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text121.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text121.setAttribute('y', valueXIncrease);
      text121.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF121, 0.001);
const text122 = document.getElementById('text122');
text122.innerHTML = number[0];
function textF122() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text122.setAttribute('fill', newColor);
  const id = text122.x['animVal'][0].value+""+text122.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text122.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text122.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text122.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text122.setAttribute('x', valueXIncrease);
      text122.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text122.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text122.setAttribute('x', valueXIncrease);
      text122.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text122.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text122.setAttribute('y', valueXIncrease);
      text122.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text122.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text122.setAttribute('y', valueXIncrease);
      text122.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF122, 0.001);
const text123 = document.getElementById('text123');
text123.innerHTML = number[0];
function textF123() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text123.setAttribute('fill', newColor);
  const id = text123.x['animVal'][0].value+""+text123.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text123.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text123.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text123.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text123.setAttribute('x', valueXIncrease);
      text123.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text123.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text123.setAttribute('x', valueXIncrease);
      text123.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text123.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text123.setAttribute('y', valueXIncrease);
      text123.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text123.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text123.setAttribute('y', valueXIncrease);
      text123.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF123, 0.001);
const text124 = document.getElementById('text124');
text124.innerHTML = number[0];
function textF124() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text124.setAttribute('fill', newColor);
  const id = text124.x['animVal'][0].value+""+text124.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text124.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text124.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text124.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text124.setAttribute('x', valueXIncrease);
      text124.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text124.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text124.setAttribute('x', valueXIncrease);
      text124.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text124.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text124.setAttribute('y', valueXIncrease);
      text124.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text124.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text124.setAttribute('y', valueXIncrease);
      text124.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF124, 0.001);
const text125 = document.getElementById('text125');
text125.innerHTML = number[0];
function textF125() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text125.setAttribute('fill', newColor);
  const id = text125.x['animVal'][0].value+""+text125.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text125.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text125.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text125.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text125.setAttribute('x', valueXIncrease);
      text125.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text125.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text125.setAttribute('x', valueXIncrease);
      text125.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text125.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text125.setAttribute('y', valueXIncrease);
      text125.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text125.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text125.setAttribute('y', valueXIncrease);
      text125.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF125, 0.001);
const text126 = document.getElementById('text126');
text126.innerHTML = number[0];
function textF126() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text126.setAttribute('fill', newColor);
  const id = text126.x['animVal'][0].value+""+text126.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text126.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text126.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text126.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text126.setAttribute('x', valueXIncrease);
      text126.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text126.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text126.setAttribute('x', valueXIncrease);
      text126.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text126.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text126.setAttribute('y', valueXIncrease);
      text126.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text126.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text126.setAttribute('y', valueXIncrease);
      text126.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF126, 0.001);
const text127 = document.getElementById('text127');
text127.innerHTML = number[0];
function textF127() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text127.setAttribute('fill', newColor);
  const id = text127.x['animVal'][0].value+""+text127.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text127.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text127.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text127.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text127.setAttribute('x', valueXIncrease);
      text127.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text127.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text127.setAttribute('x', valueXIncrease);
      text127.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text127.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text127.setAttribute('y', valueXIncrease);
      text127.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text127.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text127.setAttribute('y', valueXIncrease);
      text127.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF127, 0.001);
const text128 = document.getElementById('text128');
text128.innerHTML = number[0];
function textF128() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text128.setAttribute('fill', newColor);
  const id = text128.x['animVal'][0].value+""+text128.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text128.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text128.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text128.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text128.setAttribute('x', valueXIncrease);
      text128.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text128.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text128.setAttribute('x', valueXIncrease);
      text128.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text128.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text128.setAttribute('y', valueXIncrease);
      text128.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text128.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text128.setAttribute('y', valueXIncrease);
      text128.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF128, 0.001);
const text129 = document.getElementById('text129');
text129.innerHTML = number[0];
function textF129() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text129.setAttribute('fill', newColor);
  const id = text129.x['animVal'][0].value+""+text129.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text129.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text129.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text129.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text129.setAttribute('x', valueXIncrease);
      text129.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text129.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text129.setAttribute('x', valueXIncrease);
      text129.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text129.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text129.setAttribute('y', valueXIncrease);
      text129.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text129.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text129.setAttribute('y', valueXIncrease);
      text129.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF129, 0.001);
const text130 = document.getElementById('text130');
text130.innerHTML = number[0];
function textF130() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text130.setAttribute('fill', newColor);
  const id = text130.x['animVal'][0].value+""+text130.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text130.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text130.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text130.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text130.setAttribute('x', valueXIncrease);
      text130.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text130.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text130.setAttribute('x', valueXIncrease);
      text130.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text130.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text130.setAttribute('y', valueXIncrease);
      text130.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text130.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text130.setAttribute('y', valueXIncrease);
      text130.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF130, 0.001);
const text131 = document.getElementById('text131');
text131.innerHTML = number[0];
function textF131() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text131.setAttribute('fill', newColor);
  const id = text131.x['animVal'][0].value+""+text131.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text131.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text131.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text131.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text131.setAttribute('x', valueXIncrease);
      text131.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text131.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text131.setAttribute('x', valueXIncrease);
      text131.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text131.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text131.setAttribute('y', valueXIncrease);
      text131.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text131.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text131.setAttribute('y', valueXIncrease);
      text131.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF131, 0.001);
const text132 = document.getElementById('text132');
text132.innerHTML = number[0];
function textF132() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text132.setAttribute('fill', newColor);
  const id = text132.x['animVal'][0].value+""+text132.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text132.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text132.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text132.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text132.setAttribute('x', valueXIncrease);
      text132.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text132.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text132.setAttribute('x', valueXIncrease);
      text132.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text132.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text132.setAttribute('y', valueXIncrease);
      text132.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text132.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text132.setAttribute('y', valueXIncrease);
      text132.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF132, 0.001);
const text133 = document.getElementById('text133');
text133.innerHTML = number[0];
function textF133() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text133.setAttribute('fill', newColor);
  const id = text133.x['animVal'][0].value+""+text133.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text133.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text133.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text133.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text133.setAttribute('x', valueXIncrease);
      text133.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text133.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text133.setAttribute('x', valueXIncrease);
      text133.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text133.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text133.setAttribute('y', valueXIncrease);
      text133.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text133.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text133.setAttribute('y', valueXIncrease);
      text133.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF133, 0.001);
const text134 = document.getElementById('text134');
text134.innerHTML = number[0];
function textF134() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text134.setAttribute('fill', newColor);
  const id = text134.x['animVal'][0].value+""+text134.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text134.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text134.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text134.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text134.setAttribute('x', valueXIncrease);
      text134.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text134.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text134.setAttribute('x', valueXIncrease);
      text134.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text134.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text134.setAttribute('y', valueXIncrease);
      text134.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text134.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text134.setAttribute('y', valueXIncrease);
      text134.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF134, 0.001);
const text135 = document.getElementById('text135');
text135.innerHTML = number[0];
function textF135() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text135.setAttribute('fill', newColor);
  const id = text135.x['animVal'][0].value+""+text135.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text135.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text135.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text135.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text135.setAttribute('x', valueXIncrease);
      text135.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text135.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text135.setAttribute('x', valueXIncrease);
      text135.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text135.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text135.setAttribute('y', valueXIncrease);
      text135.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text135.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text135.setAttribute('y', valueXIncrease);
      text135.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF135, 0.001);
const text136 = document.getElementById('text136');
text136.innerHTML = number[0];
function textF136() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text136.setAttribute('fill', newColor);
  const id = text136.x['animVal'][0].value+""+text136.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text136.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text136.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text136.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text136.setAttribute('x', valueXIncrease);
      text136.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text136.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text136.setAttribute('x', valueXIncrease);
      text136.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text136.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text136.setAttribute('y', valueXIncrease);
      text136.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text136.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text136.setAttribute('y', valueXIncrease);
      text136.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF136, 0.001);
const text137 = document.getElementById('text137');
text137.innerHTML = number[0];
function textF137() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text137.setAttribute('fill', newColor);
  const id = text137.x['animVal'][0].value+""+text137.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text137.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text137.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text137.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text137.setAttribute('x', valueXIncrease);
      text137.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text137.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text137.setAttribute('x', valueXIncrease);
      text137.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text137.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text137.setAttribute('y', valueXIncrease);
      text137.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text137.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text137.setAttribute('y', valueXIncrease);
      text137.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF137, 0.001);
const text138 = document.getElementById('text138');
text138.innerHTML = number[0];
function textF138() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text138.setAttribute('fill', newColor);
  const id = text138.x['animVal'][0].value+""+text138.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text138.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text138.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text138.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text138.setAttribute('x', valueXIncrease);
      text138.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text138.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text138.setAttribute('x', valueXIncrease);
      text138.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text138.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text138.setAttribute('y', valueXIncrease);
      text138.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text138.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text138.setAttribute('y', valueXIncrease);
      text138.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF138, 0.001);
const text139 = document.getElementById('text139');
text139.innerHTML = number[0];
function textF139() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text139.setAttribute('fill', newColor);
  const id = text139.x['animVal'][0].value+""+text139.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text139.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text139.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text139.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text139.setAttribute('x', valueXIncrease);
      text139.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text139.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text139.setAttribute('x', valueXIncrease);
      text139.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text139.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text139.setAttribute('y', valueXIncrease);
      text139.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text139.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text139.setAttribute('y', valueXIncrease);
      text139.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF139, 0.001);
const text140 = document.getElementById('text140');
text140.innerHTML = number[0];
function textF140() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text140.setAttribute('fill', newColor);
  const id = text140.x['animVal'][0].value+""+text140.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text140.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text140.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text140.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text140.setAttribute('x', valueXIncrease);
      text140.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text140.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text140.setAttribute('x', valueXIncrease);
      text140.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text140.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text140.setAttribute('y', valueXIncrease);
      text140.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text140.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text140.setAttribute('y', valueXIncrease);
      text140.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF140, 0.001);
const text141 = document.getElementById('text141');
text141.innerHTML = number[0];
function textF141() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text141.setAttribute('fill', newColor);
  const id = text141.x['animVal'][0].value+""+text141.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text141.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text141.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text141.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text141.setAttribute('x', valueXIncrease);
      text141.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text141.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text141.setAttribute('x', valueXIncrease);
      text141.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text141.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text141.setAttribute('y', valueXIncrease);
      text141.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text141.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text141.setAttribute('y', valueXIncrease);
      text141.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF141, 0.001);
const text142 = document.getElementById('text142');
text142.innerHTML = number[0];
function textF142() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text142.setAttribute('fill', newColor);
  const id = text142.x['animVal'][0].value+""+text142.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text142.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text142.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text142.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text142.setAttribute('x', valueXIncrease);
      text142.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text142.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text142.setAttribute('x', valueXIncrease);
      text142.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text142.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text142.setAttribute('y', valueXIncrease);
      text142.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text142.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text142.setAttribute('y', valueXIncrease);
      text142.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF142, 0.001);
const text143 = document.getElementById('text143');
text143.innerHTML = number[0];
function textF143() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text143.setAttribute('fill', newColor);
  const id = text143.x['animVal'][0].value+""+text143.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text143.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text143.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text143.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text143.setAttribute('x', valueXIncrease);
      text143.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text143.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text143.setAttribute('x', valueXIncrease);
      text143.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text143.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text143.setAttribute('y', valueXIncrease);
      text143.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text143.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text143.setAttribute('y', valueXIncrease);
      text143.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF143, 0.001);
const text144 = document.getElementById('text144');
text144.innerHTML = number[0];
function textF144() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text144.setAttribute('fill', newColor);
  const id = text144.x['animVal'][0].value+""+text144.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text144.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text144.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text144.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text144.setAttribute('x', valueXIncrease);
      text144.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text144.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text144.setAttribute('x', valueXIncrease);
      text144.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text144.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text144.setAttribute('y', valueXIncrease);
      text144.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text144.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text144.setAttribute('y', valueXIncrease);
      text144.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF144, 0.001);
const text145 = document.getElementById('text145');
text145.innerHTML = number[0];
function textF145() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text145.setAttribute('fill', newColor);
  const id = text145.x['animVal'][0].value+""+text145.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text145.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text145.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text145.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text145.setAttribute('x', valueXIncrease);
      text145.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text145.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text145.setAttribute('x', valueXIncrease);
      text145.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text145.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text145.setAttribute('y', valueXIncrease);
      text145.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text145.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text145.setAttribute('y', valueXIncrease);
      text145.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF145, 0.001);
const text146 = document.getElementById('text146');
text146.innerHTML = number[0];
function textF146() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text146.setAttribute('fill', newColor);
  const id = text146.x['animVal'][0].value+""+text146.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text146.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text146.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text146.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text146.setAttribute('x', valueXIncrease);
      text146.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text146.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text146.setAttribute('x', valueXIncrease);
      text146.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text146.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text146.setAttribute('y', valueXIncrease);
      text146.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text146.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text146.setAttribute('y', valueXIncrease);
      text146.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF146, 0.001);
const text147 = document.getElementById('text147');
text147.innerHTML = number[0];
function textF147() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text147.setAttribute('fill', newColor);
  const id = text147.x['animVal'][0].value+""+text147.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text147.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text147.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text147.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text147.setAttribute('x', valueXIncrease);
      text147.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text147.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text147.setAttribute('x', valueXIncrease);
      text147.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text147.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text147.setAttribute('y', valueXIncrease);
      text147.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text147.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text147.setAttribute('y', valueXIncrease);
      text147.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF147, 0.001);
const text148 = document.getElementById('text148');
text148.innerHTML = number[0];
function textF148() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text148.setAttribute('fill', newColor);
  const id = text148.x['animVal'][0].value+""+text148.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text148.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text148.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text148.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text148.setAttribute('x', valueXIncrease);
      text148.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text148.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text148.setAttribute('x', valueXIncrease);
      text148.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text148.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text148.setAttribute('y', valueXIncrease);
      text148.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text148.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text148.setAttribute('y', valueXIncrease);
      text148.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF148, 0.001);
const text149 = document.getElementById('text149');
text149.innerHTML = number[0];
function textF149() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text149.setAttribute('fill', newColor);
  const id = text149.x['animVal'][0].value+""+text149.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text149.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text149.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text149.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text149.setAttribute('x', valueXIncrease);
      text149.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text149.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text149.setAttribute('x', valueXIncrease);
      text149.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text149.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text149.setAttribute('y', valueXIncrease);
      text149.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text149.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text149.setAttribute('y', valueXIncrease);
      text149.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF149, 0.001);
const text150 = document.getElementById('text150');
text150.innerHTML = number[0];
function textF150() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text150.setAttribute('fill', newColor);
  const id = text150.x['animVal'][0].value+""+text150.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text150.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text150.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text150.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text150.setAttribute('x', valueXIncrease);
      text150.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text150.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text150.setAttribute('x', valueXIncrease);
      text150.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text150.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text150.setAttribute('y', valueXIncrease);
      text150.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text150.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text150.setAttribute('y', valueXIncrease);
      text150.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF150, 0.001);
const text151 = document.getElementById('text151');
text151.innerHTML = number[0];
function textF151() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text151.setAttribute('fill', newColor);
  const id = text151.x['animVal'][0].value+""+text151.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text151.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text151.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text151.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text151.setAttribute('x', valueXIncrease);
      text151.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text151.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text151.setAttribute('x', valueXIncrease);
      text151.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text151.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text151.setAttribute('y', valueXIncrease);
      text151.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text151.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text151.setAttribute('y', valueXIncrease);
      text151.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF151, 0.001);
const text152 = document.getElementById('text152');
text152.innerHTML = number[0];
function textF152() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text152.setAttribute('fill', newColor);
  const id = text152.x['animVal'][0].value+""+text152.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text152.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text152.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text152.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text152.setAttribute('x', valueXIncrease);
      text152.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text152.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text152.setAttribute('x', valueXIncrease);
      text152.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text152.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text152.setAttribute('y', valueXIncrease);
      text152.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text152.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text152.setAttribute('y', valueXIncrease);
      text152.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF152, 0.001);
const text153 = document.getElementById('text153');
text153.innerHTML = number[0];
function textF153() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text153.setAttribute('fill', newColor);
  const id = text153.x['animVal'][0].value+""+text153.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text153.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text153.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text153.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text153.setAttribute('x', valueXIncrease);
      text153.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text153.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text153.setAttribute('x', valueXIncrease);
      text153.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text153.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text153.setAttribute('y', valueXIncrease);
      text153.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text153.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text153.setAttribute('y', valueXIncrease);
      text153.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF153, 0.001);
const text154 = document.getElementById('text154');
text154.innerHTML = number[0];
function textF154() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text154.setAttribute('fill', newColor);
  const id = text154.x['animVal'][0].value+""+text154.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text154.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text154.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text154.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text154.setAttribute('x', valueXIncrease);
      text154.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text154.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text154.setAttribute('x', valueXIncrease);
      text154.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text154.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text154.setAttribute('y', valueXIncrease);
      text154.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text154.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text154.setAttribute('y', valueXIncrease);
      text154.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF154, 0.001);
const text155 = document.getElementById('text155');
text155.innerHTML = number[0];
function textF155() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text155.setAttribute('fill', newColor);
  const id = text155.x['animVal'][0].value+""+text155.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text155.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text155.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text155.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text155.setAttribute('x', valueXIncrease);
      text155.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text155.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text155.setAttribute('x', valueXIncrease);
      text155.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text155.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text155.setAttribute('y', valueXIncrease);
      text155.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text155.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text155.setAttribute('y', valueXIncrease);
      text155.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF155, 0.001);
const text156 = document.getElementById('text156');
text156.innerHTML = number[0];
function textF156() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text156.setAttribute('fill', newColor);
  const id = text156.x['animVal'][0].value+""+text156.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text156.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text156.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text156.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text156.setAttribute('x', valueXIncrease);
      text156.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text156.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text156.setAttribute('x', valueXIncrease);
      text156.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text156.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text156.setAttribute('y', valueXIncrease);
      text156.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text156.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text156.setAttribute('y', valueXIncrease);
      text156.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF156, 0.001);
const text157 = document.getElementById('text157');
text157.innerHTML = number[0];
function textF157() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text157.setAttribute('fill', newColor);
  const id = text157.x['animVal'][0].value+""+text157.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text157.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text157.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text157.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text157.setAttribute('x', valueXIncrease);
      text157.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text157.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text157.setAttribute('x', valueXIncrease);
      text157.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text157.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text157.setAttribute('y', valueXIncrease);
      text157.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text157.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text157.setAttribute('y', valueXIncrease);
      text157.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF157, 0.001);
const text158 = document.getElementById('text158');
text158.innerHTML = number[0];
function textF158() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text158.setAttribute('fill', newColor);
  const id = text158.x['animVal'][0].value+""+text158.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text158.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text158.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text158.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text158.setAttribute('x', valueXIncrease);
      text158.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text158.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text158.setAttribute('x', valueXIncrease);
      text158.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text158.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text158.setAttribute('y', valueXIncrease);
      text158.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text158.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text158.setAttribute('y', valueXIncrease);
      text158.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF158, 0.001);
const text159 = document.getElementById('text159');
text159.innerHTML = number[0];
function textF159() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text159.setAttribute('fill', newColor);
  const id = text159.x['animVal'][0].value+""+text159.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text159.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text159.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text159.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text159.setAttribute('x', valueXIncrease);
      text159.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text159.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text159.setAttribute('x', valueXIncrease);
      text159.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text159.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text159.setAttribute('y', valueXIncrease);
      text159.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text159.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text159.setAttribute('y', valueXIncrease);
      text159.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF159, 0.001);
const text160 = document.getElementById('text160');
text160.innerHTML = number[0];
function textF160() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text160.setAttribute('fill', newColor);
  const id = text160.x['animVal'][0].value+""+text160.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text160.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text160.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text160.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text160.setAttribute('x', valueXIncrease);
      text160.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text160.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text160.setAttribute('x', valueXIncrease);
      text160.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text160.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text160.setAttribute('y', valueXIncrease);
      text160.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text160.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text160.setAttribute('y', valueXIncrease);
      text160.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF160, 0.001);
const text161 = document.getElementById('text161');
text161.innerHTML = number[0];
function textF161() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text161.setAttribute('fill', newColor);
  const id = text161.x['animVal'][0].value+""+text161.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text161.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text161.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text161.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text161.setAttribute('x', valueXIncrease);
      text161.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text161.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text161.setAttribute('x', valueXIncrease);
      text161.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text161.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text161.setAttribute('y', valueXIncrease);
      text161.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text161.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text161.setAttribute('y', valueXIncrease);
      text161.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF161, 0.001);
const text162 = document.getElementById('text162');
text162.innerHTML = number[0];
function textF162() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text162.setAttribute('fill', newColor);
  const id = text162.x['animVal'][0].value+""+text162.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text162.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text162.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text162.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text162.setAttribute('x', valueXIncrease);
      text162.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text162.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text162.setAttribute('x', valueXIncrease);
      text162.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text162.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text162.setAttribute('y', valueXIncrease);
      text162.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text162.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text162.setAttribute('y', valueXIncrease);
      text162.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF162, 0.001);
const text163 = document.getElementById('text163');
text163.innerHTML = number[0];
function textF163() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text163.setAttribute('fill', newColor);
  const id = text163.x['animVal'][0].value+""+text163.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text163.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text163.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text163.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text163.setAttribute('x', valueXIncrease);
      text163.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text163.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text163.setAttribute('x', valueXIncrease);
      text163.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text163.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text163.setAttribute('y', valueXIncrease);
      text163.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text163.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text163.setAttribute('y', valueXIncrease);
      text163.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF163, 0.001);
const text164 = document.getElementById('text164');
text164.innerHTML = number[0];
function textF164() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text164.setAttribute('fill', newColor);
  const id = text164.x['animVal'][0].value+""+text164.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text164.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text164.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text164.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text164.setAttribute('x', valueXIncrease);
      text164.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text164.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text164.setAttribute('x', valueXIncrease);
      text164.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text164.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text164.setAttribute('y', valueXIncrease);
      text164.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text164.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text164.setAttribute('y', valueXIncrease);
      text164.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF164, 0.001);
const text165 = document.getElementById('text165');
text165.innerHTML = number[0];
function textF165() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text165.setAttribute('fill', newColor);
  const id = text165.x['animVal'][0].value+""+text165.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text165.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text165.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text165.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text165.setAttribute('x', valueXIncrease);
      text165.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text165.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text165.setAttribute('x', valueXIncrease);
      text165.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text165.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text165.setAttribute('y', valueXIncrease);
      text165.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text165.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text165.setAttribute('y', valueXIncrease);
      text165.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF165, 0.001);
const text166 = document.getElementById('text166');
text166.innerHTML = number[0];
function textF166() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text166.setAttribute('fill', newColor);
  const id = text166.x['animVal'][0].value+""+text166.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text166.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text166.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text166.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text166.setAttribute('x', valueXIncrease);
      text166.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text166.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text166.setAttribute('x', valueXIncrease);
      text166.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text166.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text166.setAttribute('y', valueXIncrease);
      text166.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text166.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text166.setAttribute('y', valueXIncrease);
      text166.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF166, 0.001);
const text167 = document.getElementById('text167');
text167.innerHTML = number[0];
function textF167() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text167.setAttribute('fill', newColor);
  const id = text167.x['animVal'][0].value+""+text167.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text167.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text167.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text167.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text167.setAttribute('x', valueXIncrease);
      text167.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text167.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text167.setAttribute('x', valueXIncrease);
      text167.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text167.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text167.setAttribute('y', valueXIncrease);
      text167.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text167.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text167.setAttribute('y', valueXIncrease);
      text167.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF167, 0.001);
const text168 = document.getElementById('text168');
text168.innerHTML = number[0];
function textF168() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text168.setAttribute('fill', newColor);
  const id = text168.x['animVal'][0].value+""+text168.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text168.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text168.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text168.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text168.setAttribute('x', valueXIncrease);
      text168.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text168.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text168.setAttribute('x', valueXIncrease);
      text168.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text168.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text168.setAttribute('y', valueXIncrease);
      text168.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text168.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text168.setAttribute('y', valueXIncrease);
      text168.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF168, 0.001);
const text169 = document.getElementById('text169');
text169.innerHTML = number[0];
function textF169() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text169.setAttribute('fill', newColor);
  const id = text169.x['animVal'][0].value+""+text169.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text169.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text169.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text169.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text169.setAttribute('x', valueXIncrease);
      text169.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text169.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text169.setAttribute('x', valueXIncrease);
      text169.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text169.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text169.setAttribute('y', valueXIncrease);
      text169.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text169.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text169.setAttribute('y', valueXIncrease);
      text169.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF169, 0.001);
const text170 = document.getElementById('text170');
text170.innerHTML = number[0];
function textF170() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text170.setAttribute('fill', newColor);
  const id = text170.x['animVal'][0].value+""+text170.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text170.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text170.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text170.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text170.setAttribute('x', valueXIncrease);
      text170.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text170.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text170.setAttribute('x', valueXIncrease);
      text170.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text170.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text170.setAttribute('y', valueXIncrease);
      text170.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text170.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text170.setAttribute('y', valueXIncrease);
      text170.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF170, 0.001);
const text171 = document.getElementById('text171');
text171.innerHTML = number[0];
function textF171() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text171.setAttribute('fill', newColor);
  const id = text171.x['animVal'][0].value+""+text171.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text171.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text171.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text171.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text171.setAttribute('x', valueXIncrease);
      text171.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text171.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text171.setAttribute('x', valueXIncrease);
      text171.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text171.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text171.setAttribute('y', valueXIncrease);
      text171.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text171.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text171.setAttribute('y', valueXIncrease);
      text171.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF171, 0.001);
const text172 = document.getElementById('text172');
text172.innerHTML = number[0];
function textF172() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text172.setAttribute('fill', newColor);
  const id = text172.x['animVal'][0].value+""+text172.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text172.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text172.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text172.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text172.setAttribute('x', valueXIncrease);
      text172.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text172.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text172.setAttribute('x', valueXIncrease);
      text172.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text172.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text172.setAttribute('y', valueXIncrease);
      text172.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text172.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text172.setAttribute('y', valueXIncrease);
      text172.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF172, 0.001);
const text173 = document.getElementById('text173');
text173.innerHTML = number[0];
function textF173() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text173.setAttribute('fill', newColor);
  const id = text173.x['animVal'][0].value+""+text173.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text173.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text173.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text173.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text173.setAttribute('x', valueXIncrease);
      text173.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text173.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text173.setAttribute('x', valueXIncrease);
      text173.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text173.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text173.setAttribute('y', valueXIncrease);
      text173.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text173.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text173.setAttribute('y', valueXIncrease);
      text173.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF173, 0.001);
const text174 = document.getElementById('text174');
text174.innerHTML = number[0];
function textF174() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text174.setAttribute('fill', newColor);
  const id = text174.x['animVal'][0].value+""+text174.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text174.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text174.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text174.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text174.setAttribute('x', valueXIncrease);
      text174.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text174.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text174.setAttribute('x', valueXIncrease);
      text174.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text174.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text174.setAttribute('y', valueXIncrease);
      text174.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text174.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text174.setAttribute('y', valueXIncrease);
      text174.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF174, 0.001);
const text175 = document.getElementById('text175');
text175.innerHTML = number[0];
function textF175() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text175.setAttribute('fill', newColor);
  const id = text175.x['animVal'][0].value+""+text175.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text175.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text175.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text175.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text175.setAttribute('x', valueXIncrease);
      text175.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text175.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text175.setAttribute('x', valueXIncrease);
      text175.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text175.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text175.setAttribute('y', valueXIncrease);
      text175.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text175.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text175.setAttribute('y', valueXIncrease);
      text175.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF175, 0.001);
const text176 = document.getElementById('text176');
text176.innerHTML = number[0];
function textF176() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text176.setAttribute('fill', newColor);
  const id = text176.x['animVal'][0].value+""+text176.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text176.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text176.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text176.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text176.setAttribute('x', valueXIncrease);
      text176.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text176.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text176.setAttribute('x', valueXIncrease);
      text176.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text176.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text176.setAttribute('y', valueXIncrease);
      text176.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text176.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text176.setAttribute('y', valueXIncrease);
      text176.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF176, 0.001);
const text177 = document.getElementById('text177');
text177.innerHTML = number[0];
function textF177() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text177.setAttribute('fill', newColor);
  const id = text177.x['animVal'][0].value+""+text177.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text177.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text177.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text177.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text177.setAttribute('x', valueXIncrease);
      text177.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text177.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text177.setAttribute('x', valueXIncrease);
      text177.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text177.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text177.setAttribute('y', valueXIncrease);
      text177.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text177.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text177.setAttribute('y', valueXIncrease);
      text177.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF177, 0.001);
const text178 = document.getElementById('text178');
text178.innerHTML = number[0];
function textF178() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text178.setAttribute('fill', newColor);
  const id = text178.x['animVal'][0].value+""+text178.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text178.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text178.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text178.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text178.setAttribute('x', valueXIncrease);
      text178.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text178.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text178.setAttribute('x', valueXIncrease);
      text178.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text178.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text178.setAttribute('y', valueXIncrease);
      text178.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text178.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text178.setAttribute('y', valueXIncrease);
      text178.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF178, 0.001);
const text179 = document.getElementById('text179');
text179.innerHTML = number[0];
function textF179() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text179.setAttribute('fill', newColor);
  const id = text179.x['animVal'][0].value+""+text179.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text179.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text179.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text179.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text179.setAttribute('x', valueXIncrease);
      text179.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text179.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text179.setAttribute('x', valueXIncrease);
      text179.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text179.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text179.setAttribute('y', valueXIncrease);
      text179.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text179.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text179.setAttribute('y', valueXIncrease);
      text179.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF179, 0.001);
const text180 = document.getElementById('text180');
text180.innerHTML = number[0];
function textF180() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text180.setAttribute('fill', newColor);
  const id = text180.x['animVal'][0].value+""+text180.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text180.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text180.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text180.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text180.setAttribute('x', valueXIncrease);
      text180.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text180.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text180.setAttribute('x', valueXIncrease);
      text180.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text180.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text180.setAttribute('y', valueXIncrease);
      text180.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text180.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text180.setAttribute('y', valueXIncrease);
      text180.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF180, 0.001);
const text181 = document.getElementById('text181');
text181.innerHTML = number[0];
function textF181() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text181.setAttribute('fill', newColor);
  const id = text181.x['animVal'][0].value+""+text181.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text181.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text181.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text181.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text181.setAttribute('x', valueXIncrease);
      text181.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text181.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text181.setAttribute('x', valueXIncrease);
      text181.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text181.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text181.setAttribute('y', valueXIncrease);
      text181.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text181.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text181.setAttribute('y', valueXIncrease);
      text181.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF181, 0.001);
const text182 = document.getElementById('text182');
text182.innerHTML = number[0];
function textF182() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text182.setAttribute('fill', newColor);
  const id = text182.x['animVal'][0].value+""+text182.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text182.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text182.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text182.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text182.setAttribute('x', valueXIncrease);
      text182.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text182.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text182.setAttribute('x', valueXIncrease);
      text182.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text182.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text182.setAttribute('y', valueXIncrease);
      text182.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text182.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text182.setAttribute('y', valueXIncrease);
      text182.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF182, 0.001);
const text183 = document.getElementById('text183');
text183.innerHTML = number[0];
function textF183() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text183.setAttribute('fill', newColor);
  const id = text183.x['animVal'][0].value+""+text183.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text183.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text183.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text183.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text183.setAttribute('x', valueXIncrease);
      text183.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text183.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text183.setAttribute('x', valueXIncrease);
      text183.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text183.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text183.setAttribute('y', valueXIncrease);
      text183.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text183.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text183.setAttribute('y', valueXIncrease);
      text183.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF183, 0.001);
const text184 = document.getElementById('text184');
text184.innerHTML = number[0];
function textF184() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text184.setAttribute('fill', newColor);
  const id = text184.x['animVal'][0].value+""+text184.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text184.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text184.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text184.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text184.setAttribute('x', valueXIncrease);
      text184.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text184.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text184.setAttribute('x', valueXIncrease);
      text184.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text184.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text184.setAttribute('y', valueXIncrease);
      text184.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text184.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text184.setAttribute('y', valueXIncrease);
      text184.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF184, 0.001);
const text185 = document.getElementById('text185');
text185.innerHTML = number[0];
function textF185() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text185.setAttribute('fill', newColor);
  const id = text185.x['animVal'][0].value+""+text185.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text185.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text185.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text185.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text185.setAttribute('x', valueXIncrease);
      text185.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text185.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text185.setAttribute('x', valueXIncrease);
      text185.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text185.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text185.setAttribute('y', valueXIncrease);
      text185.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text185.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text185.setAttribute('y', valueXIncrease);
      text185.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF185, 0.001);
const text186 = document.getElementById('text186');
text186.innerHTML = number[0];
function textF186() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text186.setAttribute('fill', newColor);
  const id = text186.x['animVal'][0].value+""+text186.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text186.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text186.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text186.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text186.setAttribute('x', valueXIncrease);
      text186.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text186.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text186.setAttribute('x', valueXIncrease);
      text186.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text186.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text186.setAttribute('y', valueXIncrease);
      text186.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text186.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text186.setAttribute('y', valueXIncrease);
      text186.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF186, 0.001);
const text187 = document.getElementById('text187');
text187.innerHTML = number[0];
function textF187() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text187.setAttribute('fill', newColor);
  const id = text187.x['animVal'][0].value+""+text187.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text187.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text187.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text187.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text187.setAttribute('x', valueXIncrease);
      text187.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text187.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text187.setAttribute('x', valueXIncrease);
      text187.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text187.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text187.setAttribute('y', valueXIncrease);
      text187.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text187.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text187.setAttribute('y', valueXIncrease);
      text187.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF187, 0.001);
const text188 = document.getElementById('text188');
text188.innerHTML = number[0];
function textF188() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text188.setAttribute('fill', newColor);
  const id = text188.x['animVal'][0].value+""+text188.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text188.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text188.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text188.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text188.setAttribute('x', valueXIncrease);
      text188.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text188.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text188.setAttribute('x', valueXIncrease);
      text188.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text188.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text188.setAttribute('y', valueXIncrease);
      text188.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text188.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text188.setAttribute('y', valueXIncrease);
      text188.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF188, 0.001);
const text189 = document.getElementById('text189');
text189.innerHTML = number[0];
function textF189() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text189.setAttribute('fill', newColor);
  const id = text189.x['animVal'][0].value+""+text189.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text189.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text189.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text189.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text189.setAttribute('x', valueXIncrease);
      text189.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text189.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text189.setAttribute('x', valueXIncrease);
      text189.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text189.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text189.setAttribute('y', valueXIncrease);
      text189.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text189.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text189.setAttribute('y', valueXIncrease);
      text189.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF189, 0.001);
const text190 = document.getElementById('text190');
text190.innerHTML = number[0];
function textF190() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text190.setAttribute('fill', newColor);
  const id = text190.x['animVal'][0].value+""+text190.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text190.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text190.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text190.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text190.setAttribute('x', valueXIncrease);
      text190.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text190.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text190.setAttribute('x', valueXIncrease);
      text190.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text190.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text190.setAttribute('y', valueXIncrease);
      text190.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text190.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text190.setAttribute('y', valueXIncrease);
      text190.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF190, 0.001);
const text191 = document.getElementById('text191');
text191.innerHTML = number[0];
function textF191() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text191.setAttribute('fill', newColor);
  const id = text191.x['animVal'][0].value+""+text191.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text191.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text191.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text191.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text191.setAttribute('x', valueXIncrease);
      text191.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text191.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text191.setAttribute('x', valueXIncrease);
      text191.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text191.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text191.setAttribute('y', valueXIncrease);
      text191.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text191.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text191.setAttribute('y', valueXIncrease);
      text191.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF191, 0.001);
const text192 = document.getElementById('text192');
text192.innerHTML = number[0];
function textF192() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text192.setAttribute('fill', newColor);
  const id = text192.x['animVal'][0].value+""+text192.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text192.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text192.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text192.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text192.setAttribute('x', valueXIncrease);
      text192.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text192.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text192.setAttribute('x', valueXIncrease);
      text192.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text192.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text192.setAttribute('y', valueXIncrease);
      text192.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text192.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text192.setAttribute('y', valueXIncrease);
      text192.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF192, 0.001);
const text193 = document.getElementById('text193');
text193.innerHTML = number[0];
function textF193() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text193.setAttribute('fill', newColor);
  const id = text193.x['animVal'][0].value+""+text193.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text193.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text193.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text193.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text193.setAttribute('x', valueXIncrease);
      text193.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text193.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text193.setAttribute('x', valueXIncrease);
      text193.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text193.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text193.setAttribute('y', valueXIncrease);
      text193.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text193.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text193.setAttribute('y', valueXIncrease);
      text193.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF193, 0.001);
const text194 = document.getElementById('text194');
text194.innerHTML = number[0];
function textF194() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text194.setAttribute('fill', newColor);
  const id = text194.x['animVal'][0].value+""+text194.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text194.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text194.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text194.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text194.setAttribute('x', valueXIncrease);
      text194.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text194.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text194.setAttribute('x', valueXIncrease);
      text194.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text194.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text194.setAttribute('y', valueXIncrease);
      text194.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text194.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text194.setAttribute('y', valueXIncrease);
      text194.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF194, 0.001);
const text195 = document.getElementById('text195');
text195.innerHTML = number[0];
function textF195() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text195.setAttribute('fill', newColor);
  const id = text195.x['animVal'][0].value+""+text195.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text195.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text195.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text195.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text195.setAttribute('x', valueXIncrease);
      text195.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text195.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text195.setAttribute('x', valueXIncrease);
      text195.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text195.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text195.setAttribute('y', valueXIncrease);
      text195.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text195.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text195.setAttribute('y', valueXIncrease);
      text195.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF195, 0.001);
const text196 = document.getElementById('text196');
text196.innerHTML = number[0];
function textF196() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text196.setAttribute('fill', newColor);
  const id = text196.x['animVal'][0].value+""+text196.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text196.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text196.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text196.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text196.setAttribute('x', valueXIncrease);
      text196.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text196.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text196.setAttribute('x', valueXIncrease);
      text196.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text196.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text196.setAttribute('y', valueXIncrease);
      text196.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text196.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text196.setAttribute('y', valueXIncrease);
      text196.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF196, 0.001);
const text197 = document.getElementById('text197');
text197.innerHTML = number[0];
function textF197() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text197.setAttribute('fill', newColor);
  const id = text197.x['animVal'][0].value+""+text197.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text197.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text197.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text197.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text197.setAttribute('x', valueXIncrease);
      text197.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text197.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text197.setAttribute('x', valueXIncrease);
      text197.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text197.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text197.setAttribute('y', valueXIncrease);
      text197.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text197.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text197.setAttribute('y', valueXIncrease);
      text197.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF197, 0.001);
const text198 = document.getElementById('text198');
text198.innerHTML = number[0];
function textF198() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text198.setAttribute('fill', newColor);
  const id = text198.x['animVal'][0].value+""+text198.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text198.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text198.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text198.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text198.setAttribute('x', valueXIncrease);
      text198.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text198.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text198.setAttribute('x', valueXIncrease);
      text198.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text198.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text198.setAttribute('y', valueXIncrease);
      text198.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text198.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text198.setAttribute('y', valueXIncrease);
      text198.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF198, 0.001);
const text199 = document.getElementById('text199');
text199.innerHTML = number[0];
function textF199() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text199.setAttribute('fill', newColor);
  const id = text199.x['animVal'][0].value+""+text199.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text199.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text199.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text199.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text199.setAttribute('x', valueXIncrease);
      text199.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text199.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text199.setAttribute('x', valueXIncrease);
      text199.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text199.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text199.setAttribute('y', valueXIncrease);
      text199.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text199.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text199.setAttribute('y', valueXIncrease);
      text199.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF199, 0.001);
const text200 = document.getElementById('text200');
text200.innerHTML = number[0];
function textF200() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text200.setAttribute('fill', newColor);
  const id = text200.x['animVal'][0].value+""+text200.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text200.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text200.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text200.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text200.setAttribute('x', valueXIncrease);
      text200.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text200.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text200.setAttribute('x', valueXIncrease);
      text200.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text200.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text200.setAttribute('y', valueXIncrease);
      text200.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text200.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text200.setAttribute('y', valueXIncrease);
      text200.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF200, 0.001);
const text201 = document.getElementById('text201');
text201.innerHTML = number[0];
function textF201() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text201.setAttribute('fill', newColor);
  const id = text201.x['animVal'][0].value+""+text201.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text201.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text201.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text201.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text201.setAttribute('x', valueXIncrease);
      text201.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text201.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text201.setAttribute('x', valueXIncrease);
      text201.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text201.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text201.setAttribute('y', valueXIncrease);
      text201.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text201.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text201.setAttribute('y', valueXIncrease);
      text201.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF201, 0.001);
const text202 = document.getElementById('text202');
text202.innerHTML = number[0];
function textF202() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text202.setAttribute('fill', newColor);
  const id = text202.x['animVal'][0].value+""+text202.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text202.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text202.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text202.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text202.setAttribute('x', valueXIncrease);
      text202.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text202.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text202.setAttribute('x', valueXIncrease);
      text202.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text202.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text202.setAttribute('y', valueXIncrease);
      text202.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text202.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text202.setAttribute('y', valueXIncrease);
      text202.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF202, 0.001);
const text203 = document.getElementById('text203');
text203.innerHTML = number[0];
function textF203() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text203.setAttribute('fill', newColor);
  const id = text203.x['animVal'][0].value+""+text203.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text203.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text203.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text203.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text203.setAttribute('x', valueXIncrease);
      text203.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text203.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text203.setAttribute('x', valueXIncrease);
      text203.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text203.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text203.setAttribute('y', valueXIncrease);
      text203.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text203.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text203.setAttribute('y', valueXIncrease);
      text203.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF203, 0.001);
const text204 = document.getElementById('text204');
text204.innerHTML = number[0];
function textF204() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text204.setAttribute('fill', newColor);
  const id = text204.x['animVal'][0].value+""+text204.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text204.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text204.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text204.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text204.setAttribute('x', valueXIncrease);
      text204.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text204.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text204.setAttribute('x', valueXIncrease);
      text204.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text204.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text204.setAttribute('y', valueXIncrease);
      text204.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text204.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text204.setAttribute('y', valueXIncrease);
      text204.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF204, 0.001);
const text205 = document.getElementById('text205');
text205.innerHTML = number[0];
function textF205() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text205.setAttribute('fill', newColor);
  const id = text205.x['animVal'][0].value+""+text205.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text205.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text205.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text205.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text205.setAttribute('x', valueXIncrease);
      text205.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text205.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text205.setAttribute('x', valueXIncrease);
      text205.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text205.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text205.setAttribute('y', valueXIncrease);
      text205.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text205.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text205.setAttribute('y', valueXIncrease);
      text205.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF205, 0.001);
const text206 = document.getElementById('text206');
text206.innerHTML = number[0];
function textF206() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text206.setAttribute('fill', newColor);
  const id = text206.x['animVal'][0].value+""+text206.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text206.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text206.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text206.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text206.setAttribute('x', valueXIncrease);
      text206.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text206.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text206.setAttribute('x', valueXIncrease);
      text206.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text206.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text206.setAttribute('y', valueXIncrease);
      text206.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text206.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text206.setAttribute('y', valueXIncrease);
      text206.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF206, 0.001);
const text207 = document.getElementById('text207');
text207.innerHTML = number[0];
function textF207() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text207.setAttribute('fill', newColor);
  const id = text207.x['animVal'][0].value+""+text207.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text207.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text207.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text207.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text207.setAttribute('x', valueXIncrease);
      text207.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text207.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text207.setAttribute('x', valueXIncrease);
      text207.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text207.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text207.setAttribute('y', valueXIncrease);
      text207.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text207.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text207.setAttribute('y', valueXIncrease);
      text207.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF207, 0.001);
const text208 = document.getElementById('text208');
text208.innerHTML = number[0];
function textF208() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text208.setAttribute('fill', newColor);
  const id = text208.x['animVal'][0].value+""+text208.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text208.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text208.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text208.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text208.setAttribute('x', valueXIncrease);
      text208.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text208.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text208.setAttribute('x', valueXIncrease);
      text208.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text208.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text208.setAttribute('y', valueXIncrease);
      text208.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text208.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text208.setAttribute('y', valueXIncrease);
      text208.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF208, 0.001);
const text209 = document.getElementById('text209');
text209.innerHTML = number[0];
function textF209() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text209.setAttribute('fill', newColor);
  const id = text209.x['animVal'][0].value+""+text209.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text209.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text209.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text209.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text209.setAttribute('x', valueXIncrease);
      text209.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text209.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text209.setAttribute('x', valueXIncrease);
      text209.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text209.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text209.setAttribute('y', valueXIncrease);
      text209.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text209.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text209.setAttribute('y', valueXIncrease);
      text209.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF209, 0.001);
const text210 = document.getElementById('text210');
text210.innerHTML = number[0];
function textF210() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text210.setAttribute('fill', newColor);
  const id = text210.x['animVal'][0].value+""+text210.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text210.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text210.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text210.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text210.setAttribute('x', valueXIncrease);
      text210.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text210.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text210.setAttribute('x', valueXIncrease);
      text210.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text210.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text210.setAttribute('y', valueXIncrease);
      text210.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text210.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text210.setAttribute('y', valueXIncrease);
      text210.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF210, 0.001);
const text211 = document.getElementById('text211');
text211.innerHTML = number[0];
function textF211() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text211.setAttribute('fill', newColor);
  const id = text211.x['animVal'][0].value+""+text211.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text211.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text211.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text211.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text211.setAttribute('x', valueXIncrease);
      text211.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text211.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text211.setAttribute('x', valueXIncrease);
      text211.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text211.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text211.setAttribute('y', valueXIncrease);
      text211.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text211.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text211.setAttribute('y', valueXIncrease);
      text211.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF211, 0.001);
const text212 = document.getElementById('text212');
text212.innerHTML = number[0];
function textF212() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text212.setAttribute('fill', newColor);
  const id = text212.x['animVal'][0].value+""+text212.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text212.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text212.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text212.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text212.setAttribute('x', valueXIncrease);
      text212.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text212.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text212.setAttribute('x', valueXIncrease);
      text212.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text212.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text212.setAttribute('y', valueXIncrease);
      text212.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text212.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text212.setAttribute('y', valueXIncrease);
      text212.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF212, 0.001);
const text213 = document.getElementById('text213');
text213.innerHTML = number[0];
function textF213() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text213.setAttribute('fill', newColor);
  const id = text213.x['animVal'][0].value+""+text213.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text213.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text213.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text213.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text213.setAttribute('x', valueXIncrease);
      text213.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text213.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text213.setAttribute('x', valueXIncrease);
      text213.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text213.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text213.setAttribute('y', valueXIncrease);
      text213.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text213.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text213.setAttribute('y', valueXIncrease);
      text213.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF213, 0.001);
const text214 = document.getElementById('text214');
text214.innerHTML = number[0];
function textF214() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text214.setAttribute('fill', newColor);
  const id = text214.x['animVal'][0].value+""+text214.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text214.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text214.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text214.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text214.setAttribute('x', valueXIncrease);
      text214.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text214.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text214.setAttribute('x', valueXIncrease);
      text214.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text214.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text214.setAttribute('y', valueXIncrease);
      text214.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text214.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text214.setAttribute('y', valueXIncrease);
      text214.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF214, 0.001);
const text215 = document.getElementById('text215');
text215.innerHTML = number[0];
function textF215() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text215.setAttribute('fill', newColor);
  const id = text215.x['animVal'][0].value+""+text215.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text215.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text215.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text215.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text215.setAttribute('x', valueXIncrease);
      text215.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text215.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text215.setAttribute('x', valueXIncrease);
      text215.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text215.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text215.setAttribute('y', valueXIncrease);
      text215.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text215.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text215.setAttribute('y', valueXIncrease);
      text215.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF215, 0.001);
const text216 = document.getElementById('text216');
text216.innerHTML = number[0];
function textF216() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text216.setAttribute('fill', newColor);
  const id = text216.x['animVal'][0].value+""+text216.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text216.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text216.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text216.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text216.setAttribute('x', valueXIncrease);
      text216.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text216.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text216.setAttribute('x', valueXIncrease);
      text216.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text216.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text216.setAttribute('y', valueXIncrease);
      text216.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text216.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text216.setAttribute('y', valueXIncrease);
      text216.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF216, 0.001);
const text217 = document.getElementById('text217');
text217.innerHTML = number[0];
function textF217() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text217.setAttribute('fill', newColor);
  const id = text217.x['animVal'][0].value+""+text217.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text217.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text217.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text217.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text217.setAttribute('x', valueXIncrease);
      text217.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text217.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text217.setAttribute('x', valueXIncrease);
      text217.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text217.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text217.setAttribute('y', valueXIncrease);
      text217.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text217.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text217.setAttribute('y', valueXIncrease);
      text217.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF217, 0.001);
const text218 = document.getElementById('text218');
text218.innerHTML = number[0];
function textF218() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text218.setAttribute('fill', newColor);
  const id = text218.x['animVal'][0].value+""+text218.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text218.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text218.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text218.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text218.setAttribute('x', valueXIncrease);
      text218.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text218.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text218.setAttribute('x', valueXIncrease);
      text218.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text218.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text218.setAttribute('y', valueXIncrease);
      text218.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text218.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text218.setAttribute('y', valueXIncrease);
      text218.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF218, 0.001);
const text219 = document.getElementById('text219');
text219.innerHTML = number[0];
function textF219() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text219.setAttribute('fill', newColor);
  const id = text219.x['animVal'][0].value+""+text219.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text219.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text219.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text219.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text219.setAttribute('x', valueXIncrease);
      text219.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text219.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text219.setAttribute('x', valueXIncrease);
      text219.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text219.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text219.setAttribute('y', valueXIncrease);
      text219.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text219.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text219.setAttribute('y', valueXIncrease);
      text219.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF219, 0.001);
const text220 = document.getElementById('text220');
text220.innerHTML = number[0];
function textF220() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text220.setAttribute('fill', newColor);
  const id = text220.x['animVal'][0].value+""+text220.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text220.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text220.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text220.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text220.setAttribute('x', valueXIncrease);
      text220.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text220.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text220.setAttribute('x', valueXIncrease);
      text220.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text220.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text220.setAttribute('y', valueXIncrease);
      text220.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text220.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text220.setAttribute('y', valueXIncrease);
      text220.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF220, 0.001);
const text221 = document.getElementById('text221');
text221.innerHTML = number[0];
function textF221() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text221.setAttribute('fill', newColor);
  const id = text221.x['animVal'][0].value+""+text221.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text221.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text221.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text221.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text221.setAttribute('x', valueXIncrease);
      text221.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text221.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text221.setAttribute('x', valueXIncrease);
      text221.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text221.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text221.setAttribute('y', valueXIncrease);
      text221.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text221.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text221.setAttribute('y', valueXIncrease);
      text221.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF221, 0.001);
const text222 = document.getElementById('text222');
text222.innerHTML = number[0];
function textF222() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text222.setAttribute('fill', newColor);
  const id = text222.x['animVal'][0].value+""+text222.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text222.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text222.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text222.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text222.setAttribute('x', valueXIncrease);
      text222.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text222.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text222.setAttribute('x', valueXIncrease);
      text222.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text222.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text222.setAttribute('y', valueXIncrease);
      text222.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text222.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text222.setAttribute('y', valueXIncrease);
      text222.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF222, 0.001);
const text223 = document.getElementById('text223');
text223.innerHTML = number[0];
function textF223() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text223.setAttribute('fill', newColor);
  const id = text223.x['animVal'][0].value+""+text223.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text223.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text223.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text223.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text223.setAttribute('x', valueXIncrease);
      text223.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text223.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text223.setAttribute('x', valueXIncrease);
      text223.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text223.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text223.setAttribute('y', valueXIncrease);
      text223.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text223.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text223.setAttribute('y', valueXIncrease);
      text223.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF223, 0.001);
const text224 = document.getElementById('text224');
text224.innerHTML = number[0];
function textF224() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text224.setAttribute('fill', newColor);
  const id = text224.x['animVal'][0].value+""+text224.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text224.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text224.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text224.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text224.setAttribute('x', valueXIncrease);
      text224.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text224.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text224.setAttribute('x', valueXIncrease);
      text224.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text224.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text224.setAttribute('y', valueXIncrease);
      text224.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text224.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text224.setAttribute('y', valueXIncrease);
      text224.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF224, 0.001);
const text225 = document.getElementById('text225');
text225.innerHTML = number[0];
function textF225() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text225.setAttribute('fill', newColor);
  const id = text225.x['animVal'][0].value+""+text225.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text225.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text225.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text225.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text225.setAttribute('x', valueXIncrease);
      text225.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text225.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text225.setAttribute('x', valueXIncrease);
      text225.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text225.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text225.setAttribute('y', valueXIncrease);
      text225.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text225.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text225.setAttribute('y', valueXIncrease);
      text225.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF225, 0.001);
const text226 = document.getElementById('text226');
text226.innerHTML = number[0];
function textF226() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text226.setAttribute('fill', newColor);
  const id = text226.x['animVal'][0].value+""+text226.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text226.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text226.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text226.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text226.setAttribute('x', valueXIncrease);
      text226.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text226.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text226.setAttribute('x', valueXIncrease);
      text226.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text226.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text226.setAttribute('y', valueXIncrease);
      text226.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text226.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text226.setAttribute('y', valueXIncrease);
      text226.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF226, 0.001);
const text227 = document.getElementById('text227');
text227.innerHTML = number[0];
function textF227() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text227.setAttribute('fill', newColor);
  const id = text227.x['animVal'][0].value+""+text227.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text227.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text227.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text227.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text227.setAttribute('x', valueXIncrease);
      text227.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text227.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text227.setAttribute('x', valueXIncrease);
      text227.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text227.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text227.setAttribute('y', valueXIncrease);
      text227.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text227.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text227.setAttribute('y', valueXIncrease);
      text227.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF227, 0.001);
const text228 = document.getElementById('text228');
text228.innerHTML = number[0];
function textF228() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text228.setAttribute('fill', newColor);
  const id = text228.x['animVal'][0].value+""+text228.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text228.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text228.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text228.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text228.setAttribute('x', valueXIncrease);
      text228.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text228.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text228.setAttribute('x', valueXIncrease);
      text228.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text228.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text228.setAttribute('y', valueXIncrease);
      text228.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text228.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text228.setAttribute('y', valueXIncrease);
      text228.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF228, 0.001);
const text229 = document.getElementById('text229');
text229.innerHTML = number[0];
function textF229() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text229.setAttribute('fill', newColor);
  const id = text229.x['animVal'][0].value+""+text229.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text229.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text229.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text229.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text229.setAttribute('x', valueXIncrease);
      text229.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text229.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text229.setAttribute('x', valueXIncrease);
      text229.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text229.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text229.setAttribute('y', valueXIncrease);
      text229.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text229.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text229.setAttribute('y', valueXIncrease);
      text229.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF229, 0.001);
const text230 = document.getElementById('text230');
text230.innerHTML = number[0];
function textF230() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text230.setAttribute('fill', newColor);
  const id = text230.x['animVal'][0].value+""+text230.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text230.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text230.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text230.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text230.setAttribute('x', valueXIncrease);
      text230.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text230.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text230.setAttribute('x', valueXIncrease);
      text230.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text230.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text230.setAttribute('y', valueXIncrease);
      text230.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text230.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text230.setAttribute('y', valueXIncrease);
      text230.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF230, 0.001);
const text231 = document.getElementById('text231');
text231.innerHTML = number[0];
function textF231() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text231.setAttribute('fill', newColor);
  const id = text231.x['animVal'][0].value+""+text231.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text231.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text231.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text231.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text231.setAttribute('x', valueXIncrease);
      text231.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text231.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text231.setAttribute('x', valueXIncrease);
      text231.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text231.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text231.setAttribute('y', valueXIncrease);
      text231.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text231.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text231.setAttribute('y', valueXIncrease);
      text231.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF231, 0.001);
const text232 = document.getElementById('text232');
text232.innerHTML = number[0];
function textF232() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text232.setAttribute('fill', newColor);
  const id = text232.x['animVal'][0].value+""+text232.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text232.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text232.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text232.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text232.setAttribute('x', valueXIncrease);
      text232.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text232.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text232.setAttribute('x', valueXIncrease);
      text232.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text232.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text232.setAttribute('y', valueXIncrease);
      text232.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text232.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text232.setAttribute('y', valueXIncrease);
      text232.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF232, 0.001);
const text233 = document.getElementById('text233');
text233.innerHTML = number[0];
function textF233() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text233.setAttribute('fill', newColor);
  const id = text233.x['animVal'][0].value+""+text233.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text233.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text233.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text233.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text233.setAttribute('x', valueXIncrease);
      text233.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text233.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text233.setAttribute('x', valueXIncrease);
      text233.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text233.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text233.setAttribute('y', valueXIncrease);
      text233.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text233.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text233.setAttribute('y', valueXIncrease);
      text233.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF233, 0.001);
const text234 = document.getElementById('text234');
text234.innerHTML = number[0];
function textF234() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text234.setAttribute('fill', newColor);
  const id = text234.x['animVal'][0].value+""+text234.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text234.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text234.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text234.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text234.setAttribute('x', valueXIncrease);
      text234.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text234.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text234.setAttribute('x', valueXIncrease);
      text234.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text234.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text234.setAttribute('y', valueXIncrease);
      text234.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text234.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text234.setAttribute('y', valueXIncrease);
      text234.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF234, 0.001);
const text235 = document.getElementById('text235');
text235.innerHTML = number[0];
function textF235() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text235.setAttribute('fill', newColor);
  const id = text235.x['animVal'][0].value+""+text235.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text235.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text235.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text235.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text235.setAttribute('x', valueXIncrease);
      text235.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text235.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text235.setAttribute('x', valueXIncrease);
      text235.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text235.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text235.setAttribute('y', valueXIncrease);
      text235.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text235.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text235.setAttribute('y', valueXIncrease);
      text235.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF235, 0.001);
const text236 = document.getElementById('text236');
text236.innerHTML = number[0];
function textF236() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text236.setAttribute('fill', newColor);
  const id = text236.x['animVal'][0].value+""+text236.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text236.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text236.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text236.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text236.setAttribute('x', valueXIncrease);
      text236.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text236.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text236.setAttribute('x', valueXIncrease);
      text236.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text236.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text236.setAttribute('y', valueXIncrease);
      text236.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text236.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text236.setAttribute('y', valueXIncrease);
      text236.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF236, 0.001);
const text237 = document.getElementById('text237');
text237.innerHTML = number[0];
function textF237() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text237.setAttribute('fill', newColor);
  const id = text237.x['animVal'][0].value+""+text237.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text237.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text237.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text237.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text237.setAttribute('x', valueXIncrease);
      text237.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text237.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text237.setAttribute('x', valueXIncrease);
      text237.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text237.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text237.setAttribute('y', valueXIncrease);
      text237.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text237.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text237.setAttribute('y', valueXIncrease);
      text237.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF237, 0.001);
const text238 = document.getElementById('text238');
text238.innerHTML = number[0];
function textF238() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text238.setAttribute('fill', newColor);
  const id = text238.x['animVal'][0].value+""+text238.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text238.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text238.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text238.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text238.setAttribute('x', valueXIncrease);
      text238.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text238.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text238.setAttribute('x', valueXIncrease);
      text238.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text238.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text238.setAttribute('y', valueXIncrease);
      text238.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text238.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text238.setAttribute('y', valueXIncrease);
      text238.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF238, 0.001);
const text239 = document.getElementById('text239');
text239.innerHTML = number[0];
function textF239() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text239.setAttribute('fill', newColor);
  const id = text239.x['animVal'][0].value+""+text239.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text239.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text239.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text239.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text239.setAttribute('x', valueXIncrease);
      text239.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text239.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text239.setAttribute('x', valueXIncrease);
      text239.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text239.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text239.setAttribute('y', valueXIncrease);
      text239.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text239.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text239.setAttribute('y', valueXIncrease);
      text239.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF239, 0.001);
const text240 = document.getElementById('text240');
text240.innerHTML = number[0];
function textF240() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text240.setAttribute('fill', newColor);
  const id = text240.x['animVal'][0].value+""+text240.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text240.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text240.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text240.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text240.setAttribute('x', valueXIncrease);
      text240.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text240.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text240.setAttribute('x', valueXIncrease);
      text240.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text240.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text240.setAttribute('y', valueXIncrease);
      text240.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text240.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text240.setAttribute('y', valueXIncrease);
      text240.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF240, 0.001);
const text241 = document.getElementById('text241');
text241.innerHTML = number[0];
function textF241() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text241.setAttribute('fill', newColor);
  const id = text241.x['animVal'][0].value+""+text241.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text241.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text241.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text241.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text241.setAttribute('x', valueXIncrease);
      text241.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text241.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text241.setAttribute('x', valueXIncrease);
      text241.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text241.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text241.setAttribute('y', valueXIncrease);
      text241.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text241.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text241.setAttribute('y', valueXIncrease);
      text241.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF241, 0.001);
const text242 = document.getElementById('text242');
text242.innerHTML = number[0];
function textF242() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text242.setAttribute('fill', newColor);
  const id = text242.x['animVal'][0].value+""+text242.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text242.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text242.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text242.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text242.setAttribute('x', valueXIncrease);
      text242.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text242.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text242.setAttribute('x', valueXIncrease);
      text242.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text242.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text242.setAttribute('y', valueXIncrease);
      text242.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text242.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text242.setAttribute('y', valueXIncrease);
      text242.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF242, 0.001);
const text243 = document.getElementById('text243');
text243.innerHTML = number[0];
function textF243() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text243.setAttribute('fill', newColor);
  const id = text243.x['animVal'][0].value+""+text243.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text243.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text243.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text243.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text243.setAttribute('x', valueXIncrease);
      text243.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text243.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text243.setAttribute('x', valueXIncrease);
      text243.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text243.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text243.setAttribute('y', valueXIncrease);
      text243.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text243.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text243.setAttribute('y', valueXIncrease);
      text243.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF243, 0.001);
const text244 = document.getElementById('text244');
text244.innerHTML = number[0];
function textF244() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text244.setAttribute('fill', newColor);
  const id = text244.x['animVal'][0].value+""+text244.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text244.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text244.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text244.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text244.setAttribute('x', valueXIncrease);
      text244.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text244.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text244.setAttribute('x', valueXIncrease);
      text244.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text244.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text244.setAttribute('y', valueXIncrease);
      text244.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text244.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text244.setAttribute('y', valueXIncrease);
      text244.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF244, 0.001);
const text245 = document.getElementById('text245');
text245.innerHTML = number[0];
function textF245() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text245.setAttribute('fill', newColor);
  const id = text245.x['animVal'][0].value+""+text245.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text245.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text245.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text245.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text245.setAttribute('x', valueXIncrease);
      text245.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text245.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text245.setAttribute('x', valueXIncrease);
      text245.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text245.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text245.setAttribute('y', valueXIncrease);
      text245.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text245.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text245.setAttribute('y', valueXIncrease);
      text245.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF245, 0.001);
const text246 = document.getElementById('text246');
text246.innerHTML = number[0];
function textF246() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text246.setAttribute('fill', newColor);
  const id = text246.x['animVal'][0].value+""+text246.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text246.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text246.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text246.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text246.setAttribute('x', valueXIncrease);
      text246.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text246.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text246.setAttribute('x', valueXIncrease);
      text246.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text246.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text246.setAttribute('y', valueXIncrease);
      text246.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text246.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text246.setAttribute('y', valueXIncrease);
      text246.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF246, 0.001);
const text247 = document.getElementById('text247');
text247.innerHTML = number[0];
function textF247() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text247.setAttribute('fill', newColor);
  const id = text247.x['animVal'][0].value+""+text247.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text247.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text247.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text247.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text247.setAttribute('x', valueXIncrease);
      text247.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text247.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text247.setAttribute('x', valueXIncrease);
      text247.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text247.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text247.setAttribute('y', valueXIncrease);
      text247.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text247.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text247.setAttribute('y', valueXIncrease);
      text247.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF247, 0.001);
const text248 = document.getElementById('text248');
text248.innerHTML = number[0];
function textF248() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text248.setAttribute('fill', newColor);
  const id = text248.x['animVal'][0].value+""+text248.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text248.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text248.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text248.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text248.setAttribute('x', valueXIncrease);
      text248.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text248.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text248.setAttribute('x', valueXIncrease);
      text248.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text248.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text248.setAttribute('y', valueXIncrease);
      text248.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text248.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text248.setAttribute('y', valueXIncrease);
      text248.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF248, 0.001);
const text249 = document.getElementById('text249');
text249.innerHTML = number[0];
function textF249() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text249.setAttribute('fill', newColor);
  const id = text249.x['animVal'][0].value+""+text249.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text249.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text249.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text249.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text249.setAttribute('x', valueXIncrease);
      text249.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text249.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text249.setAttribute('x', valueXIncrease);
      text249.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text249.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text249.setAttribute('y', valueXIncrease);
      text249.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text249.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text249.setAttribute('y', valueXIncrease);
      text249.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF249, 0.001);
const text250 = document.getElementById('text250');
text250.innerHTML = number[0];
function textF250() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text250.setAttribute('fill', newColor);
  const id = text250.x['animVal'][0].value+""+text250.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text250.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text250.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text250.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text250.setAttribute('x', valueXIncrease);
      text250.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text250.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text250.setAttribute('x', valueXIncrease);
      text250.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text250.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text250.setAttribute('y', valueXIncrease);
      text250.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text250.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text250.setAttribute('y', valueXIncrease);
      text250.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF250, 0.001);
const text251 = document.getElementById('text251');
text251.innerHTML = number[0];
function textF251() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text251.setAttribute('fill', newColor);
  const id = text251.x['animVal'][0].value+""+text251.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text251.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text251.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text251.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text251.setAttribute('x', valueXIncrease);
      text251.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text251.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text251.setAttribute('x', valueXIncrease);
      text251.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text251.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text251.setAttribute('y', valueXIncrease);
      text251.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text251.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text251.setAttribute('y', valueXIncrease);
      text251.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF251, 0.001);
const text252 = document.getElementById('text252');
text252.innerHTML = number[0];
function textF252() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text252.setAttribute('fill', newColor);
  const id = text252.x['animVal'][0].value+""+text252.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text252.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text252.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text252.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text252.setAttribute('x', valueXIncrease);
      text252.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text252.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text252.setAttribute('x', valueXIncrease);
      text252.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text252.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text252.setAttribute('y', valueXIncrease);
      text252.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text252.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text252.setAttribute('y', valueXIncrease);
      text252.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF252, 0.001);
const text253 = document.getElementById('text253');
text253.innerHTML = number[0];
function textF253() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text253.setAttribute('fill', newColor);
  const id = text253.x['animVal'][0].value+""+text253.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text253.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text253.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text253.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text253.setAttribute('x', valueXIncrease);
      text253.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text253.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text253.setAttribute('x', valueXIncrease);
      text253.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text253.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text253.setAttribute('y', valueXIncrease);
      text253.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text253.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text253.setAttribute('y', valueXIncrease);
      text253.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF253, 0.001);
const text254 = document.getElementById('text254');
text254.innerHTML = number[0];
function textF254() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text254.setAttribute('fill', newColor);
  const id = text254.x['animVal'][0].value+""+text254.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text254.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text254.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text254.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text254.setAttribute('x', valueXIncrease);
      text254.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text254.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text254.setAttribute('x', valueXIncrease);
      text254.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text254.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text254.setAttribute('y', valueXIncrease);
      text254.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text254.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text254.setAttribute('y', valueXIncrease);
      text254.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF254, 0.001);
const text255 = document.getElementById('text255');
text255.innerHTML = number[0];
function textF255() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text255.setAttribute('fill', newColor);
  const id = text255.x['animVal'][0].value+""+text255.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text255.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text255.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text255.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text255.setAttribute('x', valueXIncrease);
      text255.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text255.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text255.setAttribute('x', valueXIncrease);
      text255.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text255.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text255.setAttribute('y', valueXIncrease);
      text255.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text255.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text255.setAttribute('y', valueXIncrease);
      text255.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF255, 0.001);
const text256 = document.getElementById('text256');
text256.innerHTML = number[0];
function textF256() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text256.setAttribute('fill', newColor);
  const id = text256.x['animVal'][0].value+""+text256.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text256.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text256.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text256.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text256.setAttribute('x', valueXIncrease);
      text256.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text256.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text256.setAttribute('x', valueXIncrease);
      text256.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text256.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text256.setAttribute('y', valueXIncrease);
      text256.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text256.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text256.setAttribute('y', valueXIncrease);
      text256.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF256, 0.001);
const text257 = document.getElementById('text257');
text257.innerHTML = number[0];
function textF257() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text257.setAttribute('fill', newColor);
  const id = text257.x['animVal'][0].value+""+text257.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text257.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text257.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text257.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text257.setAttribute('x', valueXIncrease);
      text257.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text257.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text257.setAttribute('x', valueXIncrease);
      text257.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text257.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text257.setAttribute('y', valueXIncrease);
      text257.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text257.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text257.setAttribute('y', valueXIncrease);
      text257.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF257, 0.001);
const text258 = document.getElementById('text258');
text258.innerHTML = number[0];
function textF258() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text258.setAttribute('fill', newColor);
  const id = text258.x['animVal'][0].value+""+text258.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text258.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text258.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text258.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text258.setAttribute('x', valueXIncrease);
      text258.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text258.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text258.setAttribute('x', valueXIncrease);
      text258.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text258.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text258.setAttribute('y', valueXIncrease);
      text258.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text258.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text258.setAttribute('y', valueXIncrease);
      text258.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF258, 0.001);
const text259 = document.getElementById('text259');
text259.innerHTML = number[0];
function textF259() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text259.setAttribute('fill', newColor);
  const id = text259.x['animVal'][0].value+""+text259.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text259.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text259.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text259.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text259.setAttribute('x', valueXIncrease);
      text259.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text259.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text259.setAttribute('x', valueXIncrease);
      text259.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text259.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text259.setAttribute('y', valueXIncrease);
      text259.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text259.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text259.setAttribute('y', valueXIncrease);
      text259.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF259, 0.001);
const text260 = document.getElementById('text260');
text260.innerHTML = number[0];
function textF260() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text260.setAttribute('fill', newColor);
  const id = text260.x['animVal'][0].value+""+text260.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text260.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text260.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text260.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text260.setAttribute('x', valueXIncrease);
      text260.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text260.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text260.setAttribute('x', valueXIncrease);
      text260.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text260.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text260.setAttribute('y', valueXIncrease);
      text260.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text260.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text260.setAttribute('y', valueXIncrease);
      text260.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF260, 0.001);
const text261 = document.getElementById('text261');
text261.innerHTML = number[0];
function textF261() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text261.setAttribute('fill', newColor);
  const id = text261.x['animVal'][0].value+""+text261.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text261.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text261.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text261.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text261.setAttribute('x', valueXIncrease);
      text261.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text261.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text261.setAttribute('x', valueXIncrease);
      text261.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text261.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text261.setAttribute('y', valueXIncrease);
      text261.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text261.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text261.setAttribute('y', valueXIncrease);
      text261.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF261, 0.001);
const text262 = document.getElementById('text262');
text262.innerHTML = number[0];
function textF262() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text262.setAttribute('fill', newColor);
  const id = text262.x['animVal'][0].value+""+text262.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text262.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text262.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text262.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text262.setAttribute('x', valueXIncrease);
      text262.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text262.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text262.setAttribute('x', valueXIncrease);
      text262.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text262.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text262.setAttribute('y', valueXIncrease);
      text262.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text262.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text262.setAttribute('y', valueXIncrease);
      text262.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF262, 0.001);
const text263 = document.getElementById('text263');
text263.innerHTML = number[0];
function textF263() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text263.setAttribute('fill', newColor);
  const id = text263.x['animVal'][0].value+""+text263.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text263.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text263.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text263.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text263.setAttribute('x', valueXIncrease);
      text263.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text263.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text263.setAttribute('x', valueXIncrease);
      text263.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text263.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text263.setAttribute('y', valueXIncrease);
      text263.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text263.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text263.setAttribute('y', valueXIncrease);
      text263.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF263, 0.001);
const text264 = document.getElementById('text264');
text264.innerHTML = number[0];
function textF264() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text264.setAttribute('fill', newColor);
  const id = text264.x['animVal'][0].value+""+text264.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text264.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text264.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text264.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text264.setAttribute('x', valueXIncrease);
      text264.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text264.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text264.setAttribute('x', valueXIncrease);
      text264.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text264.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text264.setAttribute('y', valueXIncrease);
      text264.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text264.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text264.setAttribute('y', valueXIncrease);
      text264.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF264, 0.001);
const text265 = document.getElementById('text265');
text265.innerHTML = number[0];
function textF265() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text265.setAttribute('fill', newColor);
  const id = text265.x['animVal'][0].value+""+text265.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text265.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text265.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text265.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text265.setAttribute('x', valueXIncrease);
      text265.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text265.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text265.setAttribute('x', valueXIncrease);
      text265.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text265.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text265.setAttribute('y', valueXIncrease);
      text265.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text265.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text265.setAttribute('y', valueXIncrease);
      text265.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF265, 0.001);
const text266 = document.getElementById('text266');
text266.innerHTML = number[0];
function textF266() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text266.setAttribute('fill', newColor);
  const id = text266.x['animVal'][0].value+""+text266.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text266.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text266.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text266.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text266.setAttribute('x', valueXIncrease);
      text266.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text266.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text266.setAttribute('x', valueXIncrease);
      text266.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text266.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text266.setAttribute('y', valueXIncrease);
      text266.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text266.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text266.setAttribute('y', valueXIncrease);
      text266.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF266, 0.001);
const text267 = document.getElementById('text267');
text267.innerHTML = number[0];
function textF267() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text267.setAttribute('fill', newColor);
  const id = text267.x['animVal'][0].value+""+text267.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text267.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text267.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text267.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text267.setAttribute('x', valueXIncrease);
      text267.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text267.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text267.setAttribute('x', valueXIncrease);
      text267.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text267.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text267.setAttribute('y', valueXIncrease);
      text267.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text267.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text267.setAttribute('y', valueXIncrease);
      text267.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF267, 0.001);
const text268 = document.getElementById('text268');
text268.innerHTML = number[0];
function textF268() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text268.setAttribute('fill', newColor);
  const id = text268.x['animVal'][0].value+""+text268.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text268.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text268.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text268.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text268.setAttribute('x', valueXIncrease);
      text268.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text268.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text268.setAttribute('x', valueXIncrease);
      text268.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text268.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text268.setAttribute('y', valueXIncrease);
      text268.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text268.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text268.setAttribute('y', valueXIncrease);
      text268.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF268, 0.001);
const text269 = document.getElementById('text269');
text269.innerHTML = number[0];
function textF269() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text269.setAttribute('fill', newColor);
  const id = text269.x['animVal'][0].value+""+text269.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text269.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text269.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text269.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text269.setAttribute('x', valueXIncrease);
      text269.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text269.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text269.setAttribute('x', valueXIncrease);
      text269.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text269.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text269.setAttribute('y', valueXIncrease);
      text269.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text269.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text269.setAttribute('y', valueXIncrease);
      text269.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF269, 0.001);
const text270 = document.getElementById('text270');
text270.innerHTML = number[0];
function textF270() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text270.setAttribute('fill', newColor);
  const id = text270.x['animVal'][0].value+""+text270.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text270.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text270.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text270.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text270.setAttribute('x', valueXIncrease);
      text270.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text270.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text270.setAttribute('x', valueXIncrease);
      text270.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text270.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text270.setAttribute('y', valueXIncrease);
      text270.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text270.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text270.setAttribute('y', valueXIncrease);
      text270.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF270, 0.001);
const text271 = document.getElementById('text271');
text271.innerHTML = number[0];
function textF271() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text271.setAttribute('fill', newColor);
  const id = text271.x['animVal'][0].value+""+text271.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text271.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text271.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text271.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text271.setAttribute('x', valueXIncrease);
      text271.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text271.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text271.setAttribute('x', valueXIncrease);
      text271.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text271.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text271.setAttribute('y', valueXIncrease);
      text271.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text271.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text271.setAttribute('y', valueXIncrease);
      text271.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF271, 0.001);
const text272 = document.getElementById('text272');
text272.innerHTML = number[0];
function textF272() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text272.setAttribute('fill', newColor);
  const id = text272.x['animVal'][0].value+""+text272.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text272.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text272.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text272.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text272.setAttribute('x', valueXIncrease);
      text272.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text272.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text272.setAttribute('x', valueXIncrease);
      text272.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text272.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text272.setAttribute('y', valueXIncrease);
      text272.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text272.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text272.setAttribute('y', valueXIncrease);
      text272.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF272, 0.001);
const text273 = document.getElementById('text273');
text273.innerHTML = number[0];
function textF273() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text273.setAttribute('fill', newColor);
  const id = text273.x['animVal'][0].value+""+text273.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text273.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text273.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text273.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text273.setAttribute('x', valueXIncrease);
      text273.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text273.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text273.setAttribute('x', valueXIncrease);
      text273.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text273.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text273.setAttribute('y', valueXIncrease);
      text273.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text273.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text273.setAttribute('y', valueXIncrease);
      text273.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF273, 0.001);
const text274 = document.getElementById('text274');
text274.innerHTML = number[0];
function textF274() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text274.setAttribute('fill', newColor);
  const id = text274.x['animVal'][0].value+""+text274.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text274.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text274.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text274.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text274.setAttribute('x', valueXIncrease);
      text274.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text274.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text274.setAttribute('x', valueXIncrease);
      text274.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text274.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text274.setAttribute('y', valueXIncrease);
      text274.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text274.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text274.setAttribute('y', valueXIncrease);
      text274.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF274, 0.001);
const text275 = document.getElementById('text275');
text275.innerHTML = number[0];
function textF275() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text275.setAttribute('fill', newColor);
  const id = text275.x['animVal'][0].value+""+text275.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text275.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text275.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text275.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text275.setAttribute('x', valueXIncrease);
      text275.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text275.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text275.setAttribute('x', valueXIncrease);
      text275.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text275.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text275.setAttribute('y', valueXIncrease);
      text275.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text275.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text275.setAttribute('y', valueXIncrease);
      text275.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF275, 0.001);
const text276 = document.getElementById('text276');
text276.innerHTML = number[0];
function textF276() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text276.setAttribute('fill', newColor);
  const id = text276.x['animVal'][0].value+""+text276.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text276.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text276.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text276.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text276.setAttribute('x', valueXIncrease);
      text276.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text276.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text276.setAttribute('x', valueXIncrease);
      text276.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text276.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text276.setAttribute('y', valueXIncrease);
      text276.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text276.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text276.setAttribute('y', valueXIncrease);
      text276.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF276, 0.001);
const text277 = document.getElementById('text277');
text277.innerHTML = number[0];
function textF277() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text277.setAttribute('fill', newColor);
  const id = text277.x['animVal'][0].value+""+text277.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text277.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text277.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text277.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text277.setAttribute('x', valueXIncrease);
      text277.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text277.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text277.setAttribute('x', valueXIncrease);
      text277.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text277.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text277.setAttribute('y', valueXIncrease);
      text277.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text277.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text277.setAttribute('y', valueXIncrease);
      text277.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF277, 0.001);
const text278 = document.getElementById('text278');
text278.innerHTML = number[0];
function textF278() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text278.setAttribute('fill', newColor);
  const id = text278.x['animVal'][0].value+""+text278.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text278.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text278.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text278.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text278.setAttribute('x', valueXIncrease);
      text278.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text278.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text278.setAttribute('x', valueXIncrease);
      text278.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text278.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text278.setAttribute('y', valueXIncrease);
      text278.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text278.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text278.setAttribute('y', valueXIncrease);
      text278.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF278, 0.001);
const text279 = document.getElementById('text279');
text279.innerHTML = number[0];
function textF279() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text279.setAttribute('fill', newColor);
  const id = text279.x['animVal'][0].value+""+text279.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text279.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text279.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text279.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text279.setAttribute('x', valueXIncrease);
      text279.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text279.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text279.setAttribute('x', valueXIncrease);
      text279.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text279.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text279.setAttribute('y', valueXIncrease);
      text279.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text279.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text279.setAttribute('y', valueXIncrease);
      text279.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF279, 0.001);
const text280 = document.getElementById('text280');
text280.innerHTML = number[0];
function textF280() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text280.setAttribute('fill', newColor);
  const id = text280.x['animVal'][0].value+""+text280.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text280.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text280.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text280.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text280.setAttribute('x', valueXIncrease);
      text280.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text280.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text280.setAttribute('x', valueXIncrease);
      text280.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text280.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text280.setAttribute('y', valueXIncrease);
      text280.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text280.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text280.setAttribute('y', valueXIncrease);
      text280.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF280, 0.001);
const text281 = document.getElementById('text281');
text281.innerHTML = number[0];
function textF281() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text281.setAttribute('fill', newColor);
  const id = text281.x['animVal'][0].value+""+text281.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text281.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text281.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text281.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text281.setAttribute('x', valueXIncrease);
      text281.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text281.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text281.setAttribute('x', valueXIncrease);
      text281.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text281.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text281.setAttribute('y', valueXIncrease);
      text281.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text281.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text281.setAttribute('y', valueXIncrease);
      text281.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF281, 0.001);
const text282 = document.getElementById('text282');
text282.innerHTML = number[0];
function textF282() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text282.setAttribute('fill', newColor);
  const id = text282.x['animVal'][0].value+""+text282.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text282.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text282.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text282.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text282.setAttribute('x', valueXIncrease);
      text282.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text282.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text282.setAttribute('x', valueXIncrease);
      text282.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text282.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text282.setAttribute('y', valueXIncrease);
      text282.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text282.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text282.setAttribute('y', valueXIncrease);
      text282.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF282, 0.001);
const text283 = document.getElementById('text283');
text283.innerHTML = number[0];
function textF283() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text283.setAttribute('fill', newColor);
  const id = text283.x['animVal'][0].value+""+text283.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text283.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text283.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text283.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text283.setAttribute('x', valueXIncrease);
      text283.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text283.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text283.setAttribute('x', valueXIncrease);
      text283.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text283.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text283.setAttribute('y', valueXIncrease);
      text283.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text283.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text283.setAttribute('y', valueXIncrease);
      text283.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF283, 0.001);
const text284 = document.getElementById('text284');
text284.innerHTML = number[0];
function textF284() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text284.setAttribute('fill', newColor);
  const id = text284.x['animVal'][0].value+""+text284.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text284.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text284.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text284.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text284.setAttribute('x', valueXIncrease);
      text284.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text284.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text284.setAttribute('x', valueXIncrease);
      text284.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text284.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text284.setAttribute('y', valueXIncrease);
      text284.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text284.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text284.setAttribute('y', valueXIncrease);
      text284.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF284, 0.001);
const text285 = document.getElementById('text285');
text285.innerHTML = number[0];
function textF285() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text285.setAttribute('fill', newColor);
  const id = text285.x['animVal'][0].value+""+text285.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text285.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text285.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text285.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text285.setAttribute('x', valueXIncrease);
      text285.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text285.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text285.setAttribute('x', valueXIncrease);
      text285.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text285.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text285.setAttribute('y', valueXIncrease);
      text285.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text285.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text285.setAttribute('y', valueXIncrease);
      text285.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF285, 0.001);
const text286 = document.getElementById('text286');
text286.innerHTML = number[0];
function textF286() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text286.setAttribute('fill', newColor);
  const id = text286.x['animVal'][0].value+""+text286.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text286.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text286.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text286.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text286.setAttribute('x', valueXIncrease);
      text286.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text286.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text286.setAttribute('x', valueXIncrease);
      text286.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text286.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text286.setAttribute('y', valueXIncrease);
      text286.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text286.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text286.setAttribute('y', valueXIncrease);
      text286.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF286, 0.001);
const text287 = document.getElementById('text287');
text287.innerHTML = number[0];
function textF287() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text287.setAttribute('fill', newColor);
  const id = text287.x['animVal'][0].value+""+text287.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text287.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text287.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text287.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text287.setAttribute('x', valueXIncrease);
      text287.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text287.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text287.setAttribute('x', valueXIncrease);
      text287.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text287.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text287.setAttribute('y', valueXIncrease);
      text287.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text287.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text287.setAttribute('y', valueXIncrease);
      text287.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF287, 0.001);
const text288 = document.getElementById('text288');
text288.innerHTML = number[0];
function textF288() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text288.setAttribute('fill', newColor);
  const id = text288.x['animVal'][0].value+""+text288.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text288.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text288.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text288.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text288.setAttribute('x', valueXIncrease);
      text288.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text288.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text288.setAttribute('x', valueXIncrease);
      text288.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text288.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text288.setAttribute('y', valueXIncrease);
      text288.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text288.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text288.setAttribute('y', valueXIncrease);
      text288.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF288, 0.001);
const text289 = document.getElementById('text289');
text289.innerHTML = number[0];
function textF289() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text289.setAttribute('fill', newColor);
  const id = text289.x['animVal'][0].value+""+text289.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text289.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text289.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text289.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text289.setAttribute('x', valueXIncrease);
      text289.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text289.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text289.setAttribute('x', valueXIncrease);
      text289.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text289.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text289.setAttribute('y', valueXIncrease);
      text289.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text289.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text289.setAttribute('y', valueXIncrease);
      text289.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF289, 0.001);
const text290 = document.getElementById('text290');
text290.innerHTML = number[0];
function textF290() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text290.setAttribute('fill', newColor);
  const id = text290.x['animVal'][0].value+""+text290.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text290.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text290.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text290.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text290.setAttribute('x', valueXIncrease);
      text290.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text290.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text290.setAttribute('x', valueXIncrease);
      text290.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text290.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text290.setAttribute('y', valueXIncrease);
      text290.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text290.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text290.setAttribute('y', valueXIncrease);
      text290.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF290, 0.001);
const text291 = document.getElementById('text291');
text291.innerHTML = number[0];
function textF291() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text291.setAttribute('fill', newColor);
  const id = text291.x['animVal'][0].value+""+text291.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text291.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text291.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text291.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text291.setAttribute('x', valueXIncrease);
      text291.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text291.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text291.setAttribute('x', valueXIncrease);
      text291.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text291.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text291.setAttribute('y', valueXIncrease);
      text291.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text291.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text291.setAttribute('y', valueXIncrease);
      text291.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF291, 0.001);
const text292 = document.getElementById('text292');
text292.innerHTML = number[0];
function textF292() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text292.setAttribute('fill', newColor);
  const id = text292.x['animVal'][0].value+""+text292.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text292.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text292.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text292.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text292.setAttribute('x', valueXIncrease);
      text292.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text292.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text292.setAttribute('x', valueXIncrease);
      text292.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text292.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text292.setAttribute('y', valueXIncrease);
      text292.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text292.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text292.setAttribute('y', valueXIncrease);
      text292.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF292, 0.001);
const text293 = document.getElementById('text293');
text293.innerHTML = number[0];
function textF293() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text293.setAttribute('fill', newColor);
  const id = text293.x['animVal'][0].value+""+text293.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text293.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text293.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text293.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text293.setAttribute('x', valueXIncrease);
      text293.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text293.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text293.setAttribute('x', valueXIncrease);
      text293.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text293.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text293.setAttribute('y', valueXIncrease);
      text293.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text293.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text293.setAttribute('y', valueXIncrease);
      text293.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF293, 0.001);
const text294 = document.getElementById('text294');
text294.innerHTML = number[0];
function textF294() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text294.setAttribute('fill', newColor);
  const id = text294.x['animVal'][0].value+""+text294.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text294.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text294.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text294.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text294.setAttribute('x', valueXIncrease);
      text294.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text294.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text294.setAttribute('x', valueXIncrease);
      text294.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text294.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text294.setAttribute('y', valueXIncrease);
      text294.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text294.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text294.setAttribute('y', valueXIncrease);
      text294.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF294, 0.001);
const text295 = document.getElementById('text295');
text295.innerHTML = number[0];
function textF295() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text295.setAttribute('fill', newColor);
  const id = text295.x['animVal'][0].value+""+text295.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text295.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text295.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text295.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text295.setAttribute('x', valueXIncrease);
      text295.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text295.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text295.setAttribute('x', valueXIncrease);
      text295.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text295.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text295.setAttribute('y', valueXIncrease);
      text295.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text295.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text295.setAttribute('y', valueXIncrease);
      text295.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF295, 0.001);
const text296 = document.getElementById('text296');
text296.innerHTML = number[0];
function textF296() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text296.setAttribute('fill', newColor);
  const id = text296.x['animVal'][0].value+""+text296.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text296.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text296.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text296.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text296.setAttribute('x', valueXIncrease);
      text296.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text296.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text296.setAttribute('x', valueXIncrease);
      text296.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text296.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text296.setAttribute('y', valueXIncrease);
      text296.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text296.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text296.setAttribute('y', valueXIncrease);
      text296.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF296, 0.001);
const text297 = document.getElementById('text297');
text297.innerHTML = number[0];
function textF297() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text297.setAttribute('fill', newColor);
  const id = text297.x['animVal'][0].value+""+text297.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text297.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text297.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text297.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text297.setAttribute('x', valueXIncrease);
      text297.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text297.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text297.setAttribute('x', valueXIncrease);
      text297.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text297.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text297.setAttribute('y', valueXIncrease);
      text297.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text297.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text297.setAttribute('y', valueXIncrease);
      text297.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF297, 0.001);
const text298 = document.getElementById('text298');
text298.innerHTML = number[0];
function textF298() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text298.setAttribute('fill', newColor);
  const id = text298.x['animVal'][0].value+""+text298.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text298.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text298.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text298.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text298.setAttribute('x', valueXIncrease);
      text298.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text298.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text298.setAttribute('x', valueXIncrease);
      text298.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text298.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text298.setAttribute('y', valueXIncrease);
      text298.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text298.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text298.setAttribute('y', valueXIncrease);
      text298.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF298, 0.001);
const text299 = document.getElementById('text299');
text299.innerHTML = number[0];
function textF299() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text299.setAttribute('fill', newColor);
  const id = text299.x['animVal'][0].value+""+text299.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text299.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text299.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text299.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text299.setAttribute('x', valueXIncrease);
      text299.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text299.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text299.setAttribute('x', valueXIncrease);
      text299.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text299.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text299.setAttribute('y', valueXIncrease);
      text299.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text299.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text299.setAttribute('y', valueXIncrease);
      text299.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF299, 0.001);
const text300 = document.getElementById('text300');
text300.innerHTML = number[0];
function textF300() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text300.setAttribute('fill', newColor);
  const id = text300.x['animVal'][0].value+""+text300.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text300.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text300.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text300.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text300.setAttribute('x', valueXIncrease);
      text300.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text300.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text300.setAttribute('x', valueXIncrease);
      text300.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text300.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text300.setAttribute('y', valueXIncrease);
      text300.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text300.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text300.setAttribute('y', valueXIncrease);
      text300.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF300, 0.001);
const text301 = document.getElementById('text301');
text301.innerHTML = number[0];
function textF301() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text301.setAttribute('fill', newColor);
  const id = text301.x['animVal'][0].value+""+text301.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text301.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text301.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text301.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text301.setAttribute('x', valueXIncrease);
      text301.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text301.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text301.setAttribute('x', valueXIncrease);
      text301.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text301.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text301.setAttribute('y', valueXIncrease);
      text301.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text301.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text301.setAttribute('y', valueXIncrease);
      text301.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF301, 0.001);
const text302 = document.getElementById('text302');
text302.innerHTML = number[0];
function textF302() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text302.setAttribute('fill', newColor);
  const id = text302.x['animVal'][0].value+""+text302.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text302.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text302.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text302.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text302.setAttribute('x', valueXIncrease);
      text302.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text302.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text302.setAttribute('x', valueXIncrease);
      text302.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text302.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text302.setAttribute('y', valueXIncrease);
      text302.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text302.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text302.setAttribute('y', valueXIncrease);
      text302.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF302, 0.001);
const text303 = document.getElementById('text303');
text303.innerHTML = number[0];
function textF303() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text303.setAttribute('fill', newColor);
  const id = text303.x['animVal'][0].value+""+text303.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text303.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text303.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text303.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text303.setAttribute('x', valueXIncrease);
      text303.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text303.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text303.setAttribute('x', valueXIncrease);
      text303.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text303.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text303.setAttribute('y', valueXIncrease);
      text303.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text303.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text303.setAttribute('y', valueXIncrease);
      text303.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF303, 0.001);
const text304 = document.getElementById('text304');
text304.innerHTML = number[0];
function textF304() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text304.setAttribute('fill', newColor);
  const id = text304.x['animVal'][0].value+""+text304.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text304.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text304.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text304.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text304.setAttribute('x', valueXIncrease);
      text304.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text304.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text304.setAttribute('x', valueXIncrease);
      text304.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text304.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text304.setAttribute('y', valueXIncrease);
      text304.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text304.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text304.setAttribute('y', valueXIncrease);
      text304.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF304, 0.001);
const text305 = document.getElementById('text305');
text305.innerHTML = number[0];
function textF305() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text305.setAttribute('fill', newColor);
  const id = text305.x['animVal'][0].value+""+text305.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text305.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text305.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text305.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text305.setAttribute('x', valueXIncrease);
      text305.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text305.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text305.setAttribute('x', valueXIncrease);
      text305.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text305.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text305.setAttribute('y', valueXIncrease);
      text305.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text305.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text305.setAttribute('y', valueXIncrease);
      text305.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF305, 0.001);
const text306 = document.getElementById('text306');
text306.innerHTML = number[0];
function textF306() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text306.setAttribute('fill', newColor);
  const id = text306.x['animVal'][0].value+""+text306.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text306.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text306.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text306.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text306.setAttribute('x', valueXIncrease);
      text306.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text306.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text306.setAttribute('x', valueXIncrease);
      text306.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text306.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text306.setAttribute('y', valueXIncrease);
      text306.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text306.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text306.setAttribute('y', valueXIncrease);
      text306.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF306, 0.001);
const text307 = document.getElementById('text307');
text307.innerHTML = number[0];
function textF307() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text307.setAttribute('fill', newColor);
  const id = text307.x['animVal'][0].value+""+text307.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text307.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text307.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text307.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text307.setAttribute('x', valueXIncrease);
      text307.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text307.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text307.setAttribute('x', valueXIncrease);
      text307.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text307.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text307.setAttribute('y', valueXIncrease);
      text307.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text307.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text307.setAttribute('y', valueXIncrease);
      text307.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF307, 0.001);
const text308 = document.getElementById('text308');
text308.innerHTML = number[0];
function textF308() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text308.setAttribute('fill', newColor);
  const id = text308.x['animVal'][0].value+""+text308.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text308.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text308.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text308.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text308.setAttribute('x', valueXIncrease);
      text308.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text308.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text308.setAttribute('x', valueXIncrease);
      text308.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text308.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text308.setAttribute('y', valueXIncrease);
      text308.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text308.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text308.setAttribute('y', valueXIncrease);
      text308.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF308, 0.001);
const text309 = document.getElementById('text309');
text309.innerHTML = number[0];
function textF309() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text309.setAttribute('fill', newColor);
  const id = text309.x['animVal'][0].value+""+text309.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text309.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text309.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text309.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text309.setAttribute('x', valueXIncrease);
      text309.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text309.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text309.setAttribute('x', valueXIncrease);
      text309.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text309.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text309.setAttribute('y', valueXIncrease);
      text309.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text309.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text309.setAttribute('y', valueXIncrease);
      text309.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF309, 0.001);
const text310 = document.getElementById('text310');
text310.innerHTML = number[0];
function textF310() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text310.setAttribute('fill', newColor);
  const id = text310.x['animVal'][0].value+""+text310.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text310.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text310.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text310.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text310.setAttribute('x', valueXIncrease);
      text310.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text310.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text310.setAttribute('x', valueXIncrease);
      text310.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text310.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text310.setAttribute('y', valueXIncrease);
      text310.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text310.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text310.setAttribute('y', valueXIncrease);
      text310.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF310, 0.001);
const text311 = document.getElementById('text311');
text311.innerHTML = number[0];
function textF311() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text311.setAttribute('fill', newColor);
  const id = text311.x['animVal'][0].value+""+text311.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text311.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text311.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text311.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text311.setAttribute('x', valueXIncrease);
      text311.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text311.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text311.setAttribute('x', valueXIncrease);
      text311.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text311.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text311.setAttribute('y', valueXIncrease);
      text311.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text311.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text311.setAttribute('y', valueXIncrease);
      text311.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF311, 0.001);
const text312 = document.getElementById('text312');
text312.innerHTML = number[0];
function textF312() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text312.setAttribute('fill', newColor);
  const id = text312.x['animVal'][0].value+""+text312.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text312.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text312.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text312.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text312.setAttribute('x', valueXIncrease);
      text312.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text312.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text312.setAttribute('x', valueXIncrease);
      text312.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text312.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text312.setAttribute('y', valueXIncrease);
      text312.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text312.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text312.setAttribute('y', valueXIncrease);
      text312.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF312, 0.001);
const text313 = document.getElementById('text313');
text313.innerHTML = number[0];
function textF313() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text313.setAttribute('fill', newColor);
  const id = text313.x['animVal'][0].value+""+text313.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text313.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text313.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text313.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text313.setAttribute('x', valueXIncrease);
      text313.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text313.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text313.setAttribute('x', valueXIncrease);
      text313.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text313.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text313.setAttribute('y', valueXIncrease);
      text313.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text313.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text313.setAttribute('y', valueXIncrease);
      text313.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF313, 0.001);
const text314 = document.getElementById('text314');
text314.innerHTML = number[0];
function textF314() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text314.setAttribute('fill', newColor);
  const id = text314.x['animVal'][0].value+""+text314.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text314.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text314.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text314.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text314.setAttribute('x', valueXIncrease);
      text314.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text314.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text314.setAttribute('x', valueXIncrease);
      text314.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text314.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text314.setAttribute('y', valueXIncrease);
      text314.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text314.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text314.setAttribute('y', valueXIncrease);
      text314.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF314, 0.001);
const text315 = document.getElementById('text315');
text315.innerHTML = number[0];
function textF315() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text315.setAttribute('fill', newColor);
  const id = text315.x['animVal'][0].value+""+text315.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text315.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text315.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text315.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text315.setAttribute('x', valueXIncrease);
      text315.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text315.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text315.setAttribute('x', valueXIncrease);
      text315.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text315.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text315.setAttribute('y', valueXIncrease);
      text315.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text315.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text315.setAttribute('y', valueXIncrease);
      text315.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF315, 0.001);
const text316 = document.getElementById('text316');
text316.innerHTML = number[0];
function textF316() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text316.setAttribute('fill', newColor);
  const id = text316.x['animVal'][0].value+""+text316.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text316.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text316.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text316.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text316.setAttribute('x', valueXIncrease);
      text316.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text316.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text316.setAttribute('x', valueXIncrease);
      text316.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text316.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text316.setAttribute('y', valueXIncrease);
      text316.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text316.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text316.setAttribute('y', valueXIncrease);
      text316.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF316, 0.001);
const text317 = document.getElementById('text317');
text317.innerHTML = number[0];
function textF317() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text317.setAttribute('fill', newColor);
  const id = text317.x['animVal'][0].value+""+text317.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text317.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text317.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text317.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text317.setAttribute('x', valueXIncrease);
      text317.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text317.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text317.setAttribute('x', valueXIncrease);
      text317.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text317.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text317.setAttribute('y', valueXIncrease);
      text317.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text317.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text317.setAttribute('y', valueXIncrease);
      text317.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF317, 0.001);
const text318 = document.getElementById('text318');
text318.innerHTML = number[0];
function textF318() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text318.setAttribute('fill', newColor);
  const id = text318.x['animVal'][0].value+""+text318.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text318.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text318.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text318.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text318.setAttribute('x', valueXIncrease);
      text318.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text318.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text318.setAttribute('x', valueXIncrease);
      text318.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text318.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text318.setAttribute('y', valueXIncrease);
      text318.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text318.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text318.setAttribute('y', valueXIncrease);
      text318.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF318, 0.001);
const text319 = document.getElementById('text319');
text319.innerHTML = number[0];
function textF319() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text319.setAttribute('fill', newColor);
  const id = text319.x['animVal'][0].value+""+text319.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text319.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text319.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text319.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text319.setAttribute('x', valueXIncrease);
      text319.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text319.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text319.setAttribute('x', valueXIncrease);
      text319.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text319.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text319.setAttribute('y', valueXIncrease);
      text319.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text319.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text319.setAttribute('y', valueXIncrease);
      text319.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF319, 0.001);
const text320 = document.getElementById('text320');
text320.innerHTML = number[0];
function textF320() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text320.setAttribute('fill', newColor);
  const id = text320.x['animVal'][0].value+""+text320.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text320.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text320.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text320.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text320.setAttribute('x', valueXIncrease);
      text320.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text320.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text320.setAttribute('x', valueXIncrease);
      text320.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text320.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text320.setAttribute('y', valueXIncrease);
      text320.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text320.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text320.setAttribute('y', valueXIncrease);
      text320.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF320, 0.001);
const text321 = document.getElementById('text321');
text321.innerHTML = number[0];
function textF321() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text321.setAttribute('fill', newColor);
  const id = text321.x['animVal'][0].value+""+text321.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text321.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text321.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text321.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text321.setAttribute('x', valueXIncrease);
      text321.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text321.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text321.setAttribute('x', valueXIncrease);
      text321.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text321.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text321.setAttribute('y', valueXIncrease);
      text321.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text321.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text321.setAttribute('y', valueXIncrease);
      text321.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF321, 0.001);
const text322 = document.getElementById('text322');
text322.innerHTML = number[0];
function textF322() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text322.setAttribute('fill', newColor);
  const id = text322.x['animVal'][0].value+""+text322.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text322.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text322.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text322.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text322.setAttribute('x', valueXIncrease);
      text322.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text322.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text322.setAttribute('x', valueXIncrease);
      text322.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text322.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text322.setAttribute('y', valueXIncrease);
      text322.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text322.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text322.setAttribute('y', valueXIncrease);
      text322.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF322, 0.001);
const text323 = document.getElementById('text323');
text323.innerHTML = number[0];
function textF323() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text323.setAttribute('fill', newColor);
  const id = text323.x['animVal'][0].value+""+text323.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text323.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text323.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text323.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text323.setAttribute('x', valueXIncrease);
      text323.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text323.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text323.setAttribute('x', valueXIncrease);
      text323.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text323.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text323.setAttribute('y', valueXIncrease);
      text323.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text323.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text323.setAttribute('y', valueXIncrease);
      text323.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF323, 0.001);
const text324 = document.getElementById('text324');
text324.innerHTML = number[0];
function textF324() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text324.setAttribute('fill', newColor);
  const id = text324.x['animVal'][0].value+""+text324.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text324.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text324.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text324.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text324.setAttribute('x', valueXIncrease);
      text324.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text324.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text324.setAttribute('x', valueXIncrease);
      text324.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text324.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text324.setAttribute('y', valueXIncrease);
      text324.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text324.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text324.setAttribute('y', valueXIncrease);
      text324.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF324, 0.001);
const text325 = document.getElementById('text325');
text325.innerHTML = number[0];
function textF325() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text325.setAttribute('fill', newColor);
  const id = text325.x['animVal'][0].value+""+text325.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text325.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text325.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text325.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text325.setAttribute('x', valueXIncrease);
      text325.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text325.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text325.setAttribute('x', valueXIncrease);
      text325.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text325.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text325.setAttribute('y', valueXIncrease);
      text325.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text325.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text325.setAttribute('y', valueXIncrease);
      text325.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF325, 0.001);
const text326 = document.getElementById('text326');
text326.innerHTML = number[0];
function textF326() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text326.setAttribute('fill', newColor);
  const id = text326.x['animVal'][0].value+""+text326.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text326.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text326.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text326.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text326.setAttribute('x', valueXIncrease);
      text326.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text326.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text326.setAttribute('x', valueXIncrease);
      text326.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text326.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text326.setAttribute('y', valueXIncrease);
      text326.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text326.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text326.setAttribute('y', valueXIncrease);
      text326.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF326, 0.001);
const text327 = document.getElementById('text327');
text327.innerHTML = number[0];
function textF327() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text327.setAttribute('fill', newColor);
  const id = text327.x['animVal'][0].value+""+text327.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text327.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text327.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text327.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text327.setAttribute('x', valueXIncrease);
      text327.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text327.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text327.setAttribute('x', valueXIncrease);
      text327.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text327.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text327.setAttribute('y', valueXIncrease);
      text327.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text327.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text327.setAttribute('y', valueXIncrease);
      text327.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF327, 0.001);
const text328 = document.getElementById('text328');
text328.innerHTML = number[0];
function textF328() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text328.setAttribute('fill', newColor);
  const id = text328.x['animVal'][0].value+""+text328.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text328.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text328.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text328.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text328.setAttribute('x', valueXIncrease);
      text328.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text328.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text328.setAttribute('x', valueXIncrease);
      text328.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text328.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text328.setAttribute('y', valueXIncrease);
      text328.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text328.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text328.setAttribute('y', valueXIncrease);
      text328.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF328, 0.001);
const text329 = document.getElementById('text329');
text329.innerHTML = number[0];
function textF329() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text329.setAttribute('fill', newColor);
  const id = text329.x['animVal'][0].value+""+text329.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text329.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text329.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text329.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text329.setAttribute('x', valueXIncrease);
      text329.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text329.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text329.setAttribute('x', valueXIncrease);
      text329.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text329.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text329.setAttribute('y', valueXIncrease);
      text329.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text329.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text329.setAttribute('y', valueXIncrease);
      text329.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF329, 0.001);
const text330 = document.getElementById('text330');
text330.innerHTML = number[0];
function textF330() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text330.setAttribute('fill', newColor);
  const id = text330.x['animVal'][0].value+""+text330.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text330.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text330.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text330.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text330.setAttribute('x', valueXIncrease);
      text330.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text330.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text330.setAttribute('x', valueXIncrease);
      text330.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text330.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text330.setAttribute('y', valueXIncrease);
      text330.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text330.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text330.setAttribute('y', valueXIncrease);
      text330.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF330, 0.001);
const text331 = document.getElementById('text331');
text331.innerHTML = number[0];
function textF331() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text331.setAttribute('fill', newColor);
  const id = text331.x['animVal'][0].value+""+text331.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text331.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text331.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text331.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text331.setAttribute('x', valueXIncrease);
      text331.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text331.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text331.setAttribute('x', valueXIncrease);
      text331.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text331.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text331.setAttribute('y', valueXIncrease);
      text331.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text331.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text331.setAttribute('y', valueXIncrease);
      text331.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF331, 0.001);
const text332 = document.getElementById('text332');
text332.innerHTML = number[0];
function textF332() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text332.setAttribute('fill', newColor);
  const id = text332.x['animVal'][0].value+""+text332.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text332.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text332.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text332.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text332.setAttribute('x', valueXIncrease);
      text332.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text332.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text332.setAttribute('x', valueXIncrease);
      text332.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text332.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text332.setAttribute('y', valueXIncrease);
      text332.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text332.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text332.setAttribute('y', valueXIncrease);
      text332.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF332, 0.001);
const text333 = document.getElementById('text333');
text333.innerHTML = number[0];
function textF333() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text333.setAttribute('fill', newColor);
  const id = text333.x['animVal'][0].value+""+text333.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text333.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text333.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text333.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text333.setAttribute('x', valueXIncrease);
      text333.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text333.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text333.setAttribute('x', valueXIncrease);
      text333.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text333.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text333.setAttribute('y', valueXIncrease);
      text333.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text333.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text333.setAttribute('y', valueXIncrease);
      text333.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF333, 0.001);
const text334 = document.getElementById('text334');
text334.innerHTML = number[0];
function textF334() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text334.setAttribute('fill', newColor);
  const id = text334.x['animVal'][0].value+""+text334.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text334.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text334.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text334.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text334.setAttribute('x', valueXIncrease);
      text334.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text334.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text334.setAttribute('x', valueXIncrease);
      text334.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text334.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text334.setAttribute('y', valueXIncrease);
      text334.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text334.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text334.setAttribute('y', valueXIncrease);
      text334.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF334, 0.001);
const text335 = document.getElementById('text335');
text335.innerHTML = number[0];
function textF335() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text335.setAttribute('fill', newColor);
  const id = text335.x['animVal'][0].value+""+text335.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text335.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text335.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text335.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text335.setAttribute('x', valueXIncrease);
      text335.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text335.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text335.setAttribute('x', valueXIncrease);
      text335.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text335.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text335.setAttribute('y', valueXIncrease);
      text335.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text335.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text335.setAttribute('y', valueXIncrease);
      text335.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF335, 0.001);
const text336 = document.getElementById('text336');
text336.innerHTML = number[0];
function textF336() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text336.setAttribute('fill', newColor);
  const id = text336.x['animVal'][0].value+""+text336.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text336.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text336.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text336.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text336.setAttribute('x', valueXIncrease);
      text336.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text336.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text336.setAttribute('x', valueXIncrease);
      text336.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text336.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text336.setAttribute('y', valueXIncrease);
      text336.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text336.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text336.setAttribute('y', valueXIncrease);
      text336.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF336, 0.001);
const text337 = document.getElementById('text337');
text337.innerHTML = number[0];
function textF337() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text337.setAttribute('fill', newColor);
  const id = text337.x['animVal'][0].value+""+text337.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text337.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text337.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text337.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text337.setAttribute('x', valueXIncrease);
      text337.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text337.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text337.setAttribute('x', valueXIncrease);
      text337.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text337.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text337.setAttribute('y', valueXIncrease);
      text337.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text337.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text337.setAttribute('y', valueXIncrease);
      text337.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF337, 0.001);
const text338 = document.getElementById('text338');
text338.innerHTML = number[0];
function textF338() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text338.setAttribute('fill', newColor);
  const id = text338.x['animVal'][0].value+""+text338.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text338.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text338.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text338.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text338.setAttribute('x', valueXIncrease);
      text338.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text338.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text338.setAttribute('x', valueXIncrease);
      text338.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text338.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text338.setAttribute('y', valueXIncrease);
      text338.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text338.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text338.setAttribute('y', valueXIncrease);
      text338.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF338, 0.001);
const text339 = document.getElementById('text339');
text339.innerHTML = number[0];
function textF339() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text339.setAttribute('fill', newColor);
  const id = text339.x['animVal'][0].value+""+text339.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text339.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text339.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text339.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text339.setAttribute('x', valueXIncrease);
      text339.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text339.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text339.setAttribute('x', valueXIncrease);
      text339.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text339.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text339.setAttribute('y', valueXIncrease);
      text339.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text339.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text339.setAttribute('y', valueXIncrease);
      text339.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF339, 0.001);
const text340 = document.getElementById('text340');
text340.innerHTML = number[0];
function textF340() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text340.setAttribute('fill', newColor);
  const id = text340.x['animVal'][0].value+""+text340.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text340.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text340.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text340.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text340.setAttribute('x', valueXIncrease);
      text340.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text340.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text340.setAttribute('x', valueXIncrease);
      text340.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text340.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text340.setAttribute('y', valueXIncrease);
      text340.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text340.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text340.setAttribute('y', valueXIncrease);
      text340.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF340, 0.001);
const text341 = document.getElementById('text341');
text341.innerHTML = number[0];
function textF341() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text341.setAttribute('fill', newColor);
  const id = text341.x['animVal'][0].value+""+text341.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text341.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text341.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text341.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text341.setAttribute('x', valueXIncrease);
      text341.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text341.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text341.setAttribute('x', valueXIncrease);
      text341.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text341.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text341.setAttribute('y', valueXIncrease);
      text341.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text341.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text341.setAttribute('y', valueXIncrease);
      text341.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF341, 0.001);
const text342 = document.getElementById('text342');
text342.innerHTML = number[0];
function textF342() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text342.setAttribute('fill', newColor);
  const id = text342.x['animVal'][0].value+""+text342.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text342.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text342.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text342.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text342.setAttribute('x', valueXIncrease);
      text342.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text342.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text342.setAttribute('x', valueXIncrease);
      text342.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text342.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text342.setAttribute('y', valueXIncrease);
      text342.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text342.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text342.setAttribute('y', valueXIncrease);
      text342.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF342, 0.001);
const text343 = document.getElementById('text343');
text343.innerHTML = number[0];
function textF343() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text343.setAttribute('fill', newColor);
  const id = text343.x['animVal'][0].value+""+text343.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text343.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text343.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text343.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text343.setAttribute('x', valueXIncrease);
      text343.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text343.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text343.setAttribute('x', valueXIncrease);
      text343.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text343.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text343.setAttribute('y', valueXIncrease);
      text343.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text343.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text343.setAttribute('y', valueXIncrease);
      text343.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF343, 0.001);
const text344 = document.getElementById('text344');
text344.innerHTML = number[0];
function textF344() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text344.setAttribute('fill', newColor);
  const id = text344.x['animVal'][0].value+""+text344.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text344.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text344.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text344.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text344.setAttribute('x', valueXIncrease);
      text344.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text344.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text344.setAttribute('x', valueXIncrease);
      text344.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text344.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text344.setAttribute('y', valueXIncrease);
      text344.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text344.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text344.setAttribute('y', valueXIncrease);
      text344.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF344, 0.001);
const text345 = document.getElementById('text345');
text345.innerHTML = number[0];
function textF345() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text345.setAttribute('fill', newColor);
  const id = text345.x['animVal'][0].value+""+text345.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text345.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text345.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text345.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text345.setAttribute('x', valueXIncrease);
      text345.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text345.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text345.setAttribute('x', valueXIncrease);
      text345.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text345.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text345.setAttribute('y', valueXIncrease);
      text345.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text345.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text345.setAttribute('y', valueXIncrease);
      text345.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF345, 0.001);
const text346 = document.getElementById('text346');
text346.innerHTML = number[0];
function textF346() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text346.setAttribute('fill', newColor);
  const id = text346.x['animVal'][0].value+""+text346.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text346.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text346.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text346.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text346.setAttribute('x', valueXIncrease);
      text346.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text346.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text346.setAttribute('x', valueXIncrease);
      text346.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text346.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text346.setAttribute('y', valueXIncrease);
      text346.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text346.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text346.setAttribute('y', valueXIncrease);
      text346.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF346, 0.001);
const text347 = document.getElementById('text347');
text347.innerHTML = number[0];
function textF347() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text347.setAttribute('fill', newColor);
  const id = text347.x['animVal'][0].value+""+text347.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text347.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text347.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text347.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text347.setAttribute('x', valueXIncrease);
      text347.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text347.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text347.setAttribute('x', valueXIncrease);
      text347.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text347.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text347.setAttribute('y', valueXIncrease);
      text347.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text347.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text347.setAttribute('y', valueXIncrease);
      text347.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF347, 0.001);
const text348 = document.getElementById('text348');
text348.innerHTML = number[0];
function textF348() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text348.setAttribute('fill', newColor);
  const id = text348.x['animVal'][0].value+""+text348.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text348.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text348.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text348.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text348.setAttribute('x', valueXIncrease);
      text348.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text348.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text348.setAttribute('x', valueXIncrease);
      text348.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text348.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text348.setAttribute('y', valueXIncrease);
      text348.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text348.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text348.setAttribute('y', valueXIncrease);
      text348.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF348, 0.001);
const text349 = document.getElementById('text349');
text349.innerHTML = number[0];
function textF349() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text349.setAttribute('fill', newColor);
  const id = text349.x['animVal'][0].value+""+text349.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text349.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text349.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text349.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text349.setAttribute('x', valueXIncrease);
      text349.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text349.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text349.setAttribute('x', valueXIncrease);
      text349.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text349.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text349.setAttribute('y', valueXIncrease);
      text349.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text349.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text349.setAttribute('y', valueXIncrease);
      text349.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF349, 0.001);
const text350 = document.getElementById('text350');
text350.innerHTML = number[0];
function textF350() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text350.setAttribute('fill', newColor);
  const id = text350.x['animVal'][0].value+""+text350.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text350.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text350.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text350.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text350.setAttribute('x', valueXIncrease);
      text350.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text350.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text350.setAttribute('x', valueXIncrease);
      text350.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text350.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text350.setAttribute('y', valueXIncrease);
      text350.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text350.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text350.setAttribute('y', valueXIncrease);
      text350.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF350, 0.001);
const text351 = document.getElementById('text351');
text351.innerHTML = number[0];
function textF351() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text351.setAttribute('fill', newColor);
  const id = text351.x['animVal'][0].value+""+text351.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text351.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text351.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text351.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text351.setAttribute('x', valueXIncrease);
      text351.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text351.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text351.setAttribute('x', valueXIncrease);
      text351.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text351.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text351.setAttribute('y', valueXIncrease);
      text351.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text351.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text351.setAttribute('y', valueXIncrease);
      text351.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF351, 0.001);
const text352 = document.getElementById('text352');
text352.innerHTML = number[0];
function textF352() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text352.setAttribute('fill', newColor);
  const id = text352.x['animVal'][0].value+""+text352.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text352.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text352.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text352.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text352.setAttribute('x', valueXIncrease);
      text352.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text352.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text352.setAttribute('x', valueXIncrease);
      text352.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text352.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text352.setAttribute('y', valueXIncrease);
      text352.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text352.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text352.setAttribute('y', valueXIncrease);
      text352.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF352, 0.001);
const text353 = document.getElementById('text353');
text353.innerHTML = number[0];
function textF353() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text353.setAttribute('fill', newColor);
  const id = text353.x['animVal'][0].value+""+text353.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text353.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text353.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text353.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text353.setAttribute('x', valueXIncrease);
      text353.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text353.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text353.setAttribute('x', valueXIncrease);
      text353.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text353.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text353.setAttribute('y', valueXIncrease);
      text353.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text353.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text353.setAttribute('y', valueXIncrease);
      text353.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF353, 0.001);
const text354 = document.getElementById('text354');
text354.innerHTML = number[0];
function textF354() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text354.setAttribute('fill', newColor);
  const id = text354.x['animVal'][0].value+""+text354.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text354.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text354.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text354.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text354.setAttribute('x', valueXIncrease);
      text354.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text354.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text354.setAttribute('x', valueXIncrease);
      text354.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text354.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text354.setAttribute('y', valueXIncrease);
      text354.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text354.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text354.setAttribute('y', valueXIncrease);
      text354.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF354, 0.001);
const text355 = document.getElementById('text355');
text355.innerHTML = number[0];
function textF355() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text355.setAttribute('fill', newColor);
  const id = text355.x['animVal'][0].value+""+text355.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text355.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text355.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text355.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text355.setAttribute('x', valueXIncrease);
      text355.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text355.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text355.setAttribute('x', valueXIncrease);
      text355.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text355.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text355.setAttribute('y', valueXIncrease);
      text355.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text355.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text355.setAttribute('y', valueXIncrease);
      text355.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF355, 0.001);
const text356 = document.getElementById('text356');
text356.innerHTML = number[0];
function textF356() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text356.setAttribute('fill', newColor);
  const id = text356.x['animVal'][0].value+""+text356.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text356.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text356.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text356.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text356.setAttribute('x', valueXIncrease);
      text356.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text356.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text356.setAttribute('x', valueXIncrease);
      text356.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text356.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text356.setAttribute('y', valueXIncrease);
      text356.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text356.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text356.setAttribute('y', valueXIncrease);
      text356.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF356, 0.001);
const text357 = document.getElementById('text357');
text357.innerHTML = number[0];
function textF357() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text357.setAttribute('fill', newColor);
  const id = text357.x['animVal'][0].value+""+text357.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text357.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text357.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text357.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text357.setAttribute('x', valueXIncrease);
      text357.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text357.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text357.setAttribute('x', valueXIncrease);
      text357.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text357.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text357.setAttribute('y', valueXIncrease);
      text357.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text357.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text357.setAttribute('y', valueXIncrease);
      text357.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF357, 0.001);
const text358 = document.getElementById('text358');
text358.innerHTML = number[0];
function textF358() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text358.setAttribute('fill', newColor);
  const id = text358.x['animVal'][0].value+""+text358.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text358.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text358.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text358.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text358.setAttribute('x', valueXIncrease);
      text358.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text358.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text358.setAttribute('x', valueXIncrease);
      text358.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text358.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text358.setAttribute('y', valueXIncrease);
      text358.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text358.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text358.setAttribute('y', valueXIncrease);
      text358.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF358, 0.001);
const text359 = document.getElementById('text359');
text359.innerHTML = number[0];
function textF359() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text359.setAttribute('fill', newColor);
  const id = text359.x['animVal'][0].value+""+text359.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text359.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text359.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text359.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text359.setAttribute('x', valueXIncrease);
      text359.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text359.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text359.setAttribute('x', valueXIncrease);
      text359.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text359.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text359.setAttribute('y', valueXIncrease);
      text359.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text359.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text359.setAttribute('y', valueXIncrease);
      text359.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF359, 0.001);
const text360 = document.getElementById('text360');
text360.innerHTML = number[0];
function textF360() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text360.setAttribute('fill', newColor);
  const id = text360.x['animVal'][0].value+""+text360.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text360.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text360.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text360.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text360.setAttribute('x', valueXIncrease);
      text360.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text360.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text360.setAttribute('x', valueXIncrease);
      text360.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text360.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text360.setAttribute('y', valueXIncrease);
      text360.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text360.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text360.setAttribute('y', valueXIncrease);
      text360.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF360, 0.001);
const text361 = document.getElementById('text361');
text361.innerHTML = number[0];
function textF361() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text361.setAttribute('fill', newColor);
  const id = text361.x['animVal'][0].value+""+text361.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text361.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text361.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text361.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text361.setAttribute('x', valueXIncrease);
      text361.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text361.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text361.setAttribute('x', valueXIncrease);
      text361.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text361.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text361.setAttribute('y', valueXIncrease);
      text361.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text361.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text361.setAttribute('y', valueXIncrease);
      text361.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF361, 0.001);
const text362 = document.getElementById('text362');
text362.innerHTML = number[0];
function textF362() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text362.setAttribute('fill', newColor);
  const id = text362.x['animVal'][0].value+""+text362.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text362.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text362.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text362.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text362.setAttribute('x', valueXIncrease);
      text362.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text362.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text362.setAttribute('x', valueXIncrease);
      text362.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text362.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text362.setAttribute('y', valueXIncrease);
      text362.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text362.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text362.setAttribute('y', valueXIncrease);
      text362.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF362, 0.001);
const text363 = document.getElementById('text363');
text363.innerHTML = number[0];
function textF363() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text363.setAttribute('fill', newColor);
  const id = text363.x['animVal'][0].value+""+text363.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text363.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text363.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text363.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text363.setAttribute('x', valueXIncrease);
      text363.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text363.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text363.setAttribute('x', valueXIncrease);
      text363.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text363.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text363.setAttribute('y', valueXIncrease);
      text363.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text363.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text363.setAttribute('y', valueXIncrease);
      text363.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF363, 0.001);
const text364 = document.getElementById('text364');
text364.innerHTML = number[0];
function textF364() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text364.setAttribute('fill', newColor);
  const id = text364.x['animVal'][0].value+""+text364.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text364.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text364.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text364.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text364.setAttribute('x', valueXIncrease);
      text364.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text364.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text364.setAttribute('x', valueXIncrease);
      text364.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text364.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text364.setAttribute('y', valueXIncrease);
      text364.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text364.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text364.setAttribute('y', valueXIncrease);
      text364.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF364, 0.001);
const text365 = document.getElementById('text365');
text365.innerHTML = number[0];
function textF365() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text365.setAttribute('fill', newColor);
  const id = text365.x['animVal'][0].value+""+text365.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text365.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text365.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text365.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text365.setAttribute('x', valueXIncrease);
      text365.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text365.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text365.setAttribute('x', valueXIncrease);
      text365.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text365.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text365.setAttribute('y', valueXIncrease);
      text365.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text365.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text365.setAttribute('y', valueXIncrease);
      text365.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF365, 0.001);
const text366 = document.getElementById('text366');
text366.innerHTML = number[0];
function textF366() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text366.setAttribute('fill', newColor);
  const id = text366.x['animVal'][0].value+""+text366.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text366.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text366.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text366.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text366.setAttribute('x', valueXIncrease);
      text366.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text366.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text366.setAttribute('x', valueXIncrease);
      text366.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text366.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text366.setAttribute('y', valueXIncrease);
      text366.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text366.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text366.setAttribute('y', valueXIncrease);
      text366.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF366, 0.001);
const text367 = document.getElementById('text367');
text367.innerHTML = number[0];
function textF367() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text367.setAttribute('fill', newColor);
  const id = text367.x['animVal'][0].value+""+text367.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text367.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text367.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text367.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text367.setAttribute('x', valueXIncrease);
      text367.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text367.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text367.setAttribute('x', valueXIncrease);
      text367.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text367.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text367.setAttribute('y', valueXIncrease);
      text367.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text367.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text367.setAttribute('y', valueXIncrease);
      text367.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF367, 0.001);
const text368 = document.getElementById('text368');
text368.innerHTML = number[0];
function textF368() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text368.setAttribute('fill', newColor);
  const id = text368.x['animVal'][0].value+""+text368.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text368.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text368.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text368.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text368.setAttribute('x', valueXIncrease);
      text368.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text368.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text368.setAttribute('x', valueXIncrease);
      text368.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text368.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text368.setAttribute('y', valueXIncrease);
      text368.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text368.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text368.setAttribute('y', valueXIncrease);
      text368.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF368, 0.001);
const text369 = document.getElementById('text369');
text369.innerHTML = number[0];
function textF369() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text369.setAttribute('fill', newColor);
  const id = text369.x['animVal'][0].value+""+text369.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text369.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text369.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text369.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text369.setAttribute('x', valueXIncrease);
      text369.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text369.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text369.setAttribute('x', valueXIncrease);
      text369.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text369.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text369.setAttribute('y', valueXIncrease);
      text369.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text369.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text369.setAttribute('y', valueXIncrease);
      text369.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF369, 0.001);
const text370 = document.getElementById('text370');
text370.innerHTML = number[0];
function textF370() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text370.setAttribute('fill', newColor);
  const id = text370.x['animVal'][0].value+""+text370.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text370.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text370.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text370.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text370.setAttribute('x', valueXIncrease);
      text370.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text370.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text370.setAttribute('x', valueXIncrease);
      text370.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text370.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text370.setAttribute('y', valueXIncrease);
      text370.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text370.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text370.setAttribute('y', valueXIncrease);
      text370.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF370, 0.001);
const text371 = document.getElementById('text371');
text371.innerHTML = number[0];
function textF371() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text371.setAttribute('fill', newColor);
  const id = text371.x['animVal'][0].value+""+text371.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text371.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text371.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text371.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text371.setAttribute('x', valueXIncrease);
      text371.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text371.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text371.setAttribute('x', valueXIncrease);
      text371.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text371.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text371.setAttribute('y', valueXIncrease);
      text371.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text371.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text371.setAttribute('y', valueXIncrease);
      text371.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF371, 0.001);
const text372 = document.getElementById('text372');
text372.innerHTML = number[0];
function textF372() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text372.setAttribute('fill', newColor);
  const id = text372.x['animVal'][0].value+""+text372.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text372.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text372.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text372.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text372.setAttribute('x', valueXIncrease);
      text372.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text372.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text372.setAttribute('x', valueXIncrease);
      text372.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text372.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text372.setAttribute('y', valueXIncrease);
      text372.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text372.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text372.setAttribute('y', valueXIncrease);
      text372.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF372, 0.001);
const text373 = document.getElementById('text373');
text373.innerHTML = number[0];
function textF373() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text373.setAttribute('fill', newColor);
  const id = text373.x['animVal'][0].value+""+text373.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text373.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text373.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text373.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text373.setAttribute('x', valueXIncrease);
      text373.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text373.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text373.setAttribute('x', valueXIncrease);
      text373.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text373.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text373.setAttribute('y', valueXIncrease);
      text373.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text373.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text373.setAttribute('y', valueXIncrease);
      text373.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF373, 0.001);
const text374 = document.getElementById('text374');
text374.innerHTML = number[0];
function textF374() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text374.setAttribute('fill', newColor);
  const id = text374.x['animVal'][0].value+""+text374.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text374.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text374.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text374.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text374.setAttribute('x', valueXIncrease);
      text374.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text374.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text374.setAttribute('x', valueXIncrease);
      text374.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text374.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text374.setAttribute('y', valueXIncrease);
      text374.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text374.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text374.setAttribute('y', valueXIncrease);
      text374.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF374, 0.001);
const text375 = document.getElementById('text375');
text375.innerHTML = number[0];
function textF375() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text375.setAttribute('fill', newColor);
  const id = text375.x['animVal'][0].value+""+text375.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text375.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text375.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text375.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text375.setAttribute('x', valueXIncrease);
      text375.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text375.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text375.setAttribute('x', valueXIncrease);
      text375.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text375.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text375.setAttribute('y', valueXIncrease);
      text375.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text375.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text375.setAttribute('y', valueXIncrease);
      text375.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF375, 0.001);
const text376 = document.getElementById('text376');
text376.innerHTML = number[0];
function textF376() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text376.setAttribute('fill', newColor);
  const id = text376.x['animVal'][0].value+""+text376.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text376.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text376.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text376.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text376.setAttribute('x', valueXIncrease);
      text376.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text376.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text376.setAttribute('x', valueXIncrease);
      text376.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text376.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text376.setAttribute('y', valueXIncrease);
      text376.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text376.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text376.setAttribute('y', valueXIncrease);
      text376.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF376, 0.001);
const text377 = document.getElementById('text377');
text377.innerHTML = number[0];
function textF377() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text377.setAttribute('fill', newColor);
  const id = text377.x['animVal'][0].value+""+text377.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text377.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text377.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text377.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text377.setAttribute('x', valueXIncrease);
      text377.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text377.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text377.setAttribute('x', valueXIncrease);
      text377.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text377.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text377.setAttribute('y', valueXIncrease);
      text377.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text377.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text377.setAttribute('y', valueXIncrease);
      text377.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF377, 0.001);
const text378 = document.getElementById('text378');
text378.innerHTML = number[0];
function textF378() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text378.setAttribute('fill', newColor);
  const id = text378.x['animVal'][0].value+""+text378.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text378.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text378.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text378.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text378.setAttribute('x', valueXIncrease);
      text378.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text378.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text378.setAttribute('x', valueXIncrease);
      text378.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text378.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text378.setAttribute('y', valueXIncrease);
      text378.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text378.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text378.setAttribute('y', valueXIncrease);
      text378.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF378, 0.001);
const text379 = document.getElementById('text379');
text379.innerHTML = number[0];
function textF379() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text379.setAttribute('fill', newColor);
  const id = text379.x['animVal'][0].value+""+text379.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text379.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text379.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text379.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text379.setAttribute('x', valueXIncrease);
      text379.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text379.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text379.setAttribute('x', valueXIncrease);
      text379.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text379.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text379.setAttribute('y', valueXIncrease);
      text379.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text379.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text379.setAttribute('y', valueXIncrease);
      text379.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF379, 0.001);
const text380 = document.getElementById('text380');
text380.innerHTML = number[0];
function textF380() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text380.setAttribute('fill', newColor);
  const id = text380.x['animVal'][0].value+""+text380.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text380.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text380.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text380.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text380.setAttribute('x', valueXIncrease);
      text380.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text380.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text380.setAttribute('x', valueXIncrease);
      text380.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text380.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text380.setAttribute('y', valueXIncrease);
      text380.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text380.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text380.setAttribute('y', valueXIncrease);
      text380.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF380, 0.001);
const text381 = document.getElementById('text381');
text381.innerHTML = number[0];
function textF381() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text381.setAttribute('fill', newColor);
  const id = text381.x['animVal'][0].value+""+text381.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text381.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text381.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text381.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text381.setAttribute('x', valueXIncrease);
      text381.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text381.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text381.setAttribute('x', valueXIncrease);
      text381.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text381.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text381.setAttribute('y', valueXIncrease);
      text381.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text381.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text381.setAttribute('y', valueXIncrease);
      text381.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF381, 0.001);
const text382 = document.getElementById('text382');
text382.innerHTML = number[0];
function textF382() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text382.setAttribute('fill', newColor);
  const id = text382.x['animVal'][0].value+""+text382.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text382.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text382.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text382.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text382.setAttribute('x', valueXIncrease);
      text382.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text382.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text382.setAttribute('x', valueXIncrease);
      text382.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text382.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text382.setAttribute('y', valueXIncrease);
      text382.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text382.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text382.setAttribute('y', valueXIncrease);
      text382.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF382, 0.001);
const text383 = document.getElementById('text383');
text383.innerHTML = number[0];
function textF383() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text383.setAttribute('fill', newColor);
  const id = text383.x['animVal'][0].value+""+text383.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text383.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text383.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text383.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text383.setAttribute('x', valueXIncrease);
      text383.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text383.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text383.setAttribute('x', valueXIncrease);
      text383.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text383.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text383.setAttribute('y', valueXIncrease);
      text383.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text383.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text383.setAttribute('y', valueXIncrease);
      text383.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF383, 0.001);
const text384 = document.getElementById('text384');
text384.innerHTML = number[0];
function textF384() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text384.setAttribute('fill', newColor);
  const id = text384.x['animVal'][0].value+""+text384.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text384.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text384.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text384.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text384.setAttribute('x', valueXIncrease);
      text384.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text384.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text384.setAttribute('x', valueXIncrease);
      text384.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text384.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text384.setAttribute('y', valueXIncrease);
      text384.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text384.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text384.setAttribute('y', valueXIncrease);
      text384.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF384, 0.001);
const text385 = document.getElementById('text385');
text385.innerHTML = number[0];
function textF385() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text385.setAttribute('fill', newColor);
  const id = text385.x['animVal'][0].value+""+text385.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text385.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text385.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text385.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text385.setAttribute('x', valueXIncrease);
      text385.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text385.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text385.setAttribute('x', valueXIncrease);
      text385.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text385.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text385.setAttribute('y', valueXIncrease);
      text385.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text385.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text385.setAttribute('y', valueXIncrease);
      text385.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF385, 0.001);
const text386 = document.getElementById('text386');
text386.innerHTML = number[0];
function textF386() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text386.setAttribute('fill', newColor);
  const id = text386.x['animVal'][0].value+""+text386.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text386.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text386.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text386.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text386.setAttribute('x', valueXIncrease);
      text386.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text386.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text386.setAttribute('x', valueXIncrease);
      text386.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text386.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text386.setAttribute('y', valueXIncrease);
      text386.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text386.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text386.setAttribute('y', valueXIncrease);
      text386.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF386, 0.001);
const text387 = document.getElementById('text387');
text387.innerHTML = number[0];
function textF387() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text387.setAttribute('fill', newColor);
  const id = text387.x['animVal'][0].value+""+text387.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text387.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text387.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text387.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text387.setAttribute('x', valueXIncrease);
      text387.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text387.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text387.setAttribute('x', valueXIncrease);
      text387.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text387.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text387.setAttribute('y', valueXIncrease);
      text387.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text387.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text387.setAttribute('y', valueXIncrease);
      text387.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF387, 0.001);
const text388 = document.getElementById('text388');
text388.innerHTML = number[0];
function textF388() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text388.setAttribute('fill', newColor);
  const id = text388.x['animVal'][0].value+""+text388.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text388.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text388.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text388.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text388.setAttribute('x', valueXIncrease);
      text388.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text388.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text388.setAttribute('x', valueXIncrease);
      text388.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text388.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text388.setAttribute('y', valueXIncrease);
      text388.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text388.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text388.setAttribute('y', valueXIncrease);
      text388.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF388, 0.001);
const text389 = document.getElementById('text389');
text389.innerHTML = number[0];
function textF389() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text389.setAttribute('fill', newColor);
  const id = text389.x['animVal'][0].value+""+text389.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text389.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text389.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text389.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text389.setAttribute('x', valueXIncrease);
      text389.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text389.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text389.setAttribute('x', valueXIncrease);
      text389.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text389.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text389.setAttribute('y', valueXIncrease);
      text389.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text389.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text389.setAttribute('y', valueXIncrease);
      text389.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF389, 0.001);
const text390 = document.getElementById('text390');
text390.innerHTML = number[0];
function textF390() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text390.setAttribute('fill', newColor);
  const id = text390.x['animVal'][0].value+""+text390.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text390.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text390.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text390.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text390.setAttribute('x', valueXIncrease);
      text390.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text390.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text390.setAttribute('x', valueXIncrease);
      text390.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text390.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text390.setAttribute('y', valueXIncrease);
      text390.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text390.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text390.setAttribute('y', valueXIncrease);
      text390.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF390, 0.001);
const text391 = document.getElementById('text391');
text391.innerHTML = number[0];
function textF391() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text391.setAttribute('fill', newColor);
  const id = text391.x['animVal'][0].value+""+text391.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text391.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text391.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text391.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text391.setAttribute('x', valueXIncrease);
      text391.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text391.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text391.setAttribute('x', valueXIncrease);
      text391.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text391.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text391.setAttribute('y', valueXIncrease);
      text391.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text391.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text391.setAttribute('y', valueXIncrease);
      text391.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF391, 0.001);
const text392 = document.getElementById('text392');
text392.innerHTML = number[0];
function textF392() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text392.setAttribute('fill', newColor);
  const id = text392.x['animVal'][0].value+""+text392.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text392.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text392.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text392.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text392.setAttribute('x', valueXIncrease);
      text392.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text392.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text392.setAttribute('x', valueXIncrease);
      text392.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text392.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text392.setAttribute('y', valueXIncrease);
      text392.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text392.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text392.setAttribute('y', valueXIncrease);
      text392.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF392, 0.001);
const text393 = document.getElementById('text393');
text393.innerHTML = number[0];
function textF393() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text393.setAttribute('fill', newColor);
  const id = text393.x['animVal'][0].value+""+text393.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text393.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text393.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text393.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text393.setAttribute('x', valueXIncrease);
      text393.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text393.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text393.setAttribute('x', valueXIncrease);
      text393.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text393.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text393.setAttribute('y', valueXIncrease);
      text393.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text393.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text393.setAttribute('y', valueXIncrease);
      text393.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF393, 0.001);
const text394 = document.getElementById('text394');
text394.innerHTML = number[0];
function textF394() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text394.setAttribute('fill', newColor);
  const id = text394.x['animVal'][0].value+""+text394.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text394.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text394.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text394.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text394.setAttribute('x', valueXIncrease);
      text394.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text394.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text394.setAttribute('x', valueXIncrease);
      text394.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text394.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text394.setAttribute('y', valueXIncrease);
      text394.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text394.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text394.setAttribute('y', valueXIncrease);
      text394.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF394, 0.001);
const text395 = document.getElementById('text395');
text395.innerHTML = number[0];
function textF395() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text395.setAttribute('fill', newColor);
  const id = text395.x['animVal'][0].value+""+text395.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text395.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text395.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text395.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text395.setAttribute('x', valueXIncrease);
      text395.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text395.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text395.setAttribute('x', valueXIncrease);
      text395.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text395.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text395.setAttribute('y', valueXIncrease);
      text395.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text395.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text395.setAttribute('y', valueXIncrease);
      text395.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF395, 0.001);
const text396 = document.getElementById('text396');
text396.innerHTML = number[0];
function textF396() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text396.setAttribute('fill', newColor);
  const id = text396.x['animVal'][0].value+""+text396.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text396.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text396.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text396.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text396.setAttribute('x', valueXIncrease);
      text396.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text396.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text396.setAttribute('x', valueXIncrease);
      text396.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text396.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text396.setAttribute('y', valueXIncrease);
      text396.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text396.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text396.setAttribute('y', valueXIncrease);
      text396.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF396, 0.001);
const text397 = document.getElementById('text397');
text397.innerHTML = number[0];
function textF397() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text397.setAttribute('fill', newColor);
  const id = text397.x['animVal'][0].value+""+text397.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text397.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text397.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text397.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text397.setAttribute('x', valueXIncrease);
      text397.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text397.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text397.setAttribute('x', valueXIncrease);
      text397.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text397.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text397.setAttribute('y', valueXIncrease);
      text397.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text397.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text397.setAttribute('y', valueXIncrease);
      text397.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF397, 0.001);
const text398 = document.getElementById('text398');
text398.innerHTML = number[0];
function textF398() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text398.setAttribute('fill', newColor);
  const id = text398.x['animVal'][0].value+""+text398.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text398.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text398.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text398.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text398.setAttribute('x', valueXIncrease);
      text398.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text398.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text398.setAttribute('x', valueXIncrease);
      text398.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text398.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text398.setAttribute('y', valueXIncrease);
      text398.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text398.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text398.setAttribute('y', valueXIncrease);
      text398.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF398, 0.001);
const text399 = document.getElementById('text399');
text399.innerHTML = number[0];
function textF399() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text399.setAttribute('fill', newColor);
  const id = text399.x['animVal'][0].value+""+text399.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text399.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text399.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text399.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text399.setAttribute('x', valueXIncrease);
      text399.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text399.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text399.setAttribute('x', valueXIncrease);
      text399.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text399.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text399.setAttribute('y', valueXIncrease);
      text399.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text399.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text399.setAttribute('y', valueXIncrease);
      text399.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF399, 0.001);
const text400 = document.getElementById('text400');
text400.innerHTML = number[0];
function textF400() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text400.setAttribute('fill', newColor);
  const id = text400.x['animVal'][0].value+""+text400.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text400.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text400.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text400.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text400.setAttribute('x', valueXIncrease);
      text400.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text400.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text400.setAttribute('x', valueXIncrease);
      text400.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text400.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text400.setAttribute('y', valueXIncrease);
      text400.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text400.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text400.setAttribute('y', valueXIncrease);
      text400.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF400, 0.001);
const text401 = document.getElementById('text401');
text401.innerHTML = number[0];
function textF401() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text401.setAttribute('fill', newColor);
  const id = text401.x['animVal'][0].value+""+text401.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text401.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text401.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text401.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text401.setAttribute('x', valueXIncrease);
      text401.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text401.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text401.setAttribute('x', valueXIncrease);
      text401.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text401.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text401.setAttribute('y', valueXIncrease);
      text401.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text401.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text401.setAttribute('y', valueXIncrease);
      text401.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF401, 0.001);
const text402 = document.getElementById('text402');
text402.innerHTML = number[0];
function textF402() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text402.setAttribute('fill', newColor);
  const id = text402.x['animVal'][0].value+""+text402.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text402.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text402.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text402.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text402.setAttribute('x', valueXIncrease);
      text402.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text402.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text402.setAttribute('x', valueXIncrease);
      text402.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text402.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text402.setAttribute('y', valueXIncrease);
      text402.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text402.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text402.setAttribute('y', valueXIncrease);
      text402.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF402, 0.001);
const text403 = document.getElementById('text403');
text403.innerHTML = number[0];
function textF403() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text403.setAttribute('fill', newColor);
  const id = text403.x['animVal'][0].value+""+text403.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text403.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text403.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text403.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text403.setAttribute('x', valueXIncrease);
      text403.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text403.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text403.setAttribute('x', valueXIncrease);
      text403.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text403.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text403.setAttribute('y', valueXIncrease);
      text403.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text403.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text403.setAttribute('y', valueXIncrease);
      text403.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF403, 0.001);
const text404 = document.getElementById('text404');
text404.innerHTML = number[0];
function textF404() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text404.setAttribute('fill', newColor);
  const id = text404.x['animVal'][0].value+""+text404.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text404.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text404.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text404.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text404.setAttribute('x', valueXIncrease);
      text404.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text404.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text404.setAttribute('x', valueXIncrease);
      text404.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text404.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text404.setAttribute('y', valueXIncrease);
      text404.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text404.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text404.setAttribute('y', valueXIncrease);
      text404.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF404, 0.001);
const text405 = document.getElementById('text405');
text405.innerHTML = number[0];
function textF405() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text405.setAttribute('fill', newColor);
  const id = text405.x['animVal'][0].value+""+text405.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text405.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text405.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text405.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text405.setAttribute('x', valueXIncrease);
      text405.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text405.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text405.setAttribute('x', valueXIncrease);
      text405.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text405.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text405.setAttribute('y', valueXIncrease);
      text405.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text405.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text405.setAttribute('y', valueXIncrease);
      text405.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF405, 0.001);
const text406 = document.getElementById('text406');
text406.innerHTML = number[0];
function textF406() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text406.setAttribute('fill', newColor);
  const id = text406.x['animVal'][0].value+""+text406.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text406.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text406.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text406.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text406.setAttribute('x', valueXIncrease);
      text406.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text406.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text406.setAttribute('x', valueXIncrease);
      text406.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text406.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text406.setAttribute('y', valueXIncrease);
      text406.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text406.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text406.setAttribute('y', valueXIncrease);
      text406.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF406, 0.001);
const text407 = document.getElementById('text407');
text407.innerHTML = number[0];
function textF407() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text407.setAttribute('fill', newColor);
  const id = text407.x['animVal'][0].value+""+text407.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text407.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text407.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text407.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text407.setAttribute('x', valueXIncrease);
      text407.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text407.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text407.setAttribute('x', valueXIncrease);
      text407.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text407.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text407.setAttribute('y', valueXIncrease);
      text407.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text407.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text407.setAttribute('y', valueXIncrease);
      text407.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF407, 0.001);
const text408 = document.getElementById('text408');
text408.innerHTML = number[0];
function textF408() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text408.setAttribute('fill', newColor);
  const id = text408.x['animVal'][0].value+""+text408.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text408.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text408.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text408.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text408.setAttribute('x', valueXIncrease);
      text408.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text408.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text408.setAttribute('x', valueXIncrease);
      text408.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text408.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text408.setAttribute('y', valueXIncrease);
      text408.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text408.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text408.setAttribute('y', valueXIncrease);
      text408.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF408, 0.001);
const text409 = document.getElementById('text409');
text409.innerHTML = number[0];
function textF409() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text409.setAttribute('fill', newColor);
  const id = text409.x['animVal'][0].value+""+text409.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text409.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text409.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text409.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text409.setAttribute('x', valueXIncrease);
      text409.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text409.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text409.setAttribute('x', valueXIncrease);
      text409.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text409.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text409.setAttribute('y', valueXIncrease);
      text409.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text409.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text409.setAttribute('y', valueXIncrease);
      text409.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF409, 0.001);
const text410 = document.getElementById('text410');
text410.innerHTML = number[0];
function textF410() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text410.setAttribute('fill', newColor);
  const id = text410.x['animVal'][0].value+""+text410.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text410.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text410.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text410.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text410.setAttribute('x', valueXIncrease);
      text410.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text410.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text410.setAttribute('x', valueXIncrease);
      text410.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text410.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text410.setAttribute('y', valueXIncrease);
      text410.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text410.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text410.setAttribute('y', valueXIncrease);
      text410.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF410, 0.001);
const text411 = document.getElementById('text411');
text411.innerHTML = number[0];
function textF411() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text411.setAttribute('fill', newColor);
  const id = text411.x['animVal'][0].value+""+text411.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text411.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text411.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text411.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text411.setAttribute('x', valueXIncrease);
      text411.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text411.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text411.setAttribute('x', valueXIncrease);
      text411.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text411.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text411.setAttribute('y', valueXIncrease);
      text411.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text411.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text411.setAttribute('y', valueXIncrease);
      text411.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF411, 0.001);
const text412 = document.getElementById('text412');
text412.innerHTML = number[0];
function textF412() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text412.setAttribute('fill', newColor);
  const id = text412.x['animVal'][0].value+""+text412.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text412.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text412.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text412.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text412.setAttribute('x', valueXIncrease);
      text412.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text412.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text412.setAttribute('x', valueXIncrease);
      text412.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text412.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text412.setAttribute('y', valueXIncrease);
      text412.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text412.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text412.setAttribute('y', valueXIncrease);
      text412.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF412, 0.001);
const text413 = document.getElementById('text413');
text413.innerHTML = number[0];
function textF413() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text413.setAttribute('fill', newColor);
  const id = text413.x['animVal'][0].value+""+text413.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text413.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text413.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text413.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text413.setAttribute('x', valueXIncrease);
      text413.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text413.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text413.setAttribute('x', valueXIncrease);
      text413.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text413.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text413.setAttribute('y', valueXIncrease);
      text413.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text413.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text413.setAttribute('y', valueXIncrease);
      text413.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF413, 0.001);
const text414 = document.getElementById('text414');
text414.innerHTML = number[0];
function textF414() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text414.setAttribute('fill', newColor);
  const id = text414.x['animVal'][0].value+""+text414.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text414.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text414.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text414.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text414.setAttribute('x', valueXIncrease);
      text414.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text414.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text414.setAttribute('x', valueXIncrease);
      text414.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text414.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text414.setAttribute('y', valueXIncrease);
      text414.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text414.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text414.setAttribute('y', valueXIncrease);
      text414.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF414, 0.001);
const text415 = document.getElementById('text415');
text415.innerHTML = number[0];
function textF415() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text415.setAttribute('fill', newColor);
  const id = text415.x['animVal'][0].value+""+text415.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text415.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text415.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text415.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text415.setAttribute('x', valueXIncrease);
      text415.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text415.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text415.setAttribute('x', valueXIncrease);
      text415.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text415.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text415.setAttribute('y', valueXIncrease);
      text415.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text415.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text415.setAttribute('y', valueXIncrease);
      text415.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF415, 0.001);
const text416 = document.getElementById('text416');
text416.innerHTML = number[0];
function textF416() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text416.setAttribute('fill', newColor);
  const id = text416.x['animVal'][0].value+""+text416.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text416.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text416.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text416.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text416.setAttribute('x', valueXIncrease);
      text416.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text416.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text416.setAttribute('x', valueXIncrease);
      text416.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text416.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text416.setAttribute('y', valueXIncrease);
      text416.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text416.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text416.setAttribute('y', valueXIncrease);
      text416.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF416, 0.001);
const text417 = document.getElementById('text417');
text417.innerHTML = number[0];
function textF417() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text417.setAttribute('fill', newColor);
  const id = text417.x['animVal'][0].value+""+text417.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text417.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text417.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text417.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text417.setAttribute('x', valueXIncrease);
      text417.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text417.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text417.setAttribute('x', valueXIncrease);
      text417.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text417.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text417.setAttribute('y', valueXIncrease);
      text417.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text417.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text417.setAttribute('y', valueXIncrease);
      text417.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF417, 0.001);
const text418 = document.getElementById('text418');
text418.innerHTML = number[0];
function textF418() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text418.setAttribute('fill', newColor);
  const id = text418.x['animVal'][0].value+""+text418.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text418.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text418.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text418.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text418.setAttribute('x', valueXIncrease);
      text418.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text418.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text418.setAttribute('x', valueXIncrease);
      text418.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text418.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text418.setAttribute('y', valueXIncrease);
      text418.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text418.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text418.setAttribute('y', valueXIncrease);
      text418.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF418, 0.001);
const text419 = document.getElementById('text419');
text419.innerHTML = number[0];
function textF419() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text419.setAttribute('fill', newColor);
  const id = text419.x['animVal'][0].value+""+text419.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text419.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text419.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text419.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text419.setAttribute('x', valueXIncrease);
      text419.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text419.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text419.setAttribute('x', valueXIncrease);
      text419.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text419.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text419.setAttribute('y', valueXIncrease);
      text419.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text419.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text419.setAttribute('y', valueXIncrease);
      text419.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF419, 0.001);
const text420 = document.getElementById('text420');
text420.innerHTML = number[0];
function textF420() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text420.setAttribute('fill', newColor);
  const id = text420.x['animVal'][0].value+""+text420.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text420.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text420.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text420.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text420.setAttribute('x', valueXIncrease);
      text420.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text420.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text420.setAttribute('x', valueXIncrease);
      text420.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text420.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text420.setAttribute('y', valueXIncrease);
      text420.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text420.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text420.setAttribute('y', valueXIncrease);
      text420.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF420, 0.001);
const text421 = document.getElementById('text421');
text421.innerHTML = number[0];
function textF421() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text421.setAttribute('fill', newColor);
  const id = text421.x['animVal'][0].value+""+text421.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text421.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text421.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text421.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text421.setAttribute('x', valueXIncrease);
      text421.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text421.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text421.setAttribute('x', valueXIncrease);
      text421.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text421.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text421.setAttribute('y', valueXIncrease);
      text421.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text421.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text421.setAttribute('y', valueXIncrease);
      text421.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF421, 0.001);
const text422 = document.getElementById('text422');
text422.innerHTML = number[0];
function textF422() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text422.setAttribute('fill', newColor);
  const id = text422.x['animVal'][0].value+""+text422.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text422.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text422.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text422.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text422.setAttribute('x', valueXIncrease);
      text422.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text422.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text422.setAttribute('x', valueXIncrease);
      text422.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text422.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text422.setAttribute('y', valueXIncrease);
      text422.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text422.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text422.setAttribute('y', valueXIncrease);
      text422.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF422, 0.001);
const text423 = document.getElementById('text423');
text423.innerHTML = number[0];
function textF423() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text423.setAttribute('fill', newColor);
  const id = text423.x['animVal'][0].value+""+text423.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text423.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text423.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text423.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text423.setAttribute('x', valueXIncrease);
      text423.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text423.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text423.setAttribute('x', valueXIncrease);
      text423.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text423.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text423.setAttribute('y', valueXIncrease);
      text423.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text423.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text423.setAttribute('y', valueXIncrease);
      text423.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF423, 0.001);
const text424 = document.getElementById('text424');
text424.innerHTML = number[0];
function textF424() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text424.setAttribute('fill', newColor);
  const id = text424.x['animVal'][0].value+""+text424.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text424.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text424.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text424.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text424.setAttribute('x', valueXIncrease);
      text424.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text424.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text424.setAttribute('x', valueXIncrease);
      text424.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text424.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text424.setAttribute('y', valueXIncrease);
      text424.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text424.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text424.setAttribute('y', valueXIncrease);
      text424.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF424, 0.001);
const text425 = document.getElementById('text425');
text425.innerHTML = number[0];
function textF425() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text425.setAttribute('fill', newColor);
  const id = text425.x['animVal'][0].value+""+text425.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text425.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text425.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text425.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text425.setAttribute('x', valueXIncrease);
      text425.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text425.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text425.setAttribute('x', valueXIncrease);
      text425.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text425.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text425.setAttribute('y', valueXIncrease);
      text425.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text425.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text425.setAttribute('y', valueXIncrease);
      text425.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF425, 0.001);
const text426 = document.getElementById('text426');
text426.innerHTML = number[0];
function textF426() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text426.setAttribute('fill', newColor);
  const id = text426.x['animVal'][0].value+""+text426.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text426.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text426.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text426.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text426.setAttribute('x', valueXIncrease);
      text426.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text426.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text426.setAttribute('x', valueXIncrease);
      text426.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text426.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text426.setAttribute('y', valueXIncrease);
      text426.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text426.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text426.setAttribute('y', valueXIncrease);
      text426.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF426, 0.001);
const text427 = document.getElementById('text427');
text427.innerHTML = number[0];
function textF427() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text427.setAttribute('fill', newColor);
  const id = text427.x['animVal'][0].value+""+text427.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text427.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text427.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text427.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text427.setAttribute('x', valueXIncrease);
      text427.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text427.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text427.setAttribute('x', valueXIncrease);
      text427.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text427.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text427.setAttribute('y', valueXIncrease);
      text427.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text427.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text427.setAttribute('y', valueXIncrease);
      text427.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF427, 0.001);
const text428 = document.getElementById('text428');
text428.innerHTML = number[0];
function textF428() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text428.setAttribute('fill', newColor);
  const id = text428.x['animVal'][0].value+""+text428.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text428.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text428.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text428.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text428.setAttribute('x', valueXIncrease);
      text428.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text428.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text428.setAttribute('x', valueXIncrease);
      text428.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text428.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text428.setAttribute('y', valueXIncrease);
      text428.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text428.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text428.setAttribute('y', valueXIncrease);
      text428.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF428, 0.001);
const text429 = document.getElementById('text429');
text429.innerHTML = number[0];
function textF429() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text429.setAttribute('fill', newColor);
  const id = text429.x['animVal'][0].value+""+text429.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text429.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text429.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text429.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text429.setAttribute('x', valueXIncrease);
      text429.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text429.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text429.setAttribute('x', valueXIncrease);
      text429.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text429.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text429.setAttribute('y', valueXIncrease);
      text429.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text429.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text429.setAttribute('y', valueXIncrease);
      text429.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF429, 0.001);
const text430 = document.getElementById('text430');
text430.innerHTML = number[0];
function textF430() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text430.setAttribute('fill', newColor);
  const id = text430.x['animVal'][0].value+""+text430.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text430.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text430.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text430.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text430.setAttribute('x', valueXIncrease);
      text430.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text430.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text430.setAttribute('x', valueXIncrease);
      text430.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text430.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text430.setAttribute('y', valueXIncrease);
      text430.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text430.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text430.setAttribute('y', valueXIncrease);
      text430.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF430, 0.001);
const text431 = document.getElementById('text431');
text431.innerHTML = number[0];
function textF431() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text431.setAttribute('fill', newColor);
  const id = text431.x['animVal'][0].value+""+text431.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text431.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text431.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text431.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text431.setAttribute('x', valueXIncrease);
      text431.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text431.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text431.setAttribute('x', valueXIncrease);
      text431.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text431.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text431.setAttribute('y', valueXIncrease);
      text431.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text431.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text431.setAttribute('y', valueXIncrease);
      text431.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF431, 0.001);
const text432 = document.getElementById('text432');
text432.innerHTML = number[0];
function textF432() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text432.setAttribute('fill', newColor);
  const id = text432.x['animVal'][0].value+""+text432.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text432.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text432.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text432.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text432.setAttribute('x', valueXIncrease);
      text432.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text432.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text432.setAttribute('x', valueXIncrease);
      text432.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text432.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text432.setAttribute('y', valueXIncrease);
      text432.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text432.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text432.setAttribute('y', valueXIncrease);
      text432.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF432, 0.001);
const text433 = document.getElementById('text433');
text433.innerHTML = number[0];
function textF433() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text433.setAttribute('fill', newColor);
  const id = text433.x['animVal'][0].value+""+text433.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text433.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text433.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text433.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text433.setAttribute('x', valueXIncrease);
      text433.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text433.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text433.setAttribute('x', valueXIncrease);
      text433.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text433.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text433.setAttribute('y', valueXIncrease);
      text433.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text433.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text433.setAttribute('y', valueXIncrease);
      text433.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF433, 0.001);
const text434 = document.getElementById('text434');
text434.innerHTML = number[0];
function textF434() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text434.setAttribute('fill', newColor);
  const id = text434.x['animVal'][0].value+""+text434.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text434.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text434.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text434.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text434.setAttribute('x', valueXIncrease);
      text434.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text434.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text434.setAttribute('x', valueXIncrease);
      text434.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text434.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text434.setAttribute('y', valueXIncrease);
      text434.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text434.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text434.setAttribute('y', valueXIncrease);
      text434.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF434, 0.001);
const text435 = document.getElementById('text435');
text435.innerHTML = number[0];
function textF435() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text435.setAttribute('fill', newColor);
  const id = text435.x['animVal'][0].value+""+text435.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text435.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text435.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text435.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text435.setAttribute('x', valueXIncrease);
      text435.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text435.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text435.setAttribute('x', valueXIncrease);
      text435.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text435.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text435.setAttribute('y', valueXIncrease);
      text435.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text435.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text435.setAttribute('y', valueXIncrease);
      text435.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF435, 0.001);
const text436 = document.getElementById('text436');
text436.innerHTML = number[0];
function textF436() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text436.setAttribute('fill', newColor);
  const id = text436.x['animVal'][0].value+""+text436.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text436.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text436.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text436.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text436.setAttribute('x', valueXIncrease);
      text436.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text436.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text436.setAttribute('x', valueXIncrease);
      text436.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text436.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text436.setAttribute('y', valueXIncrease);
      text436.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text436.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text436.setAttribute('y', valueXIncrease);
      text436.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF436, 0.001);
const text437 = document.getElementById('text437');
text437.innerHTML = number[0];
function textF437() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text437.setAttribute('fill', newColor);
  const id = text437.x['animVal'][0].value+""+text437.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text437.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text437.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text437.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text437.setAttribute('x', valueXIncrease);
      text437.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text437.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text437.setAttribute('x', valueXIncrease);
      text437.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text437.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text437.setAttribute('y', valueXIncrease);
      text437.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text437.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text437.setAttribute('y', valueXIncrease);
      text437.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF437, 0.001);
const text438 = document.getElementById('text438');
text438.innerHTML = number[0];
function textF438() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text438.setAttribute('fill', newColor);
  const id = text438.x['animVal'][0].value+""+text438.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text438.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text438.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text438.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text438.setAttribute('x', valueXIncrease);
      text438.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text438.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text438.setAttribute('x', valueXIncrease);
      text438.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text438.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text438.setAttribute('y', valueXIncrease);
      text438.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text438.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text438.setAttribute('y', valueXIncrease);
      text438.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF438, 0.001);
const text439 = document.getElementById('text439');
text439.innerHTML = number[0];
function textF439() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text439.setAttribute('fill', newColor);
  const id = text439.x['animVal'][0].value+""+text439.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text439.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text439.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text439.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text439.setAttribute('x', valueXIncrease);
      text439.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text439.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text439.setAttribute('x', valueXIncrease);
      text439.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text439.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text439.setAttribute('y', valueXIncrease);
      text439.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text439.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text439.setAttribute('y', valueXIncrease);
      text439.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF439, 0.001);
const text440 = document.getElementById('text440');
text440.innerHTML = number[0];
function textF440() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text440.setAttribute('fill', newColor);
  const id = text440.x['animVal'][0].value+""+text440.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text440.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text440.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text440.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text440.setAttribute('x', valueXIncrease);
      text440.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text440.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text440.setAttribute('x', valueXIncrease);
      text440.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text440.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text440.setAttribute('y', valueXIncrease);
      text440.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text440.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text440.setAttribute('y', valueXIncrease);
      text440.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF440, 0.001);
const text441 = document.getElementById('text441');
text441.innerHTML = number[0];
function textF441() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text441.setAttribute('fill', newColor);
  const id = text441.x['animVal'][0].value+""+text441.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text441.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text441.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text441.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text441.setAttribute('x', valueXIncrease);
      text441.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text441.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text441.setAttribute('x', valueXIncrease);
      text441.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text441.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text441.setAttribute('y', valueXIncrease);
      text441.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text441.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text441.setAttribute('y', valueXIncrease);
      text441.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF441, 0.001);
const text442 = document.getElementById('text442');
text442.innerHTML = number[0];
function textF442() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text442.setAttribute('fill', newColor);
  const id = text442.x['animVal'][0].value+""+text442.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text442.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text442.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text442.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text442.setAttribute('x', valueXIncrease);
      text442.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text442.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text442.setAttribute('x', valueXIncrease);
      text442.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text442.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text442.setAttribute('y', valueXIncrease);
      text442.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text442.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text442.setAttribute('y', valueXIncrease);
      text442.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF442, 0.001);
const text443 = document.getElementById('text443');
text443.innerHTML = number[0];
function textF443() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text443.setAttribute('fill', newColor);
  const id = text443.x['animVal'][0].value+""+text443.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text443.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text443.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text443.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text443.setAttribute('x', valueXIncrease);
      text443.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text443.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text443.setAttribute('x', valueXIncrease);
      text443.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text443.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text443.setAttribute('y', valueXIncrease);
      text443.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text443.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text443.setAttribute('y', valueXIncrease);
      text443.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF443, 0.001);
const text444 = document.getElementById('text444');
text444.innerHTML = number[0];
function textF444() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text444.setAttribute('fill', newColor);
  const id = text444.x['animVal'][0].value+""+text444.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text444.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text444.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text444.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text444.setAttribute('x', valueXIncrease);
      text444.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text444.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text444.setAttribute('x', valueXIncrease);
      text444.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text444.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text444.setAttribute('y', valueXIncrease);
      text444.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text444.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text444.setAttribute('y', valueXIncrease);
      text444.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF444, 0.001);
const text445 = document.getElementById('text445');
text445.innerHTML = number[0];
function textF445() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text445.setAttribute('fill', newColor);
  const id = text445.x['animVal'][0].value+""+text445.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text445.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text445.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text445.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text445.setAttribute('x', valueXIncrease);
      text445.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text445.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text445.setAttribute('x', valueXIncrease);
      text445.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text445.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text445.setAttribute('y', valueXIncrease);
      text445.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text445.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text445.setAttribute('y', valueXIncrease);
      text445.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF445, 0.001);
const text446 = document.getElementById('text446');
text446.innerHTML = number[0];
function textF446() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text446.setAttribute('fill', newColor);
  const id = text446.x['animVal'][0].value+""+text446.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text446.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text446.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text446.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text446.setAttribute('x', valueXIncrease);
      text446.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text446.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text446.setAttribute('x', valueXIncrease);
      text446.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text446.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text446.setAttribute('y', valueXIncrease);
      text446.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text446.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text446.setAttribute('y', valueXIncrease);
      text446.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF446, 0.001);
const text447 = document.getElementById('text447');
text447.innerHTML = number[0];
function textF447() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text447.setAttribute('fill', newColor);
  const id = text447.x['animVal'][0].value+""+text447.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text447.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text447.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text447.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text447.setAttribute('x', valueXIncrease);
      text447.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text447.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text447.setAttribute('x', valueXIncrease);
      text447.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text447.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text447.setAttribute('y', valueXIncrease);
      text447.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text447.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text447.setAttribute('y', valueXIncrease);
      text447.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF447, 0.001);
const text448 = document.getElementById('text448');
text448.innerHTML = number[0];
function textF448() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text448.setAttribute('fill', newColor);
  const id = text448.x['animVal'][0].value+""+text448.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text448.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text448.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text448.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text448.setAttribute('x', valueXIncrease);
      text448.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text448.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text448.setAttribute('x', valueXIncrease);
      text448.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text448.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text448.setAttribute('y', valueXIncrease);
      text448.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text448.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text448.setAttribute('y', valueXIncrease);
      text448.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF448, 0.001);
const text449 = document.getElementById('text449');
text449.innerHTML = number[0];
function textF449() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text449.setAttribute('fill', newColor);
  const id = text449.x['animVal'][0].value+""+text449.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text449.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text449.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text449.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text449.setAttribute('x', valueXIncrease);
      text449.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text449.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text449.setAttribute('x', valueXIncrease);
      text449.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text449.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text449.setAttribute('y', valueXIncrease);
      text449.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text449.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text449.setAttribute('y', valueXIncrease);
      text449.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF449, 0.001);
const text450 = document.getElementById('text450');
text450.innerHTML = number[0];
function textF450() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text450.setAttribute('fill', newColor);
  const id = text450.x['animVal'][0].value+""+text450.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text450.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text450.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text450.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text450.setAttribute('x', valueXIncrease);
      text450.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text450.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text450.setAttribute('x', valueXIncrease);
      text450.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text450.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text450.setAttribute('y', valueXIncrease);
      text450.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text450.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text450.setAttribute('y', valueXIncrease);
      text450.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF450, 0.001);
const text451 = document.getElementById('text451');
text451.innerHTML = number[0];
function textF451() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text451.setAttribute('fill', newColor);
  const id = text451.x['animVal'][0].value+""+text451.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text451.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text451.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text451.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text451.setAttribute('x', valueXIncrease);
      text451.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text451.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text451.setAttribute('x', valueXIncrease);
      text451.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text451.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text451.setAttribute('y', valueXIncrease);
      text451.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text451.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text451.setAttribute('y', valueXIncrease);
      text451.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF451, 0.001);
const text452 = document.getElementById('text452');
text452.innerHTML = number[0];
function textF452() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text452.setAttribute('fill', newColor);
  const id = text452.x['animVal'][0].value+""+text452.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text452.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text452.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text452.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text452.setAttribute('x', valueXIncrease);
      text452.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text452.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text452.setAttribute('x', valueXIncrease);
      text452.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text452.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text452.setAttribute('y', valueXIncrease);
      text452.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text452.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text452.setAttribute('y', valueXIncrease);
      text452.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF452, 0.001);
const text453 = document.getElementById('text453');
text453.innerHTML = number[0];
function textF453() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text453.setAttribute('fill', newColor);
  const id = text453.x['animVal'][0].value+""+text453.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text453.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text453.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text453.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text453.setAttribute('x', valueXIncrease);
      text453.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text453.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text453.setAttribute('x', valueXIncrease);
      text453.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text453.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text453.setAttribute('y', valueXIncrease);
      text453.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text453.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text453.setAttribute('y', valueXIncrease);
      text453.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF453, 0.001);
const text454 = document.getElementById('text454');
text454.innerHTML = number[0];
function textF454() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text454.setAttribute('fill', newColor);
  const id = text454.x['animVal'][0].value+""+text454.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text454.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text454.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text454.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text454.setAttribute('x', valueXIncrease);
      text454.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text454.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text454.setAttribute('x', valueXIncrease);
      text454.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text454.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text454.setAttribute('y', valueXIncrease);
      text454.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text454.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text454.setAttribute('y', valueXIncrease);
      text454.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF454, 0.001);
const text455 = document.getElementById('text455');
text455.innerHTML = number[0];
function textF455() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text455.setAttribute('fill', newColor);
  const id = text455.x['animVal'][0].value+""+text455.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text455.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text455.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text455.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text455.setAttribute('x', valueXIncrease);
      text455.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text455.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text455.setAttribute('x', valueXIncrease);
      text455.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text455.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text455.setAttribute('y', valueXIncrease);
      text455.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text455.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text455.setAttribute('y', valueXIncrease);
      text455.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF455, 0.001);
const text456 = document.getElementById('text456');
text456.innerHTML = number[0];
function textF456() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text456.setAttribute('fill', newColor);
  const id = text456.x['animVal'][0].value+""+text456.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text456.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text456.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text456.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text456.setAttribute('x', valueXIncrease);
      text456.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text456.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text456.setAttribute('x', valueXIncrease);
      text456.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text456.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text456.setAttribute('y', valueXIncrease);
      text456.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text456.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text456.setAttribute('y', valueXIncrease);
      text456.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF456, 0.001);
const text457 = document.getElementById('text457');
text457.innerHTML = number[0];
function textF457() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text457.setAttribute('fill', newColor);
  const id = text457.x['animVal'][0].value+""+text457.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text457.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text457.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text457.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text457.setAttribute('x', valueXIncrease);
      text457.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text457.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text457.setAttribute('x', valueXIncrease);
      text457.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text457.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text457.setAttribute('y', valueXIncrease);
      text457.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text457.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text457.setAttribute('y', valueXIncrease);
      text457.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF457, 0.001);
const text458 = document.getElementById('text458');
text458.innerHTML = number[0];
function textF458() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text458.setAttribute('fill', newColor);
  const id = text458.x['animVal'][0].value+""+text458.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text458.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text458.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text458.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text458.setAttribute('x', valueXIncrease);
      text458.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text458.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text458.setAttribute('x', valueXIncrease);
      text458.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text458.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text458.setAttribute('y', valueXIncrease);
      text458.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text458.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text458.setAttribute('y', valueXIncrease);
      text458.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF458, 0.001);
const text459 = document.getElementById('text459');
text459.innerHTML = number[0];
function textF459() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text459.setAttribute('fill', newColor);
  const id = text459.x['animVal'][0].value+""+text459.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text459.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text459.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text459.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text459.setAttribute('x', valueXIncrease);
      text459.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text459.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text459.setAttribute('x', valueXIncrease);
      text459.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text459.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text459.setAttribute('y', valueXIncrease);
      text459.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text459.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text459.setAttribute('y', valueXIncrease);
      text459.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF459, 0.001);
const text460 = document.getElementById('text460');
text460.innerHTML = number[0];
function textF460() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text460.setAttribute('fill', newColor);
  const id = text460.x['animVal'][0].value+""+text460.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text460.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text460.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text460.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text460.setAttribute('x', valueXIncrease);
      text460.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text460.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text460.setAttribute('x', valueXIncrease);
      text460.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text460.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text460.setAttribute('y', valueXIncrease);
      text460.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text460.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text460.setAttribute('y', valueXIncrease);
      text460.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF460, 0.001);
const text461 = document.getElementById('text461');
text461.innerHTML = number[0];
function textF461() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text461.setAttribute('fill', newColor);
  const id = text461.x['animVal'][0].value+""+text461.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text461.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text461.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text461.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text461.setAttribute('x', valueXIncrease);
      text461.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text461.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text461.setAttribute('x', valueXIncrease);
      text461.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text461.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text461.setAttribute('y', valueXIncrease);
      text461.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text461.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text461.setAttribute('y', valueXIncrease);
      text461.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF461, 0.001);
const text462 = document.getElementById('text462');
text462.innerHTML = number[0];
function textF462() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text462.setAttribute('fill', newColor);
  const id = text462.x['animVal'][0].value+""+text462.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text462.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text462.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text462.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text462.setAttribute('x', valueXIncrease);
      text462.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text462.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text462.setAttribute('x', valueXIncrease);
      text462.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text462.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text462.setAttribute('y', valueXIncrease);
      text462.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text462.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text462.setAttribute('y', valueXIncrease);
      text462.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF462, 0.001);
const text463 = document.getElementById('text463');
text463.innerHTML = number[0];
function textF463() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text463.setAttribute('fill', newColor);
  const id = text463.x['animVal'][0].value+""+text463.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text463.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text463.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text463.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text463.setAttribute('x', valueXIncrease);
      text463.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text463.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text463.setAttribute('x', valueXIncrease);
      text463.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text463.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text463.setAttribute('y', valueXIncrease);
      text463.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text463.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text463.setAttribute('y', valueXIncrease);
      text463.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF463, 0.001);
const text464 = document.getElementById('text464');
text464.innerHTML = number[0];
function textF464() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text464.setAttribute('fill', newColor);
  const id = text464.x['animVal'][0].value+""+text464.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text464.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text464.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text464.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text464.setAttribute('x', valueXIncrease);
      text464.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text464.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text464.setAttribute('x', valueXIncrease);
      text464.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text464.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text464.setAttribute('y', valueXIncrease);
      text464.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text464.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text464.setAttribute('y', valueXIncrease);
      text464.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF464, 0.001);
const text465 = document.getElementById('text465');
text465.innerHTML = number[0];
function textF465() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text465.setAttribute('fill', newColor);
  const id = text465.x['animVal'][0].value+""+text465.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text465.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text465.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text465.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text465.setAttribute('x', valueXIncrease);
      text465.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text465.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text465.setAttribute('x', valueXIncrease);
      text465.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text465.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text465.setAttribute('y', valueXIncrease);
      text465.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text465.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text465.setAttribute('y', valueXIncrease);
      text465.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF465, 0.001);
const text466 = document.getElementById('text466');
text466.innerHTML = number[0];
function textF466() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text466.setAttribute('fill', newColor);
  const id = text466.x['animVal'][0].value+""+text466.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text466.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text466.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text466.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text466.setAttribute('x', valueXIncrease);
      text466.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text466.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text466.setAttribute('x', valueXIncrease);
      text466.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text466.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text466.setAttribute('y', valueXIncrease);
      text466.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text466.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text466.setAttribute('y', valueXIncrease);
      text466.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF466, 0.001);
const text467 = document.getElementById('text467');
text467.innerHTML = number[0];
function textF467() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text467.setAttribute('fill', newColor);
  const id = text467.x['animVal'][0].value+""+text467.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text467.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text467.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text467.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text467.setAttribute('x', valueXIncrease);
      text467.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text467.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text467.setAttribute('x', valueXIncrease);
      text467.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text467.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text467.setAttribute('y', valueXIncrease);
      text467.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text467.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text467.setAttribute('y', valueXIncrease);
      text467.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF467, 0.001);
const text468 = document.getElementById('text468');
text468.innerHTML = number[0];
function textF468() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text468.setAttribute('fill', newColor);
  const id = text468.x['animVal'][0].value+""+text468.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text468.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text468.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text468.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text468.setAttribute('x', valueXIncrease);
      text468.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text468.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text468.setAttribute('x', valueXIncrease);
      text468.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text468.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text468.setAttribute('y', valueXIncrease);
      text468.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text468.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text468.setAttribute('y', valueXIncrease);
      text468.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF468, 0.001);
const text469 = document.getElementById('text469');
text469.innerHTML = number[0];
function textF469() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text469.setAttribute('fill', newColor);
  const id = text469.x['animVal'][0].value+""+text469.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text469.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text469.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text469.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text469.setAttribute('x', valueXIncrease);
      text469.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text469.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text469.setAttribute('x', valueXIncrease);
      text469.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text469.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text469.setAttribute('y', valueXIncrease);
      text469.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text469.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text469.setAttribute('y', valueXIncrease);
      text469.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF469, 0.001);
const text470 = document.getElementById('text470');
text470.innerHTML = number[0];
function textF470() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text470.setAttribute('fill', newColor);
  const id = text470.x['animVal'][0].value+""+text470.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text470.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text470.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text470.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text470.setAttribute('x', valueXIncrease);
      text470.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text470.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text470.setAttribute('x', valueXIncrease);
      text470.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text470.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text470.setAttribute('y', valueXIncrease);
      text470.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text470.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text470.setAttribute('y', valueXIncrease);
      text470.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF470, 0.001);
const text471 = document.getElementById('text471');
text471.innerHTML = number[0];
function textF471() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text471.setAttribute('fill', newColor);
  const id = text471.x['animVal'][0].value+""+text471.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text471.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text471.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text471.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text471.setAttribute('x', valueXIncrease);
      text471.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text471.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text471.setAttribute('x', valueXIncrease);
      text471.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text471.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text471.setAttribute('y', valueXIncrease);
      text471.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text471.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text471.setAttribute('y', valueXIncrease);
      text471.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF471, 0.001);
const text472 = document.getElementById('text472');
text472.innerHTML = number[0];
function textF472() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text472.setAttribute('fill', newColor);
  const id = text472.x['animVal'][0].value+""+text472.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text472.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text472.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text472.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text472.setAttribute('x', valueXIncrease);
      text472.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text472.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text472.setAttribute('x', valueXIncrease);
      text472.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text472.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text472.setAttribute('y', valueXIncrease);
      text472.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text472.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text472.setAttribute('y', valueXIncrease);
      text472.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF472, 0.001);
const text473 = document.getElementById('text473');
text473.innerHTML = number[0];
function textF473() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text473.setAttribute('fill', newColor);
  const id = text473.x['animVal'][0].value+""+text473.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text473.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text473.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text473.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text473.setAttribute('x', valueXIncrease);
      text473.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text473.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text473.setAttribute('x', valueXIncrease);
      text473.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text473.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text473.setAttribute('y', valueXIncrease);
      text473.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text473.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text473.setAttribute('y', valueXIncrease);
      text473.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF473, 0.001);
const text474 = document.getElementById('text474');
text474.innerHTML = number[0];
function textF474() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text474.setAttribute('fill', newColor);
  const id = text474.x['animVal'][0].value+""+text474.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text474.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text474.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text474.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text474.setAttribute('x', valueXIncrease);
      text474.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text474.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text474.setAttribute('x', valueXIncrease);
      text474.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text474.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text474.setAttribute('y', valueXIncrease);
      text474.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text474.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text474.setAttribute('y', valueXIncrease);
      text474.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF474, 0.001);
const text475 = document.getElementById('text475');
text475.innerHTML = number[0];
function textF475() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text475.setAttribute('fill', newColor);
  const id = text475.x['animVal'][0].value+""+text475.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text475.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text475.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text475.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text475.setAttribute('x', valueXIncrease);
      text475.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text475.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text475.setAttribute('x', valueXIncrease);
      text475.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text475.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text475.setAttribute('y', valueXIncrease);
      text475.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text475.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text475.setAttribute('y', valueXIncrease);
      text475.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF475, 0.001);
const text476 = document.getElementById('text476');
text476.innerHTML = number[0];
function textF476() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text476.setAttribute('fill', newColor);
  const id = text476.x['animVal'][0].value+""+text476.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text476.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text476.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text476.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text476.setAttribute('x', valueXIncrease);
      text476.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text476.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text476.setAttribute('x', valueXIncrease);
      text476.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text476.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text476.setAttribute('y', valueXIncrease);
      text476.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text476.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text476.setAttribute('y', valueXIncrease);
      text476.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF476, 0.001);
const text477 = document.getElementById('text477');
text477.innerHTML = number[0];
function textF477() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text477.setAttribute('fill', newColor);
  const id = text477.x['animVal'][0].value+""+text477.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text477.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text477.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text477.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text477.setAttribute('x', valueXIncrease);
      text477.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text477.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text477.setAttribute('x', valueXIncrease);
      text477.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text477.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text477.setAttribute('y', valueXIncrease);
      text477.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text477.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text477.setAttribute('y', valueXIncrease);
      text477.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF477, 0.001);
const text478 = document.getElementById('text478');
text478.innerHTML = number[0];
function textF478() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text478.setAttribute('fill', newColor);
  const id = text478.x['animVal'][0].value+""+text478.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text478.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text478.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text478.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text478.setAttribute('x', valueXIncrease);
      text478.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text478.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text478.setAttribute('x', valueXIncrease);
      text478.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text478.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text478.setAttribute('y', valueXIncrease);
      text478.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text478.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text478.setAttribute('y', valueXIncrease);
      text478.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF478, 0.001);
const text479 = document.getElementById('text479');
text479.innerHTML = number[0];
function textF479() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text479.setAttribute('fill', newColor);
  const id = text479.x['animVal'][0].value+""+text479.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text479.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text479.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text479.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text479.setAttribute('x', valueXIncrease);
      text479.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text479.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text479.setAttribute('x', valueXIncrease);
      text479.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text479.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text479.setAttribute('y', valueXIncrease);
      text479.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text479.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text479.setAttribute('y', valueXIncrease);
      text479.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF479, 0.001);
const text480 = document.getElementById('text480');
text480.innerHTML = number[0];
function textF480() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text480.setAttribute('fill', newColor);
  const id = text480.x['animVal'][0].value+""+text480.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text480.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text480.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text480.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text480.setAttribute('x', valueXIncrease);
      text480.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text480.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text480.setAttribute('x', valueXIncrease);
      text480.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text480.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text480.setAttribute('y', valueXIncrease);
      text480.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text480.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text480.setAttribute('y', valueXIncrease);
      text480.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF480, 0.001);
const text481 = document.getElementById('text481');
text481.innerHTML = number[0];
function textF481() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text481.setAttribute('fill', newColor);
  const id = text481.x['animVal'][0].value+""+text481.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text481.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text481.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text481.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text481.setAttribute('x', valueXIncrease);
      text481.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text481.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text481.setAttribute('x', valueXIncrease);
      text481.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text481.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text481.setAttribute('y', valueXIncrease);
      text481.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text481.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text481.setAttribute('y', valueXIncrease);
      text481.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF481, 0.001);
const text482 = document.getElementById('text482');
text482.innerHTML = number[0];
function textF482() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text482.setAttribute('fill', newColor);
  const id = text482.x['animVal'][0].value+""+text482.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text482.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text482.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text482.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text482.setAttribute('x', valueXIncrease);
      text482.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text482.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text482.setAttribute('x', valueXIncrease);
      text482.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text482.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text482.setAttribute('y', valueXIncrease);
      text482.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text482.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text482.setAttribute('y', valueXIncrease);
      text482.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF482, 0.001);
const text483 = document.getElementById('text483');
text483.innerHTML = number[0];
function textF483() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text483.setAttribute('fill', newColor);
  const id = text483.x['animVal'][0].value+""+text483.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text483.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text483.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text483.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text483.setAttribute('x', valueXIncrease);
      text483.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text483.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text483.setAttribute('x', valueXIncrease);
      text483.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text483.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text483.setAttribute('y', valueXIncrease);
      text483.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text483.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text483.setAttribute('y', valueXIncrease);
      text483.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF483, 0.001);
const text484 = document.getElementById('text484');
text484.innerHTML = number[0];
function textF484() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text484.setAttribute('fill', newColor);
  const id = text484.x['animVal'][0].value+""+text484.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text484.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text484.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text484.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text484.setAttribute('x', valueXIncrease);
      text484.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text484.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text484.setAttribute('x', valueXIncrease);
      text484.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text484.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text484.setAttribute('y', valueXIncrease);
      text484.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text484.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text484.setAttribute('y', valueXIncrease);
      text484.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF484, 0.001);
const text485 = document.getElementById('text485');
text485.innerHTML = number[0];
function textF485() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text485.setAttribute('fill', newColor);
  const id = text485.x['animVal'][0].value+""+text485.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text485.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text485.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text485.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text485.setAttribute('x', valueXIncrease);
      text485.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text485.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text485.setAttribute('x', valueXIncrease);
      text485.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text485.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text485.setAttribute('y', valueXIncrease);
      text485.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text485.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text485.setAttribute('y', valueXIncrease);
      text485.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF485, 0.001);
const text486 = document.getElementById('text486');
text486.innerHTML = number[0];
function textF486() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text486.setAttribute('fill', newColor);
  const id = text486.x['animVal'][0].value+""+text486.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text486.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text486.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text486.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text486.setAttribute('x', valueXIncrease);
      text486.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text486.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text486.setAttribute('x', valueXIncrease);
      text486.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text486.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text486.setAttribute('y', valueXIncrease);
      text486.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text486.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text486.setAttribute('y', valueXIncrease);
      text486.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF486, 0.001);
const text487 = document.getElementById('text487');
text487.innerHTML = number[0];
function textF487() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text487.setAttribute('fill', newColor);
  const id = text487.x['animVal'][0].value+""+text487.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text487.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text487.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text487.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text487.setAttribute('x', valueXIncrease);
      text487.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text487.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text487.setAttribute('x', valueXIncrease);
      text487.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text487.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text487.setAttribute('y', valueXIncrease);
      text487.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text487.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text487.setAttribute('y', valueXIncrease);
      text487.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF487, 0.001);
const text488 = document.getElementById('text488');
text488.innerHTML = number[0];
function textF488() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text488.setAttribute('fill', newColor);
  const id = text488.x['animVal'][0].value+""+text488.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text488.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text488.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text488.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text488.setAttribute('x', valueXIncrease);
      text488.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text488.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text488.setAttribute('x', valueXIncrease);
      text488.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text488.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text488.setAttribute('y', valueXIncrease);
      text488.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text488.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text488.setAttribute('y', valueXIncrease);
      text488.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF488, 0.001);
const text489 = document.getElementById('text489');
text489.innerHTML = number[0];
function textF489() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text489.setAttribute('fill', newColor);
  const id = text489.x['animVal'][0].value+""+text489.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text489.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text489.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text489.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text489.setAttribute('x', valueXIncrease);
      text489.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text489.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text489.setAttribute('x', valueXIncrease);
      text489.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text489.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text489.setAttribute('y', valueXIncrease);
      text489.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text489.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text489.setAttribute('y', valueXIncrease);
      text489.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF489, 0.001);
const text490 = document.getElementById('text490');
text490.innerHTML = number[0];
function textF490() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text490.setAttribute('fill', newColor);
  const id = text490.x['animVal'][0].value+""+text490.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text490.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text490.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text490.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text490.setAttribute('x', valueXIncrease);
      text490.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text490.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text490.setAttribute('x', valueXIncrease);
      text490.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text490.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text490.setAttribute('y', valueXIncrease);
      text490.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text490.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text490.setAttribute('y', valueXIncrease);
      text490.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF490, 0.001);
const text491 = document.getElementById('text491');
text491.innerHTML = number[0];
function textF491() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text491.setAttribute('fill', newColor);
  const id = text491.x['animVal'][0].value+""+text491.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text491.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text491.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text491.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text491.setAttribute('x', valueXIncrease);
      text491.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text491.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text491.setAttribute('x', valueXIncrease);
      text491.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text491.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text491.setAttribute('y', valueXIncrease);
      text491.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text491.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text491.setAttribute('y', valueXIncrease);
      text491.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF491, 0.001);
const text492 = document.getElementById('text492');
text492.innerHTML = number[0];
function textF492() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text492.setAttribute('fill', newColor);
  const id = text492.x['animVal'][0].value+""+text492.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text492.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text492.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text492.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text492.setAttribute('x', valueXIncrease);
      text492.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text492.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text492.setAttribute('x', valueXIncrease);
      text492.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text492.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text492.setAttribute('y', valueXIncrease);
      text492.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text492.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text492.setAttribute('y', valueXIncrease);
      text492.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF492, 0.001);
const text493 = document.getElementById('text493');
text493.innerHTML = number[0];
function textF493() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text493.setAttribute('fill', newColor);
  const id = text493.x['animVal'][0].value+""+text493.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text493.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text493.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text493.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text493.setAttribute('x', valueXIncrease);
      text493.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text493.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text493.setAttribute('x', valueXIncrease);
      text493.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text493.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text493.setAttribute('y', valueXIncrease);
      text493.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text493.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text493.setAttribute('y', valueXIncrease);
      text493.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF493, 0.001);
const text494 = document.getElementById('text494');
text494.innerHTML = number[0];
function textF494() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text494.setAttribute('fill', newColor);
  const id = text494.x['animVal'][0].value+""+text494.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text494.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text494.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text494.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text494.setAttribute('x', valueXIncrease);
      text494.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text494.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text494.setAttribute('x', valueXIncrease);
      text494.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text494.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text494.setAttribute('y', valueXIncrease);
      text494.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text494.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text494.setAttribute('y', valueXIncrease);
      text494.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF494, 0.001);
const text495 = document.getElementById('text495');
text495.innerHTML = number[0];
function textF495() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text495.setAttribute('fill', newColor);
  const id = text495.x['animVal'][0].value+""+text495.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text495.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text495.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text495.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text495.setAttribute('x', valueXIncrease);
      text495.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text495.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text495.setAttribute('x', valueXIncrease);
      text495.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text495.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text495.setAttribute('y', valueXIncrease);
      text495.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text495.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text495.setAttribute('y', valueXIncrease);
      text495.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF495, 0.001);
const text496 = document.getElementById('text496');
text496.innerHTML = number[0];
function textF496() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text496.setAttribute('fill', newColor);
  const id = text496.x['animVal'][0].value+""+text496.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text496.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text496.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text496.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text496.setAttribute('x', valueXIncrease);
      text496.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text496.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text496.setAttribute('x', valueXIncrease);
      text496.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text496.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text496.setAttribute('y', valueXIncrease);
      text496.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text496.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text496.setAttribute('y', valueXIncrease);
      text496.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF496, 0.001);
const text497 = document.getElementById('text497');
text497.innerHTML = number[0];
function textF497() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text497.setAttribute('fill', newColor);
  const id = text497.x['animVal'][0].value+""+text497.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text497.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text497.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text497.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text497.setAttribute('x', valueXIncrease);
      text497.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text497.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text497.setAttribute('x', valueXIncrease);
      text497.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text497.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text497.setAttribute('y', valueXIncrease);
      text497.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text497.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text497.setAttribute('y', valueXIncrease);
      text497.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF497, 0.001);
const text498 = document.getElementById('text498');
text498.innerHTML = number[0];
function textF498() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text498.setAttribute('fill', newColor);
  const id = text498.x['animVal'][0].value+""+text498.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text498.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text498.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text498.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text498.setAttribute('x', valueXIncrease);
      text498.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text498.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text498.setAttribute('x', valueXIncrease);
      text498.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text498.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text498.setAttribute('y', valueXIncrease);
      text498.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text498.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text498.setAttribute('y', valueXIncrease);
      text498.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF498, 0.001);
const text499 = document.getElementById('text499');
text499.innerHTML = number[0];
function textF499() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text499.setAttribute('fill', newColor);
  const id = text499.x['animVal'][0].value+""+text499.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text499.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text499.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text499.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text499.setAttribute('x', valueXIncrease);
      text499.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text499.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text499.setAttribute('x', valueXIncrease);
      text499.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text499.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text499.setAttribute('y', valueXIncrease);
      text499.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text499.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text499.setAttribute('y', valueXIncrease);
      text499.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF499, 0.001);
const text500 = document.getElementById('text500');
text500.innerHTML = number[0];
function textF500() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text500.setAttribute('fill', newColor);
  const id = text500.x['animVal'][0].value+""+text500.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text500.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text500.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text500.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text500.setAttribute('x', valueXIncrease);
      text500.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text500.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text500.setAttribute('x', valueXIncrease);
      text500.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text500.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text500.setAttribute('y', valueXIncrease);
      text500.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text500.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text500.setAttribute('y', valueXIncrease);
      text500.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF500, 0.001);
const text501 = document.getElementById('text501');
text501.innerHTML = number[0];
function textF501() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text501.setAttribute('fill', newColor);
  const id = text501.x['animVal'][0].value+""+text501.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text501.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text501.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text501.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text501.setAttribute('x', valueXIncrease);
      text501.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text501.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text501.setAttribute('x', valueXIncrease);
      text501.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text501.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text501.setAttribute('y', valueXIncrease);
      text501.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text501.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text501.setAttribute('y', valueXIncrease);
      text501.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF501, 0.001);
const text502 = document.getElementById('text502');
text502.innerHTML = number[0];
function textF502() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text502.setAttribute('fill', newColor);
  const id = text502.x['animVal'][0].value+""+text502.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text502.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text502.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text502.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text502.setAttribute('x', valueXIncrease);
      text502.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text502.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text502.setAttribute('x', valueXIncrease);
      text502.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text502.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text502.setAttribute('y', valueXIncrease);
      text502.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text502.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text502.setAttribute('y', valueXIncrease);
      text502.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF502, 0.001);
const text503 = document.getElementById('text503');
text503.innerHTML = number[0];
function textF503() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text503.setAttribute('fill', newColor);
  const id = text503.x['animVal'][0].value+""+text503.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text503.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text503.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text503.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text503.setAttribute('x', valueXIncrease);
      text503.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text503.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text503.setAttribute('x', valueXIncrease);
      text503.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text503.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text503.setAttribute('y', valueXIncrease);
      text503.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text503.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text503.setAttribute('y', valueXIncrease);
      text503.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF503, 0.001);
const text504 = document.getElementById('text504');
text504.innerHTML = number[0];
function textF504() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text504.setAttribute('fill', newColor);
  const id = text504.x['animVal'][0].value+""+text504.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text504.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text504.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text504.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text504.setAttribute('x', valueXIncrease);
      text504.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text504.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text504.setAttribute('x', valueXIncrease);
      text504.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text504.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text504.setAttribute('y', valueXIncrease);
      text504.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text504.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text504.setAttribute('y', valueXIncrease);
      text504.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF504, 0.001);
const text505 = document.getElementById('text505');
text505.innerHTML = number[0];
function textF505() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text505.setAttribute('fill', newColor);
  const id = text505.x['animVal'][0].value+""+text505.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text505.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text505.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text505.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text505.setAttribute('x', valueXIncrease);
      text505.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text505.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text505.setAttribute('x', valueXIncrease);
      text505.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text505.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text505.setAttribute('y', valueXIncrease);
      text505.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text505.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text505.setAttribute('y', valueXIncrease);
      text505.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF505, 0.001);
const text506 = document.getElementById('text506');
text506.innerHTML = number[0];
function textF506() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text506.setAttribute('fill', newColor);
  const id = text506.x['animVal'][0].value+""+text506.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text506.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text506.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text506.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text506.setAttribute('x', valueXIncrease);
      text506.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text506.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text506.setAttribute('x', valueXIncrease);
      text506.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text506.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text506.setAttribute('y', valueXIncrease);
      text506.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text506.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text506.setAttribute('y', valueXIncrease);
      text506.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF506, 0.001);
const text507 = document.getElementById('text507');
text507.innerHTML = number[0];
function textF507() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text507.setAttribute('fill', newColor);
  const id = text507.x['animVal'][0].value+""+text507.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text507.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text507.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text507.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text507.setAttribute('x', valueXIncrease);
      text507.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text507.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text507.setAttribute('x', valueXIncrease);
      text507.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text507.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text507.setAttribute('y', valueXIncrease);
      text507.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text507.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text507.setAttribute('y', valueXIncrease);
      text507.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF507, 0.001);
const text508 = document.getElementById('text508');
text508.innerHTML = number[0];
function textF508() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text508.setAttribute('fill', newColor);
  const id = text508.x['animVal'][0].value+""+text508.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text508.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text508.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text508.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text508.setAttribute('x', valueXIncrease);
      text508.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text508.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text508.setAttribute('x', valueXIncrease);
      text508.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text508.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text508.setAttribute('y', valueXIncrease);
      text508.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text508.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text508.setAttribute('y', valueXIncrease);
      text508.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF508, 0.001);
const text509 = document.getElementById('text509');
text509.innerHTML = number[0];
function textF509() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text509.setAttribute('fill', newColor);
  const id = text509.x['animVal'][0].value+""+text509.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text509.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text509.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text509.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text509.setAttribute('x', valueXIncrease);
      text509.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text509.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text509.setAttribute('x', valueXIncrease);
      text509.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text509.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text509.setAttribute('y', valueXIncrease);
      text509.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text509.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text509.setAttribute('y', valueXIncrease);
      text509.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF509, 0.001);
const text510 = document.getElementById('text510');
text510.innerHTML = number[0];
function textF510() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text510.setAttribute('fill', newColor);
  const id = text510.x['animVal'][0].value+""+text510.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text510.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text510.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text510.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text510.setAttribute('x', valueXIncrease);
      text510.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text510.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text510.setAttribute('x', valueXIncrease);
      text510.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text510.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text510.setAttribute('y', valueXIncrease);
      text510.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text510.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text510.setAttribute('y', valueXIncrease);
      text510.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF510, 0.001);
const text511 = document.getElementById('text511');
text511.innerHTML = number[0];
function textF511() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text511.setAttribute('fill', newColor);
  const id = text511.x['animVal'][0].value+""+text511.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text511.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text511.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text511.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text511.setAttribute('x', valueXIncrease);
      text511.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text511.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text511.setAttribute('x', valueXIncrease);
      text511.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text511.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text511.setAttribute('y', valueXIncrease);
      text511.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text511.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text511.setAttribute('y', valueXIncrease);
      text511.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF511, 0.001);
const text512 = document.getElementById('text512');
text512.innerHTML = number[0];
function textF512() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text512.setAttribute('fill', newColor);
  const id = text512.x['animVal'][0].value+""+text512.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text512.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text512.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text512.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text512.setAttribute('x', valueXIncrease);
      text512.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text512.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text512.setAttribute('x', valueXIncrease);
      text512.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text512.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text512.setAttribute('y', valueXIncrease);
      text512.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text512.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text512.setAttribute('y', valueXIncrease);
      text512.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF512, 0.001);
const text513 = document.getElementById('text513');
text513.innerHTML = number[0];
function textF513() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text513.setAttribute('fill', newColor);
  const id = text513.x['animVal'][0].value+""+text513.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text513.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text513.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text513.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text513.setAttribute('x', valueXIncrease);
      text513.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text513.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text513.setAttribute('x', valueXIncrease);
      text513.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text513.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text513.setAttribute('y', valueXIncrease);
      text513.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text513.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text513.setAttribute('y', valueXIncrease);
      text513.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF513, 0.001);
const text514 = document.getElementById('text514');
text514.innerHTML = number[0];
function textF514() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text514.setAttribute('fill', newColor);
  const id = text514.x['animVal'][0].value+""+text514.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text514.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text514.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text514.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text514.setAttribute('x', valueXIncrease);
      text514.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text514.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text514.setAttribute('x', valueXIncrease);
      text514.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text514.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text514.setAttribute('y', valueXIncrease);
      text514.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text514.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text514.setAttribute('y', valueXIncrease);
      text514.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF514, 0.001);
const text515 = document.getElementById('text515');
text515.innerHTML = number[0];
function textF515() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text515.setAttribute('fill', newColor);
  const id = text515.x['animVal'][0].value+""+text515.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text515.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text515.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text515.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text515.setAttribute('x', valueXIncrease);
      text515.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text515.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text515.setAttribute('x', valueXIncrease);
      text515.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text515.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text515.setAttribute('y', valueXIncrease);
      text515.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text515.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text515.setAttribute('y', valueXIncrease);
      text515.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF515, 0.001);
const text516 = document.getElementById('text516');
text516.innerHTML = number[0];
function textF516() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text516.setAttribute('fill', newColor);
  const id = text516.x['animVal'][0].value+""+text516.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text516.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text516.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text516.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text516.setAttribute('x', valueXIncrease);
      text516.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text516.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text516.setAttribute('x', valueXIncrease);
      text516.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text516.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text516.setAttribute('y', valueXIncrease);
      text516.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text516.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text516.setAttribute('y', valueXIncrease);
      text516.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF516, 0.001);
const text517 = document.getElementById('text517');
text517.innerHTML = number[0];
function textF517() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text517.setAttribute('fill', newColor);
  const id = text517.x['animVal'][0].value+""+text517.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text517.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text517.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text517.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text517.setAttribute('x', valueXIncrease);
      text517.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text517.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text517.setAttribute('x', valueXIncrease);
      text517.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text517.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text517.setAttribute('y', valueXIncrease);
      text517.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text517.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text517.setAttribute('y', valueXIncrease);
      text517.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF517, 0.001);
const text518 = document.getElementById('text518');
text518.innerHTML = number[0];
function textF518() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text518.setAttribute('fill', newColor);
  const id = text518.x['animVal'][0].value+""+text518.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text518.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text518.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text518.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text518.setAttribute('x', valueXIncrease);
      text518.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text518.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text518.setAttribute('x', valueXIncrease);
      text518.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text518.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text518.setAttribute('y', valueXIncrease);
      text518.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text518.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text518.setAttribute('y', valueXIncrease);
      text518.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF518, 0.001);
const text519 = document.getElementById('text519');
text519.innerHTML = number[0];
function textF519() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text519.setAttribute('fill', newColor);
  const id = text519.x['animVal'][0].value+""+text519.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text519.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text519.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text519.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text519.setAttribute('x', valueXIncrease);
      text519.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text519.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text519.setAttribute('x', valueXIncrease);
      text519.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text519.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text519.setAttribute('y', valueXIncrease);
      text519.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text519.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text519.setAttribute('y', valueXIncrease);
      text519.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF519, 0.001);
const text520 = document.getElementById('text520');
text520.innerHTML = number[0];
function textF520() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text520.setAttribute('fill', newColor);
  const id = text520.x['animVal'][0].value+""+text520.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text520.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text520.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text520.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text520.setAttribute('x', valueXIncrease);
      text520.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text520.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text520.setAttribute('x', valueXIncrease);
      text520.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text520.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text520.setAttribute('y', valueXIncrease);
      text520.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text520.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text520.setAttribute('y', valueXIncrease);
      text520.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF520, 0.001);
const text521 = document.getElementById('text521');
text521.innerHTML = number[0];
function textF521() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text521.setAttribute('fill', newColor);
  const id = text521.x['animVal'][0].value+""+text521.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text521.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text521.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text521.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text521.setAttribute('x', valueXIncrease);
      text521.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text521.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text521.setAttribute('x', valueXIncrease);
      text521.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text521.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text521.setAttribute('y', valueXIncrease);
      text521.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text521.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text521.setAttribute('y', valueXIncrease);
      text521.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF521, 0.001);
const text522 = document.getElementById('text522');
text522.innerHTML = number[0];
function textF522() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text522.setAttribute('fill', newColor);
  const id = text522.x['animVal'][0].value+""+text522.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text522.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text522.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text522.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text522.setAttribute('x', valueXIncrease);
      text522.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text522.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text522.setAttribute('x', valueXIncrease);
      text522.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text522.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text522.setAttribute('y', valueXIncrease);
      text522.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text522.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text522.setAttribute('y', valueXIncrease);
      text522.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF522, 0.001);
const text523 = document.getElementById('text523');
text523.innerHTML = number[0];
function textF523() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text523.setAttribute('fill', newColor);
  const id = text523.x['animVal'][0].value+""+text523.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text523.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text523.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text523.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text523.setAttribute('x', valueXIncrease);
      text523.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text523.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text523.setAttribute('x', valueXIncrease);
      text523.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text523.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text523.setAttribute('y', valueXIncrease);
      text523.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text523.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text523.setAttribute('y', valueXIncrease);
      text523.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF523, 0.001);
const text524 = document.getElementById('text524');
text524.innerHTML = number[0];
function textF524() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text524.setAttribute('fill', newColor);
  const id = text524.x['animVal'][0].value+""+text524.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text524.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text524.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text524.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text524.setAttribute('x', valueXIncrease);
      text524.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text524.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text524.setAttribute('x', valueXIncrease);
      text524.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text524.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text524.setAttribute('y', valueXIncrease);
      text524.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text524.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text524.setAttribute('y', valueXIncrease);
      text524.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF524, 0.001);
const text525 = document.getElementById('text525');
text525.innerHTML = number[0];
function textF525() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text525.setAttribute('fill', newColor);
  const id = text525.x['animVal'][0].value+""+text525.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text525.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text525.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text525.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text525.setAttribute('x', valueXIncrease);
      text525.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text525.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text525.setAttribute('x', valueXIncrease);
      text525.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text525.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text525.setAttribute('y', valueXIncrease);
      text525.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text525.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text525.setAttribute('y', valueXIncrease);
      text525.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF525, 0.001);
const text526 = document.getElementById('text526');
text526.innerHTML = number[0];
function textF526() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text526.setAttribute('fill', newColor);
  const id = text526.x['animVal'][0].value+""+text526.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text526.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text526.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text526.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text526.setAttribute('x', valueXIncrease);
      text526.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text526.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text526.setAttribute('x', valueXIncrease);
      text526.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text526.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text526.setAttribute('y', valueXIncrease);
      text526.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text526.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text526.setAttribute('y', valueXIncrease);
      text526.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF526, 0.001);
const text527 = document.getElementById('text527');
text527.innerHTML = number[0];
function textF527() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text527.setAttribute('fill', newColor);
  const id = text527.x['animVal'][0].value+""+text527.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text527.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text527.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text527.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text527.setAttribute('x', valueXIncrease);
      text527.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text527.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text527.setAttribute('x', valueXIncrease);
      text527.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text527.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text527.setAttribute('y', valueXIncrease);
      text527.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text527.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text527.setAttribute('y', valueXIncrease);
      text527.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF527, 0.001);
const text528 = document.getElementById('text528');
text528.innerHTML = number[0];
function textF528() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text528.setAttribute('fill', newColor);
  const id = text528.x['animVal'][0].value+""+text528.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text528.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text528.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text528.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text528.setAttribute('x', valueXIncrease);
      text528.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text528.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text528.setAttribute('x', valueXIncrease);
      text528.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text528.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text528.setAttribute('y', valueXIncrease);
      text528.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text528.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text528.setAttribute('y', valueXIncrease);
      text528.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF528, 0.001);
const text529 = document.getElementById('text529');
text529.innerHTML = number[0];
function textF529() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text529.setAttribute('fill', newColor);
  const id = text529.x['animVal'][0].value+""+text529.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text529.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text529.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text529.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text529.setAttribute('x', valueXIncrease);
      text529.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text529.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text529.setAttribute('x', valueXIncrease);
      text529.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text529.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text529.setAttribute('y', valueXIncrease);
      text529.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text529.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text529.setAttribute('y', valueXIncrease);
      text529.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF529, 0.001);
const text530 = document.getElementById('text530');
text530.innerHTML = number[0];
function textF530() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text530.setAttribute('fill', newColor);
  const id = text530.x['animVal'][0].value+""+text530.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text530.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text530.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text530.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text530.setAttribute('x', valueXIncrease);
      text530.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text530.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text530.setAttribute('x', valueXIncrease);
      text530.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text530.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text530.setAttribute('y', valueXIncrease);
      text530.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text530.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text530.setAttribute('y', valueXIncrease);
      text530.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF530, 0.001);
const text531 = document.getElementById('text531');
text531.innerHTML = number[0];
function textF531() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text531.setAttribute('fill', newColor);
  const id = text531.x['animVal'][0].value+""+text531.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text531.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text531.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text531.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text531.setAttribute('x', valueXIncrease);
      text531.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text531.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text531.setAttribute('x', valueXIncrease);
      text531.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text531.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text531.setAttribute('y', valueXIncrease);
      text531.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text531.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text531.setAttribute('y', valueXIncrease);
      text531.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF531, 0.001);
const text532 = document.getElementById('text532');
text532.innerHTML = number[0];
function textF532() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text532.setAttribute('fill', newColor);
  const id = text532.x['animVal'][0].value+""+text532.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text532.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text532.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text532.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text532.setAttribute('x', valueXIncrease);
      text532.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text532.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text532.setAttribute('x', valueXIncrease);
      text532.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text532.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text532.setAttribute('y', valueXIncrease);
      text532.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text532.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text532.setAttribute('y', valueXIncrease);
      text532.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF532, 0.001);
const text533 = document.getElementById('text533');
text533.innerHTML = number[0];
function textF533() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text533.setAttribute('fill', newColor);
  const id = text533.x['animVal'][0].value+""+text533.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text533.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text533.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text533.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text533.setAttribute('x', valueXIncrease);
      text533.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text533.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text533.setAttribute('x', valueXIncrease);
      text533.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text533.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text533.setAttribute('y', valueXIncrease);
      text533.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text533.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text533.setAttribute('y', valueXIncrease);
      text533.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF533, 0.001);
const text534 = document.getElementById('text534');
text534.innerHTML = number[0];
function textF534() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text534.setAttribute('fill', newColor);
  const id = text534.x['animVal'][0].value+""+text534.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text534.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text534.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text534.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text534.setAttribute('x', valueXIncrease);
      text534.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text534.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text534.setAttribute('x', valueXIncrease);
      text534.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text534.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text534.setAttribute('y', valueXIncrease);
      text534.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text534.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text534.setAttribute('y', valueXIncrease);
      text534.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF534, 0.001);
const text535 = document.getElementById('text535');
text535.innerHTML = number[0];
function textF535() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text535.setAttribute('fill', newColor);
  const id = text535.x['animVal'][0].value+""+text535.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text535.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text535.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text535.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text535.setAttribute('x', valueXIncrease);
      text535.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text535.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text535.setAttribute('x', valueXIncrease);
      text535.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text535.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text535.setAttribute('y', valueXIncrease);
      text535.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text535.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text535.setAttribute('y', valueXIncrease);
      text535.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF535, 0.001);
const text536 = document.getElementById('text536');
text536.innerHTML = number[0];
function textF536() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text536.setAttribute('fill', newColor);
  const id = text536.x['animVal'][0].value+""+text536.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text536.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text536.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text536.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text536.setAttribute('x', valueXIncrease);
      text536.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text536.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text536.setAttribute('x', valueXIncrease);
      text536.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text536.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text536.setAttribute('y', valueXIncrease);
      text536.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text536.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text536.setAttribute('y', valueXIncrease);
      text536.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF536, 0.001);
const text537 = document.getElementById('text537');
text537.innerHTML = number[0];
function textF537() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text537.setAttribute('fill', newColor);
  const id = text537.x['animVal'][0].value+""+text537.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text537.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text537.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text537.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text537.setAttribute('x', valueXIncrease);
      text537.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text537.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text537.setAttribute('x', valueXIncrease);
      text537.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text537.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text537.setAttribute('y', valueXIncrease);
      text537.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text537.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text537.setAttribute('y', valueXIncrease);
      text537.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF537, 0.001);
const text538 = document.getElementById('text538');
text538.innerHTML = number[0];
function textF538() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text538.setAttribute('fill', newColor);
  const id = text538.x['animVal'][0].value+""+text538.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text538.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text538.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text538.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text538.setAttribute('x', valueXIncrease);
      text538.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text538.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text538.setAttribute('x', valueXIncrease);
      text538.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text538.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text538.setAttribute('y', valueXIncrease);
      text538.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text538.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text538.setAttribute('y', valueXIncrease);
      text538.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF538, 0.001);
const text539 = document.getElementById('text539');
text539.innerHTML = number[0];
function textF539() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text539.setAttribute('fill', newColor);
  const id = text539.x['animVal'][0].value+""+text539.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text539.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text539.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text539.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text539.setAttribute('x', valueXIncrease);
      text539.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text539.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text539.setAttribute('x', valueXIncrease);
      text539.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text539.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text539.setAttribute('y', valueXIncrease);
      text539.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text539.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text539.setAttribute('y', valueXIncrease);
      text539.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF539, 0.001);
const text540 = document.getElementById('text540');
text540.innerHTML = number[0];
function textF540() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text540.setAttribute('fill', newColor);
  const id = text540.x['animVal'][0].value+""+text540.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text540.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text540.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text540.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text540.setAttribute('x', valueXIncrease);
      text540.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text540.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text540.setAttribute('x', valueXIncrease);
      text540.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text540.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text540.setAttribute('y', valueXIncrease);
      text540.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text540.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text540.setAttribute('y', valueXIncrease);
      text540.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF540, 0.001);
const text541 = document.getElementById('text541');
text541.innerHTML = number[0];
function textF541() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text541.setAttribute('fill', newColor);
  const id = text541.x['animVal'][0].value+""+text541.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text541.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text541.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text541.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text541.setAttribute('x', valueXIncrease);
      text541.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text541.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text541.setAttribute('x', valueXIncrease);
      text541.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text541.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text541.setAttribute('y', valueXIncrease);
      text541.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text541.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text541.setAttribute('y', valueXIncrease);
      text541.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF541, 0.001);
const text542 = document.getElementById('text542');
text542.innerHTML = number[0];
function textF542() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text542.setAttribute('fill', newColor);
  const id = text542.x['animVal'][0].value+""+text542.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text542.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text542.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text542.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text542.setAttribute('x', valueXIncrease);
      text542.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text542.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text542.setAttribute('x', valueXIncrease);
      text542.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text542.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text542.setAttribute('y', valueXIncrease);
      text542.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text542.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text542.setAttribute('y', valueXIncrease);
      text542.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF542, 0.001);
const text543 = document.getElementById('text543');
text543.innerHTML = number[0];
function textF543() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text543.setAttribute('fill', newColor);
  const id = text543.x['animVal'][0].value+""+text543.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text543.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text543.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text543.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text543.setAttribute('x', valueXIncrease);
      text543.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text543.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text543.setAttribute('x', valueXIncrease);
      text543.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text543.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text543.setAttribute('y', valueXIncrease);
      text543.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text543.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text543.setAttribute('y', valueXIncrease);
      text543.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF543, 0.001);
const text544 = document.getElementById('text544');
text544.innerHTML = number[0];
function textF544() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text544.setAttribute('fill', newColor);
  const id = text544.x['animVal'][0].value+""+text544.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text544.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text544.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text544.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text544.setAttribute('x', valueXIncrease);
      text544.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text544.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text544.setAttribute('x', valueXIncrease);
      text544.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text544.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text544.setAttribute('y', valueXIncrease);
      text544.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text544.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text544.setAttribute('y', valueXIncrease);
      text544.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF544, 0.001);
const text545 = document.getElementById('text545');
text545.innerHTML = number[0];
function textF545() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text545.setAttribute('fill', newColor);
  const id = text545.x['animVal'][0].value+""+text545.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text545.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text545.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text545.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text545.setAttribute('x', valueXIncrease);
      text545.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text545.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text545.setAttribute('x', valueXIncrease);
      text545.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text545.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text545.setAttribute('y', valueXIncrease);
      text545.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text545.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text545.setAttribute('y', valueXIncrease);
      text545.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF545, 0.001);
const text546 = document.getElementById('text546');
text546.innerHTML = number[0];
function textF546() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text546.setAttribute('fill', newColor);
  const id = text546.x['animVal'][0].value+""+text546.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text546.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text546.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text546.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text546.setAttribute('x', valueXIncrease);
      text546.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text546.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text546.setAttribute('x', valueXIncrease);
      text546.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text546.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text546.setAttribute('y', valueXIncrease);
      text546.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text546.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text546.setAttribute('y', valueXIncrease);
      text546.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF546, 0.001);
const text547 = document.getElementById('text547');
text547.innerHTML = number[0];
function textF547() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text547.setAttribute('fill', newColor);
  const id = text547.x['animVal'][0].value+""+text547.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text547.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text547.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text547.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text547.setAttribute('x', valueXIncrease);
      text547.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text547.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text547.setAttribute('x', valueXIncrease);
      text547.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text547.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text547.setAttribute('y', valueXIncrease);
      text547.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text547.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text547.setAttribute('y', valueXIncrease);
      text547.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF547, 0.001);
const text548 = document.getElementById('text548');
text548.innerHTML = number[0];
function textF548() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text548.setAttribute('fill', newColor);
  const id = text548.x['animVal'][0].value+""+text548.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text548.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text548.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text548.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text548.setAttribute('x', valueXIncrease);
      text548.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text548.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text548.setAttribute('x', valueXIncrease);
      text548.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text548.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text548.setAttribute('y', valueXIncrease);
      text548.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text548.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text548.setAttribute('y', valueXIncrease);
      text548.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF548, 0.001);
const text549 = document.getElementById('text549');
text549.innerHTML = number[0];
function textF549() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text549.setAttribute('fill', newColor);
  const id = text549.x['animVal'][0].value+""+text549.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text549.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text549.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text549.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text549.setAttribute('x', valueXIncrease);
      text549.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text549.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text549.setAttribute('x', valueXIncrease);
      text549.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text549.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text549.setAttribute('y', valueXIncrease);
      text549.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text549.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text549.setAttribute('y', valueXIncrease);
      text549.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF549, 0.001);
const text550 = document.getElementById('text550');
text550.innerHTML = number[0];
function textF550() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text550.setAttribute('fill', newColor);
  const id = text550.x['animVal'][0].value+""+text550.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text550.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text550.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text550.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text550.setAttribute('x', valueXIncrease);
      text550.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text550.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text550.setAttribute('x', valueXIncrease);
      text550.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text550.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text550.setAttribute('y', valueXIncrease);
      text550.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text550.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text550.setAttribute('y', valueXIncrease);
      text550.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF550, 0.001);
const text551 = document.getElementById('text551');
text551.innerHTML = number[0];
function textF551() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text551.setAttribute('fill', newColor);
  const id = text551.x['animVal'][0].value+""+text551.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text551.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text551.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text551.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text551.setAttribute('x', valueXIncrease);
      text551.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text551.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text551.setAttribute('x', valueXIncrease);
      text551.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text551.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text551.setAttribute('y', valueXIncrease);
      text551.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text551.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text551.setAttribute('y', valueXIncrease);
      text551.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF551, 0.001);
const text552 = document.getElementById('text552');
text552.innerHTML = number[0];
function textF552() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text552.setAttribute('fill', newColor);
  const id = text552.x['animVal'][0].value+""+text552.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text552.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text552.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text552.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text552.setAttribute('x', valueXIncrease);
      text552.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text552.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text552.setAttribute('x', valueXIncrease);
      text552.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text552.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text552.setAttribute('y', valueXIncrease);
      text552.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text552.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text552.setAttribute('y', valueXIncrease);
      text552.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF552, 0.001);
const text553 = document.getElementById('text553');
text553.innerHTML = number[0];
function textF553() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text553.setAttribute('fill', newColor);
  const id = text553.x['animVal'][0].value+""+text553.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text553.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text553.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text553.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text553.setAttribute('x', valueXIncrease);
      text553.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text553.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text553.setAttribute('x', valueXIncrease);
      text553.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text553.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text553.setAttribute('y', valueXIncrease);
      text553.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text553.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text553.setAttribute('y', valueXIncrease);
      text553.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF553, 0.001);
const text554 = document.getElementById('text554');
text554.innerHTML = number[0];
function textF554() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text554.setAttribute('fill', newColor);
  const id = text554.x['animVal'][0].value+""+text554.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text554.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text554.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text554.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text554.setAttribute('x', valueXIncrease);
      text554.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text554.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text554.setAttribute('x', valueXIncrease);
      text554.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text554.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text554.setAttribute('y', valueXIncrease);
      text554.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text554.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text554.setAttribute('y', valueXIncrease);
      text554.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF554, 0.001);
const text555 = document.getElementById('text555');
text555.innerHTML = number[0];
function textF555() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text555.setAttribute('fill', newColor);
  const id = text555.x['animVal'][0].value+""+text555.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text555.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text555.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text555.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text555.setAttribute('x', valueXIncrease);
      text555.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text555.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text555.setAttribute('x', valueXIncrease);
      text555.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text555.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text555.setAttribute('y', valueXIncrease);
      text555.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text555.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text555.setAttribute('y', valueXIncrease);
      text555.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF555, 0.001);
const text556 = document.getElementById('text556');
text556.innerHTML = number[0];
function textF556() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text556.setAttribute('fill', newColor);
  const id = text556.x['animVal'][0].value+""+text556.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text556.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text556.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text556.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text556.setAttribute('x', valueXIncrease);
      text556.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text556.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text556.setAttribute('x', valueXIncrease);
      text556.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text556.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text556.setAttribute('y', valueXIncrease);
      text556.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text556.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text556.setAttribute('y', valueXIncrease);
      text556.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF556, 0.001);
const text557 = document.getElementById('text557');
text557.innerHTML = number[0];
function textF557() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text557.setAttribute('fill', newColor);
  const id = text557.x['animVal'][0].value+""+text557.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text557.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text557.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text557.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text557.setAttribute('x', valueXIncrease);
      text557.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text557.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text557.setAttribute('x', valueXIncrease);
      text557.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text557.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text557.setAttribute('y', valueXIncrease);
      text557.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text557.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text557.setAttribute('y', valueXIncrease);
      text557.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF557, 0.001);
const text558 = document.getElementById('text558');
text558.innerHTML = number[0];
function textF558() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text558.setAttribute('fill', newColor);
  const id = text558.x['animVal'][0].value+""+text558.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text558.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text558.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text558.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text558.setAttribute('x', valueXIncrease);
      text558.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text558.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text558.setAttribute('x', valueXIncrease);
      text558.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text558.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text558.setAttribute('y', valueXIncrease);
      text558.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text558.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text558.setAttribute('y', valueXIncrease);
      text558.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF558, 0.001);
const text559 = document.getElementById('text559');
text559.innerHTML = number[0];
function textF559() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text559.setAttribute('fill', newColor);
  const id = text559.x['animVal'][0].value+""+text559.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text559.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text559.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text559.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text559.setAttribute('x', valueXIncrease);
      text559.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text559.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text559.setAttribute('x', valueXIncrease);
      text559.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text559.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text559.setAttribute('y', valueXIncrease);
      text559.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text559.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text559.setAttribute('y', valueXIncrease);
      text559.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF559, 0.001);
const text560 = document.getElementById('text560');
text560.innerHTML = number[0];
function textF560() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text560.setAttribute('fill', newColor);
  const id = text560.x['animVal'][0].value+""+text560.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text560.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text560.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text560.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text560.setAttribute('x', valueXIncrease);
      text560.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text560.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text560.setAttribute('x', valueXIncrease);
      text560.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text560.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text560.setAttribute('y', valueXIncrease);
      text560.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text560.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text560.setAttribute('y', valueXIncrease);
      text560.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF560, 0.001);
const text561 = document.getElementById('text561');
text561.innerHTML = number[0];
function textF561() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text561.setAttribute('fill', newColor);
  const id = text561.x['animVal'][0].value+""+text561.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text561.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text561.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text561.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text561.setAttribute('x', valueXIncrease);
      text561.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text561.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text561.setAttribute('x', valueXIncrease);
      text561.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text561.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text561.setAttribute('y', valueXIncrease);
      text561.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text561.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text561.setAttribute('y', valueXIncrease);
      text561.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF561, 0.001);
const text562 = document.getElementById('text562');
text562.innerHTML = number[0];
function textF562() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text562.setAttribute('fill', newColor);
  const id = text562.x['animVal'][0].value+""+text562.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text562.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text562.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text562.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text562.setAttribute('x', valueXIncrease);
      text562.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text562.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text562.setAttribute('x', valueXIncrease);
      text562.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text562.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text562.setAttribute('y', valueXIncrease);
      text562.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text562.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text562.setAttribute('y', valueXIncrease);
      text562.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF562, 0.001);
const text563 = document.getElementById('text563');
text563.innerHTML = number[0];
function textF563() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text563.setAttribute('fill', newColor);
  const id = text563.x['animVal'][0].value+""+text563.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text563.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text563.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text563.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text563.setAttribute('x', valueXIncrease);
      text563.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text563.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text563.setAttribute('x', valueXIncrease);
      text563.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text563.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text563.setAttribute('y', valueXIncrease);
      text563.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text563.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text563.setAttribute('y', valueXIncrease);
      text563.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF563, 0.001);
const text564 = document.getElementById('text564');
text564.innerHTML = number[0];
function textF564() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text564.setAttribute('fill', newColor);
  const id = text564.x['animVal'][0].value+""+text564.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text564.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text564.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text564.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text564.setAttribute('x', valueXIncrease);
      text564.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text564.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text564.setAttribute('x', valueXIncrease);
      text564.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text564.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text564.setAttribute('y', valueXIncrease);
      text564.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text564.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text564.setAttribute('y', valueXIncrease);
      text564.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF564, 0.001);
const text565 = document.getElementById('text565');
text565.innerHTML = number[0];
function textF565() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text565.setAttribute('fill', newColor);
  const id = text565.x['animVal'][0].value+""+text565.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text565.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text565.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text565.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text565.setAttribute('x', valueXIncrease);
      text565.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text565.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text565.setAttribute('x', valueXIncrease);
      text565.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text565.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text565.setAttribute('y', valueXIncrease);
      text565.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text565.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text565.setAttribute('y', valueXIncrease);
      text565.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF565, 0.001);
const text566 = document.getElementById('text566');
text566.innerHTML = number[0];
function textF566() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text566.setAttribute('fill', newColor);
  const id = text566.x['animVal'][0].value+""+text566.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text566.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text566.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text566.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text566.setAttribute('x', valueXIncrease);
      text566.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text566.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text566.setAttribute('x', valueXIncrease);
      text566.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text566.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text566.setAttribute('y', valueXIncrease);
      text566.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text566.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text566.setAttribute('y', valueXIncrease);
      text566.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF566, 0.001);
const text567 = document.getElementById('text567');
text567.innerHTML = number[0];
function textF567() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text567.setAttribute('fill', newColor);
  const id = text567.x['animVal'][0].value+""+text567.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text567.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text567.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text567.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text567.setAttribute('x', valueXIncrease);
      text567.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text567.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text567.setAttribute('x', valueXIncrease);
      text567.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text567.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text567.setAttribute('y', valueXIncrease);
      text567.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text567.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text567.setAttribute('y', valueXIncrease);
      text567.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF567, 0.001);
const text568 = document.getElementById('text568');
text568.innerHTML = number[0];
function textF568() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text568.setAttribute('fill', newColor);
  const id = text568.x['animVal'][0].value+""+text568.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text568.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text568.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text568.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text568.setAttribute('x', valueXIncrease);
      text568.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text568.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text568.setAttribute('x', valueXIncrease);
      text568.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text568.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text568.setAttribute('y', valueXIncrease);
      text568.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text568.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text568.setAttribute('y', valueXIncrease);
      text568.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF568, 0.001);
const text569 = document.getElementById('text569');
text569.innerHTML = number[0];
function textF569() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text569.setAttribute('fill', newColor);
  const id = text569.x['animVal'][0].value+""+text569.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text569.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text569.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text569.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text569.setAttribute('x', valueXIncrease);
      text569.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text569.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text569.setAttribute('x', valueXIncrease);
      text569.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text569.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text569.setAttribute('y', valueXIncrease);
      text569.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text569.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text569.setAttribute('y', valueXIncrease);
      text569.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF569, 0.001);
const text570 = document.getElementById('text570');
text570.innerHTML = number[0];
function textF570() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text570.setAttribute('fill', newColor);
  const id = text570.x['animVal'][0].value+""+text570.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text570.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text570.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text570.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text570.setAttribute('x', valueXIncrease);
      text570.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text570.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text570.setAttribute('x', valueXIncrease);
      text570.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text570.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text570.setAttribute('y', valueXIncrease);
      text570.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text570.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text570.setAttribute('y', valueXIncrease);
      text570.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF570, 0.001);
const text571 = document.getElementById('text571');
text571.innerHTML = number[0];
function textF571() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text571.setAttribute('fill', newColor);
  const id = text571.x['animVal'][0].value+""+text571.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text571.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text571.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text571.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text571.setAttribute('x', valueXIncrease);
      text571.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text571.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text571.setAttribute('x', valueXIncrease);
      text571.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text571.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text571.setAttribute('y', valueXIncrease);
      text571.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text571.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text571.setAttribute('y', valueXIncrease);
      text571.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF571, 0.001);
const text572 = document.getElementById('text572');
text572.innerHTML = number[0];
function textF572() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text572.setAttribute('fill', newColor);
  const id = text572.x['animVal'][0].value+""+text572.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text572.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text572.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text572.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text572.setAttribute('x', valueXIncrease);
      text572.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text572.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text572.setAttribute('x', valueXIncrease);
      text572.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text572.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text572.setAttribute('y', valueXIncrease);
      text572.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text572.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text572.setAttribute('y', valueXIncrease);
      text572.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF572, 0.001);
const text573 = document.getElementById('text573');
text573.innerHTML = number[0];
function textF573() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text573.setAttribute('fill', newColor);
  const id = text573.x['animVal'][0].value+""+text573.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text573.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text573.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text573.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text573.setAttribute('x', valueXIncrease);
      text573.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text573.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text573.setAttribute('x', valueXIncrease);
      text573.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text573.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text573.setAttribute('y', valueXIncrease);
      text573.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text573.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text573.setAttribute('y', valueXIncrease);
      text573.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF573, 0.001);
const text574 = document.getElementById('text574');
text574.innerHTML = number[0];
function textF574() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text574.setAttribute('fill', newColor);
  const id = text574.x['animVal'][0].value+""+text574.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text574.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text574.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text574.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text574.setAttribute('x', valueXIncrease);
      text574.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text574.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text574.setAttribute('x', valueXIncrease);
      text574.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text574.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text574.setAttribute('y', valueXIncrease);
      text574.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text574.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text574.setAttribute('y', valueXIncrease);
      text574.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF574, 0.001);
const text575 = document.getElementById('text575');
text575.innerHTML = number[0];
function textF575() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text575.setAttribute('fill', newColor);
  const id = text575.x['animVal'][0].value+""+text575.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text575.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text575.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text575.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text575.setAttribute('x', valueXIncrease);
      text575.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text575.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text575.setAttribute('x', valueXIncrease);
      text575.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text575.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text575.setAttribute('y', valueXIncrease);
      text575.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text575.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text575.setAttribute('y', valueXIncrease);
      text575.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF575, 0.001);
const text576 = document.getElementById('text576');
text576.innerHTML = number[0];
function textF576() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text576.setAttribute('fill', newColor);
  const id = text576.x['animVal'][0].value+""+text576.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text576.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text576.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text576.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text576.setAttribute('x', valueXIncrease);
      text576.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text576.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text576.setAttribute('x', valueXIncrease);
      text576.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text576.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text576.setAttribute('y', valueXIncrease);
      text576.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text576.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text576.setAttribute('y', valueXIncrease);
      text576.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF576, 0.001);
const text577 = document.getElementById('text577');
text577.innerHTML = number[0];
function textF577() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text577.setAttribute('fill', newColor);
  const id = text577.x['animVal'][0].value+""+text577.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text577.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text577.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text577.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text577.setAttribute('x', valueXIncrease);
      text577.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text577.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text577.setAttribute('x', valueXIncrease);
      text577.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text577.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text577.setAttribute('y', valueXIncrease);
      text577.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text577.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text577.setAttribute('y', valueXIncrease);
      text577.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF577, 0.001);
const text578 = document.getElementById('text578');
text578.innerHTML = number[0];
function textF578() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text578.setAttribute('fill', newColor);
  const id = text578.x['animVal'][0].value+""+text578.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text578.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text578.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text578.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text578.setAttribute('x', valueXIncrease);
      text578.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text578.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text578.setAttribute('x', valueXIncrease);
      text578.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text578.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text578.setAttribute('y', valueXIncrease);
      text578.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text578.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text578.setAttribute('y', valueXIncrease);
      text578.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF578, 0.001);
const text579 = document.getElementById('text579');
text579.innerHTML = number[0];
function textF579() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text579.setAttribute('fill', newColor);
  const id = text579.x['animVal'][0].value+""+text579.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text579.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text579.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text579.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text579.setAttribute('x', valueXIncrease);
      text579.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text579.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text579.setAttribute('x', valueXIncrease);
      text579.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text579.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text579.setAttribute('y', valueXIncrease);
      text579.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text579.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text579.setAttribute('y', valueXIncrease);
      text579.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF579, 0.001);
const text580 = document.getElementById('text580');
text580.innerHTML = number[0];
function textF580() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text580.setAttribute('fill', newColor);
  const id = text580.x['animVal'][0].value+""+text580.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text580.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text580.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text580.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text580.setAttribute('x', valueXIncrease);
      text580.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text580.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text580.setAttribute('x', valueXIncrease);
      text580.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text580.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text580.setAttribute('y', valueXIncrease);
      text580.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text580.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text580.setAttribute('y', valueXIncrease);
      text580.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF580, 0.001);
const text581 = document.getElementById('text581');
text581.innerHTML = number[0];
function textF581() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text581.setAttribute('fill', newColor);
  const id = text581.x['animVal'][0].value+""+text581.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text581.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text581.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text581.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text581.setAttribute('x', valueXIncrease);
      text581.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text581.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text581.setAttribute('x', valueXIncrease);
      text581.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text581.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text581.setAttribute('y', valueXIncrease);
      text581.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text581.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text581.setAttribute('y', valueXIncrease);
      text581.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF581, 0.001);
const text582 = document.getElementById('text582');
text582.innerHTML = number[0];
function textF582() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text582.setAttribute('fill', newColor);
  const id = text582.x['animVal'][0].value+""+text582.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text582.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text582.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text582.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text582.setAttribute('x', valueXIncrease);
      text582.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text582.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text582.setAttribute('x', valueXIncrease);
      text582.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text582.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text582.setAttribute('y', valueXIncrease);
      text582.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text582.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text582.setAttribute('y', valueXIncrease);
      text582.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF582, 0.001);
const text583 = document.getElementById('text583');
text583.innerHTML = number[0];
function textF583() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text583.setAttribute('fill', newColor);
  const id = text583.x['animVal'][0].value+""+text583.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text583.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text583.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text583.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text583.setAttribute('x', valueXIncrease);
      text583.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text583.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text583.setAttribute('x', valueXIncrease);
      text583.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text583.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text583.setAttribute('y', valueXIncrease);
      text583.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text583.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text583.setAttribute('y', valueXIncrease);
      text583.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF583, 0.001);
const text584 = document.getElementById('text584');
text584.innerHTML = number[0];
function textF584() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text584.setAttribute('fill', newColor);
  const id = text584.x['animVal'][0].value+""+text584.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text584.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text584.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text584.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text584.setAttribute('x', valueXIncrease);
      text584.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text584.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text584.setAttribute('x', valueXIncrease);
      text584.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text584.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text584.setAttribute('y', valueXIncrease);
      text584.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text584.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text584.setAttribute('y', valueXIncrease);
      text584.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF584, 0.001);
const text585 = document.getElementById('text585');
text585.innerHTML = number[0];
function textF585() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text585.setAttribute('fill', newColor);
  const id = text585.x['animVal'][0].value+""+text585.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text585.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text585.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text585.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text585.setAttribute('x', valueXIncrease);
      text585.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text585.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text585.setAttribute('x', valueXIncrease);
      text585.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text585.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text585.setAttribute('y', valueXIncrease);
      text585.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text585.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text585.setAttribute('y', valueXIncrease);
      text585.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF585, 0.001);
const text586 = document.getElementById('text586');
text586.innerHTML = number[0];
function textF586() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text586.setAttribute('fill', newColor);
  const id = text586.x['animVal'][0].value+""+text586.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text586.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text586.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text586.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text586.setAttribute('x', valueXIncrease);
      text586.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text586.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text586.setAttribute('x', valueXIncrease);
      text586.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text586.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text586.setAttribute('y', valueXIncrease);
      text586.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text586.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text586.setAttribute('y', valueXIncrease);
      text586.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF586, 0.001);
const text587 = document.getElementById('text587');
text587.innerHTML = number[0];
function textF587() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text587.setAttribute('fill', newColor);
  const id = text587.x['animVal'][0].value+""+text587.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text587.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text587.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text587.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text587.setAttribute('x', valueXIncrease);
      text587.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text587.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text587.setAttribute('x', valueXIncrease);
      text587.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text587.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text587.setAttribute('y', valueXIncrease);
      text587.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text587.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text587.setAttribute('y', valueXIncrease);
      text587.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF587, 0.001);
const text588 = document.getElementById('text588');
text588.innerHTML = number[0];
function textF588() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text588.setAttribute('fill', newColor);
  const id = text588.x['animVal'][0].value+""+text588.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text588.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text588.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text588.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text588.setAttribute('x', valueXIncrease);
      text588.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text588.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text588.setAttribute('x', valueXIncrease);
      text588.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text588.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text588.setAttribute('y', valueXIncrease);
      text588.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text588.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text588.setAttribute('y', valueXIncrease);
      text588.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF588, 0.001);
const text589 = document.getElementById('text589');
text589.innerHTML = number[0];
function textF589() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text589.setAttribute('fill', newColor);
  const id = text589.x['animVal'][0].value+""+text589.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text589.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text589.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text589.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text589.setAttribute('x', valueXIncrease);
      text589.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text589.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text589.setAttribute('x', valueXIncrease);
      text589.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text589.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text589.setAttribute('y', valueXIncrease);
      text589.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text589.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text589.setAttribute('y', valueXIncrease);
      text589.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF589, 0.001);
const text590 = document.getElementById('text590');
text590.innerHTML = number[0];
function textF590() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text590.setAttribute('fill', newColor);
  const id = text590.x['animVal'][0].value+""+text590.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text590.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text590.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text590.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text590.setAttribute('x', valueXIncrease);
      text590.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text590.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text590.setAttribute('x', valueXIncrease);
      text590.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text590.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text590.setAttribute('y', valueXIncrease);
      text590.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text590.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text590.setAttribute('y', valueXIncrease);
      text590.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF590, 0.001);
const text591 = document.getElementById('text591');
text591.innerHTML = number[0];
function textF591() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text591.setAttribute('fill', newColor);
  const id = text591.x['animVal'][0].value+""+text591.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text591.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text591.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text591.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text591.setAttribute('x', valueXIncrease);
      text591.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text591.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text591.setAttribute('x', valueXIncrease);
      text591.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text591.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text591.setAttribute('y', valueXIncrease);
      text591.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text591.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text591.setAttribute('y', valueXIncrease);
      text591.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF591, 0.001);
const text592 = document.getElementById('text592');
text592.innerHTML = number[0];
function textF592() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text592.setAttribute('fill', newColor);
  const id = text592.x['animVal'][0].value+""+text592.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text592.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text592.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text592.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text592.setAttribute('x', valueXIncrease);
      text592.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text592.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text592.setAttribute('x', valueXIncrease);
      text592.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text592.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text592.setAttribute('y', valueXIncrease);
      text592.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text592.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text592.setAttribute('y', valueXIncrease);
      text592.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF592, 0.001);
const text593 = document.getElementById('text593');
text593.innerHTML = number[0];
function textF593() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text593.setAttribute('fill', newColor);
  const id = text593.x['animVal'][0].value+""+text593.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text593.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text593.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text593.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text593.setAttribute('x', valueXIncrease);
      text593.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text593.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text593.setAttribute('x', valueXIncrease);
      text593.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text593.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text593.setAttribute('y', valueXIncrease);
      text593.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text593.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text593.setAttribute('y', valueXIncrease);
      text593.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF593, 0.001);
const text594 = document.getElementById('text594');
text594.innerHTML = number[0];
function textF594() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text594.setAttribute('fill', newColor);
  const id = text594.x['animVal'][0].value+""+text594.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text594.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text594.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text594.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text594.setAttribute('x', valueXIncrease);
      text594.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text594.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text594.setAttribute('x', valueXIncrease);
      text594.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text594.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text594.setAttribute('y', valueXIncrease);
      text594.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text594.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text594.setAttribute('y', valueXIncrease);
      text594.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF594, 0.001);
const text595 = document.getElementById('text595');
text595.innerHTML = number[0];
function textF595() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text595.setAttribute('fill', newColor);
  const id = text595.x['animVal'][0].value+""+text595.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text595.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text595.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text595.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text595.setAttribute('x', valueXIncrease);
      text595.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text595.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text595.setAttribute('x', valueXIncrease);
      text595.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text595.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text595.setAttribute('y', valueXIncrease);
      text595.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text595.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text595.setAttribute('y', valueXIncrease);
      text595.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF595, 0.001);
const text596 = document.getElementById('text596');
text596.innerHTML = number[0];
function textF596() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text596.setAttribute('fill', newColor);
  const id = text596.x['animVal'][0].value+""+text596.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text596.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text596.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text596.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text596.setAttribute('x', valueXIncrease);
      text596.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text596.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text596.setAttribute('x', valueXIncrease);
      text596.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text596.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text596.setAttribute('y', valueXIncrease);
      text596.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text596.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text596.setAttribute('y', valueXIncrease);
      text596.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF596, 0.001);
const text597 = document.getElementById('text597');
text597.innerHTML = number[0];
function textF597() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text597.setAttribute('fill', newColor);
  const id = text597.x['animVal'][0].value+""+text597.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text597.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text597.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text597.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text597.setAttribute('x', valueXIncrease);
      text597.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text597.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text597.setAttribute('x', valueXIncrease);
      text597.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text597.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text597.setAttribute('y', valueXIncrease);
      text597.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text597.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text597.setAttribute('y', valueXIncrease);
      text597.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF597, 0.001);
const text598 = document.getElementById('text598');
text598.innerHTML = number[0];
function textF598() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text598.setAttribute('fill', newColor);
  const id = text598.x['animVal'][0].value+""+text598.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text598.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text598.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text598.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text598.setAttribute('x', valueXIncrease);
      text598.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text598.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text598.setAttribute('x', valueXIncrease);
      text598.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text598.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text598.setAttribute('y', valueXIncrease);
      text598.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text598.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text598.setAttribute('y', valueXIncrease);
      text598.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF598, 0.001);
const text599 = document.getElementById('text599');
text599.innerHTML = number[0];
function textF599() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text599.setAttribute('fill', newColor);
  const id = text599.x['animVal'][0].value+""+text599.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text599.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text599.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text599.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text599.setAttribute('x', valueXIncrease);
      text599.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text599.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text599.setAttribute('x', valueXIncrease);
      text599.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text599.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text599.setAttribute('y', valueXIncrease);
      text599.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text599.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text599.setAttribute('y', valueXIncrease);
      text599.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF599, 0.001);
const text600 = document.getElementById('text600');
text600.innerHTML = number[0];
function textF600() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text600.setAttribute('fill', newColor);
  const id = text600.x['animVal'][0].value+""+text600.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text600.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text600.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text600.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text600.setAttribute('x', valueXIncrease);
      text600.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text600.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text600.setAttribute('x', valueXIncrease);
      text600.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text600.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text600.setAttribute('y', valueXIncrease);
      text600.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text600.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text600.setAttribute('y', valueXIncrease);
      text600.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF600, 0.001);
const text601 = document.getElementById('text601');
text601.innerHTML = number[0];
function textF601() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text601.setAttribute('fill', newColor);
  const id = text601.x['animVal'][0].value+""+text601.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text601.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text601.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text601.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text601.setAttribute('x', valueXIncrease);
      text601.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text601.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text601.setAttribute('x', valueXIncrease);
      text601.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text601.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text601.setAttribute('y', valueXIncrease);
      text601.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text601.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text601.setAttribute('y', valueXIncrease);
      text601.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF601, 0.001);
const text602 = document.getElementById('text602');
text602.innerHTML = number[0];
function textF602() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text602.setAttribute('fill', newColor);
  const id = text602.x['animVal'][0].value+""+text602.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text602.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text602.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text602.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text602.setAttribute('x', valueXIncrease);
      text602.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text602.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text602.setAttribute('x', valueXIncrease);
      text602.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text602.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text602.setAttribute('y', valueXIncrease);
      text602.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text602.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text602.setAttribute('y', valueXIncrease);
      text602.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF602, 0.001);
const text603 = document.getElementById('text603');
text603.innerHTML = number[0];
function textF603() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text603.setAttribute('fill', newColor);
  const id = text603.x['animVal'][0].value+""+text603.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text603.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text603.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text603.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text603.setAttribute('x', valueXIncrease);
      text603.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text603.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text603.setAttribute('x', valueXIncrease);
      text603.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text603.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text603.setAttribute('y', valueXIncrease);
      text603.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text603.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text603.setAttribute('y', valueXIncrease);
      text603.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF603, 0.001);
const text604 = document.getElementById('text604');
text604.innerHTML = number[0];
function textF604() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text604.setAttribute('fill', newColor);
  const id = text604.x['animVal'][0].value+""+text604.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text604.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text604.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text604.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text604.setAttribute('x', valueXIncrease);
      text604.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text604.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text604.setAttribute('x', valueXIncrease);
      text604.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text604.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text604.setAttribute('y', valueXIncrease);
      text604.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text604.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text604.setAttribute('y', valueXIncrease);
      text604.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF604, 0.001);
const text605 = document.getElementById('text605');
text605.innerHTML = number[0];
function textF605() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text605.setAttribute('fill', newColor);
  const id = text605.x['animVal'][0].value+""+text605.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text605.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text605.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text605.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text605.setAttribute('x', valueXIncrease);
      text605.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text605.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text605.setAttribute('x', valueXIncrease);
      text605.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text605.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text605.setAttribute('y', valueXIncrease);
      text605.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text605.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text605.setAttribute('y', valueXIncrease);
      text605.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF605, 0.001);
const text606 = document.getElementById('text606');
text606.innerHTML = number[0];
function textF606() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text606.setAttribute('fill', newColor);
  const id = text606.x['animVal'][0].value+""+text606.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text606.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text606.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text606.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text606.setAttribute('x', valueXIncrease);
      text606.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text606.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text606.setAttribute('x', valueXIncrease);
      text606.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text606.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text606.setAttribute('y', valueXIncrease);
      text606.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text606.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text606.setAttribute('y', valueXIncrease);
      text606.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF606, 0.001);
const text607 = document.getElementById('text607');
text607.innerHTML = number[0];
function textF607() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text607.setAttribute('fill', newColor);
  const id = text607.x['animVal'][0].value+""+text607.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text607.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text607.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text607.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text607.setAttribute('x', valueXIncrease);
      text607.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text607.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text607.setAttribute('x', valueXIncrease);
      text607.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text607.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text607.setAttribute('y', valueXIncrease);
      text607.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text607.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text607.setAttribute('y', valueXIncrease);
      text607.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF607, 0.001);
const text608 = document.getElementById('text608');
text608.innerHTML = number[0];
function textF608() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text608.setAttribute('fill', newColor);
  const id = text608.x['animVal'][0].value+""+text608.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text608.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text608.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text608.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text608.setAttribute('x', valueXIncrease);
      text608.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text608.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text608.setAttribute('x', valueXIncrease);
      text608.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text608.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text608.setAttribute('y', valueXIncrease);
      text608.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text608.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text608.setAttribute('y', valueXIncrease);
      text608.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF608, 0.001);
const text609 = document.getElementById('text609');
text609.innerHTML = number[0];
function textF609() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text609.setAttribute('fill', newColor);
  const id = text609.x['animVal'][0].value+""+text609.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text609.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text609.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text609.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text609.setAttribute('x', valueXIncrease);
      text609.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text609.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text609.setAttribute('x', valueXIncrease);
      text609.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text609.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text609.setAttribute('y', valueXIncrease);
      text609.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text609.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text609.setAttribute('y', valueXIncrease);
      text609.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF609, 0.001);
const text610 = document.getElementById('text610');
text610.innerHTML = number[0];
function textF610() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text610.setAttribute('fill', newColor);
  const id = text610.x['animVal'][0].value+""+text610.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text610.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text610.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text610.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text610.setAttribute('x', valueXIncrease);
      text610.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text610.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text610.setAttribute('x', valueXIncrease);
      text610.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text610.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text610.setAttribute('y', valueXIncrease);
      text610.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text610.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text610.setAttribute('y', valueXIncrease);
      text610.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF610, 0.001);
const text611 = document.getElementById('text611');
text611.innerHTML = number[0];
function textF611() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text611.setAttribute('fill', newColor);
  const id = text611.x['animVal'][0].value+""+text611.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text611.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text611.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text611.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text611.setAttribute('x', valueXIncrease);
      text611.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text611.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text611.setAttribute('x', valueXIncrease);
      text611.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text611.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text611.setAttribute('y', valueXIncrease);
      text611.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text611.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text611.setAttribute('y', valueXIncrease);
      text611.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF611, 0.001);
const text612 = document.getElementById('text612');
text612.innerHTML = number[0];
function textF612() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text612.setAttribute('fill', newColor);
  const id = text612.x['animVal'][0].value+""+text612.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text612.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text612.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text612.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text612.setAttribute('x', valueXIncrease);
      text612.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text612.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text612.setAttribute('x', valueXIncrease);
      text612.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text612.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text612.setAttribute('y', valueXIncrease);
      text612.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text612.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text612.setAttribute('y', valueXIncrease);
      text612.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF612, 0.001);
const text613 = document.getElementById('text613');
text613.innerHTML = number[0];
function textF613() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text613.setAttribute('fill', newColor);
  const id = text613.x['animVal'][0].value+""+text613.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text613.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text613.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text613.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text613.setAttribute('x', valueXIncrease);
      text613.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text613.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text613.setAttribute('x', valueXIncrease);
      text613.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text613.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text613.setAttribute('y', valueXIncrease);
      text613.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text613.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text613.setAttribute('y', valueXIncrease);
      text613.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF613, 0.001);
const text614 = document.getElementById('text614');
text614.innerHTML = number[0];
function textF614() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text614.setAttribute('fill', newColor);
  const id = text614.x['animVal'][0].value+""+text614.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text614.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text614.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text614.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text614.setAttribute('x', valueXIncrease);
      text614.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text614.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text614.setAttribute('x', valueXIncrease);
      text614.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text614.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text614.setAttribute('y', valueXIncrease);
      text614.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text614.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text614.setAttribute('y', valueXIncrease);
      text614.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF614, 0.001);
const text615 = document.getElementById('text615');
text615.innerHTML = number[0];
function textF615() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text615.setAttribute('fill', newColor);
  const id = text615.x['animVal'][0].value+""+text615.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text615.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text615.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text615.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text615.setAttribute('x', valueXIncrease);
      text615.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text615.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text615.setAttribute('x', valueXIncrease);
      text615.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text615.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text615.setAttribute('y', valueXIncrease);
      text615.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text615.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text615.setAttribute('y', valueXIncrease);
      text615.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF615, 0.001);
const text616 = document.getElementById('text616');
text616.innerHTML = number[0];
function textF616() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text616.setAttribute('fill', newColor);
  const id = text616.x['animVal'][0].value+""+text616.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text616.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text616.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text616.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text616.setAttribute('x', valueXIncrease);
      text616.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text616.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text616.setAttribute('x', valueXIncrease);
      text616.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text616.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text616.setAttribute('y', valueXIncrease);
      text616.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text616.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text616.setAttribute('y', valueXIncrease);
      text616.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF616, 0.001);
const text617 = document.getElementById('text617');
text617.innerHTML = number[0];
function textF617() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text617.setAttribute('fill', newColor);
  const id = text617.x['animVal'][0].value+""+text617.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text617.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text617.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text617.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text617.setAttribute('x', valueXIncrease);
      text617.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text617.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text617.setAttribute('x', valueXIncrease);
      text617.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text617.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text617.setAttribute('y', valueXIncrease);
      text617.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text617.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text617.setAttribute('y', valueXIncrease);
      text617.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF617, 0.001);
const text618 = document.getElementById('text618');
text618.innerHTML = number[0];
function textF618() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text618.setAttribute('fill', newColor);
  const id = text618.x['animVal'][0].value+""+text618.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text618.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text618.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text618.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text618.setAttribute('x', valueXIncrease);
      text618.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text618.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text618.setAttribute('x', valueXIncrease);
      text618.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text618.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text618.setAttribute('y', valueXIncrease);
      text618.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text618.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text618.setAttribute('y', valueXIncrease);
      text618.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF618, 0.001);
const text619 = document.getElementById('text619');
text619.innerHTML = number[0];
function textF619() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text619.setAttribute('fill', newColor);
  const id = text619.x['animVal'][0].value+""+text619.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text619.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text619.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text619.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text619.setAttribute('x', valueXIncrease);
      text619.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text619.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text619.setAttribute('x', valueXIncrease);
      text619.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text619.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text619.setAttribute('y', valueXIncrease);
      text619.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text619.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text619.setAttribute('y', valueXIncrease);
      text619.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF619, 0.001);
const text620 = document.getElementById('text620');
text620.innerHTML = number[0];
function textF620() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text620.setAttribute('fill', newColor);
  const id = text620.x['animVal'][0].value+""+text620.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text620.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text620.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text620.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text620.setAttribute('x', valueXIncrease);
      text620.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text620.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text620.setAttribute('x', valueXIncrease);
      text620.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text620.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text620.setAttribute('y', valueXIncrease);
      text620.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text620.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text620.setAttribute('y', valueXIncrease);
      text620.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF620, 0.001);
const text621 = document.getElementById('text621');
text621.innerHTML = number[0];
function textF621() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text621.setAttribute('fill', newColor);
  const id = text621.x['animVal'][0].value+""+text621.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text621.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text621.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text621.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text621.setAttribute('x', valueXIncrease);
      text621.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text621.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text621.setAttribute('x', valueXIncrease);
      text621.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text621.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text621.setAttribute('y', valueXIncrease);
      text621.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text621.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text621.setAttribute('y', valueXIncrease);
      text621.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF621, 0.001);
const text622 = document.getElementById('text622');
text622.innerHTML = number[0];
function textF622() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text622.setAttribute('fill', newColor);
  const id = text622.x['animVal'][0].value+""+text622.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text622.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text622.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text622.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text622.setAttribute('x', valueXIncrease);
      text622.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text622.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text622.setAttribute('x', valueXIncrease);
      text622.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text622.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text622.setAttribute('y', valueXIncrease);
      text622.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text622.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text622.setAttribute('y', valueXIncrease);
      text622.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF622, 0.001);
const text623 = document.getElementById('text623');
text623.innerHTML = number[0];
function textF623() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text623.setAttribute('fill', newColor);
  const id = text623.x['animVal'][0].value+""+text623.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text623.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text623.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text623.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text623.setAttribute('x', valueXIncrease);
      text623.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text623.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text623.setAttribute('x', valueXIncrease);
      text623.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text623.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text623.setAttribute('y', valueXIncrease);
      text623.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text623.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text623.setAttribute('y', valueXIncrease);
      text623.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF623, 0.001);
const text624 = document.getElementById('text624');
text624.innerHTML = number[0];
function textF624() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text624.setAttribute('fill', newColor);
  const id = text624.x['animVal'][0].value+""+text624.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text624.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text624.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text624.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text624.setAttribute('x', valueXIncrease);
      text624.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text624.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text624.setAttribute('x', valueXIncrease);
      text624.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text624.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text624.setAttribute('y', valueXIncrease);
      text624.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text624.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text624.setAttribute('y', valueXIncrease);
      text624.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF624, 0.001);
const text625 = document.getElementById('text625');
text625.innerHTML = number[0];
function textF625() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text625.setAttribute('fill', newColor);
  const id = text625.x['animVal'][0].value+""+text625.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text625.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text625.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text625.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text625.setAttribute('x', valueXIncrease);
      text625.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text625.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text625.setAttribute('x', valueXIncrease);
      text625.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text625.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text625.setAttribute('y', valueXIncrease);
      text625.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text625.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text625.setAttribute('y', valueXIncrease);
      text625.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF625, 0.001);
const text626 = document.getElementById('text626');
text626.innerHTML = number[0];
function textF626() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text626.setAttribute('fill', newColor);
  const id = text626.x['animVal'][0].value+""+text626.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text626.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text626.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text626.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text626.setAttribute('x', valueXIncrease);
      text626.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text626.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text626.setAttribute('x', valueXIncrease);
      text626.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text626.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text626.setAttribute('y', valueXIncrease);
      text626.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text626.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text626.setAttribute('y', valueXIncrease);
      text626.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF626, 0.001);
const text627 = document.getElementById('text627');
text627.innerHTML = number[0];
function textF627() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text627.setAttribute('fill', newColor);
  const id = text627.x['animVal'][0].value+""+text627.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text627.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text627.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text627.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text627.setAttribute('x', valueXIncrease);
      text627.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text627.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text627.setAttribute('x', valueXIncrease);
      text627.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text627.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text627.setAttribute('y', valueXIncrease);
      text627.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text627.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text627.setAttribute('y', valueXIncrease);
      text627.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF627, 0.001);
const text628 = document.getElementById('text628');
text628.innerHTML = number[0];
function textF628() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text628.setAttribute('fill', newColor);
  const id = text628.x['animVal'][0].value+""+text628.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text628.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text628.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text628.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text628.setAttribute('x', valueXIncrease);
      text628.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text628.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text628.setAttribute('x', valueXIncrease);
      text628.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text628.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text628.setAttribute('y', valueXIncrease);
      text628.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text628.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text628.setAttribute('y', valueXIncrease);
      text628.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF628, 0.001);
const text629 = document.getElementById('text629');
text629.innerHTML = number[0];
function textF629() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text629.setAttribute('fill', newColor);
  const id = text629.x['animVal'][0].value+""+text629.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text629.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text629.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text629.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text629.setAttribute('x', valueXIncrease);
      text629.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text629.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text629.setAttribute('x', valueXIncrease);
      text629.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text629.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text629.setAttribute('y', valueXIncrease);
      text629.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text629.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text629.setAttribute('y', valueXIncrease);
      text629.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF629, 0.001);
const text630 = document.getElementById('text630');
text630.innerHTML = number[0];
function textF630() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text630.setAttribute('fill', newColor);
  const id = text630.x['animVal'][0].value+""+text630.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text630.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text630.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text630.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text630.setAttribute('x', valueXIncrease);
      text630.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text630.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text630.setAttribute('x', valueXIncrease);
      text630.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text630.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text630.setAttribute('y', valueXIncrease);
      text630.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text630.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text630.setAttribute('y', valueXIncrease);
      text630.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF630, 0.001);
const text631 = document.getElementById('text631');
text631.innerHTML = number[0];
function textF631() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text631.setAttribute('fill', newColor);
  const id = text631.x['animVal'][0].value+""+text631.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text631.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text631.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text631.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text631.setAttribute('x', valueXIncrease);
      text631.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text631.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text631.setAttribute('x', valueXIncrease);
      text631.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text631.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text631.setAttribute('y', valueXIncrease);
      text631.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text631.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text631.setAttribute('y', valueXIncrease);
      text631.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF631, 0.001);
const text632 = document.getElementById('text632');
text632.innerHTML = number[0];
function textF632() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text632.setAttribute('fill', newColor);
  const id = text632.x['animVal'][0].value+""+text632.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text632.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text632.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text632.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text632.setAttribute('x', valueXIncrease);
      text632.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text632.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text632.setAttribute('x', valueXIncrease);
      text632.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text632.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text632.setAttribute('y', valueXIncrease);
      text632.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text632.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text632.setAttribute('y', valueXIncrease);
      text632.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF632, 0.001);
const text633 = document.getElementById('text633');
text633.innerHTML = number[0];
function textF633() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text633.setAttribute('fill', newColor);
  const id = text633.x['animVal'][0].value+""+text633.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text633.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text633.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text633.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text633.setAttribute('x', valueXIncrease);
      text633.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text633.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text633.setAttribute('x', valueXIncrease);
      text633.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text633.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text633.setAttribute('y', valueXIncrease);
      text633.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text633.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text633.setAttribute('y', valueXIncrease);
      text633.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF633, 0.001);
const text634 = document.getElementById('text634');
text634.innerHTML = number[0];
function textF634() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text634.setAttribute('fill', newColor);
  const id = text634.x['animVal'][0].value+""+text634.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text634.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text634.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text634.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text634.setAttribute('x', valueXIncrease);
      text634.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text634.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text634.setAttribute('x', valueXIncrease);
      text634.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text634.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text634.setAttribute('y', valueXIncrease);
      text634.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text634.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text634.setAttribute('y', valueXIncrease);
      text634.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF634, 0.001);
const text635 = document.getElementById('text635');
text635.innerHTML = number[0];
function textF635() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text635.setAttribute('fill', newColor);
  const id = text635.x['animVal'][0].value+""+text635.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text635.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text635.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text635.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text635.setAttribute('x', valueXIncrease);
      text635.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text635.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text635.setAttribute('x', valueXIncrease);
      text635.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text635.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text635.setAttribute('y', valueXIncrease);
      text635.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text635.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text635.setAttribute('y', valueXIncrease);
      text635.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF635, 0.001);
const text636 = document.getElementById('text636');
text636.innerHTML = number[0];
function textF636() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text636.setAttribute('fill', newColor);
  const id = text636.x['animVal'][0].value+""+text636.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text636.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text636.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text636.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text636.setAttribute('x', valueXIncrease);
      text636.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text636.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text636.setAttribute('x', valueXIncrease);
      text636.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text636.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text636.setAttribute('y', valueXIncrease);
      text636.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text636.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text636.setAttribute('y', valueXIncrease);
      text636.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF636, 0.001);
const text637 = document.getElementById('text637');
text637.innerHTML = number[0];
function textF637() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text637.setAttribute('fill', newColor);
  const id = text637.x['animVal'][0].value+""+text637.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text637.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text637.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text637.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text637.setAttribute('x', valueXIncrease);
      text637.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text637.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text637.setAttribute('x', valueXIncrease);
      text637.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text637.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text637.setAttribute('y', valueXIncrease);
      text637.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text637.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text637.setAttribute('y', valueXIncrease);
      text637.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF637, 0.001);
const text638 = document.getElementById('text638');
text638.innerHTML = number[0];
function textF638() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text638.setAttribute('fill', newColor);
  const id = text638.x['animVal'][0].value+""+text638.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text638.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text638.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text638.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text638.setAttribute('x', valueXIncrease);
      text638.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text638.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text638.setAttribute('x', valueXIncrease);
      text638.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text638.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text638.setAttribute('y', valueXIncrease);
      text638.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text638.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text638.setAttribute('y', valueXIncrease);
      text638.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF638, 0.001);
const text639 = document.getElementById('text639');
text639.innerHTML = number[0];
function textF639() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text639.setAttribute('fill', newColor);
  const id = text639.x['animVal'][0].value+""+text639.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text639.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text639.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text639.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text639.setAttribute('x', valueXIncrease);
      text639.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text639.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text639.setAttribute('x', valueXIncrease);
      text639.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text639.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text639.setAttribute('y', valueXIncrease);
      text639.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text639.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text639.setAttribute('y', valueXIncrease);
      text639.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF639, 0.001);
const text640 = document.getElementById('text640');
text640.innerHTML = number[0];
function textF640() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text640.setAttribute('fill', newColor);
  const id = text640.x['animVal'][0].value+""+text640.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text640.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text640.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text640.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text640.setAttribute('x', valueXIncrease);
      text640.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text640.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text640.setAttribute('x', valueXIncrease);
      text640.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text640.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text640.setAttribute('y', valueXIncrease);
      text640.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text640.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text640.setAttribute('y', valueXIncrease);
      text640.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF640, 0.001);
const text641 = document.getElementById('text641');
text641.innerHTML = number[0];
function textF641() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text641.setAttribute('fill', newColor);
  const id = text641.x['animVal'][0].value+""+text641.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text641.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text641.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text641.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text641.setAttribute('x', valueXIncrease);
      text641.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text641.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text641.setAttribute('x', valueXIncrease);
      text641.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text641.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text641.setAttribute('y', valueXIncrease);
      text641.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text641.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text641.setAttribute('y', valueXIncrease);
      text641.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF641, 0.001);
const text642 = document.getElementById('text642');
text642.innerHTML = number[0];
function textF642() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text642.setAttribute('fill', newColor);
  const id = text642.x['animVal'][0].value+""+text642.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text642.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text642.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text642.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text642.setAttribute('x', valueXIncrease);
      text642.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text642.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text642.setAttribute('x', valueXIncrease);
      text642.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text642.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text642.setAttribute('y', valueXIncrease);
      text642.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text642.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text642.setAttribute('y', valueXIncrease);
      text642.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF642, 0.001);
const text643 = document.getElementById('text643');
text643.innerHTML = number[0];
function textF643() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text643.setAttribute('fill', newColor);
  const id = text643.x['animVal'][0].value+""+text643.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text643.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text643.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text643.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text643.setAttribute('x', valueXIncrease);
      text643.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text643.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text643.setAttribute('x', valueXIncrease);
      text643.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text643.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text643.setAttribute('y', valueXIncrease);
      text643.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text643.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text643.setAttribute('y', valueXIncrease);
      text643.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF643, 0.001);
const text644 = document.getElementById('text644');
text644.innerHTML = number[0];
function textF644() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text644.setAttribute('fill', newColor);
  const id = text644.x['animVal'][0].value+""+text644.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text644.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text644.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text644.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text644.setAttribute('x', valueXIncrease);
      text644.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text644.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text644.setAttribute('x', valueXIncrease);
      text644.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text644.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text644.setAttribute('y', valueXIncrease);
      text644.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text644.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text644.setAttribute('y', valueXIncrease);
      text644.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF644, 0.001);
const text645 = document.getElementById('text645');
text645.innerHTML = number[0];
function textF645() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text645.setAttribute('fill', newColor);
  const id = text645.x['animVal'][0].value+""+text645.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text645.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text645.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text645.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text645.setAttribute('x', valueXIncrease);
      text645.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text645.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text645.setAttribute('x', valueXIncrease);
      text645.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text645.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text645.setAttribute('y', valueXIncrease);
      text645.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text645.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text645.setAttribute('y', valueXIncrease);
      text645.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF645, 0.001);
const text646 = document.getElementById('text646');
text646.innerHTML = number[0];
function textF646() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text646.setAttribute('fill', newColor);
  const id = text646.x['animVal'][0].value+""+text646.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text646.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text646.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text646.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text646.setAttribute('x', valueXIncrease);
      text646.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text646.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text646.setAttribute('x', valueXIncrease);
      text646.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text646.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text646.setAttribute('y', valueXIncrease);
      text646.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text646.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text646.setAttribute('y', valueXIncrease);
      text646.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF646, 0.001);
const text647 = document.getElementById('text647');
text647.innerHTML = number[0];
function textF647() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text647.setAttribute('fill', newColor);
  const id = text647.x['animVal'][0].value+""+text647.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text647.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text647.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text647.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text647.setAttribute('x', valueXIncrease);
      text647.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text647.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text647.setAttribute('x', valueXIncrease);
      text647.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text647.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text647.setAttribute('y', valueXIncrease);
      text647.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text647.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text647.setAttribute('y', valueXIncrease);
      text647.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF647, 0.001);
const text648 = document.getElementById('text648');
text648.innerHTML = number[0];
function textF648() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text648.setAttribute('fill', newColor);
  const id = text648.x['animVal'][0].value+""+text648.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text648.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text648.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text648.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text648.setAttribute('x', valueXIncrease);
      text648.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text648.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text648.setAttribute('x', valueXIncrease);
      text648.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text648.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text648.setAttribute('y', valueXIncrease);
      text648.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text648.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text648.setAttribute('y', valueXIncrease);
      text648.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF648, 0.001);
const text649 = document.getElementById('text649');
text649.innerHTML = number[0];
function textF649() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text649.setAttribute('fill', newColor);
  const id = text649.x['animVal'][0].value+""+text649.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text649.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text649.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text649.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text649.setAttribute('x', valueXIncrease);
      text649.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text649.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text649.setAttribute('x', valueXIncrease);
      text649.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text649.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text649.setAttribute('y', valueXIncrease);
      text649.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text649.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text649.setAttribute('y', valueXIncrease);
      text649.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF649, 0.001);
const text650 = document.getElementById('text650');
text650.innerHTML = number[0];
function textF650() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text650.setAttribute('fill', newColor);
  const id = text650.x['animVal'][0].value+""+text650.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text650.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text650.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text650.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text650.setAttribute('x', valueXIncrease);
      text650.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text650.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text650.setAttribute('x', valueXIncrease);
      text650.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text650.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text650.setAttribute('y', valueXIncrease);
      text650.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text650.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text650.setAttribute('y', valueXIncrease);
      text650.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF650, 0.001);
const text651 = document.getElementById('text651');
text651.innerHTML = number[0];
function textF651() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text651.setAttribute('fill', newColor);
  const id = text651.x['animVal'][0].value+""+text651.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text651.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text651.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text651.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text651.setAttribute('x', valueXIncrease);
      text651.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text651.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text651.setAttribute('x', valueXIncrease);
      text651.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text651.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text651.setAttribute('y', valueXIncrease);
      text651.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text651.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text651.setAttribute('y', valueXIncrease);
      text651.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF651, 0.001);
const text652 = document.getElementById('text652');
text652.innerHTML = number[0];
function textF652() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text652.setAttribute('fill', newColor);
  const id = text652.x['animVal'][0].value+""+text652.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text652.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text652.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text652.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text652.setAttribute('x', valueXIncrease);
      text652.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text652.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text652.setAttribute('x', valueXIncrease);
      text652.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text652.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text652.setAttribute('y', valueXIncrease);
      text652.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text652.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text652.setAttribute('y', valueXIncrease);
      text652.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF652, 0.001);
const text653 = document.getElementById('text653');
text653.innerHTML = number[0];
function textF653() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text653.setAttribute('fill', newColor);
  const id = text653.x['animVal'][0].value+""+text653.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text653.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text653.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text653.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text653.setAttribute('x', valueXIncrease);
      text653.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text653.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text653.setAttribute('x', valueXIncrease);
      text653.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text653.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text653.setAttribute('y', valueXIncrease);
      text653.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text653.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text653.setAttribute('y', valueXIncrease);
      text653.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF653, 0.001);
const text654 = document.getElementById('text654');
text654.innerHTML = number[0];
function textF654() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text654.setAttribute('fill', newColor);
  const id = text654.x['animVal'][0].value+""+text654.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text654.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text654.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text654.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text654.setAttribute('x', valueXIncrease);
      text654.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text654.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text654.setAttribute('x', valueXIncrease);
      text654.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text654.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text654.setAttribute('y', valueXIncrease);
      text654.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text654.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text654.setAttribute('y', valueXIncrease);
      text654.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF654, 0.001);
const text655 = document.getElementById('text655');
text655.innerHTML = number[0];
function textF655() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text655.setAttribute('fill', newColor);
  const id = text655.x['animVal'][0].value+""+text655.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text655.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text655.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text655.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text655.setAttribute('x', valueXIncrease);
      text655.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text655.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text655.setAttribute('x', valueXIncrease);
      text655.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text655.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text655.setAttribute('y', valueXIncrease);
      text655.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text655.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text655.setAttribute('y', valueXIncrease);
      text655.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF655, 0.001);
const text656 = document.getElementById('text656');
text656.innerHTML = number[0];
function textF656() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text656.setAttribute('fill', newColor);
  const id = text656.x['animVal'][0].value+""+text656.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text656.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text656.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text656.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text656.setAttribute('x', valueXIncrease);
      text656.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text656.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text656.setAttribute('x', valueXIncrease);
      text656.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text656.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text656.setAttribute('y', valueXIncrease);
      text656.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text656.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text656.setAttribute('y', valueXIncrease);
      text656.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF656, 0.001);
const text657 = document.getElementById('text657');
text657.innerHTML = number[0];
function textF657() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text657.setAttribute('fill', newColor);
  const id = text657.x['animVal'][0].value+""+text657.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text657.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text657.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text657.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text657.setAttribute('x', valueXIncrease);
      text657.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text657.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text657.setAttribute('x', valueXIncrease);
      text657.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text657.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text657.setAttribute('y', valueXIncrease);
      text657.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text657.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text657.setAttribute('y', valueXIncrease);
      text657.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF657, 0.001);
const text658 = document.getElementById('text658');
text658.innerHTML = number[0];
function textF658() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text658.setAttribute('fill', newColor);
  const id = text658.x['animVal'][0].value+""+text658.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text658.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text658.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text658.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text658.setAttribute('x', valueXIncrease);
      text658.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text658.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text658.setAttribute('x', valueXIncrease);
      text658.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text658.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text658.setAttribute('y', valueXIncrease);
      text658.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text658.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text658.setAttribute('y', valueXIncrease);
      text658.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF658, 0.001);
const text659 = document.getElementById('text659');
text659.innerHTML = number[0];
function textF659() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text659.setAttribute('fill', newColor);
  const id = text659.x['animVal'][0].value+""+text659.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text659.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text659.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text659.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text659.setAttribute('x', valueXIncrease);
      text659.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text659.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text659.setAttribute('x', valueXIncrease);
      text659.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text659.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text659.setAttribute('y', valueXIncrease);
      text659.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text659.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text659.setAttribute('y', valueXIncrease);
      text659.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF659, 0.001);
const text660 = document.getElementById('text660');
text660.innerHTML = number[0];
function textF660() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text660.setAttribute('fill', newColor);
  const id = text660.x['animVal'][0].value+""+text660.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text660.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text660.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text660.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text660.setAttribute('x', valueXIncrease);
      text660.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text660.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text660.setAttribute('x', valueXIncrease);
      text660.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text660.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text660.setAttribute('y', valueXIncrease);
      text660.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text660.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text660.setAttribute('y', valueXIncrease);
      text660.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF660, 0.001);
const text661 = document.getElementById('text661');
text661.innerHTML = number[0];
function textF661() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text661.setAttribute('fill', newColor);
  const id = text661.x['animVal'][0].value+""+text661.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text661.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text661.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text661.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text661.setAttribute('x', valueXIncrease);
      text661.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text661.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text661.setAttribute('x', valueXIncrease);
      text661.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text661.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text661.setAttribute('y', valueXIncrease);
      text661.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text661.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text661.setAttribute('y', valueXIncrease);
      text661.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF661, 0.001);
const text662 = document.getElementById('text662');
text662.innerHTML = number[0];
function textF662() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text662.setAttribute('fill', newColor);
  const id = text662.x['animVal'][0].value+""+text662.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text662.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text662.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text662.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text662.setAttribute('x', valueXIncrease);
      text662.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text662.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text662.setAttribute('x', valueXIncrease);
      text662.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text662.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text662.setAttribute('y', valueXIncrease);
      text662.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text662.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text662.setAttribute('y', valueXIncrease);
      text662.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF662, 0.001);
const text663 = document.getElementById('text663');
text663.innerHTML = number[0];
function textF663() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text663.setAttribute('fill', newColor);
  const id = text663.x['animVal'][0].value+""+text663.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text663.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text663.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text663.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text663.setAttribute('x', valueXIncrease);
      text663.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text663.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text663.setAttribute('x', valueXIncrease);
      text663.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text663.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text663.setAttribute('y', valueXIncrease);
      text663.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text663.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text663.setAttribute('y', valueXIncrease);
      text663.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF663, 0.001);
const text664 = document.getElementById('text664');
text664.innerHTML = number[0];
function textF664() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text664.setAttribute('fill', newColor);
  const id = text664.x['animVal'][0].value+""+text664.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text664.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text664.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text664.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text664.setAttribute('x', valueXIncrease);
      text664.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text664.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text664.setAttribute('x', valueXIncrease);
      text664.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text664.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text664.setAttribute('y', valueXIncrease);
      text664.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text664.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text664.setAttribute('y', valueXIncrease);
      text664.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF664, 0.001);
const text665 = document.getElementById('text665');
text665.innerHTML = number[0];
function textF665() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text665.setAttribute('fill', newColor);
  const id = text665.x['animVal'][0].value+""+text665.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text665.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text665.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text665.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text665.setAttribute('x', valueXIncrease);
      text665.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text665.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text665.setAttribute('x', valueXIncrease);
      text665.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text665.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text665.setAttribute('y', valueXIncrease);
      text665.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text665.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text665.setAttribute('y', valueXIncrease);
      text665.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF665, 0.001);
const text666 = document.getElementById('text666');
text666.innerHTML = number[0];
function textF666() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text666.setAttribute('fill', newColor);
  const id = text666.x['animVal'][0].value+""+text666.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text666.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text666.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text666.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text666.setAttribute('x', valueXIncrease);
      text666.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text666.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text666.setAttribute('x', valueXIncrease);
      text666.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text666.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text666.setAttribute('y', valueXIncrease);
      text666.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text666.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text666.setAttribute('y', valueXIncrease);
      text666.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF666, 0.001);
const text667 = document.getElementById('text667');
text667.innerHTML = number[0];
function textF667() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text667.setAttribute('fill', newColor);
  const id = text667.x['animVal'][0].value+""+text667.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text667.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text667.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text667.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text667.setAttribute('x', valueXIncrease);
      text667.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text667.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text667.setAttribute('x', valueXIncrease);
      text667.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text667.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text667.setAttribute('y', valueXIncrease);
      text667.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text667.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text667.setAttribute('y', valueXIncrease);
      text667.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF667, 0.001);
const text668 = document.getElementById('text668');
text668.innerHTML = number[0];
function textF668() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text668.setAttribute('fill', newColor);
  const id = text668.x['animVal'][0].value+""+text668.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text668.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text668.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text668.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text668.setAttribute('x', valueXIncrease);
      text668.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text668.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text668.setAttribute('x', valueXIncrease);
      text668.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text668.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text668.setAttribute('y', valueXIncrease);
      text668.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text668.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text668.setAttribute('y', valueXIncrease);
      text668.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF668, 0.001);
const text669 = document.getElementById('text669');
text669.innerHTML = number[0];
function textF669() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text669.setAttribute('fill', newColor);
  const id = text669.x['animVal'][0].value+""+text669.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text669.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text669.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text669.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text669.setAttribute('x', valueXIncrease);
      text669.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text669.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text669.setAttribute('x', valueXIncrease);
      text669.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text669.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text669.setAttribute('y', valueXIncrease);
      text669.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text669.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text669.setAttribute('y', valueXIncrease);
      text669.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF669, 0.001);
const text670 = document.getElementById('text670');
text670.innerHTML = number[0];
function textF670() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text670.setAttribute('fill', newColor);
  const id = text670.x['animVal'][0].value+""+text670.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text670.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text670.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text670.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text670.setAttribute('x', valueXIncrease);
      text670.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text670.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text670.setAttribute('x', valueXIncrease);
      text670.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text670.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text670.setAttribute('y', valueXIncrease);
      text670.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text670.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text670.setAttribute('y', valueXIncrease);
      text670.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF670, 0.001);
const text671 = document.getElementById('text671');
text671.innerHTML = number[0];
function textF671() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text671.setAttribute('fill', newColor);
  const id = text671.x['animVal'][0].value+""+text671.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text671.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text671.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text671.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text671.setAttribute('x', valueXIncrease);
      text671.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text671.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text671.setAttribute('x', valueXIncrease);
      text671.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text671.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text671.setAttribute('y', valueXIncrease);
      text671.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text671.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text671.setAttribute('y', valueXIncrease);
      text671.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF671, 0.001);
const text672 = document.getElementById('text672');
text672.innerHTML = number[0];
function textF672() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text672.setAttribute('fill', newColor);
  const id = text672.x['animVal'][0].value+""+text672.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text672.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text672.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text672.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text672.setAttribute('x', valueXIncrease);
      text672.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text672.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text672.setAttribute('x', valueXIncrease);
      text672.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text672.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text672.setAttribute('y', valueXIncrease);
      text672.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text672.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text672.setAttribute('y', valueXIncrease);
      text672.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF672, 0.001);
const text673 = document.getElementById('text673');
text673.innerHTML = number[0];
function textF673() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text673.setAttribute('fill', newColor);
  const id = text673.x['animVal'][0].value+""+text673.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text673.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text673.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text673.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text673.setAttribute('x', valueXIncrease);
      text673.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text673.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text673.setAttribute('x', valueXIncrease);
      text673.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text673.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text673.setAttribute('y', valueXIncrease);
      text673.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text673.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text673.setAttribute('y', valueXIncrease);
      text673.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF673, 0.001);
const text674 = document.getElementById('text674');
text674.innerHTML = number[0];
function textF674() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text674.setAttribute('fill', newColor);
  const id = text674.x['animVal'][0].value+""+text674.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text674.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text674.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text674.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text674.setAttribute('x', valueXIncrease);
      text674.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text674.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text674.setAttribute('x', valueXIncrease);
      text674.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text674.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text674.setAttribute('y', valueXIncrease);
      text674.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text674.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text674.setAttribute('y', valueXIncrease);
      text674.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF674, 0.001);
const text675 = document.getElementById('text675');
text675.innerHTML = number[0];
function textF675() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text675.setAttribute('fill', newColor);
  const id = text675.x['animVal'][0].value+""+text675.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text675.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text675.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text675.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text675.setAttribute('x', valueXIncrease);
      text675.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text675.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text675.setAttribute('x', valueXIncrease);
      text675.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text675.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text675.setAttribute('y', valueXIncrease);
      text675.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text675.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text675.setAttribute('y', valueXIncrease);
      text675.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF675, 0.001);
const text676 = document.getElementById('text676');
text676.innerHTML = number[0];
function textF676() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text676.setAttribute('fill', newColor);
  const id = text676.x['animVal'][0].value+""+text676.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text676.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text676.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text676.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text676.setAttribute('x', valueXIncrease);
      text676.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text676.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text676.setAttribute('x', valueXIncrease);
      text676.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text676.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text676.setAttribute('y', valueXIncrease);
      text676.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text676.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text676.setAttribute('y', valueXIncrease);
      text676.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF676, 0.001);
const text677 = document.getElementById('text677');
text677.innerHTML = number[0];
function textF677() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text677.setAttribute('fill', newColor);
  const id = text677.x['animVal'][0].value+""+text677.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text677.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text677.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text677.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text677.setAttribute('x', valueXIncrease);
      text677.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text677.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text677.setAttribute('x', valueXIncrease);
      text677.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text677.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text677.setAttribute('y', valueXIncrease);
      text677.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text677.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text677.setAttribute('y', valueXIncrease);
      text677.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF677, 0.001);
const text678 = document.getElementById('text678');
text678.innerHTML = number[0];
function textF678() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text678.setAttribute('fill', newColor);
  const id = text678.x['animVal'][0].value+""+text678.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text678.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text678.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text678.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text678.setAttribute('x', valueXIncrease);
      text678.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text678.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text678.setAttribute('x', valueXIncrease);
      text678.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text678.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text678.setAttribute('y', valueXIncrease);
      text678.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text678.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text678.setAttribute('y', valueXIncrease);
      text678.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF678, 0.001);
const text679 = document.getElementById('text679');
text679.innerHTML = number[0];
function textF679() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text679.setAttribute('fill', newColor);
  const id = text679.x['animVal'][0].value+""+text679.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text679.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text679.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text679.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text679.setAttribute('x', valueXIncrease);
      text679.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text679.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text679.setAttribute('x', valueXIncrease);
      text679.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text679.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text679.setAttribute('y', valueXIncrease);
      text679.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text679.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text679.setAttribute('y', valueXIncrease);
      text679.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF679, 0.001);
const text680 = document.getElementById('text680');
text680.innerHTML = number[0];
function textF680() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text680.setAttribute('fill', newColor);
  const id = text680.x['animVal'][0].value+""+text680.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text680.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text680.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text680.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text680.setAttribute('x', valueXIncrease);
      text680.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text680.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text680.setAttribute('x', valueXIncrease);
      text680.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text680.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text680.setAttribute('y', valueXIncrease);
      text680.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text680.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text680.setAttribute('y', valueXIncrease);
      text680.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF680, 0.001);
const text681 = document.getElementById('text681');
text681.innerHTML = number[0];
function textF681() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text681.setAttribute('fill', newColor);
  const id = text681.x['animVal'][0].value+""+text681.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text681.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text681.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text681.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text681.setAttribute('x', valueXIncrease);
      text681.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text681.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text681.setAttribute('x', valueXIncrease);
      text681.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text681.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text681.setAttribute('y', valueXIncrease);
      text681.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text681.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text681.setAttribute('y', valueXIncrease);
      text681.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF681, 0.001);
const text682 = document.getElementById('text682');
text682.innerHTML = number[0];
function textF682() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text682.setAttribute('fill', newColor);
  const id = text682.x['animVal'][0].value+""+text682.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text682.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text682.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text682.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text682.setAttribute('x', valueXIncrease);
      text682.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text682.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text682.setAttribute('x', valueXIncrease);
      text682.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text682.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text682.setAttribute('y', valueXIncrease);
      text682.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text682.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text682.setAttribute('y', valueXIncrease);
      text682.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF682, 0.001);
const text683 = document.getElementById('text683');
text683.innerHTML = number[0];
function textF683() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text683.setAttribute('fill', newColor);
  const id = text683.x['animVal'][0].value+""+text683.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text683.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text683.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text683.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text683.setAttribute('x', valueXIncrease);
      text683.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text683.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text683.setAttribute('x', valueXIncrease);
      text683.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text683.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text683.setAttribute('y', valueXIncrease);
      text683.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text683.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text683.setAttribute('y', valueXIncrease);
      text683.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF683, 0.001);
const text684 = document.getElementById('text684');
text684.innerHTML = number[0];
function textF684() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text684.setAttribute('fill', newColor);
  const id = text684.x['animVal'][0].value+""+text684.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text684.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text684.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text684.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text684.setAttribute('x', valueXIncrease);
      text684.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text684.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text684.setAttribute('x', valueXIncrease);
      text684.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text684.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text684.setAttribute('y', valueXIncrease);
      text684.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text684.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text684.setAttribute('y', valueXIncrease);
      text684.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF684, 0.001);
const text685 = document.getElementById('text685');
text685.innerHTML = number[0];
function textF685() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text685.setAttribute('fill', newColor);
  const id = text685.x['animVal'][0].value+""+text685.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text685.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text685.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text685.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text685.setAttribute('x', valueXIncrease);
      text685.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text685.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text685.setAttribute('x', valueXIncrease);
      text685.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text685.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text685.setAttribute('y', valueXIncrease);
      text685.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text685.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text685.setAttribute('y', valueXIncrease);
      text685.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF685, 0.001);
const text686 = document.getElementById('text686');
text686.innerHTML = number[0];
function textF686() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text686.setAttribute('fill', newColor);
  const id = text686.x['animVal'][0].value+""+text686.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text686.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text686.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text686.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text686.setAttribute('x', valueXIncrease);
      text686.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text686.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text686.setAttribute('x', valueXIncrease);
      text686.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text686.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text686.setAttribute('y', valueXIncrease);
      text686.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text686.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text686.setAttribute('y', valueXIncrease);
      text686.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF686, 0.001);
const text687 = document.getElementById('text687');
text687.innerHTML = number[0];
function textF687() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text687.setAttribute('fill', newColor);
  const id = text687.x['animVal'][0].value+""+text687.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text687.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text687.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text687.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text687.setAttribute('x', valueXIncrease);
      text687.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text687.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text687.setAttribute('x', valueXIncrease);
      text687.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text687.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text687.setAttribute('y', valueXIncrease);
      text687.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text687.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text687.setAttribute('y', valueXIncrease);
      text687.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF687, 0.001);
const text688 = document.getElementById('text688');
text688.innerHTML = number[0];
function textF688() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text688.setAttribute('fill', newColor);
  const id = text688.x['animVal'][0].value+""+text688.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text688.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text688.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text688.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text688.setAttribute('x', valueXIncrease);
      text688.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text688.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text688.setAttribute('x', valueXIncrease);
      text688.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text688.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text688.setAttribute('y', valueXIncrease);
      text688.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text688.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text688.setAttribute('y', valueXIncrease);
      text688.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF688, 0.001);
const text689 = document.getElementById('text689');
text689.innerHTML = number[0];
function textF689() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text689.setAttribute('fill', newColor);
  const id = text689.x['animVal'][0].value+""+text689.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text689.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text689.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text689.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text689.setAttribute('x', valueXIncrease);
      text689.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text689.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text689.setAttribute('x', valueXIncrease);
      text689.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text689.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text689.setAttribute('y', valueXIncrease);
      text689.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text689.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text689.setAttribute('y', valueXIncrease);
      text689.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF689, 0.001);
const text690 = document.getElementById('text690');
text690.innerHTML = number[0];
function textF690() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text690.setAttribute('fill', newColor);
  const id = text690.x['animVal'][0].value+""+text690.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text690.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text690.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text690.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text690.setAttribute('x', valueXIncrease);
      text690.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text690.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text690.setAttribute('x', valueXIncrease);
      text690.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text690.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text690.setAttribute('y', valueXIncrease);
      text690.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text690.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text690.setAttribute('y', valueXIncrease);
      text690.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF690, 0.001);
const text691 = document.getElementById('text691');
text691.innerHTML = number[0];
function textF691() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text691.setAttribute('fill', newColor);
  const id = text691.x['animVal'][0].value+""+text691.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text691.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text691.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text691.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text691.setAttribute('x', valueXIncrease);
      text691.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text691.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text691.setAttribute('x', valueXIncrease);
      text691.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text691.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text691.setAttribute('y', valueXIncrease);
      text691.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text691.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text691.setAttribute('y', valueXIncrease);
      text691.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF691, 0.001);
const text692 = document.getElementById('text692');
text692.innerHTML = number[0];
function textF692() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text692.setAttribute('fill', newColor);
  const id = text692.x['animVal'][0].value+""+text692.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text692.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text692.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text692.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text692.setAttribute('x', valueXIncrease);
      text692.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text692.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text692.setAttribute('x', valueXIncrease);
      text692.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text692.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text692.setAttribute('y', valueXIncrease);
      text692.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text692.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text692.setAttribute('y', valueXIncrease);
      text692.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF692, 0.001);
const text693 = document.getElementById('text693');
text693.innerHTML = number[0];
function textF693() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text693.setAttribute('fill', newColor);
  const id = text693.x['animVal'][0].value+""+text693.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text693.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text693.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text693.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text693.setAttribute('x', valueXIncrease);
      text693.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text693.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text693.setAttribute('x', valueXIncrease);
      text693.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text693.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text693.setAttribute('y', valueXIncrease);
      text693.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text693.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text693.setAttribute('y', valueXIncrease);
      text693.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF693, 0.001);
const text694 = document.getElementById('text694');
text694.innerHTML = number[0];
function textF694() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text694.setAttribute('fill', newColor);
  const id = text694.x['animVal'][0].value+""+text694.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text694.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text694.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text694.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text694.setAttribute('x', valueXIncrease);
      text694.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text694.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text694.setAttribute('x', valueXIncrease);
      text694.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text694.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text694.setAttribute('y', valueXIncrease);
      text694.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text694.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text694.setAttribute('y', valueXIncrease);
      text694.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF694, 0.001);
const text695 = document.getElementById('text695');
text695.innerHTML = number[0];
function textF695() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text695.setAttribute('fill', newColor);
  const id = text695.x['animVal'][0].value+""+text695.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text695.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text695.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text695.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text695.setAttribute('x', valueXIncrease);
      text695.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text695.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text695.setAttribute('x', valueXIncrease);
      text695.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text695.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text695.setAttribute('y', valueXIncrease);
      text695.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text695.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text695.setAttribute('y', valueXIncrease);
      text695.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF695, 0.001);
const text696 = document.getElementById('text696');
text696.innerHTML = number[0];
function textF696() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text696.setAttribute('fill', newColor);
  const id = text696.x['animVal'][0].value+""+text696.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text696.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text696.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text696.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text696.setAttribute('x', valueXIncrease);
      text696.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text696.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text696.setAttribute('x', valueXIncrease);
      text696.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text696.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text696.setAttribute('y', valueXIncrease);
      text696.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text696.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text696.setAttribute('y', valueXIncrease);
      text696.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF696, 0.001);
const text697 = document.getElementById('text697');
text697.innerHTML = number[0];
function textF697() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text697.setAttribute('fill', newColor);
  const id = text697.x['animVal'][0].value+""+text697.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text697.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text697.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text697.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text697.setAttribute('x', valueXIncrease);
      text697.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text697.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text697.setAttribute('x', valueXIncrease);
      text697.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text697.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text697.setAttribute('y', valueXIncrease);
      text697.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text697.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text697.setAttribute('y', valueXIncrease);
      text697.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF697, 0.001);
const text698 = document.getElementById('text698');
text698.innerHTML = number[0];
function textF698() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text698.setAttribute('fill', newColor);
  const id = text698.x['animVal'][0].value+""+text698.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text698.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text698.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text698.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text698.setAttribute('x', valueXIncrease);
      text698.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text698.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text698.setAttribute('x', valueXIncrease);
      text698.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text698.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text698.setAttribute('y', valueXIncrease);
      text698.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text698.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text698.setAttribute('y', valueXIncrease);
      text698.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF698, 0.001);
const text699 = document.getElementById('text699');
text699.innerHTML = number[0];
function textF699() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text699.setAttribute('fill', newColor);
  const id = text699.x['animVal'][0].value+""+text699.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text699.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text699.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text699.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text699.setAttribute('x', valueXIncrease);
      text699.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text699.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text699.setAttribute('x', valueXIncrease);
      text699.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text699.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text699.setAttribute('y', valueXIncrease);
      text699.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text699.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text699.setAttribute('y', valueXIncrease);
      text699.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF699, 0.001);
const text700 = document.getElementById('text700');
text700.innerHTML = number[0];
function textF700() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text700.setAttribute('fill', newColor);
  const id = text700.x['animVal'][0].value+""+text700.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text700.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text700.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text700.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text700.setAttribute('x', valueXIncrease);
      text700.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text700.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text700.setAttribute('x', valueXIncrease);
      text700.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text700.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text700.setAttribute('y', valueXIncrease);
      text700.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text700.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text700.setAttribute('y', valueXIncrease);
      text700.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF700, 0.001);
const text701 = document.getElementById('text701');
text701.innerHTML = number[0];
function textF701() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text701.setAttribute('fill', newColor);
  const id = text701.x['animVal'][0].value+""+text701.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text701.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text701.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text701.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text701.setAttribute('x', valueXIncrease);
      text701.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text701.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text701.setAttribute('x', valueXIncrease);
      text701.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text701.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text701.setAttribute('y', valueXIncrease);
      text701.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text701.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text701.setAttribute('y', valueXIncrease);
      text701.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF701, 0.001);
const text702 = document.getElementById('text702');
text702.innerHTML = number[0];
function textF702() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text702.setAttribute('fill', newColor);
  const id = text702.x['animVal'][0].value+""+text702.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text702.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text702.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text702.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text702.setAttribute('x', valueXIncrease);
      text702.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text702.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text702.setAttribute('x', valueXIncrease);
      text702.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text702.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text702.setAttribute('y', valueXIncrease);
      text702.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text702.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text702.setAttribute('y', valueXIncrease);
      text702.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF702, 0.001);
const text703 = document.getElementById('text703');
text703.innerHTML = number[0];
function textF703() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text703.setAttribute('fill', newColor);
  const id = text703.x['animVal'][0].value+""+text703.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text703.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text703.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text703.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text703.setAttribute('x', valueXIncrease);
      text703.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text703.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text703.setAttribute('x', valueXIncrease);
      text703.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text703.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text703.setAttribute('y', valueXIncrease);
      text703.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text703.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text703.setAttribute('y', valueXIncrease);
      text703.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF703, 0.001);
const text704 = document.getElementById('text704');
text704.innerHTML = number[0];
function textF704() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text704.setAttribute('fill', newColor);
  const id = text704.x['animVal'][0].value+""+text704.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text704.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text704.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text704.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text704.setAttribute('x', valueXIncrease);
      text704.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text704.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text704.setAttribute('x', valueXIncrease);
      text704.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text704.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text704.setAttribute('y', valueXIncrease);
      text704.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text704.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text704.setAttribute('y', valueXIncrease);
      text704.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF704, 0.001);
const text705 = document.getElementById('text705');
text705.innerHTML = number[0];
function textF705() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text705.setAttribute('fill', newColor);
  const id = text705.x['animVal'][0].value+""+text705.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text705.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text705.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text705.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text705.setAttribute('x', valueXIncrease);
      text705.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text705.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text705.setAttribute('x', valueXIncrease);
      text705.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text705.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text705.setAttribute('y', valueXIncrease);
      text705.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text705.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text705.setAttribute('y', valueXIncrease);
      text705.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF705, 0.001);
const text706 = document.getElementById('text706');
text706.innerHTML = number[0];
function textF706() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text706.setAttribute('fill', newColor);
  const id = text706.x['animVal'][0].value+""+text706.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text706.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text706.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text706.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text706.setAttribute('x', valueXIncrease);
      text706.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text706.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text706.setAttribute('x', valueXIncrease);
      text706.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text706.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text706.setAttribute('y', valueXIncrease);
      text706.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text706.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text706.setAttribute('y', valueXIncrease);
      text706.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF706, 0.001);
const text707 = document.getElementById('text707');
text707.innerHTML = number[0];
function textF707() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text707.setAttribute('fill', newColor);
  const id = text707.x['animVal'][0].value+""+text707.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text707.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text707.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text707.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text707.setAttribute('x', valueXIncrease);
      text707.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text707.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text707.setAttribute('x', valueXIncrease);
      text707.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text707.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text707.setAttribute('y', valueXIncrease);
      text707.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text707.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text707.setAttribute('y', valueXIncrease);
      text707.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF707, 0.001);
const text708 = document.getElementById('text708');
text708.innerHTML = number[0];
function textF708() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text708.setAttribute('fill', newColor);
  const id = text708.x['animVal'][0].value+""+text708.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text708.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text708.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text708.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text708.setAttribute('x', valueXIncrease);
      text708.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text708.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text708.setAttribute('x', valueXIncrease);
      text708.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text708.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text708.setAttribute('y', valueXIncrease);
      text708.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text708.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text708.setAttribute('y', valueXIncrease);
      text708.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF708, 0.001);
const text709 = document.getElementById('text709');
text709.innerHTML = number[0];
function textF709() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text709.setAttribute('fill', newColor);
  const id = text709.x['animVal'][0].value+""+text709.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text709.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text709.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text709.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text709.setAttribute('x', valueXIncrease);
      text709.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text709.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text709.setAttribute('x', valueXIncrease);
      text709.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text709.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text709.setAttribute('y', valueXIncrease);
      text709.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text709.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text709.setAttribute('y', valueXIncrease);
      text709.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF709, 0.001);
const text710 = document.getElementById('text710');
text710.innerHTML = number[0];
function textF710() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text710.setAttribute('fill', newColor);
  const id = text710.x['animVal'][0].value+""+text710.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text710.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text710.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text710.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text710.setAttribute('x', valueXIncrease);
      text710.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text710.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text710.setAttribute('x', valueXIncrease);
      text710.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text710.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text710.setAttribute('y', valueXIncrease);
      text710.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text710.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text710.setAttribute('y', valueXIncrease);
      text710.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF710, 0.001);
const text711 = document.getElementById('text711');
text711.innerHTML = number[0];
function textF711() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text711.setAttribute('fill', newColor);
  const id = text711.x['animVal'][0].value+""+text711.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text711.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text711.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text711.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text711.setAttribute('x', valueXIncrease);
      text711.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text711.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text711.setAttribute('x', valueXIncrease);
      text711.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text711.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text711.setAttribute('y', valueXIncrease);
      text711.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text711.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text711.setAttribute('y', valueXIncrease);
      text711.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF711, 0.001);
const text712 = document.getElementById('text712');
text712.innerHTML = number[0];
function textF712() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text712.setAttribute('fill', newColor);
  const id = text712.x['animVal'][0].value+""+text712.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text712.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text712.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text712.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text712.setAttribute('x', valueXIncrease);
      text712.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text712.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text712.setAttribute('x', valueXIncrease);
      text712.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text712.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text712.setAttribute('y', valueXIncrease);
      text712.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text712.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text712.setAttribute('y', valueXIncrease);
      text712.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF712, 0.001);
const text713 = document.getElementById('text713');
text713.innerHTML = number[0];
function textF713() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text713.setAttribute('fill', newColor);
  const id = text713.x['animVal'][0].value+""+text713.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text713.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text713.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text713.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text713.setAttribute('x', valueXIncrease);
      text713.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text713.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text713.setAttribute('x', valueXIncrease);
      text713.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text713.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text713.setAttribute('y', valueXIncrease);
      text713.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text713.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text713.setAttribute('y', valueXIncrease);
      text713.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF713, 0.001);
const text714 = document.getElementById('text714');
text714.innerHTML = number[0];
function textF714() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text714.setAttribute('fill', newColor);
  const id = text714.x['animVal'][0].value+""+text714.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text714.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text714.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text714.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text714.setAttribute('x', valueXIncrease);
      text714.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text714.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text714.setAttribute('x', valueXIncrease);
      text714.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text714.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text714.setAttribute('y', valueXIncrease);
      text714.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text714.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text714.setAttribute('y', valueXIncrease);
      text714.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF714, 0.001);
const text715 = document.getElementById('text715');
text715.innerHTML = number[0];
function textF715() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text715.setAttribute('fill', newColor);
  const id = text715.x['animVal'][0].value+""+text715.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text715.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text715.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text715.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text715.setAttribute('x', valueXIncrease);
      text715.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text715.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text715.setAttribute('x', valueXIncrease);
      text715.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text715.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text715.setAttribute('y', valueXIncrease);
      text715.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text715.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text715.setAttribute('y', valueXIncrease);
      text715.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF715, 0.001);
const text716 = document.getElementById('text716');
text716.innerHTML = number[0];
function textF716() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text716.setAttribute('fill', newColor);
  const id = text716.x['animVal'][0].value+""+text716.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text716.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text716.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text716.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text716.setAttribute('x', valueXIncrease);
      text716.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text716.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text716.setAttribute('x', valueXIncrease);
      text716.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text716.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text716.setAttribute('y', valueXIncrease);
      text716.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text716.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text716.setAttribute('y', valueXIncrease);
      text716.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF716, 0.001);
const text717 = document.getElementById('text717');
text717.innerHTML = number[0];
function textF717() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text717.setAttribute('fill', newColor);
  const id = text717.x['animVal'][0].value+""+text717.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text717.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text717.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text717.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text717.setAttribute('x', valueXIncrease);
      text717.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text717.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text717.setAttribute('x', valueXIncrease);
      text717.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text717.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text717.setAttribute('y', valueXIncrease);
      text717.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text717.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text717.setAttribute('y', valueXIncrease);
      text717.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF717, 0.001);
const text718 = document.getElementById('text718');
text718.innerHTML = number[0];
function textF718() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text718.setAttribute('fill', newColor);
  const id = text718.x['animVal'][0].value+""+text718.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text718.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text718.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text718.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text718.setAttribute('x', valueXIncrease);
      text718.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text718.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text718.setAttribute('x', valueXIncrease);
      text718.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text718.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text718.setAttribute('y', valueXIncrease);
      text718.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text718.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text718.setAttribute('y', valueXIncrease);
      text718.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF718, 0.001);
const text719 = document.getElementById('text719');
text719.innerHTML = number[0];
function textF719() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text719.setAttribute('fill', newColor);
  const id = text719.x['animVal'][0].value+""+text719.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text719.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text719.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text719.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text719.setAttribute('x', valueXIncrease);
      text719.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text719.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text719.setAttribute('x', valueXIncrease);
      text719.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text719.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text719.setAttribute('y', valueXIncrease);
      text719.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text719.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text719.setAttribute('y', valueXIncrease);
      text719.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF719, 0.001);
const text720 = document.getElementById('text720');
text720.innerHTML = number[0];
function textF720() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text720.setAttribute('fill', newColor);
  const id = text720.x['animVal'][0].value+""+text720.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text720.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text720.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text720.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text720.setAttribute('x', valueXIncrease);
      text720.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text720.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text720.setAttribute('x', valueXIncrease);
      text720.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text720.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text720.setAttribute('y', valueXIncrease);
      text720.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text720.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text720.setAttribute('y', valueXIncrease);
      text720.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF720, 0.001);
const text721 = document.getElementById('text721');
text721.innerHTML = number[0];
function textF721() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text721.setAttribute('fill', newColor);
  const id = text721.x['animVal'][0].value+""+text721.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text721.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text721.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text721.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text721.setAttribute('x', valueXIncrease);
      text721.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text721.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text721.setAttribute('x', valueXIncrease);
      text721.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text721.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text721.setAttribute('y', valueXIncrease);
      text721.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text721.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text721.setAttribute('y', valueXIncrease);
      text721.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF721, 0.001);
const text722 = document.getElementById('text722');
text722.innerHTML = number[0];
function textF722() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text722.setAttribute('fill', newColor);
  const id = text722.x['animVal'][0].value+""+text722.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text722.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text722.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text722.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text722.setAttribute('x', valueXIncrease);
      text722.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text722.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text722.setAttribute('x', valueXIncrease);
      text722.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text722.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text722.setAttribute('y', valueXIncrease);
      text722.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text722.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text722.setAttribute('y', valueXIncrease);
      text722.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF722, 0.001);
const text723 = document.getElementById('text723');
text723.innerHTML = number[0];
function textF723() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text723.setAttribute('fill', newColor);
  const id = text723.x['animVal'][0].value+""+text723.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text723.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text723.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text723.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text723.setAttribute('x', valueXIncrease);
      text723.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text723.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text723.setAttribute('x', valueXIncrease);
      text723.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text723.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text723.setAttribute('y', valueXIncrease);
      text723.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text723.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text723.setAttribute('y', valueXIncrease);
      text723.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF723, 0.001);
const text724 = document.getElementById('text724');
text724.innerHTML = number[0];
function textF724() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text724.setAttribute('fill', newColor);
  const id = text724.x['animVal'][0].value+""+text724.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text724.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text724.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text724.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text724.setAttribute('x', valueXIncrease);
      text724.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text724.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text724.setAttribute('x', valueXIncrease);
      text724.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text724.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text724.setAttribute('y', valueXIncrease);
      text724.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text724.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text724.setAttribute('y', valueXIncrease);
      text724.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF724, 0.001);
const text725 = document.getElementById('text725');
text725.innerHTML = number[0];
function textF725() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text725.setAttribute('fill', newColor);
  const id = text725.x['animVal'][0].value+""+text725.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text725.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text725.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text725.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text725.setAttribute('x', valueXIncrease);
      text725.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text725.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text725.setAttribute('x', valueXIncrease);
      text725.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text725.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text725.setAttribute('y', valueXIncrease);
      text725.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text725.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text725.setAttribute('y', valueXIncrease);
      text725.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF725, 0.001);
const text726 = document.getElementById('text726');
text726.innerHTML = number[0];
function textF726() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text726.setAttribute('fill', newColor);
  const id = text726.x['animVal'][0].value+""+text726.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text726.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text726.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text726.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text726.setAttribute('x', valueXIncrease);
      text726.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text726.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text726.setAttribute('x', valueXIncrease);
      text726.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text726.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text726.setAttribute('y', valueXIncrease);
      text726.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text726.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text726.setAttribute('y', valueXIncrease);
      text726.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF726, 0.001);
const text727 = document.getElementById('text727');
text727.innerHTML = number[0];
function textF727() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text727.setAttribute('fill', newColor);
  const id = text727.x['animVal'][0].value+""+text727.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text727.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text727.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text727.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text727.setAttribute('x', valueXIncrease);
      text727.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text727.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text727.setAttribute('x', valueXIncrease);
      text727.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text727.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text727.setAttribute('y', valueXIncrease);
      text727.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text727.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text727.setAttribute('y', valueXIncrease);
      text727.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF727, 0.001);
const text728 = document.getElementById('text728');
text728.innerHTML = number[0];
function textF728() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text728.setAttribute('fill', newColor);
  const id = text728.x['animVal'][0].value+""+text728.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text728.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text728.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text728.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text728.setAttribute('x', valueXIncrease);
      text728.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text728.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text728.setAttribute('x', valueXIncrease);
      text728.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text728.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text728.setAttribute('y', valueXIncrease);
      text728.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text728.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text728.setAttribute('y', valueXIncrease);
      text728.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF728, 0.001);
const text729 = document.getElementById('text729');
text729.innerHTML = number[0];
function textF729() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text729.setAttribute('fill', newColor);
  const id = text729.x['animVal'][0].value+""+text729.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text729.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text729.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text729.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text729.setAttribute('x', valueXIncrease);
      text729.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text729.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text729.setAttribute('x', valueXIncrease);
      text729.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text729.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text729.setAttribute('y', valueXIncrease);
      text729.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text729.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text729.setAttribute('y', valueXIncrease);
      text729.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF729, 0.001);
const text730 = document.getElementById('text730');
text730.innerHTML = number[0];
function textF730() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text730.setAttribute('fill', newColor);
  const id = text730.x['animVal'][0].value+""+text730.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text730.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text730.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text730.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text730.setAttribute('x', valueXIncrease);
      text730.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text730.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text730.setAttribute('x', valueXIncrease);
      text730.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text730.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text730.setAttribute('y', valueXIncrease);
      text730.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text730.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text730.setAttribute('y', valueXIncrease);
      text730.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF730, 0.001);
const text731 = document.getElementById('text731');
text731.innerHTML = number[0];
function textF731() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text731.setAttribute('fill', newColor);
  const id = text731.x['animVal'][0].value+""+text731.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text731.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text731.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text731.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text731.setAttribute('x', valueXIncrease);
      text731.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text731.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text731.setAttribute('x', valueXIncrease);
      text731.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text731.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text731.setAttribute('y', valueXIncrease);
      text731.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text731.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text731.setAttribute('y', valueXIncrease);
      text731.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF731, 0.001);
const text732 = document.getElementById('text732');
text732.innerHTML = number[0];
function textF732() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text732.setAttribute('fill', newColor);
  const id = text732.x['animVal'][0].value+""+text732.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text732.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text732.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text732.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text732.setAttribute('x', valueXIncrease);
      text732.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text732.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text732.setAttribute('x', valueXIncrease);
      text732.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text732.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text732.setAttribute('y', valueXIncrease);
      text732.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text732.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text732.setAttribute('y', valueXIncrease);
      text732.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF732, 0.001);
const text733 = document.getElementById('text733');
text733.innerHTML = number[0];
function textF733() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text733.setAttribute('fill', newColor);
  const id = text733.x['animVal'][0].value+""+text733.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text733.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text733.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text733.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text733.setAttribute('x', valueXIncrease);
      text733.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text733.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text733.setAttribute('x', valueXIncrease);
      text733.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text733.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text733.setAttribute('y', valueXIncrease);
      text733.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text733.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text733.setAttribute('y', valueXIncrease);
      text733.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF733, 0.001);
const text734 = document.getElementById('text734');
text734.innerHTML = number[0];
function textF734() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text734.setAttribute('fill', newColor);
  const id = text734.x['animVal'][0].value+""+text734.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text734.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text734.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text734.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text734.setAttribute('x', valueXIncrease);
      text734.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text734.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text734.setAttribute('x', valueXIncrease);
      text734.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text734.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text734.setAttribute('y', valueXIncrease);
      text734.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text734.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text734.setAttribute('y', valueXIncrease);
      text734.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF734, 0.001);
const text735 = document.getElementById('text735');
text735.innerHTML = number[0];
function textF735() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text735.setAttribute('fill', newColor);
  const id = text735.x['animVal'][0].value+""+text735.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text735.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text735.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text735.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text735.setAttribute('x', valueXIncrease);
      text735.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text735.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text735.setAttribute('x', valueXIncrease);
      text735.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text735.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text735.setAttribute('y', valueXIncrease);
      text735.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text735.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text735.setAttribute('y', valueXIncrease);
      text735.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF735, 0.001);
const text736 = document.getElementById('text736');
text736.innerHTML = number[0];
function textF736() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text736.setAttribute('fill', newColor);
  const id = text736.x['animVal'][0].value+""+text736.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text736.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text736.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text736.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text736.setAttribute('x', valueXIncrease);
      text736.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text736.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text736.setAttribute('x', valueXIncrease);
      text736.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text736.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text736.setAttribute('y', valueXIncrease);
      text736.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text736.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text736.setAttribute('y', valueXIncrease);
      text736.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF736, 0.001);
const text737 = document.getElementById('text737');
text737.innerHTML = number[0];
function textF737() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text737.setAttribute('fill', newColor);
  const id = text737.x['animVal'][0].value+""+text737.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text737.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text737.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text737.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text737.setAttribute('x', valueXIncrease);
      text737.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text737.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text737.setAttribute('x', valueXIncrease);
      text737.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text737.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text737.setAttribute('y', valueXIncrease);
      text737.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text737.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text737.setAttribute('y', valueXIncrease);
      text737.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF737, 0.001);
const text738 = document.getElementById('text738');
text738.innerHTML = number[0];
function textF738() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text738.setAttribute('fill', newColor);
  const id = text738.x['animVal'][0].value+""+text738.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text738.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text738.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text738.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text738.setAttribute('x', valueXIncrease);
      text738.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text738.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text738.setAttribute('x', valueXIncrease);
      text738.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text738.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text738.setAttribute('y', valueXIncrease);
      text738.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text738.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text738.setAttribute('y', valueXIncrease);
      text738.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF738, 0.001);
const text739 = document.getElementById('text739');
text739.innerHTML = number[0];
function textF739() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text739.setAttribute('fill', newColor);
  const id = text739.x['animVal'][0].value+""+text739.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text739.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text739.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text739.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text739.setAttribute('x', valueXIncrease);
      text739.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text739.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text739.setAttribute('x', valueXIncrease);
      text739.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text739.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text739.setAttribute('y', valueXIncrease);
      text739.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text739.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text739.setAttribute('y', valueXIncrease);
      text739.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF739, 0.001);
const text740 = document.getElementById('text740');
text740.innerHTML = number[0];
function textF740() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text740.setAttribute('fill', newColor);
  const id = text740.x['animVal'][0].value+""+text740.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text740.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text740.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text740.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text740.setAttribute('x', valueXIncrease);
      text740.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text740.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text740.setAttribute('x', valueXIncrease);
      text740.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text740.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text740.setAttribute('y', valueXIncrease);
      text740.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text740.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text740.setAttribute('y', valueXIncrease);
      text740.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF740, 0.001);
const text741 = document.getElementById('text741');
text741.innerHTML = number[0];
function textF741() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text741.setAttribute('fill', newColor);
  const id = text741.x['animVal'][0].value+""+text741.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text741.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text741.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text741.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text741.setAttribute('x', valueXIncrease);
      text741.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text741.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text741.setAttribute('x', valueXIncrease);
      text741.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text741.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text741.setAttribute('y', valueXIncrease);
      text741.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text741.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text741.setAttribute('y', valueXIncrease);
      text741.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF741, 0.001);
const text742 = document.getElementById('text742');
text742.innerHTML = number[0];
function textF742() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text742.setAttribute('fill', newColor);
  const id = text742.x['animVal'][0].value+""+text742.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text742.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text742.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text742.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text742.setAttribute('x', valueXIncrease);
      text742.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text742.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text742.setAttribute('x', valueXIncrease);
      text742.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text742.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text742.setAttribute('y', valueXIncrease);
      text742.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text742.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text742.setAttribute('y', valueXIncrease);
      text742.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF742, 0.001);
const text743 = document.getElementById('text743');
text743.innerHTML = number[0];
function textF743() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text743.setAttribute('fill', newColor);
  const id = text743.x['animVal'][0].value+""+text743.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text743.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text743.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text743.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text743.setAttribute('x', valueXIncrease);
      text743.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text743.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text743.setAttribute('x', valueXIncrease);
      text743.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text743.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text743.setAttribute('y', valueXIncrease);
      text743.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text743.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text743.setAttribute('y', valueXIncrease);
      text743.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF743, 0.001);
const text744 = document.getElementById('text744');
text744.innerHTML = number[0];
function textF744() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text744.setAttribute('fill', newColor);
  const id = text744.x['animVal'][0].value+""+text744.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text744.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text744.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text744.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text744.setAttribute('x', valueXIncrease);
      text744.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text744.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text744.setAttribute('x', valueXIncrease);
      text744.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text744.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text744.setAttribute('y', valueXIncrease);
      text744.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text744.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text744.setAttribute('y', valueXIncrease);
      text744.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF744, 0.001);
const text745 = document.getElementById('text745');
text745.innerHTML = number[0];
function textF745() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text745.setAttribute('fill', newColor);
  const id = text745.x['animVal'][0].value+""+text745.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text745.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text745.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text745.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text745.setAttribute('x', valueXIncrease);
      text745.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text745.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text745.setAttribute('x', valueXIncrease);
      text745.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text745.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text745.setAttribute('y', valueXIncrease);
      text745.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text745.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text745.setAttribute('y', valueXIncrease);
      text745.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF745, 0.001);
const text746 = document.getElementById('text746');
text746.innerHTML = number[0];
function textF746() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text746.setAttribute('fill', newColor);
  const id = text746.x['animVal'][0].value+""+text746.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text746.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text746.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text746.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text746.setAttribute('x', valueXIncrease);
      text746.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text746.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text746.setAttribute('x', valueXIncrease);
      text746.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text746.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text746.setAttribute('y', valueXIncrease);
      text746.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text746.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text746.setAttribute('y', valueXIncrease);
      text746.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF746, 0.001);
const text747 = document.getElementById('text747');
text747.innerHTML = number[0];
function textF747() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text747.setAttribute('fill', newColor);
  const id = text747.x['animVal'][0].value+""+text747.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text747.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text747.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text747.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text747.setAttribute('x', valueXIncrease);
      text747.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text747.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text747.setAttribute('x', valueXIncrease);
      text747.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text747.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text747.setAttribute('y', valueXIncrease);
      text747.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text747.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text747.setAttribute('y', valueXIncrease);
      text747.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF747, 0.001);
const text748 = document.getElementById('text748');
text748.innerHTML = number[0];
function textF748() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text748.setAttribute('fill', newColor);
  const id = text748.x['animVal'][0].value+""+text748.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text748.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text748.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text748.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text748.setAttribute('x', valueXIncrease);
      text748.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text748.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text748.setAttribute('x', valueXIncrease);
      text748.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text748.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text748.setAttribute('y', valueXIncrease);
      text748.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text748.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text748.setAttribute('y', valueXIncrease);
      text748.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF748, 0.001);
const text749 = document.getElementById('text749');
text749.innerHTML = number[0];
function textF749() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text749.setAttribute('fill', newColor);
  const id = text749.x['animVal'][0].value+""+text749.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text749.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text749.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text749.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text749.setAttribute('x', valueXIncrease);
      text749.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text749.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text749.setAttribute('x', valueXIncrease);
      text749.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text749.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text749.setAttribute('y', valueXIncrease);
      text749.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text749.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text749.setAttribute('y', valueXIncrease);
      text749.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF749, 0.001);
const text750 = document.getElementById('text750');
text750.innerHTML = number[0];
function textF750() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text750.setAttribute('fill', newColor);
  const id = text750.x['animVal'][0].value+""+text750.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text750.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text750.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text750.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text750.setAttribute('x', valueXIncrease);
      text750.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text750.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text750.setAttribute('x', valueXIncrease);
      text750.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text750.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text750.setAttribute('y', valueXIncrease);
      text750.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text750.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text750.setAttribute('y', valueXIncrease);
      text750.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF750, 0.001);
const text751 = document.getElementById('text751');
text751.innerHTML = number[0];
function textF751() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text751.setAttribute('fill', newColor);
  const id = text751.x['animVal'][0].value+""+text751.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text751.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text751.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text751.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text751.setAttribute('x', valueXIncrease);
      text751.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text751.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text751.setAttribute('x', valueXIncrease);
      text751.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text751.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text751.setAttribute('y', valueXIncrease);
      text751.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text751.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text751.setAttribute('y', valueXIncrease);
      text751.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF751, 0.001);
const text752 = document.getElementById('text752');
text752.innerHTML = number[0];
function textF752() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text752.setAttribute('fill', newColor);
  const id = text752.x['animVal'][0].value+""+text752.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text752.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text752.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text752.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text752.setAttribute('x', valueXIncrease);
      text752.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text752.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text752.setAttribute('x', valueXIncrease);
      text752.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text752.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text752.setAttribute('y', valueXIncrease);
      text752.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text752.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text752.setAttribute('y', valueXIncrease);
      text752.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF752, 0.001);
const text753 = document.getElementById('text753');
text753.innerHTML = number[0];
function textF753() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text753.setAttribute('fill', newColor);
  const id = text753.x['animVal'][0].value+""+text753.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text753.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text753.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text753.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text753.setAttribute('x', valueXIncrease);
      text753.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text753.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text753.setAttribute('x', valueXIncrease);
      text753.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text753.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text753.setAttribute('y', valueXIncrease);
      text753.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text753.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text753.setAttribute('y', valueXIncrease);
      text753.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF753, 0.001);
const text754 = document.getElementById('text754');
text754.innerHTML = number[0];
function textF754() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text754.setAttribute('fill', newColor);
  const id = text754.x['animVal'][0].value+""+text754.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text754.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text754.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text754.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text754.setAttribute('x', valueXIncrease);
      text754.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text754.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text754.setAttribute('x', valueXIncrease);
      text754.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text754.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text754.setAttribute('y', valueXIncrease);
      text754.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text754.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text754.setAttribute('y', valueXIncrease);
      text754.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF754, 0.001);
const text755 = document.getElementById('text755');
text755.innerHTML = number[0];
function textF755() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text755.setAttribute('fill', newColor);
  const id = text755.x['animVal'][0].value+""+text755.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text755.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text755.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text755.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text755.setAttribute('x', valueXIncrease);
      text755.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text755.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text755.setAttribute('x', valueXIncrease);
      text755.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text755.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text755.setAttribute('y', valueXIncrease);
      text755.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text755.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text755.setAttribute('y', valueXIncrease);
      text755.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF755, 0.001);
const text756 = document.getElementById('text756');
text756.innerHTML = number[0];
function textF756() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text756.setAttribute('fill', newColor);
  const id = text756.x['animVal'][0].value+""+text756.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text756.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text756.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text756.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text756.setAttribute('x', valueXIncrease);
      text756.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text756.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text756.setAttribute('x', valueXIncrease);
      text756.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text756.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text756.setAttribute('y', valueXIncrease);
      text756.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text756.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text756.setAttribute('y', valueXIncrease);
      text756.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF756, 0.001);
const text757 = document.getElementById('text757');
text757.innerHTML = number[0];
function textF757() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text757.setAttribute('fill', newColor);
  const id = text757.x['animVal'][0].value+""+text757.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text757.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text757.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text757.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text757.setAttribute('x', valueXIncrease);
      text757.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text757.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text757.setAttribute('x', valueXIncrease);
      text757.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text757.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text757.setAttribute('y', valueXIncrease);
      text757.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text757.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text757.setAttribute('y', valueXIncrease);
      text757.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF757, 0.001);
const text758 = document.getElementById('text758');
text758.innerHTML = number[0];
function textF758() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text758.setAttribute('fill', newColor);
  const id = text758.x['animVal'][0].value+""+text758.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text758.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text758.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text758.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text758.setAttribute('x', valueXIncrease);
      text758.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text758.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text758.setAttribute('x', valueXIncrease);
      text758.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text758.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text758.setAttribute('y', valueXIncrease);
      text758.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text758.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text758.setAttribute('y', valueXIncrease);
      text758.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF758, 0.001);
const text759 = document.getElementById('text759');
text759.innerHTML = number[0];
function textF759() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text759.setAttribute('fill', newColor);
  const id = text759.x['animVal'][0].value+""+text759.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text759.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text759.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text759.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text759.setAttribute('x', valueXIncrease);
      text759.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text759.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text759.setAttribute('x', valueXIncrease);
      text759.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text759.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text759.setAttribute('y', valueXIncrease);
      text759.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text759.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text759.setAttribute('y', valueXIncrease);
      text759.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF759, 0.001);
const text760 = document.getElementById('text760');
text760.innerHTML = number[0];
function textF760() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text760.setAttribute('fill', newColor);
  const id = text760.x['animVal'][0].value+""+text760.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text760.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text760.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text760.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text760.setAttribute('x', valueXIncrease);
      text760.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text760.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text760.setAttribute('x', valueXIncrease);
      text760.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text760.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text760.setAttribute('y', valueXIncrease);
      text760.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text760.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text760.setAttribute('y', valueXIncrease);
      text760.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF760, 0.001);
const text761 = document.getElementById('text761');
text761.innerHTML = number[0];
function textF761() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text761.setAttribute('fill', newColor);
  const id = text761.x['animVal'][0].value+""+text761.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text761.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text761.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text761.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text761.setAttribute('x', valueXIncrease);
      text761.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text761.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text761.setAttribute('x', valueXIncrease);
      text761.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text761.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text761.setAttribute('y', valueXIncrease);
      text761.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text761.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text761.setAttribute('y', valueXIncrease);
      text761.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF761, 0.001);
const text762 = document.getElementById('text762');
text762.innerHTML = number[0];
function textF762() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text762.setAttribute('fill', newColor);
  const id = text762.x['animVal'][0].value+""+text762.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text762.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text762.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text762.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text762.setAttribute('x', valueXIncrease);
      text762.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text762.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text762.setAttribute('x', valueXIncrease);
      text762.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text762.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text762.setAttribute('y', valueXIncrease);
      text762.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text762.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text762.setAttribute('y', valueXIncrease);
      text762.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF762, 0.001);
const text763 = document.getElementById('text763');
text763.innerHTML = number[0];
function textF763() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text763.setAttribute('fill', newColor);
  const id = text763.x['animVal'][0].value+""+text763.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text763.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text763.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text763.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text763.setAttribute('x', valueXIncrease);
      text763.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text763.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text763.setAttribute('x', valueXIncrease);
      text763.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text763.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text763.setAttribute('y', valueXIncrease);
      text763.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text763.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text763.setAttribute('y', valueXIncrease);
      text763.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF763, 0.001);
const text764 = document.getElementById('text764');
text764.innerHTML = number[0];
function textF764() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text764.setAttribute('fill', newColor);
  const id = text764.x['animVal'][0].value+""+text764.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text764.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text764.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text764.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text764.setAttribute('x', valueXIncrease);
      text764.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text764.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text764.setAttribute('x', valueXIncrease);
      text764.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text764.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text764.setAttribute('y', valueXIncrease);
      text764.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text764.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text764.setAttribute('y', valueXIncrease);
      text764.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF764, 0.001);
const text765 = document.getElementById('text765');
text765.innerHTML = number[0];
function textF765() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text765.setAttribute('fill', newColor);
  const id = text765.x['animVal'][0].value+""+text765.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text765.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text765.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text765.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text765.setAttribute('x', valueXIncrease);
      text765.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text765.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text765.setAttribute('x', valueXIncrease);
      text765.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text765.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text765.setAttribute('y', valueXIncrease);
      text765.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text765.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text765.setAttribute('y', valueXIncrease);
      text765.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF765, 0.001);
const text766 = document.getElementById('text766');
text766.innerHTML = number[0];
function textF766() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text766.setAttribute('fill', newColor);
  const id = text766.x['animVal'][0].value+""+text766.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text766.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text766.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text766.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text766.setAttribute('x', valueXIncrease);
      text766.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text766.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text766.setAttribute('x', valueXIncrease);
      text766.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text766.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text766.setAttribute('y', valueXIncrease);
      text766.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text766.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text766.setAttribute('y', valueXIncrease);
      text766.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF766, 0.001);
const text767 = document.getElementById('text767');
text767.innerHTML = number[0];
function textF767() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text767.setAttribute('fill', newColor);
  const id = text767.x['animVal'][0].value+""+text767.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text767.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text767.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text767.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text767.setAttribute('x', valueXIncrease);
      text767.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text767.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text767.setAttribute('x', valueXIncrease);
      text767.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text767.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text767.setAttribute('y', valueXIncrease);
      text767.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text767.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text767.setAttribute('y', valueXIncrease);
      text767.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF767, 0.001);
const text768 = document.getElementById('text768');
text768.innerHTML = number[0];
function textF768() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text768.setAttribute('fill', newColor);
  const id = text768.x['animVal'][0].value+""+text768.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text768.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text768.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text768.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text768.setAttribute('x', valueXIncrease);
      text768.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text768.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text768.setAttribute('x', valueXIncrease);
      text768.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text768.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text768.setAttribute('y', valueXIncrease);
      text768.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text768.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text768.setAttribute('y', valueXIncrease);
      text768.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF768, 0.001);
const text769 = document.getElementById('text769');
text769.innerHTML = number[0];
function textF769() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text769.setAttribute('fill', newColor);
  const id = text769.x['animVal'][0].value+""+text769.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text769.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text769.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text769.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text769.setAttribute('x', valueXIncrease);
      text769.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text769.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text769.setAttribute('x', valueXIncrease);
      text769.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text769.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text769.setAttribute('y', valueXIncrease);
      text769.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text769.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text769.setAttribute('y', valueXIncrease);
      text769.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF769, 0.001);
const text770 = document.getElementById('text770');
text770.innerHTML = number[0];
function textF770() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text770.setAttribute('fill', newColor);
  const id = text770.x['animVal'][0].value+""+text770.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text770.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text770.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text770.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text770.setAttribute('x', valueXIncrease);
      text770.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text770.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text770.setAttribute('x', valueXIncrease);
      text770.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text770.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text770.setAttribute('y', valueXIncrease);
      text770.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text770.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text770.setAttribute('y', valueXIncrease);
      text770.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF770, 0.001);
const text771 = document.getElementById('text771');
text771.innerHTML = number[0];
function textF771() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text771.setAttribute('fill', newColor);
  const id = text771.x['animVal'][0].value+""+text771.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text771.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text771.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text771.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text771.setAttribute('x', valueXIncrease);
      text771.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text771.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text771.setAttribute('x', valueXIncrease);
      text771.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text771.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text771.setAttribute('y', valueXIncrease);
      text771.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text771.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text771.setAttribute('y', valueXIncrease);
      text771.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF771, 0.001);
const text772 = document.getElementById('text772');
text772.innerHTML = number[0];
function textF772() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text772.setAttribute('fill', newColor);
  const id = text772.x['animVal'][0].value+""+text772.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text772.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text772.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text772.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text772.setAttribute('x', valueXIncrease);
      text772.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text772.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text772.setAttribute('x', valueXIncrease);
      text772.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text772.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text772.setAttribute('y', valueXIncrease);
      text772.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text772.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text772.setAttribute('y', valueXIncrease);
      text772.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF772, 0.001);
const text773 = document.getElementById('text773');
text773.innerHTML = number[0];
function textF773() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text773.setAttribute('fill', newColor);
  const id = text773.x['animVal'][0].value+""+text773.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text773.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text773.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text773.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text773.setAttribute('x', valueXIncrease);
      text773.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text773.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text773.setAttribute('x', valueXIncrease);
      text773.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text773.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text773.setAttribute('y', valueXIncrease);
      text773.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text773.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text773.setAttribute('y', valueXIncrease);
      text773.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF773, 0.001);
const text774 = document.getElementById('text774');
text774.innerHTML = number[0];
function textF774() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text774.setAttribute('fill', newColor);
  const id = text774.x['animVal'][0].value+""+text774.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text774.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text774.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text774.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text774.setAttribute('x', valueXIncrease);
      text774.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text774.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text774.setAttribute('x', valueXIncrease);
      text774.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text774.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text774.setAttribute('y', valueXIncrease);
      text774.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text774.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text774.setAttribute('y', valueXIncrease);
      text774.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF774, 0.001);
const text775 = document.getElementById('text775');
text775.innerHTML = number[0];
function textF775() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text775.setAttribute('fill', newColor);
  const id = text775.x['animVal'][0].value+""+text775.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text775.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text775.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text775.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text775.setAttribute('x', valueXIncrease);
      text775.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text775.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text775.setAttribute('x', valueXIncrease);
      text775.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text775.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text775.setAttribute('y', valueXIncrease);
      text775.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text775.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text775.setAttribute('y', valueXIncrease);
      text775.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF775, 0.001);
const text776 = document.getElementById('text776');
text776.innerHTML = number[0];
function textF776() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text776.setAttribute('fill', newColor);
  const id = text776.x['animVal'][0].value+""+text776.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text776.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text776.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text776.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text776.setAttribute('x', valueXIncrease);
      text776.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text776.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text776.setAttribute('x', valueXIncrease);
      text776.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text776.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text776.setAttribute('y', valueXIncrease);
      text776.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text776.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text776.setAttribute('y', valueXIncrease);
      text776.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF776, 0.001);
const text777 = document.getElementById('text777');
text777.innerHTML = number[0];
function textF777() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text777.setAttribute('fill', newColor);
  const id = text777.x['animVal'][0].value+""+text777.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text777.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text777.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text777.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text777.setAttribute('x', valueXIncrease);
      text777.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text777.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text777.setAttribute('x', valueXIncrease);
      text777.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text777.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text777.setAttribute('y', valueXIncrease);
      text777.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text777.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text777.setAttribute('y', valueXIncrease);
      text777.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF777, 0.001);
const text778 = document.getElementById('text778');
text778.innerHTML = number[0];
function textF778() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text778.setAttribute('fill', newColor);
  const id = text778.x['animVal'][0].value+""+text778.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text778.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text778.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text778.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text778.setAttribute('x', valueXIncrease);
      text778.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text778.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text778.setAttribute('x', valueXIncrease);
      text778.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text778.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text778.setAttribute('y', valueXIncrease);
      text778.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text778.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text778.setAttribute('y', valueXIncrease);
      text778.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF778, 0.001);
const text779 = document.getElementById('text779');
text779.innerHTML = number[0];
function textF779() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text779.setAttribute('fill', newColor);
  const id = text779.x['animVal'][0].value+""+text779.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text779.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text779.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text779.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text779.setAttribute('x', valueXIncrease);
      text779.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text779.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text779.setAttribute('x', valueXIncrease);
      text779.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text779.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text779.setAttribute('y', valueXIncrease);
      text779.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text779.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text779.setAttribute('y', valueXIncrease);
      text779.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF779, 0.001);
const text780 = document.getElementById('text780');
text780.innerHTML = number[0];
function textF780() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text780.setAttribute('fill', newColor);
  const id = text780.x['animVal'][0].value+""+text780.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text780.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text780.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text780.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text780.setAttribute('x', valueXIncrease);
      text780.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text780.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text780.setAttribute('x', valueXIncrease);
      text780.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text780.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text780.setAttribute('y', valueXIncrease);
      text780.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text780.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text780.setAttribute('y', valueXIncrease);
      text780.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF780, 0.001);
const text781 = document.getElementById('text781');
text781.innerHTML = number[0];
function textF781() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text781.setAttribute('fill', newColor);
  const id = text781.x['animVal'][0].value+""+text781.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text781.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text781.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text781.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text781.setAttribute('x', valueXIncrease);
      text781.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text781.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text781.setAttribute('x', valueXIncrease);
      text781.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text781.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text781.setAttribute('y', valueXIncrease);
      text781.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text781.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text781.setAttribute('y', valueXIncrease);
      text781.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF781, 0.001);
const text782 = document.getElementById('text782');
text782.innerHTML = number[0];
function textF782() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text782.setAttribute('fill', newColor);
  const id = text782.x['animVal'][0].value+""+text782.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text782.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text782.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text782.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text782.setAttribute('x', valueXIncrease);
      text782.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text782.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text782.setAttribute('x', valueXIncrease);
      text782.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text782.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text782.setAttribute('y', valueXIncrease);
      text782.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text782.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text782.setAttribute('y', valueXIncrease);
      text782.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF782, 0.001);
const text783 = document.getElementById('text783');
text783.innerHTML = number[0];
function textF783() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text783.setAttribute('fill', newColor);
  const id = text783.x['animVal'][0].value+""+text783.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text783.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text783.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text783.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text783.setAttribute('x', valueXIncrease);
      text783.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text783.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text783.setAttribute('x', valueXIncrease);
      text783.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text783.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text783.setAttribute('y', valueXIncrease);
      text783.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text783.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text783.setAttribute('y', valueXIncrease);
      text783.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF783, 0.001);
const text784 = document.getElementById('text784');
text784.innerHTML = number[0];
function textF784() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text784.setAttribute('fill', newColor);
  const id = text784.x['animVal'][0].value+""+text784.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text784.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text784.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text784.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text784.setAttribute('x', valueXIncrease);
      text784.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text784.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text784.setAttribute('x', valueXIncrease);
      text784.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text784.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text784.setAttribute('y', valueXIncrease);
      text784.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text784.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text784.setAttribute('y', valueXIncrease);
      text784.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF784, 0.001);
const text785 = document.getElementById('text785');
text785.innerHTML = number[0];
function textF785() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text785.setAttribute('fill', newColor);
  const id = text785.x['animVal'][0].value+""+text785.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text785.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text785.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text785.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text785.setAttribute('x', valueXIncrease);
      text785.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text785.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text785.setAttribute('x', valueXIncrease);
      text785.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text785.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text785.setAttribute('y', valueXIncrease);
      text785.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text785.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text785.setAttribute('y', valueXIncrease);
      text785.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF785, 0.001);
const text786 = document.getElementById('text786');
text786.innerHTML = number[0];
function textF786() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text786.setAttribute('fill', newColor);
  const id = text786.x['animVal'][0].value+""+text786.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text786.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text786.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text786.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text786.setAttribute('x', valueXIncrease);
      text786.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text786.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text786.setAttribute('x', valueXIncrease);
      text786.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text786.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text786.setAttribute('y', valueXIncrease);
      text786.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text786.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text786.setAttribute('y', valueXIncrease);
      text786.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF786, 0.001);
const text787 = document.getElementById('text787');
text787.innerHTML = number[0];
function textF787() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text787.setAttribute('fill', newColor);
  const id = text787.x['animVal'][0].value+""+text787.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text787.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text787.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text787.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text787.setAttribute('x', valueXIncrease);
      text787.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text787.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text787.setAttribute('x', valueXIncrease);
      text787.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text787.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text787.setAttribute('y', valueXIncrease);
      text787.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text787.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text787.setAttribute('y', valueXIncrease);
      text787.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF787, 0.001);
const text788 = document.getElementById('text788');
text788.innerHTML = number[0];
function textF788() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text788.setAttribute('fill', newColor);
  const id = text788.x['animVal'][0].value+""+text788.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text788.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text788.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text788.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text788.setAttribute('x', valueXIncrease);
      text788.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text788.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text788.setAttribute('x', valueXIncrease);
      text788.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text788.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text788.setAttribute('y', valueXIncrease);
      text788.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text788.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text788.setAttribute('y', valueXIncrease);
      text788.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF788, 0.001);
const text789 = document.getElementById('text789');
text789.innerHTML = number[0];
function textF789() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text789.setAttribute('fill', newColor);
  const id = text789.x['animVal'][0].value+""+text789.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text789.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text789.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text789.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text789.setAttribute('x', valueXIncrease);
      text789.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text789.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text789.setAttribute('x', valueXIncrease);
      text789.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text789.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text789.setAttribute('y', valueXIncrease);
      text789.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text789.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text789.setAttribute('y', valueXIncrease);
      text789.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF789, 0.001);
const text790 = document.getElementById('text790');
text790.innerHTML = number[0];
function textF790() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text790.setAttribute('fill', newColor);
  const id = text790.x['animVal'][0].value+""+text790.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text790.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text790.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text790.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text790.setAttribute('x', valueXIncrease);
      text790.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text790.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text790.setAttribute('x', valueXIncrease);
      text790.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text790.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text790.setAttribute('y', valueXIncrease);
      text790.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text790.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text790.setAttribute('y', valueXIncrease);
      text790.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF790, 0.001);
const text791 = document.getElementById('text791');
text791.innerHTML = number[0];
function textF791() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text791.setAttribute('fill', newColor);
  const id = text791.x['animVal'][0].value+""+text791.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text791.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text791.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text791.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text791.setAttribute('x', valueXIncrease);
      text791.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text791.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text791.setAttribute('x', valueXIncrease);
      text791.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text791.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text791.setAttribute('y', valueXIncrease);
      text791.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text791.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text791.setAttribute('y', valueXIncrease);
      text791.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF791, 0.001);
const text792 = document.getElementById('text792');
text792.innerHTML = number[0];
function textF792() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text792.setAttribute('fill', newColor);
  const id = text792.x['animVal'][0].value+""+text792.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text792.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text792.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text792.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text792.setAttribute('x', valueXIncrease);
      text792.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text792.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text792.setAttribute('x', valueXIncrease);
      text792.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text792.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text792.setAttribute('y', valueXIncrease);
      text792.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text792.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text792.setAttribute('y', valueXIncrease);
      text792.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF792, 0.001);
const text793 = document.getElementById('text793');
text793.innerHTML = number[0];
function textF793() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text793.setAttribute('fill', newColor);
  const id = text793.x['animVal'][0].value+""+text793.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text793.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text793.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text793.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text793.setAttribute('x', valueXIncrease);
      text793.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text793.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text793.setAttribute('x', valueXIncrease);
      text793.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text793.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text793.setAttribute('y', valueXIncrease);
      text793.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text793.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text793.setAttribute('y', valueXIncrease);
      text793.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF793, 0.001);
const text794 = document.getElementById('text794');
text794.innerHTML = number[0];
function textF794() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text794.setAttribute('fill', newColor);
  const id = text794.x['animVal'][0].value+""+text794.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text794.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text794.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text794.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text794.setAttribute('x', valueXIncrease);
      text794.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text794.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text794.setAttribute('x', valueXIncrease);
      text794.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text794.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text794.setAttribute('y', valueXIncrease);
      text794.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text794.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text794.setAttribute('y', valueXIncrease);
      text794.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF794, 0.001);
const text795 = document.getElementById('text795');
text795.innerHTML = number[0];
function textF795() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text795.setAttribute('fill', newColor);
  const id = text795.x['animVal'][0].value+""+text795.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text795.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text795.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text795.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text795.setAttribute('x', valueXIncrease);
      text795.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text795.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text795.setAttribute('x', valueXIncrease);
      text795.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text795.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text795.setAttribute('y', valueXIncrease);
      text795.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text795.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text795.setAttribute('y', valueXIncrease);
      text795.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF795, 0.001);
const text796 = document.getElementById('text796');
text796.innerHTML = number[0];
function textF796() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text796.setAttribute('fill', newColor);
  const id = text796.x['animVal'][0].value+""+text796.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text796.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text796.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text796.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text796.setAttribute('x', valueXIncrease);
      text796.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text796.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text796.setAttribute('x', valueXIncrease);
      text796.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text796.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text796.setAttribute('y', valueXIncrease);
      text796.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text796.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text796.setAttribute('y', valueXIncrease);
      text796.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF796, 0.001);
const text797 = document.getElementById('text797');
text797.innerHTML = number[0];
function textF797() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text797.setAttribute('fill', newColor);
  const id = text797.x['animVal'][0].value+""+text797.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text797.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text797.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text797.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text797.setAttribute('x', valueXIncrease);
      text797.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text797.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text797.setAttribute('x', valueXIncrease);
      text797.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text797.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text797.setAttribute('y', valueXIncrease);
      text797.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text797.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text797.setAttribute('y', valueXIncrease);
      text797.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF797, 0.001);
const text798 = document.getElementById('text798');
text798.innerHTML = number[0];
function textF798() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text798.setAttribute('fill', newColor);
  const id = text798.x['animVal'][0].value+""+text798.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text798.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text798.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text798.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text798.setAttribute('x', valueXIncrease);
      text798.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text798.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text798.setAttribute('x', valueXIncrease);
      text798.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text798.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text798.setAttribute('y', valueXIncrease);
      text798.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text798.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text798.setAttribute('y', valueXIncrease);
      text798.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF798, 0.001);
const text799 = document.getElementById('text799');
text799.innerHTML = number[0];
function textF799() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text799.setAttribute('fill', newColor);
  const id = text799.x['animVal'][0].value+""+text799.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text799.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text799.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text799.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text799.setAttribute('x', valueXIncrease);
      text799.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text799.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text799.setAttribute('x', valueXIncrease);
      text799.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text799.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text799.setAttribute('y', valueXIncrease);
      text799.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text799.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text799.setAttribute('y', valueXIncrease);
      text799.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF799, 0.001);
const text800 = document.getElementById('text800');
text800.innerHTML = number[0];
function textF800() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text800.setAttribute('fill', newColor);
  const id = text800.x['animVal'][0].value+""+text800.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text800.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text800.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text800.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text800.setAttribute('x', valueXIncrease);
      text800.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text800.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text800.setAttribute('x', valueXIncrease);
      text800.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text800.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text800.setAttribute('y', valueXIncrease);
      text800.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text800.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text800.setAttribute('y', valueXIncrease);
      text800.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF800, 0.001);
const text801 = document.getElementById('text801');
text801.innerHTML = number[0];
function textF801() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text801.setAttribute('fill', newColor);
  const id = text801.x['animVal'][0].value+""+text801.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text801.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text801.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text801.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text801.setAttribute('x', valueXIncrease);
      text801.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text801.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text801.setAttribute('x', valueXIncrease);
      text801.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text801.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text801.setAttribute('y', valueXIncrease);
      text801.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text801.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text801.setAttribute('y', valueXIncrease);
      text801.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF801, 0.001);
const text802 = document.getElementById('text802');
text802.innerHTML = number[0];
function textF802() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text802.setAttribute('fill', newColor);
  const id = text802.x['animVal'][0].value+""+text802.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text802.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text802.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text802.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text802.setAttribute('x', valueXIncrease);
      text802.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text802.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text802.setAttribute('x', valueXIncrease);
      text802.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text802.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text802.setAttribute('y', valueXIncrease);
      text802.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text802.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text802.setAttribute('y', valueXIncrease);
      text802.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF802, 0.001);
const text803 = document.getElementById('text803');
text803.innerHTML = number[0];
function textF803() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text803.setAttribute('fill', newColor);
  const id = text803.x['animVal'][0].value+""+text803.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text803.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text803.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text803.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text803.setAttribute('x', valueXIncrease);
      text803.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text803.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text803.setAttribute('x', valueXIncrease);
      text803.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text803.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text803.setAttribute('y', valueXIncrease);
      text803.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text803.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text803.setAttribute('y', valueXIncrease);
      text803.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF803, 0.001);
const text804 = document.getElementById('text804');
text804.innerHTML = number[0];
function textF804() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text804.setAttribute('fill', newColor);
  const id = text804.x['animVal'][0].value+""+text804.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text804.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text804.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text804.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text804.setAttribute('x', valueXIncrease);
      text804.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text804.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text804.setAttribute('x', valueXIncrease);
      text804.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text804.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text804.setAttribute('y', valueXIncrease);
      text804.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text804.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text804.setAttribute('y', valueXIncrease);
      text804.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF804, 0.001);
const text805 = document.getElementById('text805');
text805.innerHTML = number[0];
function textF805() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text805.setAttribute('fill', newColor);
  const id = text805.x['animVal'][0].value+""+text805.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text805.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text805.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text805.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text805.setAttribute('x', valueXIncrease);
      text805.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text805.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text805.setAttribute('x', valueXIncrease);
      text805.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text805.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text805.setAttribute('y', valueXIncrease);
      text805.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text805.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text805.setAttribute('y', valueXIncrease);
      text805.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF805, 0.001);
const text806 = document.getElementById('text806');
text806.innerHTML = number[0];
function textF806() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text806.setAttribute('fill', newColor);
  const id = text806.x['animVal'][0].value+""+text806.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text806.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text806.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text806.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text806.setAttribute('x', valueXIncrease);
      text806.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text806.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text806.setAttribute('x', valueXIncrease);
      text806.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text806.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text806.setAttribute('y', valueXIncrease);
      text806.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text806.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text806.setAttribute('y', valueXIncrease);
      text806.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF806, 0.001);
const text807 = document.getElementById('text807');
text807.innerHTML = number[0];
function textF807() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text807.setAttribute('fill', newColor);
  const id = text807.x['animVal'][0].value+""+text807.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text807.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text807.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text807.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text807.setAttribute('x', valueXIncrease);
      text807.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text807.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text807.setAttribute('x', valueXIncrease);
      text807.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text807.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text807.setAttribute('y', valueXIncrease);
      text807.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text807.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text807.setAttribute('y', valueXIncrease);
      text807.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF807, 0.001);
const text808 = document.getElementById('text808');
text808.innerHTML = number[0];
function textF808() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text808.setAttribute('fill', newColor);
  const id = text808.x['animVal'][0].value+""+text808.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text808.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text808.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text808.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text808.setAttribute('x', valueXIncrease);
      text808.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text808.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text808.setAttribute('x', valueXIncrease);
      text808.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text808.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text808.setAttribute('y', valueXIncrease);
      text808.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text808.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text808.setAttribute('y', valueXIncrease);
      text808.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF808, 0.001);
const text809 = document.getElementById('text809');
text809.innerHTML = number[0];
function textF809() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text809.setAttribute('fill', newColor);
  const id = text809.x['animVal'][0].value+""+text809.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text809.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text809.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text809.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text809.setAttribute('x', valueXIncrease);
      text809.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text809.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text809.setAttribute('x', valueXIncrease);
      text809.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text809.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text809.setAttribute('y', valueXIncrease);
      text809.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text809.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text809.setAttribute('y', valueXIncrease);
      text809.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF809, 0.001);
const text810 = document.getElementById('text810');
text810.innerHTML = number[0];
function textF810() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text810.setAttribute('fill', newColor);
  const id = text810.x['animVal'][0].value+""+text810.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text810.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text810.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text810.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text810.setAttribute('x', valueXIncrease);
      text810.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text810.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text810.setAttribute('x', valueXIncrease);
      text810.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text810.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text810.setAttribute('y', valueXIncrease);
      text810.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text810.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text810.setAttribute('y', valueXIncrease);
      text810.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF810, 0.001);
const text811 = document.getElementById('text811');
text811.innerHTML = number[0];
function textF811() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text811.setAttribute('fill', newColor);
  const id = text811.x['animVal'][0].value+""+text811.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text811.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text811.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text811.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text811.setAttribute('x', valueXIncrease);
      text811.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text811.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text811.setAttribute('x', valueXIncrease);
      text811.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text811.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text811.setAttribute('y', valueXIncrease);
      text811.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text811.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text811.setAttribute('y', valueXIncrease);
      text811.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF811, 0.001);
const text812 = document.getElementById('text812');
text812.innerHTML = number[0];
function textF812() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text812.setAttribute('fill', newColor);
  const id = text812.x['animVal'][0].value+""+text812.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text812.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text812.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text812.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text812.setAttribute('x', valueXIncrease);
      text812.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text812.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text812.setAttribute('x', valueXIncrease);
      text812.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text812.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text812.setAttribute('y', valueXIncrease);
      text812.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text812.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text812.setAttribute('y', valueXIncrease);
      text812.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF812, 0.001);
const text813 = document.getElementById('text813');
text813.innerHTML = number[0];
function textF813() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text813.setAttribute('fill', newColor);
  const id = text813.x['animVal'][0].value+""+text813.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text813.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text813.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text813.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text813.setAttribute('x', valueXIncrease);
      text813.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text813.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text813.setAttribute('x', valueXIncrease);
      text813.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text813.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text813.setAttribute('y', valueXIncrease);
      text813.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text813.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text813.setAttribute('y', valueXIncrease);
      text813.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF813, 0.001);
const text814 = document.getElementById('text814');
text814.innerHTML = number[0];
function textF814() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text814.setAttribute('fill', newColor);
  const id = text814.x['animVal'][0].value+""+text814.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text814.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text814.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text814.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text814.setAttribute('x', valueXIncrease);
      text814.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text814.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text814.setAttribute('x', valueXIncrease);
      text814.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text814.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text814.setAttribute('y', valueXIncrease);
      text814.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text814.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text814.setAttribute('y', valueXIncrease);
      text814.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF814, 0.001);
const text815 = document.getElementById('text815');
text815.innerHTML = number[0];
function textF815() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text815.setAttribute('fill', newColor);
  const id = text815.x['animVal'][0].value+""+text815.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text815.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text815.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text815.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text815.setAttribute('x', valueXIncrease);
      text815.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text815.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text815.setAttribute('x', valueXIncrease);
      text815.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text815.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text815.setAttribute('y', valueXIncrease);
      text815.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text815.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text815.setAttribute('y', valueXIncrease);
      text815.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF815, 0.001);
const text816 = document.getElementById('text816');
text816.innerHTML = number[0];
function textF816() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text816.setAttribute('fill', newColor);
  const id = text816.x['animVal'][0].value+""+text816.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text816.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text816.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text816.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text816.setAttribute('x', valueXIncrease);
      text816.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text816.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text816.setAttribute('x', valueXIncrease);
      text816.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text816.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text816.setAttribute('y', valueXIncrease);
      text816.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text816.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text816.setAttribute('y', valueXIncrease);
      text816.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF816, 0.001);
const text817 = document.getElementById('text817');
text817.innerHTML = number[0];
function textF817() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text817.setAttribute('fill', newColor);
  const id = text817.x['animVal'][0].value+""+text817.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text817.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text817.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text817.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text817.setAttribute('x', valueXIncrease);
      text817.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text817.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text817.setAttribute('x', valueXIncrease);
      text817.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text817.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text817.setAttribute('y', valueXIncrease);
      text817.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text817.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text817.setAttribute('y', valueXIncrease);
      text817.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF817, 0.001);
const text818 = document.getElementById('text818');
text818.innerHTML = number[0];
function textF818() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text818.setAttribute('fill', newColor);
  const id = text818.x['animVal'][0].value+""+text818.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text818.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text818.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text818.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text818.setAttribute('x', valueXIncrease);
      text818.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text818.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text818.setAttribute('x', valueXIncrease);
      text818.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text818.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text818.setAttribute('y', valueXIncrease);
      text818.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text818.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text818.setAttribute('y', valueXIncrease);
      text818.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF818, 0.001);
const text819 = document.getElementById('text819');
text819.innerHTML = number[0];
function textF819() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text819.setAttribute('fill', newColor);
  const id = text819.x['animVal'][0].value+""+text819.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text819.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text819.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text819.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text819.setAttribute('x', valueXIncrease);
      text819.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text819.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text819.setAttribute('x', valueXIncrease);
      text819.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text819.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text819.setAttribute('y', valueXIncrease);
      text819.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text819.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text819.setAttribute('y', valueXIncrease);
      text819.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF819, 0.001);
const text820 = document.getElementById('text820');
text820.innerHTML = number[0];
function textF820() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text820.setAttribute('fill', newColor);
  const id = text820.x['animVal'][0].value+""+text820.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text820.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text820.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text820.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text820.setAttribute('x', valueXIncrease);
      text820.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text820.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text820.setAttribute('x', valueXIncrease);
      text820.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text820.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text820.setAttribute('y', valueXIncrease);
      text820.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text820.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text820.setAttribute('y', valueXIncrease);
      text820.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF820, 0.001);
const text821 = document.getElementById('text821');
text821.innerHTML = number[0];
function textF821() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text821.setAttribute('fill', newColor);
  const id = text821.x['animVal'][0].value+""+text821.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text821.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text821.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text821.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text821.setAttribute('x', valueXIncrease);
      text821.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text821.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text821.setAttribute('x', valueXIncrease);
      text821.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text821.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text821.setAttribute('y', valueXIncrease);
      text821.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text821.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text821.setAttribute('y', valueXIncrease);
      text821.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF821, 0.001);
const text822 = document.getElementById('text822');
text822.innerHTML = number[0];
function textF822() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text822.setAttribute('fill', newColor);
  const id = text822.x['animVal'][0].value+""+text822.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text822.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text822.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text822.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text822.setAttribute('x', valueXIncrease);
      text822.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text822.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text822.setAttribute('x', valueXIncrease);
      text822.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text822.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text822.setAttribute('y', valueXIncrease);
      text822.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text822.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text822.setAttribute('y', valueXIncrease);
      text822.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF822, 0.001);
const text823 = document.getElementById('text823');
text823.innerHTML = number[0];
function textF823() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text823.setAttribute('fill', newColor);
  const id = text823.x['animVal'][0].value+""+text823.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text823.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text823.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text823.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text823.setAttribute('x', valueXIncrease);
      text823.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text823.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text823.setAttribute('x', valueXIncrease);
      text823.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text823.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text823.setAttribute('y', valueXIncrease);
      text823.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text823.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text823.setAttribute('y', valueXIncrease);
      text823.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF823, 0.001);
const text824 = document.getElementById('text824');
text824.innerHTML = number[0];
function textF824() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text824.setAttribute('fill', newColor);
  const id = text824.x['animVal'][0].value+""+text824.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text824.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text824.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text824.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text824.setAttribute('x', valueXIncrease);
      text824.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text824.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text824.setAttribute('x', valueXIncrease);
      text824.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text824.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text824.setAttribute('y', valueXIncrease);
      text824.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text824.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text824.setAttribute('y', valueXIncrease);
      text824.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF824, 0.001);
const text825 = document.getElementById('text825');
text825.innerHTML = number[0];
function textF825() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text825.setAttribute('fill', newColor);
  const id = text825.x['animVal'][0].value+""+text825.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text825.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text825.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text825.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text825.setAttribute('x', valueXIncrease);
      text825.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text825.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text825.setAttribute('x', valueXIncrease);
      text825.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text825.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text825.setAttribute('y', valueXIncrease);
      text825.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text825.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text825.setAttribute('y', valueXIncrease);
      text825.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF825, 0.001);
const text826 = document.getElementById('text826');
text826.innerHTML = number[0];
function textF826() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text826.setAttribute('fill', newColor);
  const id = text826.x['animVal'][0].value+""+text826.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text826.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text826.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text826.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text826.setAttribute('x', valueXIncrease);
      text826.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text826.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text826.setAttribute('x', valueXIncrease);
      text826.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text826.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text826.setAttribute('y', valueXIncrease);
      text826.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text826.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text826.setAttribute('y', valueXIncrease);
      text826.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF826, 0.001);
const text827 = document.getElementById('text827');
text827.innerHTML = number[0];
function textF827() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text827.setAttribute('fill', newColor);
  const id = text827.x['animVal'][0].value+""+text827.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text827.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text827.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text827.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text827.setAttribute('x', valueXIncrease);
      text827.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text827.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text827.setAttribute('x', valueXIncrease);
      text827.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text827.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text827.setAttribute('y', valueXIncrease);
      text827.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text827.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text827.setAttribute('y', valueXIncrease);
      text827.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF827, 0.001);
const text828 = document.getElementById('text828');
text828.innerHTML = number[0];
function textF828() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text828.setAttribute('fill', newColor);
  const id = text828.x['animVal'][0].value+""+text828.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text828.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text828.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text828.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text828.setAttribute('x', valueXIncrease);
      text828.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text828.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text828.setAttribute('x', valueXIncrease);
      text828.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text828.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text828.setAttribute('y', valueXIncrease);
      text828.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text828.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text828.setAttribute('y', valueXIncrease);
      text828.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF828, 0.001);
const text829 = document.getElementById('text829');
text829.innerHTML = number[0];
function textF829() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text829.setAttribute('fill', newColor);
  const id = text829.x['animVal'][0].value+""+text829.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text829.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text829.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text829.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text829.setAttribute('x', valueXIncrease);
      text829.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text829.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text829.setAttribute('x', valueXIncrease);
      text829.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text829.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text829.setAttribute('y', valueXIncrease);
      text829.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text829.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text829.setAttribute('y', valueXIncrease);
      text829.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF829, 0.001);
const text830 = document.getElementById('text830');
text830.innerHTML = number[0];
function textF830() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text830.setAttribute('fill', newColor);
  const id = text830.x['animVal'][0].value+""+text830.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text830.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text830.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text830.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text830.setAttribute('x', valueXIncrease);
      text830.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text830.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text830.setAttribute('x', valueXIncrease);
      text830.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text830.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text830.setAttribute('y', valueXIncrease);
      text830.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text830.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text830.setAttribute('y', valueXIncrease);
      text830.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF830, 0.001);
const text831 = document.getElementById('text831');
text831.innerHTML = number[0];
function textF831() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text831.setAttribute('fill', newColor);
  const id = text831.x['animVal'][0].value+""+text831.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text831.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text831.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text831.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text831.setAttribute('x', valueXIncrease);
      text831.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text831.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text831.setAttribute('x', valueXIncrease);
      text831.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text831.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text831.setAttribute('y', valueXIncrease);
      text831.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text831.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text831.setAttribute('y', valueXIncrease);
      text831.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF831, 0.001);
const text832 = document.getElementById('text832');
text832.innerHTML = number[0];
function textF832() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text832.setAttribute('fill', newColor);
  const id = text832.x['animVal'][0].value+""+text832.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text832.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text832.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text832.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text832.setAttribute('x', valueXIncrease);
      text832.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text832.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text832.setAttribute('x', valueXIncrease);
      text832.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text832.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text832.setAttribute('y', valueXIncrease);
      text832.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text832.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text832.setAttribute('y', valueXIncrease);
      text832.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF832, 0.001);
const text833 = document.getElementById('text833');
text833.innerHTML = number[0];
function textF833() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text833.setAttribute('fill', newColor);
  const id = text833.x['animVal'][0].value+""+text833.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text833.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text833.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text833.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text833.setAttribute('x', valueXIncrease);
      text833.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text833.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text833.setAttribute('x', valueXIncrease);
      text833.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text833.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text833.setAttribute('y', valueXIncrease);
      text833.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text833.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text833.setAttribute('y', valueXIncrease);
      text833.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF833, 0.001);
const text834 = document.getElementById('text834');
text834.innerHTML = number[0];
function textF834() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text834.setAttribute('fill', newColor);
  const id = text834.x['animVal'][0].value+""+text834.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text834.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text834.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text834.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text834.setAttribute('x', valueXIncrease);
      text834.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text834.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text834.setAttribute('x', valueXIncrease);
      text834.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text834.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text834.setAttribute('y', valueXIncrease);
      text834.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text834.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text834.setAttribute('y', valueXIncrease);
      text834.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF834, 0.001);
const text835 = document.getElementById('text835');
text835.innerHTML = number[0];
function textF835() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text835.setAttribute('fill', newColor);
  const id = text835.x['animVal'][0].value+""+text835.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text835.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text835.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text835.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text835.setAttribute('x', valueXIncrease);
      text835.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text835.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text835.setAttribute('x', valueXIncrease);
      text835.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text835.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text835.setAttribute('y', valueXIncrease);
      text835.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text835.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text835.setAttribute('y', valueXIncrease);
      text835.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF835, 0.001);
const text836 = document.getElementById('text836');
text836.innerHTML = number[0];
function textF836() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text836.setAttribute('fill', newColor);
  const id = text836.x['animVal'][0].value+""+text836.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text836.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text836.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text836.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text836.setAttribute('x', valueXIncrease);
      text836.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text836.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text836.setAttribute('x', valueXIncrease);
      text836.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text836.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text836.setAttribute('y', valueXIncrease);
      text836.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text836.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text836.setAttribute('y', valueXIncrease);
      text836.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF836, 0.001);
const text837 = document.getElementById('text837');
text837.innerHTML = number[0];
function textF837() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text837.setAttribute('fill', newColor);
  const id = text837.x['animVal'][0].value+""+text837.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text837.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text837.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text837.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text837.setAttribute('x', valueXIncrease);
      text837.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text837.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text837.setAttribute('x', valueXIncrease);
      text837.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text837.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text837.setAttribute('y', valueXIncrease);
      text837.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text837.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text837.setAttribute('y', valueXIncrease);
      text837.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF837, 0.001);
const text838 = document.getElementById('text838');
text838.innerHTML = number[0];
function textF838() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text838.setAttribute('fill', newColor);
  const id = text838.x['animVal'][0].value+""+text838.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text838.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text838.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text838.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text838.setAttribute('x', valueXIncrease);
      text838.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text838.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text838.setAttribute('x', valueXIncrease);
      text838.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text838.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text838.setAttribute('y', valueXIncrease);
      text838.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text838.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text838.setAttribute('y', valueXIncrease);
      text838.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF838, 0.001);
const text839 = document.getElementById('text839');
text839.innerHTML = number[0];
function textF839() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text839.setAttribute('fill', newColor);
  const id = text839.x['animVal'][0].value+""+text839.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text839.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text839.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text839.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text839.setAttribute('x', valueXIncrease);
      text839.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text839.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text839.setAttribute('x', valueXIncrease);
      text839.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text839.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text839.setAttribute('y', valueXIncrease);
      text839.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text839.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text839.setAttribute('y', valueXIncrease);
      text839.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF839, 0.001);
const text840 = document.getElementById('text840');
text840.innerHTML = number[0];
function textF840() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text840.setAttribute('fill', newColor);
  const id = text840.x['animVal'][0].value+""+text840.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text840.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text840.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text840.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text840.setAttribute('x', valueXIncrease);
      text840.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text840.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text840.setAttribute('x', valueXIncrease);
      text840.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text840.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text840.setAttribute('y', valueXIncrease);
      text840.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text840.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text840.setAttribute('y', valueXIncrease);
      text840.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF840, 0.001);
const text841 = document.getElementById('text841');
text841.innerHTML = number[0];
function textF841() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text841.setAttribute('fill', newColor);
  const id = text841.x['animVal'][0].value+""+text841.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text841.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text841.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text841.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text841.setAttribute('x', valueXIncrease);
      text841.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text841.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text841.setAttribute('x', valueXIncrease);
      text841.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text841.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text841.setAttribute('y', valueXIncrease);
      text841.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text841.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text841.setAttribute('y', valueXIncrease);
      text841.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF841, 0.001);
const text842 = document.getElementById('text842');
text842.innerHTML = number[0];
function textF842() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text842.setAttribute('fill', newColor);
  const id = text842.x['animVal'][0].value+""+text842.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text842.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text842.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text842.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text842.setAttribute('x', valueXIncrease);
      text842.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text842.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text842.setAttribute('x', valueXIncrease);
      text842.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text842.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text842.setAttribute('y', valueXIncrease);
      text842.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text842.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text842.setAttribute('y', valueXIncrease);
      text842.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF842, 0.001);
const text843 = document.getElementById('text843');
text843.innerHTML = number[0];
function textF843() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text843.setAttribute('fill', newColor);
  const id = text843.x['animVal'][0].value+""+text843.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text843.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text843.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text843.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text843.setAttribute('x', valueXIncrease);
      text843.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text843.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text843.setAttribute('x', valueXIncrease);
      text843.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text843.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text843.setAttribute('y', valueXIncrease);
      text843.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text843.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text843.setAttribute('y', valueXIncrease);
      text843.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF843, 0.001);
const text844 = document.getElementById('text844');
text844.innerHTML = number[0];
function textF844() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text844.setAttribute('fill', newColor);
  const id = text844.x['animVal'][0].value+""+text844.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text844.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text844.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text844.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text844.setAttribute('x', valueXIncrease);
      text844.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text844.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text844.setAttribute('x', valueXIncrease);
      text844.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text844.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text844.setAttribute('y', valueXIncrease);
      text844.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text844.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text844.setAttribute('y', valueXIncrease);
      text844.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF844, 0.001);
const text845 = document.getElementById('text845');
text845.innerHTML = number[0];
function textF845() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text845.setAttribute('fill', newColor);
  const id = text845.x['animVal'][0].value+""+text845.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text845.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text845.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text845.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text845.setAttribute('x', valueXIncrease);
      text845.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text845.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text845.setAttribute('x', valueXIncrease);
      text845.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text845.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text845.setAttribute('y', valueXIncrease);
      text845.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text845.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text845.setAttribute('y', valueXIncrease);
      text845.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF845, 0.001);
const text846 = document.getElementById('text846');
text846.innerHTML = number[0];
function textF846() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text846.setAttribute('fill', newColor);
  const id = text846.x['animVal'][0].value+""+text846.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text846.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text846.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text846.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text846.setAttribute('x', valueXIncrease);
      text846.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text846.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text846.setAttribute('x', valueXIncrease);
      text846.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text846.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text846.setAttribute('y', valueXIncrease);
      text846.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text846.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text846.setAttribute('y', valueXIncrease);
      text846.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF846, 0.001);
const text847 = document.getElementById('text847');
text847.innerHTML = number[0];
function textF847() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text847.setAttribute('fill', newColor);
  const id = text847.x['animVal'][0].value+""+text847.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text847.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text847.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text847.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text847.setAttribute('x', valueXIncrease);
      text847.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text847.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text847.setAttribute('x', valueXIncrease);
      text847.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text847.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text847.setAttribute('y', valueXIncrease);
      text847.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text847.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text847.setAttribute('y', valueXIncrease);
      text847.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF847, 0.001);
const text848 = document.getElementById('text848');
text848.innerHTML = number[0];
function textF848() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text848.setAttribute('fill', newColor);
  const id = text848.x['animVal'][0].value+""+text848.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text848.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text848.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text848.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text848.setAttribute('x', valueXIncrease);
      text848.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text848.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text848.setAttribute('x', valueXIncrease);
      text848.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text848.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text848.setAttribute('y', valueXIncrease);
      text848.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text848.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text848.setAttribute('y', valueXIncrease);
      text848.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF848, 0.001);
const text849 = document.getElementById('text849');
text849.innerHTML = number[0];
function textF849() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text849.setAttribute('fill', newColor);
  const id = text849.x['animVal'][0].value+""+text849.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text849.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text849.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text849.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text849.setAttribute('x', valueXIncrease);
      text849.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text849.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text849.setAttribute('x', valueXIncrease);
      text849.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text849.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text849.setAttribute('y', valueXIncrease);
      text849.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text849.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text849.setAttribute('y', valueXIncrease);
      text849.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF849, 0.001);
const text850 = document.getElementById('text850');
text850.innerHTML = number[0];
function textF850() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text850.setAttribute('fill', newColor);
  const id = text850.x['animVal'][0].value+""+text850.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text850.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text850.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text850.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text850.setAttribute('x', valueXIncrease);
      text850.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text850.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text850.setAttribute('x', valueXIncrease);
      text850.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text850.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text850.setAttribute('y', valueXIncrease);
      text850.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text850.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text850.setAttribute('y', valueXIncrease);
      text850.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF850, 0.001);
const text851 = document.getElementById('text851');
text851.innerHTML = number[0];
function textF851() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text851.setAttribute('fill', newColor);
  const id = text851.x['animVal'][0].value+""+text851.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text851.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text851.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text851.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text851.setAttribute('x', valueXIncrease);
      text851.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text851.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text851.setAttribute('x', valueXIncrease);
      text851.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text851.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text851.setAttribute('y', valueXIncrease);
      text851.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text851.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text851.setAttribute('y', valueXIncrease);
      text851.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF851, 0.001);
const text852 = document.getElementById('text852');
text852.innerHTML = number[0];
function textF852() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text852.setAttribute('fill', newColor);
  const id = text852.x['animVal'][0].value+""+text852.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text852.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text852.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text852.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text852.setAttribute('x', valueXIncrease);
      text852.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text852.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text852.setAttribute('x', valueXIncrease);
      text852.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text852.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text852.setAttribute('y', valueXIncrease);
      text852.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text852.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text852.setAttribute('y', valueXIncrease);
      text852.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF852, 0.001);
const text853 = document.getElementById('text853');
text853.innerHTML = number[0];
function textF853() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text853.setAttribute('fill', newColor);
  const id = text853.x['animVal'][0].value+""+text853.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text853.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text853.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text853.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text853.setAttribute('x', valueXIncrease);
      text853.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text853.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text853.setAttribute('x', valueXIncrease);
      text853.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text853.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text853.setAttribute('y', valueXIncrease);
      text853.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text853.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text853.setAttribute('y', valueXIncrease);
      text853.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF853, 0.001);
const text854 = document.getElementById('text854');
text854.innerHTML = number[0];
function textF854() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text854.setAttribute('fill', newColor);
  const id = text854.x['animVal'][0].value+""+text854.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text854.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text854.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text854.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text854.setAttribute('x', valueXIncrease);
      text854.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text854.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text854.setAttribute('x', valueXIncrease);
      text854.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text854.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text854.setAttribute('y', valueXIncrease);
      text854.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text854.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text854.setAttribute('y', valueXIncrease);
      text854.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF854, 0.001);
const text855 = document.getElementById('text855');
text855.innerHTML = number[0];
function textF855() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text855.setAttribute('fill', newColor);
  const id = text855.x['animVal'][0].value+""+text855.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text855.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text855.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text855.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text855.setAttribute('x', valueXIncrease);
      text855.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text855.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text855.setAttribute('x', valueXIncrease);
      text855.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text855.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text855.setAttribute('y', valueXIncrease);
      text855.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text855.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text855.setAttribute('y', valueXIncrease);
      text855.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF855, 0.001);
const text856 = document.getElementById('text856');
text856.innerHTML = number[0];
function textF856() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text856.setAttribute('fill', newColor);
  const id = text856.x['animVal'][0].value+""+text856.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text856.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text856.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text856.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text856.setAttribute('x', valueXIncrease);
      text856.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text856.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text856.setAttribute('x', valueXIncrease);
      text856.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text856.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text856.setAttribute('y', valueXIncrease);
      text856.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text856.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text856.setAttribute('y', valueXIncrease);
      text856.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF856, 0.001);
const text857 = document.getElementById('text857');
text857.innerHTML = number[0];
function textF857() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text857.setAttribute('fill', newColor);
  const id = text857.x['animVal'][0].value+""+text857.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text857.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text857.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text857.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text857.setAttribute('x', valueXIncrease);
      text857.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text857.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text857.setAttribute('x', valueXIncrease);
      text857.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text857.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text857.setAttribute('y', valueXIncrease);
      text857.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text857.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text857.setAttribute('y', valueXIncrease);
      text857.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF857, 0.001);
const text858 = document.getElementById('text858');
text858.innerHTML = number[0];
function textF858() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text858.setAttribute('fill', newColor);
  const id = text858.x['animVal'][0].value+""+text858.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text858.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text858.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text858.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text858.setAttribute('x', valueXIncrease);
      text858.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text858.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text858.setAttribute('x', valueXIncrease);
      text858.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text858.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text858.setAttribute('y', valueXIncrease);
      text858.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text858.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text858.setAttribute('y', valueXIncrease);
      text858.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF858, 0.001);
const text859 = document.getElementById('text859');
text859.innerHTML = number[0];
function textF859() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text859.setAttribute('fill', newColor);
  const id = text859.x['animVal'][0].value+""+text859.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text859.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text859.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text859.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text859.setAttribute('x', valueXIncrease);
      text859.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text859.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text859.setAttribute('x', valueXIncrease);
      text859.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text859.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text859.setAttribute('y', valueXIncrease);
      text859.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text859.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text859.setAttribute('y', valueXIncrease);
      text859.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF859, 0.001);
const text860 = document.getElementById('text860');
text860.innerHTML = number[0];
function textF860() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text860.setAttribute('fill', newColor);
  const id = text860.x['animVal'][0].value+""+text860.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text860.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text860.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text860.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text860.setAttribute('x', valueXIncrease);
      text860.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text860.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text860.setAttribute('x', valueXIncrease);
      text860.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text860.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text860.setAttribute('y', valueXIncrease);
      text860.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text860.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text860.setAttribute('y', valueXIncrease);
      text860.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF860, 0.001);
const text861 = document.getElementById('text861');
text861.innerHTML = number[0];
function textF861() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text861.setAttribute('fill', newColor);
  const id = text861.x['animVal'][0].value+""+text861.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text861.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text861.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text861.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text861.setAttribute('x', valueXIncrease);
      text861.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text861.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text861.setAttribute('x', valueXIncrease);
      text861.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text861.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text861.setAttribute('y', valueXIncrease);
      text861.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text861.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text861.setAttribute('y', valueXIncrease);
      text861.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF861, 0.001);
const text862 = document.getElementById('text862');
text862.innerHTML = number[0];
function textF862() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text862.setAttribute('fill', newColor);
  const id = text862.x['animVal'][0].value+""+text862.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text862.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text862.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text862.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text862.setAttribute('x', valueXIncrease);
      text862.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text862.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text862.setAttribute('x', valueXIncrease);
      text862.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text862.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text862.setAttribute('y', valueXIncrease);
      text862.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text862.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text862.setAttribute('y', valueXIncrease);
      text862.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF862, 0.001);
const text863 = document.getElementById('text863');
text863.innerHTML = number[0];
function textF863() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text863.setAttribute('fill', newColor);
  const id = text863.x['animVal'][0].value+""+text863.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text863.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text863.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text863.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text863.setAttribute('x', valueXIncrease);
      text863.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text863.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text863.setAttribute('x', valueXIncrease);
      text863.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text863.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text863.setAttribute('y', valueXIncrease);
      text863.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text863.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text863.setAttribute('y', valueXIncrease);
      text863.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF863, 0.001);
const text864 = document.getElementById('text864');
text864.innerHTML = number[0];
function textF864() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text864.setAttribute('fill', newColor);
  const id = text864.x['animVal'][0].value+""+text864.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text864.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text864.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text864.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text864.setAttribute('x', valueXIncrease);
      text864.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text864.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text864.setAttribute('x', valueXIncrease);
      text864.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text864.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text864.setAttribute('y', valueXIncrease);
      text864.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text864.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text864.setAttribute('y', valueXIncrease);
      text864.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF864, 0.001);
const text865 = document.getElementById('text865');
text865.innerHTML = number[0];
function textF865() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text865.setAttribute('fill', newColor);
  const id = text865.x['animVal'][0].value+""+text865.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text865.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text865.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text865.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text865.setAttribute('x', valueXIncrease);
      text865.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text865.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text865.setAttribute('x', valueXIncrease);
      text865.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text865.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text865.setAttribute('y', valueXIncrease);
      text865.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text865.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text865.setAttribute('y', valueXIncrease);
      text865.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF865, 0.001);
const text866 = document.getElementById('text866');
text866.innerHTML = number[0];
function textF866() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text866.setAttribute('fill', newColor);
  const id = text866.x['animVal'][0].value+""+text866.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text866.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text866.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text866.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text866.setAttribute('x', valueXIncrease);
      text866.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text866.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text866.setAttribute('x', valueXIncrease);
      text866.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text866.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text866.setAttribute('y', valueXIncrease);
      text866.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text866.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text866.setAttribute('y', valueXIncrease);
      text866.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF866, 0.001);
const text867 = document.getElementById('text867');
text867.innerHTML = number[0];
function textF867() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text867.setAttribute('fill', newColor);
  const id = text867.x['animVal'][0].value+""+text867.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text867.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text867.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text867.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text867.setAttribute('x', valueXIncrease);
      text867.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text867.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text867.setAttribute('x', valueXIncrease);
      text867.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text867.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text867.setAttribute('y', valueXIncrease);
      text867.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text867.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text867.setAttribute('y', valueXIncrease);
      text867.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF867, 0.001);
const text868 = document.getElementById('text868');
text868.innerHTML = number[0];
function textF868() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text868.setAttribute('fill', newColor);
  const id = text868.x['animVal'][0].value+""+text868.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text868.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text868.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text868.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text868.setAttribute('x', valueXIncrease);
      text868.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text868.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text868.setAttribute('x', valueXIncrease);
      text868.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text868.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text868.setAttribute('y', valueXIncrease);
      text868.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text868.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text868.setAttribute('y', valueXIncrease);
      text868.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF868, 0.001);
const text869 = document.getElementById('text869');
text869.innerHTML = number[0];
function textF869() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text869.setAttribute('fill', newColor);
  const id = text869.x['animVal'][0].value+""+text869.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text869.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text869.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text869.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text869.setAttribute('x', valueXIncrease);
      text869.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text869.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text869.setAttribute('x', valueXIncrease);
      text869.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text869.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text869.setAttribute('y', valueXIncrease);
      text869.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text869.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text869.setAttribute('y', valueXIncrease);
      text869.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF869, 0.001);
const text870 = document.getElementById('text870');
text870.innerHTML = number[0];
function textF870() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text870.setAttribute('fill', newColor);
  const id = text870.x['animVal'][0].value+""+text870.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text870.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text870.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text870.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text870.setAttribute('x', valueXIncrease);
      text870.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text870.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text870.setAttribute('x', valueXIncrease);
      text870.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text870.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text870.setAttribute('y', valueXIncrease);
      text870.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text870.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text870.setAttribute('y', valueXIncrease);
      text870.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF870, 0.001);
const text871 = document.getElementById('text871');
text871.innerHTML = number[0];
function textF871() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text871.setAttribute('fill', newColor);
  const id = text871.x['animVal'][0].value+""+text871.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text871.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text871.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text871.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text871.setAttribute('x', valueXIncrease);
      text871.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text871.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text871.setAttribute('x', valueXIncrease);
      text871.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text871.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text871.setAttribute('y', valueXIncrease);
      text871.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text871.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text871.setAttribute('y', valueXIncrease);
      text871.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF871, 0.001);
const text872 = document.getElementById('text872');
text872.innerHTML = number[0];
function textF872() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text872.setAttribute('fill', newColor);
  const id = text872.x['animVal'][0].value+""+text872.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text872.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text872.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text872.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text872.setAttribute('x', valueXIncrease);
      text872.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text872.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text872.setAttribute('x', valueXIncrease);
      text872.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text872.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text872.setAttribute('y', valueXIncrease);
      text872.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text872.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text872.setAttribute('y', valueXIncrease);
      text872.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF872, 0.001);
const text873 = document.getElementById('text873');
text873.innerHTML = number[0];
function textF873() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text873.setAttribute('fill', newColor);
  const id = text873.x['animVal'][0].value+""+text873.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text873.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text873.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text873.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text873.setAttribute('x', valueXIncrease);
      text873.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text873.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text873.setAttribute('x', valueXIncrease);
      text873.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text873.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text873.setAttribute('y', valueXIncrease);
      text873.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text873.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text873.setAttribute('y', valueXIncrease);
      text873.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF873, 0.001);
const text874 = document.getElementById('text874');
text874.innerHTML = number[0];
function textF874() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text874.setAttribute('fill', newColor);
  const id = text874.x['animVal'][0].value+""+text874.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text874.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text874.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text874.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text874.setAttribute('x', valueXIncrease);
      text874.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text874.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text874.setAttribute('x', valueXIncrease);
      text874.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text874.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text874.setAttribute('y', valueXIncrease);
      text874.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text874.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text874.setAttribute('y', valueXIncrease);
      text874.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF874, 0.001);
const text875 = document.getElementById('text875');
text875.innerHTML = number[0];
function textF875() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text875.setAttribute('fill', newColor);
  const id = text875.x['animVal'][0].value+""+text875.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text875.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text875.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text875.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text875.setAttribute('x', valueXIncrease);
      text875.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text875.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text875.setAttribute('x', valueXIncrease);
      text875.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text875.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text875.setAttribute('y', valueXIncrease);
      text875.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text875.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text875.setAttribute('y', valueXIncrease);
      text875.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF875, 0.001);
const text876 = document.getElementById('text876');
text876.innerHTML = number[0];
function textF876() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text876.setAttribute('fill', newColor);
  const id = text876.x['animVal'][0].value+""+text876.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text876.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text876.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text876.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text876.setAttribute('x', valueXIncrease);
      text876.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text876.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text876.setAttribute('x', valueXIncrease);
      text876.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text876.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text876.setAttribute('y', valueXIncrease);
      text876.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text876.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text876.setAttribute('y', valueXIncrease);
      text876.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF876, 0.001);
const text877 = document.getElementById('text877');
text877.innerHTML = number[0];
function textF877() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text877.setAttribute('fill', newColor);
  const id = text877.x['animVal'][0].value+""+text877.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text877.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text877.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text877.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text877.setAttribute('x', valueXIncrease);
      text877.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text877.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text877.setAttribute('x', valueXIncrease);
      text877.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text877.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text877.setAttribute('y', valueXIncrease);
      text877.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text877.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text877.setAttribute('y', valueXIncrease);
      text877.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF877, 0.001);
const text878 = document.getElementById('text878');
text878.innerHTML = number[0];
function textF878() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text878.setAttribute('fill', newColor);
  const id = text878.x['animVal'][0].value+""+text878.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text878.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text878.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text878.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text878.setAttribute('x', valueXIncrease);
      text878.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text878.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text878.setAttribute('x', valueXIncrease);
      text878.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text878.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text878.setAttribute('y', valueXIncrease);
      text878.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text878.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text878.setAttribute('y', valueXIncrease);
      text878.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF878, 0.001);
const text879 = document.getElementById('text879');
text879.innerHTML = number[0];
function textF879() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text879.setAttribute('fill', newColor);
  const id = text879.x['animVal'][0].value+""+text879.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text879.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text879.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text879.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text879.setAttribute('x', valueXIncrease);
      text879.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text879.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text879.setAttribute('x', valueXIncrease);
      text879.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text879.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text879.setAttribute('y', valueXIncrease);
      text879.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text879.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text879.setAttribute('y', valueXIncrease);
      text879.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF879, 0.001);
const text880 = document.getElementById('text880');
text880.innerHTML = number[0];
function textF880() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text880.setAttribute('fill', newColor);
  const id = text880.x['animVal'][0].value+""+text880.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text880.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text880.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text880.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text880.setAttribute('x', valueXIncrease);
      text880.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text880.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text880.setAttribute('x', valueXIncrease);
      text880.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text880.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text880.setAttribute('y', valueXIncrease);
      text880.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text880.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text880.setAttribute('y', valueXIncrease);
      text880.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF880, 0.001);
const text881 = document.getElementById('text881');
text881.innerHTML = number[0];
function textF881() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text881.setAttribute('fill', newColor);
  const id = text881.x['animVal'][0].value+""+text881.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text881.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text881.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text881.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text881.setAttribute('x', valueXIncrease);
      text881.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text881.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text881.setAttribute('x', valueXIncrease);
      text881.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text881.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text881.setAttribute('y', valueXIncrease);
      text881.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text881.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text881.setAttribute('y', valueXIncrease);
      text881.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF881, 0.001);
const text882 = document.getElementById('text882');
text882.innerHTML = number[0];
function textF882() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text882.setAttribute('fill', newColor);
  const id = text882.x['animVal'][0].value+""+text882.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text882.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text882.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text882.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text882.setAttribute('x', valueXIncrease);
      text882.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text882.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text882.setAttribute('x', valueXIncrease);
      text882.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text882.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text882.setAttribute('y', valueXIncrease);
      text882.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text882.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text882.setAttribute('y', valueXIncrease);
      text882.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF882, 0.001);
const text883 = document.getElementById('text883');
text883.innerHTML = number[0];
function textF883() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text883.setAttribute('fill', newColor);
  const id = text883.x['animVal'][0].value+""+text883.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text883.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text883.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text883.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text883.setAttribute('x', valueXIncrease);
      text883.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text883.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text883.setAttribute('x', valueXIncrease);
      text883.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text883.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text883.setAttribute('y', valueXIncrease);
      text883.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text883.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text883.setAttribute('y', valueXIncrease);
      text883.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF883, 0.001);
const text884 = document.getElementById('text884');
text884.innerHTML = number[0];
function textF884() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text884.setAttribute('fill', newColor);
  const id = text884.x['animVal'][0].value+""+text884.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text884.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text884.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text884.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text884.setAttribute('x', valueXIncrease);
      text884.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text884.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text884.setAttribute('x', valueXIncrease);
      text884.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text884.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text884.setAttribute('y', valueXIncrease);
      text884.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text884.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text884.setAttribute('y', valueXIncrease);
      text884.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF884, 0.001);
const text885 = document.getElementById('text885');
text885.innerHTML = number[0];
function textF885() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text885.setAttribute('fill', newColor);
  const id = text885.x['animVal'][0].value+""+text885.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text885.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text885.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text885.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text885.setAttribute('x', valueXIncrease);
      text885.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text885.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text885.setAttribute('x', valueXIncrease);
      text885.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text885.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text885.setAttribute('y', valueXIncrease);
      text885.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text885.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text885.setAttribute('y', valueXIncrease);
      text885.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF885, 0.001);
const text886 = document.getElementById('text886');
text886.innerHTML = number[0];
function textF886() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text886.setAttribute('fill', newColor);
  const id = text886.x['animVal'][0].value+""+text886.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text886.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text886.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text886.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text886.setAttribute('x', valueXIncrease);
      text886.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text886.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text886.setAttribute('x', valueXIncrease);
      text886.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text886.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text886.setAttribute('y', valueXIncrease);
      text886.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text886.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text886.setAttribute('y', valueXIncrease);
      text886.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF886, 0.001);
const text887 = document.getElementById('text887');
text887.innerHTML = number[0];
function textF887() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text887.setAttribute('fill', newColor);
  const id = text887.x['animVal'][0].value+""+text887.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text887.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text887.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text887.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text887.setAttribute('x', valueXIncrease);
      text887.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text887.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text887.setAttribute('x', valueXIncrease);
      text887.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text887.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text887.setAttribute('y', valueXIncrease);
      text887.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text887.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text887.setAttribute('y', valueXIncrease);
      text887.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF887, 0.001);
const text888 = document.getElementById('text888');
text888.innerHTML = number[0];
function textF888() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text888.setAttribute('fill', newColor);
  const id = text888.x['animVal'][0].value+""+text888.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text888.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text888.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text888.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text888.setAttribute('x', valueXIncrease);
      text888.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text888.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text888.setAttribute('x', valueXIncrease);
      text888.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text888.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text888.setAttribute('y', valueXIncrease);
      text888.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text888.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text888.setAttribute('y', valueXIncrease);
      text888.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF888, 0.001);
const text889 = document.getElementById('text889');
text889.innerHTML = number[0];
function textF889() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text889.setAttribute('fill', newColor);
  const id = text889.x['animVal'][0].value+""+text889.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text889.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text889.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text889.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text889.setAttribute('x', valueXIncrease);
      text889.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text889.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text889.setAttribute('x', valueXIncrease);
      text889.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text889.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text889.setAttribute('y', valueXIncrease);
      text889.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text889.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text889.setAttribute('y', valueXIncrease);
      text889.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF889, 0.001);
const text890 = document.getElementById('text890');
text890.innerHTML = number[0];
function textF890() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text890.setAttribute('fill', newColor);
  const id = text890.x['animVal'][0].value+""+text890.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text890.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text890.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text890.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text890.setAttribute('x', valueXIncrease);
      text890.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text890.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text890.setAttribute('x', valueXIncrease);
      text890.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text890.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text890.setAttribute('y', valueXIncrease);
      text890.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text890.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text890.setAttribute('y', valueXIncrease);
      text890.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF890, 0.001);
const text891 = document.getElementById('text891');
text891.innerHTML = number[0];
function textF891() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text891.setAttribute('fill', newColor);
  const id = text891.x['animVal'][0].value+""+text891.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text891.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text891.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text891.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text891.setAttribute('x', valueXIncrease);
      text891.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text891.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text891.setAttribute('x', valueXIncrease);
      text891.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text891.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text891.setAttribute('y', valueXIncrease);
      text891.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text891.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text891.setAttribute('y', valueXIncrease);
      text891.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF891, 0.001);
const text892 = document.getElementById('text892');
text892.innerHTML = number[0];
function textF892() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text892.setAttribute('fill', newColor);
  const id = text892.x['animVal'][0].value+""+text892.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text892.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text892.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text892.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text892.setAttribute('x', valueXIncrease);
      text892.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text892.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text892.setAttribute('x', valueXIncrease);
      text892.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text892.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text892.setAttribute('y', valueXIncrease);
      text892.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text892.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text892.setAttribute('y', valueXIncrease);
      text892.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF892, 0.001);
const text893 = document.getElementById('text893');
text893.innerHTML = number[0];
function textF893() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text893.setAttribute('fill', newColor);
  const id = text893.x['animVal'][0].value+""+text893.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text893.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text893.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text893.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text893.setAttribute('x', valueXIncrease);
      text893.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text893.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text893.setAttribute('x', valueXIncrease);
      text893.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text893.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text893.setAttribute('y', valueXIncrease);
      text893.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text893.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text893.setAttribute('y', valueXIncrease);
      text893.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF893, 0.001);
const text894 = document.getElementById('text894');
text894.innerHTML = number[0];
function textF894() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text894.setAttribute('fill', newColor);
  const id = text894.x['animVal'][0].value+""+text894.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text894.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text894.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text894.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text894.setAttribute('x', valueXIncrease);
      text894.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text894.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text894.setAttribute('x', valueXIncrease);
      text894.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text894.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text894.setAttribute('y', valueXIncrease);
      text894.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text894.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text894.setAttribute('y', valueXIncrease);
      text894.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF894, 0.001);
const text895 = document.getElementById('text895');
text895.innerHTML = number[0];
function textF895() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text895.setAttribute('fill', newColor);
  const id = text895.x['animVal'][0].value+""+text895.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text895.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text895.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text895.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text895.setAttribute('x', valueXIncrease);
      text895.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text895.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text895.setAttribute('x', valueXIncrease);
      text895.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text895.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text895.setAttribute('y', valueXIncrease);
      text895.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text895.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text895.setAttribute('y', valueXIncrease);
      text895.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF895, 0.001);
const text896 = document.getElementById('text896');
text896.innerHTML = number[0];
function textF896() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text896.setAttribute('fill', newColor);
  const id = text896.x['animVal'][0].value+""+text896.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text896.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text896.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text896.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text896.setAttribute('x', valueXIncrease);
      text896.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text896.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text896.setAttribute('x', valueXIncrease);
      text896.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text896.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text896.setAttribute('y', valueXIncrease);
      text896.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text896.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text896.setAttribute('y', valueXIncrease);
      text896.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF896, 0.001);
const text897 = document.getElementById('text897');
text897.innerHTML = number[0];
function textF897() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text897.setAttribute('fill', newColor);
  const id = text897.x['animVal'][0].value+""+text897.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text897.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text897.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text897.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text897.setAttribute('x', valueXIncrease);
      text897.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text897.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text897.setAttribute('x', valueXIncrease);
      text897.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text897.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text897.setAttribute('y', valueXIncrease);
      text897.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text897.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text897.setAttribute('y', valueXIncrease);
      text897.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF897, 0.001);
const text898 = document.getElementById('text898');
text898.innerHTML = number[0];
function textF898() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text898.setAttribute('fill', newColor);
  const id = text898.x['animVal'][0].value+""+text898.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text898.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text898.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text898.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text898.setAttribute('x', valueXIncrease);
      text898.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text898.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text898.setAttribute('x', valueXIncrease);
      text898.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text898.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text898.setAttribute('y', valueXIncrease);
      text898.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text898.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text898.setAttribute('y', valueXIncrease);
      text898.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF898, 0.001);
const text899 = document.getElementById('text899');
text899.innerHTML = number[0];
function textF899() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text899.setAttribute('fill', newColor);
  const id = text899.x['animVal'][0].value+""+text899.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text899.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text899.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text899.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text899.setAttribute('x', valueXIncrease);
      text899.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text899.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text899.setAttribute('x', valueXIncrease);
      text899.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text899.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text899.setAttribute('y', valueXIncrease);
      text899.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text899.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text899.setAttribute('y', valueXIncrease);
      text899.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF899, 0.001);
const text900 = document.getElementById('text900');
text900.innerHTML = number[0];
function textF900() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text900.setAttribute('fill', newColor);
  const id = text900.x['animVal'][0].value+""+text900.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text900.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text900.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text900.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text900.setAttribute('x', valueXIncrease);
      text900.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text900.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text900.setAttribute('x', valueXIncrease);
      text900.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text900.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text900.setAttribute('y', valueXIncrease);
      text900.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text900.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text900.setAttribute('y', valueXIncrease);
      text900.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF900, 0.001);
const text901 = document.getElementById('text901');
text901.innerHTML = number[0];
function textF901() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text901.setAttribute('fill', newColor);
  const id = text901.x['animVal'][0].value+""+text901.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text901.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text901.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text901.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text901.setAttribute('x', valueXIncrease);
      text901.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text901.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text901.setAttribute('x', valueXIncrease);
      text901.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text901.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text901.setAttribute('y', valueXIncrease);
      text901.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text901.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text901.setAttribute('y', valueXIncrease);
      text901.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF901, 0.001);
const text902 = document.getElementById('text902');
text902.innerHTML = number[0];
function textF902() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text902.setAttribute('fill', newColor);
  const id = text902.x['animVal'][0].value+""+text902.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text902.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text902.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text902.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text902.setAttribute('x', valueXIncrease);
      text902.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text902.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text902.setAttribute('x', valueXIncrease);
      text902.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text902.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text902.setAttribute('y', valueXIncrease);
      text902.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text902.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text902.setAttribute('y', valueXIncrease);
      text902.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF902, 0.001);
const text903 = document.getElementById('text903');
text903.innerHTML = number[0];
function textF903() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text903.setAttribute('fill', newColor);
  const id = text903.x['animVal'][0].value+""+text903.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text903.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text903.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text903.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text903.setAttribute('x', valueXIncrease);
      text903.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text903.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text903.setAttribute('x', valueXIncrease);
      text903.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text903.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text903.setAttribute('y', valueXIncrease);
      text903.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text903.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text903.setAttribute('y', valueXIncrease);
      text903.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF903, 0.001);
const text904 = document.getElementById('text904');
text904.innerHTML = number[0];
function textF904() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text904.setAttribute('fill', newColor);
  const id = text904.x['animVal'][0].value+""+text904.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text904.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text904.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text904.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text904.setAttribute('x', valueXIncrease);
      text904.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text904.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text904.setAttribute('x', valueXIncrease);
      text904.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text904.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text904.setAttribute('y', valueXIncrease);
      text904.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text904.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text904.setAttribute('y', valueXIncrease);
      text904.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF904, 0.001);
const text905 = document.getElementById('text905');
text905.innerHTML = number[0];
function textF905() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text905.setAttribute('fill', newColor);
  const id = text905.x['animVal'][0].value+""+text905.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text905.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text905.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text905.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text905.setAttribute('x', valueXIncrease);
      text905.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text905.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text905.setAttribute('x', valueXIncrease);
      text905.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text905.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text905.setAttribute('y', valueXIncrease);
      text905.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text905.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text905.setAttribute('y', valueXIncrease);
      text905.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF905, 0.001);
const text906 = document.getElementById('text906');
text906.innerHTML = number[0];
function textF906() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text906.setAttribute('fill', newColor);
  const id = text906.x['animVal'][0].value+""+text906.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text906.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text906.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text906.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text906.setAttribute('x', valueXIncrease);
      text906.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text906.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text906.setAttribute('x', valueXIncrease);
      text906.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text906.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text906.setAttribute('y', valueXIncrease);
      text906.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text906.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text906.setAttribute('y', valueXIncrease);
      text906.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF906, 0.001);
const text907 = document.getElementById('text907');
text907.innerHTML = number[0];
function textF907() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text907.setAttribute('fill', newColor);
  const id = text907.x['animVal'][0].value+""+text907.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text907.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text907.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text907.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text907.setAttribute('x', valueXIncrease);
      text907.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text907.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text907.setAttribute('x', valueXIncrease);
      text907.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text907.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text907.setAttribute('y', valueXIncrease);
      text907.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text907.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text907.setAttribute('y', valueXIncrease);
      text907.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF907, 0.001);
const text908 = document.getElementById('text908');
text908.innerHTML = number[0];
function textF908() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text908.setAttribute('fill', newColor);
  const id = text908.x['animVal'][0].value+""+text908.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text908.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text908.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text908.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text908.setAttribute('x', valueXIncrease);
      text908.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text908.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text908.setAttribute('x', valueXIncrease);
      text908.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text908.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text908.setAttribute('y', valueXIncrease);
      text908.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text908.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text908.setAttribute('y', valueXIncrease);
      text908.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF908, 0.001);
const text909 = document.getElementById('text909');
text909.innerHTML = number[0];
function textF909() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text909.setAttribute('fill', newColor);
  const id = text909.x['animVal'][0].value+""+text909.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text909.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text909.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text909.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text909.setAttribute('x', valueXIncrease);
      text909.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text909.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text909.setAttribute('x', valueXIncrease);
      text909.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text909.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text909.setAttribute('y', valueXIncrease);
      text909.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text909.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text909.setAttribute('y', valueXIncrease);
      text909.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF909, 0.001);
const text910 = document.getElementById('text910');
text910.innerHTML = number[0];
function textF910() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text910.setAttribute('fill', newColor);
  const id = text910.x['animVal'][0].value+""+text910.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text910.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text910.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text910.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text910.setAttribute('x', valueXIncrease);
      text910.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text910.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text910.setAttribute('x', valueXIncrease);
      text910.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text910.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text910.setAttribute('y', valueXIncrease);
      text910.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text910.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text910.setAttribute('y', valueXIncrease);
      text910.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF910, 0.001);
const text911 = document.getElementById('text911');
text911.innerHTML = number[0];
function textF911() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text911.setAttribute('fill', newColor);
  const id = text911.x['animVal'][0].value+""+text911.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text911.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text911.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text911.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text911.setAttribute('x', valueXIncrease);
      text911.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text911.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text911.setAttribute('x', valueXIncrease);
      text911.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text911.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text911.setAttribute('y', valueXIncrease);
      text911.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text911.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text911.setAttribute('y', valueXIncrease);
      text911.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF911, 0.001);
const text912 = document.getElementById('text912');
text912.innerHTML = number[0];
function textF912() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text912.setAttribute('fill', newColor);
  const id = text912.x['animVal'][0].value+""+text912.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text912.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text912.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text912.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text912.setAttribute('x', valueXIncrease);
      text912.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text912.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text912.setAttribute('x', valueXIncrease);
      text912.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text912.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text912.setAttribute('y', valueXIncrease);
      text912.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text912.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text912.setAttribute('y', valueXIncrease);
      text912.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF912, 0.001);
const text913 = document.getElementById('text913');
text913.innerHTML = number[0];
function textF913() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text913.setAttribute('fill', newColor);
  const id = text913.x['animVal'][0].value+""+text913.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text913.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text913.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text913.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text913.setAttribute('x', valueXIncrease);
      text913.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text913.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text913.setAttribute('x', valueXIncrease);
      text913.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text913.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text913.setAttribute('y', valueXIncrease);
      text913.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text913.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text913.setAttribute('y', valueXIncrease);
      text913.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF913, 0.001);
const text914 = document.getElementById('text914');
text914.innerHTML = number[0];
function textF914() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text914.setAttribute('fill', newColor);
  const id = text914.x['animVal'][0].value+""+text914.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text914.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text914.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text914.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text914.setAttribute('x', valueXIncrease);
      text914.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text914.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text914.setAttribute('x', valueXIncrease);
      text914.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text914.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text914.setAttribute('y', valueXIncrease);
      text914.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text914.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text914.setAttribute('y', valueXIncrease);
      text914.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF914, 0.001);
const text915 = document.getElementById('text915');
text915.innerHTML = number[0];
function textF915() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text915.setAttribute('fill', newColor);
  const id = text915.x['animVal'][0].value+""+text915.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text915.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text915.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text915.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text915.setAttribute('x', valueXIncrease);
      text915.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text915.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text915.setAttribute('x', valueXIncrease);
      text915.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text915.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text915.setAttribute('y', valueXIncrease);
      text915.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text915.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text915.setAttribute('y', valueXIncrease);
      text915.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF915, 0.001);
const text916 = document.getElementById('text916');
text916.innerHTML = number[0];
function textF916() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text916.setAttribute('fill', newColor);
  const id = text916.x['animVal'][0].value+""+text916.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text916.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text916.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text916.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text916.setAttribute('x', valueXIncrease);
      text916.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text916.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text916.setAttribute('x', valueXIncrease);
      text916.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text916.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text916.setAttribute('y', valueXIncrease);
      text916.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text916.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text916.setAttribute('y', valueXIncrease);
      text916.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF916, 0.001);
const text917 = document.getElementById('text917');
text917.innerHTML = number[0];
function textF917() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text917.setAttribute('fill', newColor);
  const id = text917.x['animVal'][0].value+""+text917.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text917.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text917.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text917.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text917.setAttribute('x', valueXIncrease);
      text917.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text917.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text917.setAttribute('x', valueXIncrease);
      text917.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text917.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text917.setAttribute('y', valueXIncrease);
      text917.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text917.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text917.setAttribute('y', valueXIncrease);
      text917.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF917, 0.001);
const text918 = document.getElementById('text918');
text918.innerHTML = number[0];
function textF918() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text918.setAttribute('fill', newColor);
  const id = text918.x['animVal'][0].value+""+text918.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text918.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text918.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text918.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text918.setAttribute('x', valueXIncrease);
      text918.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text918.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text918.setAttribute('x', valueXIncrease);
      text918.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text918.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text918.setAttribute('y', valueXIncrease);
      text918.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text918.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text918.setAttribute('y', valueXIncrease);
      text918.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF918, 0.001);
const text919 = document.getElementById('text919');
text919.innerHTML = number[0];
function textF919() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text919.setAttribute('fill', newColor);
  const id = text919.x['animVal'][0].value+""+text919.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text919.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text919.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text919.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text919.setAttribute('x', valueXIncrease);
      text919.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text919.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text919.setAttribute('x', valueXIncrease);
      text919.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text919.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text919.setAttribute('y', valueXIncrease);
      text919.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text919.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text919.setAttribute('y', valueXIncrease);
      text919.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF919, 0.001);
const text920 = document.getElementById('text920');
text920.innerHTML = number[0];
function textF920() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text920.setAttribute('fill', newColor);
  const id = text920.x['animVal'][0].value+""+text920.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text920.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text920.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text920.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text920.setAttribute('x', valueXIncrease);
      text920.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text920.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text920.setAttribute('x', valueXIncrease);
      text920.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text920.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text920.setAttribute('y', valueXIncrease);
      text920.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text920.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text920.setAttribute('y', valueXIncrease);
      text920.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF920, 0.001);
const text921 = document.getElementById('text921');
text921.innerHTML = number[0];
function textF921() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text921.setAttribute('fill', newColor);
  const id = text921.x['animVal'][0].value+""+text921.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text921.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text921.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text921.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text921.setAttribute('x', valueXIncrease);
      text921.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text921.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text921.setAttribute('x', valueXIncrease);
      text921.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text921.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text921.setAttribute('y', valueXIncrease);
      text921.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text921.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text921.setAttribute('y', valueXIncrease);
      text921.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF921, 0.001);
const text922 = document.getElementById('text922');
text922.innerHTML = number[0];
function textF922() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text922.setAttribute('fill', newColor);
  const id = text922.x['animVal'][0].value+""+text922.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text922.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text922.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text922.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text922.setAttribute('x', valueXIncrease);
      text922.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text922.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text922.setAttribute('x', valueXIncrease);
      text922.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text922.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text922.setAttribute('y', valueXIncrease);
      text922.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text922.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text922.setAttribute('y', valueXIncrease);
      text922.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF922, 0.001);
const text923 = document.getElementById('text923');
text923.innerHTML = number[0];
function textF923() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text923.setAttribute('fill', newColor);
  const id = text923.x['animVal'][0].value+""+text923.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text923.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text923.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text923.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text923.setAttribute('x', valueXIncrease);
      text923.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text923.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text923.setAttribute('x', valueXIncrease);
      text923.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text923.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text923.setAttribute('y', valueXIncrease);
      text923.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text923.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text923.setAttribute('y', valueXIncrease);
      text923.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF923, 0.001);
const text924 = document.getElementById('text924');
text924.innerHTML = number[0];
function textF924() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text924.setAttribute('fill', newColor);
  const id = text924.x['animVal'][0].value+""+text924.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text924.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text924.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text924.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text924.setAttribute('x', valueXIncrease);
      text924.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text924.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text924.setAttribute('x', valueXIncrease);
      text924.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text924.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text924.setAttribute('y', valueXIncrease);
      text924.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text924.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text924.setAttribute('y', valueXIncrease);
      text924.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF924, 0.001);
const text925 = document.getElementById('text925');
text925.innerHTML = number[0];
function textF925() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text925.setAttribute('fill', newColor);
  const id = text925.x['animVal'][0].value+""+text925.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text925.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text925.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text925.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text925.setAttribute('x', valueXIncrease);
      text925.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text925.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text925.setAttribute('x', valueXIncrease);
      text925.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text925.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text925.setAttribute('y', valueXIncrease);
      text925.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text925.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text925.setAttribute('y', valueXIncrease);
      text925.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF925, 0.001);
const text926 = document.getElementById('text926');
text926.innerHTML = number[0];
function textF926() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text926.setAttribute('fill', newColor);
  const id = text926.x['animVal'][0].value+""+text926.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text926.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text926.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text926.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text926.setAttribute('x', valueXIncrease);
      text926.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text926.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text926.setAttribute('x', valueXIncrease);
      text926.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text926.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text926.setAttribute('y', valueXIncrease);
      text926.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text926.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text926.setAttribute('y', valueXIncrease);
      text926.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF926, 0.001);
const text927 = document.getElementById('text927');
text927.innerHTML = number[0];
function textF927() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text927.setAttribute('fill', newColor);
  const id = text927.x['animVal'][0].value+""+text927.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text927.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text927.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text927.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text927.setAttribute('x', valueXIncrease);
      text927.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text927.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text927.setAttribute('x', valueXIncrease);
      text927.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text927.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text927.setAttribute('y', valueXIncrease);
      text927.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text927.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text927.setAttribute('y', valueXIncrease);
      text927.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF927, 0.001);
const text928 = document.getElementById('text928');
text928.innerHTML = number[0];
function textF928() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text928.setAttribute('fill', newColor);
  const id = text928.x['animVal'][0].value+""+text928.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text928.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text928.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text928.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text928.setAttribute('x', valueXIncrease);
      text928.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text928.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text928.setAttribute('x', valueXIncrease);
      text928.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text928.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text928.setAttribute('y', valueXIncrease);
      text928.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text928.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text928.setAttribute('y', valueXIncrease);
      text928.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF928, 0.001);
const text929 = document.getElementById('text929');
text929.innerHTML = number[0];
function textF929() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text929.setAttribute('fill', newColor);
  const id = text929.x['animVal'][0].value+""+text929.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text929.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text929.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text929.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text929.setAttribute('x', valueXIncrease);
      text929.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text929.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text929.setAttribute('x', valueXIncrease);
      text929.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text929.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text929.setAttribute('y', valueXIncrease);
      text929.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text929.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text929.setAttribute('y', valueXIncrease);
      text929.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF929, 0.001);
const text930 = document.getElementById('text930');
text930.innerHTML = number[0];
function textF930() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text930.setAttribute('fill', newColor);
  const id = text930.x['animVal'][0].value+""+text930.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text930.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text930.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text930.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text930.setAttribute('x', valueXIncrease);
      text930.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text930.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text930.setAttribute('x', valueXIncrease);
      text930.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text930.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text930.setAttribute('y', valueXIncrease);
      text930.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text930.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text930.setAttribute('y', valueXIncrease);
      text930.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF930, 0.001);
const text931 = document.getElementById('text931');
text931.innerHTML = number[0];
function textF931() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text931.setAttribute('fill', newColor);
  const id = text931.x['animVal'][0].value+""+text931.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text931.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text931.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text931.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text931.setAttribute('x', valueXIncrease);
      text931.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text931.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text931.setAttribute('x', valueXIncrease);
      text931.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text931.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text931.setAttribute('y', valueXIncrease);
      text931.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text931.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text931.setAttribute('y', valueXIncrease);
      text931.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF931, 0.001);
const text932 = document.getElementById('text932');
text932.innerHTML = number[0];
function textF932() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text932.setAttribute('fill', newColor);
  const id = text932.x['animVal'][0].value+""+text932.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text932.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text932.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text932.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text932.setAttribute('x', valueXIncrease);
      text932.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text932.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text932.setAttribute('x', valueXIncrease);
      text932.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text932.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text932.setAttribute('y', valueXIncrease);
      text932.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text932.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text932.setAttribute('y', valueXIncrease);
      text932.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF932, 0.001);
const text933 = document.getElementById('text933');
text933.innerHTML = number[0];
function textF933() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text933.setAttribute('fill', newColor);
  const id = text933.x['animVal'][0].value+""+text933.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text933.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text933.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text933.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text933.setAttribute('x', valueXIncrease);
      text933.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text933.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text933.setAttribute('x', valueXIncrease);
      text933.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text933.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text933.setAttribute('y', valueXIncrease);
      text933.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text933.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text933.setAttribute('y', valueXIncrease);
      text933.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF933, 0.001);
const text934 = document.getElementById('text934');
text934.innerHTML = number[0];
function textF934() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text934.setAttribute('fill', newColor);
  const id = text934.x['animVal'][0].value+""+text934.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text934.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text934.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text934.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text934.setAttribute('x', valueXIncrease);
      text934.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text934.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text934.setAttribute('x', valueXIncrease);
      text934.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text934.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text934.setAttribute('y', valueXIncrease);
      text934.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text934.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text934.setAttribute('y', valueXIncrease);
      text934.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF934, 0.001);
const text935 = document.getElementById('text935');
text935.innerHTML = number[0];
function textF935() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text935.setAttribute('fill', newColor);
  const id = text935.x['animVal'][0].value+""+text935.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text935.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text935.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text935.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text935.setAttribute('x', valueXIncrease);
      text935.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text935.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text935.setAttribute('x', valueXIncrease);
      text935.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text935.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text935.setAttribute('y', valueXIncrease);
      text935.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text935.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text935.setAttribute('y', valueXIncrease);
      text935.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF935, 0.001);
const text936 = document.getElementById('text936');
text936.innerHTML = number[0];
function textF936() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text936.setAttribute('fill', newColor);
  const id = text936.x['animVal'][0].value+""+text936.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text936.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text936.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text936.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text936.setAttribute('x', valueXIncrease);
      text936.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text936.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text936.setAttribute('x', valueXIncrease);
      text936.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text936.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text936.setAttribute('y', valueXIncrease);
      text936.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text936.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text936.setAttribute('y', valueXIncrease);
      text936.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF936, 0.001);
const text937 = document.getElementById('text937');
text937.innerHTML = number[0];
function textF937() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text937.setAttribute('fill', newColor);
  const id = text937.x['animVal'][0].value+""+text937.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text937.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text937.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text937.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text937.setAttribute('x', valueXIncrease);
      text937.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text937.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text937.setAttribute('x', valueXIncrease);
      text937.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text937.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text937.setAttribute('y', valueXIncrease);
      text937.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text937.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text937.setAttribute('y', valueXIncrease);
      text937.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF937, 0.001);
const text938 = document.getElementById('text938');
text938.innerHTML = number[0];
function textF938() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text938.setAttribute('fill', newColor);
  const id = text938.x['animVal'][0].value+""+text938.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text938.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text938.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text938.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text938.setAttribute('x', valueXIncrease);
      text938.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text938.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text938.setAttribute('x', valueXIncrease);
      text938.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text938.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text938.setAttribute('y', valueXIncrease);
      text938.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text938.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text938.setAttribute('y', valueXIncrease);
      text938.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF938, 0.001);
const text939 = document.getElementById('text939');
text939.innerHTML = number[0];
function textF939() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text939.setAttribute('fill', newColor);
  const id = text939.x['animVal'][0].value+""+text939.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text939.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text939.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text939.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text939.setAttribute('x', valueXIncrease);
      text939.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text939.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text939.setAttribute('x', valueXIncrease);
      text939.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text939.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text939.setAttribute('y', valueXIncrease);
      text939.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text939.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text939.setAttribute('y', valueXIncrease);
      text939.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF939, 0.001);
const text940 = document.getElementById('text940');
text940.innerHTML = number[0];
function textF940() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text940.setAttribute('fill', newColor);
  const id = text940.x['animVal'][0].value+""+text940.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text940.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text940.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text940.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text940.setAttribute('x', valueXIncrease);
      text940.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text940.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text940.setAttribute('x', valueXIncrease);
      text940.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text940.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text940.setAttribute('y', valueXIncrease);
      text940.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text940.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text940.setAttribute('y', valueXIncrease);
      text940.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF940, 0.001);
const text941 = document.getElementById('text941');
text941.innerHTML = number[0];
function textF941() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text941.setAttribute('fill', newColor);
  const id = text941.x['animVal'][0].value+""+text941.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text941.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text941.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text941.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text941.setAttribute('x', valueXIncrease);
      text941.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text941.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text941.setAttribute('x', valueXIncrease);
      text941.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text941.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text941.setAttribute('y', valueXIncrease);
      text941.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text941.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text941.setAttribute('y', valueXIncrease);
      text941.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF941, 0.001);
const text942 = document.getElementById('text942');
text942.innerHTML = number[0];
function textF942() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text942.setAttribute('fill', newColor);
  const id = text942.x['animVal'][0].value+""+text942.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text942.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text942.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text942.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text942.setAttribute('x', valueXIncrease);
      text942.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text942.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text942.setAttribute('x', valueXIncrease);
      text942.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text942.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text942.setAttribute('y', valueXIncrease);
      text942.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text942.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text942.setAttribute('y', valueXIncrease);
      text942.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF942, 0.001);
const text943 = document.getElementById('text943');
text943.innerHTML = number[0];
function textF943() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text943.setAttribute('fill', newColor);
  const id = text943.x['animVal'][0].value+""+text943.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text943.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text943.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text943.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text943.setAttribute('x', valueXIncrease);
      text943.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text943.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text943.setAttribute('x', valueXIncrease);
      text943.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text943.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text943.setAttribute('y', valueXIncrease);
      text943.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text943.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text943.setAttribute('y', valueXIncrease);
      text943.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF943, 0.001);
const text944 = document.getElementById('text944');
text944.innerHTML = number[0];
function textF944() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text944.setAttribute('fill', newColor);
  const id = text944.x['animVal'][0].value+""+text944.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text944.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text944.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text944.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text944.setAttribute('x', valueXIncrease);
      text944.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text944.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text944.setAttribute('x', valueXIncrease);
      text944.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text944.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text944.setAttribute('y', valueXIncrease);
      text944.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text944.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text944.setAttribute('y', valueXIncrease);
      text944.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF944, 0.001);
const text945 = document.getElementById('text945');
text945.innerHTML = number[0];
function textF945() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text945.setAttribute('fill', newColor);
  const id = text945.x['animVal'][0].value+""+text945.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text945.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text945.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text945.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text945.setAttribute('x', valueXIncrease);
      text945.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text945.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text945.setAttribute('x', valueXIncrease);
      text945.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text945.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text945.setAttribute('y', valueXIncrease);
      text945.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text945.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text945.setAttribute('y', valueXIncrease);
      text945.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF945, 0.001);
const text946 = document.getElementById('text946');
text946.innerHTML = number[0];
function textF946() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text946.setAttribute('fill', newColor);
  const id = text946.x['animVal'][0].value+""+text946.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text946.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text946.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text946.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text946.setAttribute('x', valueXIncrease);
      text946.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text946.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text946.setAttribute('x', valueXIncrease);
      text946.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text946.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text946.setAttribute('y', valueXIncrease);
      text946.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text946.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text946.setAttribute('y', valueXIncrease);
      text946.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF946, 0.001);
const text947 = document.getElementById('text947');
text947.innerHTML = number[0];
function textF947() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text947.setAttribute('fill', newColor);
  const id = text947.x['animVal'][0].value+""+text947.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text947.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text947.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text947.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text947.setAttribute('x', valueXIncrease);
      text947.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text947.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text947.setAttribute('x', valueXIncrease);
      text947.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text947.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text947.setAttribute('y', valueXIncrease);
      text947.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text947.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text947.setAttribute('y', valueXIncrease);
      text947.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF947, 0.001);
const text948 = document.getElementById('text948');
text948.innerHTML = number[0];
function textF948() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text948.setAttribute('fill', newColor);
  const id = text948.x['animVal'][0].value+""+text948.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text948.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text948.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text948.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text948.setAttribute('x', valueXIncrease);
      text948.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text948.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text948.setAttribute('x', valueXIncrease);
      text948.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text948.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text948.setAttribute('y', valueXIncrease);
      text948.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text948.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text948.setAttribute('y', valueXIncrease);
      text948.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF948, 0.001);
const text949 = document.getElementById('text949');
text949.innerHTML = number[0];
function textF949() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text949.setAttribute('fill', newColor);
  const id = text949.x['animVal'][0].value+""+text949.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text949.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text949.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text949.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text949.setAttribute('x', valueXIncrease);
      text949.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text949.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text949.setAttribute('x', valueXIncrease);
      text949.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text949.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text949.setAttribute('y', valueXIncrease);
      text949.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text949.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text949.setAttribute('y', valueXIncrease);
      text949.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF949, 0.001);
const text950 = document.getElementById('text950');
text950.innerHTML = number[0];
function textF950() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text950.setAttribute('fill', newColor);
  const id = text950.x['animVal'][0].value+""+text950.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text950.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text950.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text950.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text950.setAttribute('x', valueXIncrease);
      text950.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text950.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text950.setAttribute('x', valueXIncrease);
      text950.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text950.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text950.setAttribute('y', valueXIncrease);
      text950.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text950.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text950.setAttribute('y', valueXIncrease);
      text950.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF950, 0.001);
const text951 = document.getElementById('text951');
text951.innerHTML = number[0];
function textF951() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text951.setAttribute('fill', newColor);
  const id = text951.x['animVal'][0].value+""+text951.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text951.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text951.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text951.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text951.setAttribute('x', valueXIncrease);
      text951.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text951.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text951.setAttribute('x', valueXIncrease);
      text951.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text951.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text951.setAttribute('y', valueXIncrease);
      text951.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text951.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text951.setAttribute('y', valueXIncrease);
      text951.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF951, 0.001);
const text952 = document.getElementById('text952');
text952.innerHTML = number[0];
function textF952() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text952.setAttribute('fill', newColor);
  const id = text952.x['animVal'][0].value+""+text952.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text952.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text952.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text952.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text952.setAttribute('x', valueXIncrease);
      text952.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text952.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text952.setAttribute('x', valueXIncrease);
      text952.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text952.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text952.setAttribute('y', valueXIncrease);
      text952.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text952.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text952.setAttribute('y', valueXIncrease);
      text952.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF952, 0.001);
const text953 = document.getElementById('text953');
text953.innerHTML = number[0];
function textF953() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text953.setAttribute('fill', newColor);
  const id = text953.x['animVal'][0].value+""+text953.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text953.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text953.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text953.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text953.setAttribute('x', valueXIncrease);
      text953.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text953.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text953.setAttribute('x', valueXIncrease);
      text953.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text953.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text953.setAttribute('y', valueXIncrease);
      text953.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text953.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text953.setAttribute('y', valueXIncrease);
      text953.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF953, 0.001);
const text954 = document.getElementById('text954');
text954.innerHTML = number[0];
function textF954() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text954.setAttribute('fill', newColor);
  const id = text954.x['animVal'][0].value+""+text954.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text954.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text954.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text954.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text954.setAttribute('x', valueXIncrease);
      text954.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text954.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text954.setAttribute('x', valueXIncrease);
      text954.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text954.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text954.setAttribute('y', valueXIncrease);
      text954.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text954.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text954.setAttribute('y', valueXIncrease);
      text954.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF954, 0.001);
const text955 = document.getElementById('text955');
text955.innerHTML = number[0];
function textF955() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text955.setAttribute('fill', newColor);
  const id = text955.x['animVal'][0].value+""+text955.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text955.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text955.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text955.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text955.setAttribute('x', valueXIncrease);
      text955.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text955.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text955.setAttribute('x', valueXIncrease);
      text955.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text955.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text955.setAttribute('y', valueXIncrease);
      text955.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text955.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text955.setAttribute('y', valueXIncrease);
      text955.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF955, 0.001);
const text956 = document.getElementById('text956');
text956.innerHTML = number[0];
function textF956() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text956.setAttribute('fill', newColor);
  const id = text956.x['animVal'][0].value+""+text956.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text956.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text956.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text956.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text956.setAttribute('x', valueXIncrease);
      text956.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text956.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text956.setAttribute('x', valueXIncrease);
      text956.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text956.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text956.setAttribute('y', valueXIncrease);
      text956.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text956.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text956.setAttribute('y', valueXIncrease);
      text956.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF956, 0.001);
const text957 = document.getElementById('text957');
text957.innerHTML = number[0];
function textF957() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text957.setAttribute('fill', newColor);
  const id = text957.x['animVal'][0].value+""+text957.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text957.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text957.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text957.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text957.setAttribute('x', valueXIncrease);
      text957.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text957.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text957.setAttribute('x', valueXIncrease);
      text957.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text957.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text957.setAttribute('y', valueXIncrease);
      text957.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text957.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text957.setAttribute('y', valueXIncrease);
      text957.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF957, 0.001);
const text958 = document.getElementById('text958');
text958.innerHTML = number[0];
function textF958() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text958.setAttribute('fill', newColor);
  const id = text958.x['animVal'][0].value+""+text958.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text958.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text958.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text958.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text958.setAttribute('x', valueXIncrease);
      text958.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text958.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text958.setAttribute('x', valueXIncrease);
      text958.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text958.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text958.setAttribute('y', valueXIncrease);
      text958.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text958.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text958.setAttribute('y', valueXIncrease);
      text958.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF958, 0.001);
const text959 = document.getElementById('text959');
text959.innerHTML = number[0];
function textF959() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text959.setAttribute('fill', newColor);
  const id = text959.x['animVal'][0].value+""+text959.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text959.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text959.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text959.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text959.setAttribute('x', valueXIncrease);
      text959.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text959.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text959.setAttribute('x', valueXIncrease);
      text959.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text959.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text959.setAttribute('y', valueXIncrease);
      text959.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text959.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text959.setAttribute('y', valueXIncrease);
      text959.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF959, 0.001);
const text960 = document.getElementById('text960');
text960.innerHTML = number[0];
function textF960() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text960.setAttribute('fill', newColor);
  const id = text960.x['animVal'][0].value+""+text960.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text960.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text960.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text960.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text960.setAttribute('x', valueXIncrease);
      text960.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text960.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text960.setAttribute('x', valueXIncrease);
      text960.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text960.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text960.setAttribute('y', valueXIncrease);
      text960.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text960.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text960.setAttribute('y', valueXIncrease);
      text960.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF960, 0.001);
const text961 = document.getElementById('text961');
text961.innerHTML = number[0];
function textF961() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text961.setAttribute('fill', newColor);
  const id = text961.x['animVal'][0].value+""+text961.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text961.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text961.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text961.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text961.setAttribute('x', valueXIncrease);
      text961.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text961.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text961.setAttribute('x', valueXIncrease);
      text961.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text961.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text961.setAttribute('y', valueXIncrease);
      text961.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text961.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text961.setAttribute('y', valueXIncrease);
      text961.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF961, 0.001);
const text962 = document.getElementById('text962');
text962.innerHTML = number[0];
function textF962() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text962.setAttribute('fill', newColor);
  const id = text962.x['animVal'][0].value+""+text962.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text962.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text962.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text962.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text962.setAttribute('x', valueXIncrease);
      text962.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text962.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text962.setAttribute('x', valueXIncrease);
      text962.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text962.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text962.setAttribute('y', valueXIncrease);
      text962.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text962.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text962.setAttribute('y', valueXIncrease);
      text962.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF962, 0.001);
const text963 = document.getElementById('text963');
text963.innerHTML = number[0];
function textF963() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text963.setAttribute('fill', newColor);
  const id = text963.x['animVal'][0].value+""+text963.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text963.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text963.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text963.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text963.setAttribute('x', valueXIncrease);
      text963.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text963.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text963.setAttribute('x', valueXIncrease);
      text963.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text963.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text963.setAttribute('y', valueXIncrease);
      text963.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text963.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text963.setAttribute('y', valueXIncrease);
      text963.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF963, 0.001);
const text964 = document.getElementById('text964');
text964.innerHTML = number[0];
function textF964() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text964.setAttribute('fill', newColor);
  const id = text964.x['animVal'][0].value+""+text964.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text964.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text964.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text964.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text964.setAttribute('x', valueXIncrease);
      text964.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text964.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text964.setAttribute('x', valueXIncrease);
      text964.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text964.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text964.setAttribute('y', valueXIncrease);
      text964.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text964.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text964.setAttribute('y', valueXIncrease);
      text964.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF964, 0.001);
const text965 = document.getElementById('text965');
text965.innerHTML = number[0];
function textF965() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text965.setAttribute('fill', newColor);
  const id = text965.x['animVal'][0].value+""+text965.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text965.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text965.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text965.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text965.setAttribute('x', valueXIncrease);
      text965.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text965.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text965.setAttribute('x', valueXIncrease);
      text965.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text965.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text965.setAttribute('y', valueXIncrease);
      text965.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text965.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text965.setAttribute('y', valueXIncrease);
      text965.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF965, 0.001);
const text966 = document.getElementById('text966');
text966.innerHTML = number[0];
function textF966() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text966.setAttribute('fill', newColor);
  const id = text966.x['animVal'][0].value+""+text966.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text966.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text966.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text966.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text966.setAttribute('x', valueXIncrease);
      text966.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text966.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text966.setAttribute('x', valueXIncrease);
      text966.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text966.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text966.setAttribute('y', valueXIncrease);
      text966.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text966.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text966.setAttribute('y', valueXIncrease);
      text966.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF966, 0.001);
const text967 = document.getElementById('text967');
text967.innerHTML = number[0];
function textF967() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text967.setAttribute('fill', newColor);
  const id = text967.x['animVal'][0].value+""+text967.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text967.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text967.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text967.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text967.setAttribute('x', valueXIncrease);
      text967.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text967.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text967.setAttribute('x', valueXIncrease);
      text967.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text967.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text967.setAttribute('y', valueXIncrease);
      text967.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text967.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text967.setAttribute('y', valueXIncrease);
      text967.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF967, 0.001);
const text968 = document.getElementById('text968');
text968.innerHTML = number[0];
function textF968() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text968.setAttribute('fill', newColor);
  const id = text968.x['animVal'][0].value+""+text968.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text968.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text968.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text968.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text968.setAttribute('x', valueXIncrease);
      text968.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text968.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text968.setAttribute('x', valueXIncrease);
      text968.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text968.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text968.setAttribute('y', valueXIncrease);
      text968.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text968.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text968.setAttribute('y', valueXIncrease);
      text968.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF968, 0.001);
const text969 = document.getElementById('text969');
text969.innerHTML = number[0];
function textF969() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text969.setAttribute('fill', newColor);
  const id = text969.x['animVal'][0].value+""+text969.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text969.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text969.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text969.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text969.setAttribute('x', valueXIncrease);
      text969.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text969.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text969.setAttribute('x', valueXIncrease);
      text969.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text969.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text969.setAttribute('y', valueXIncrease);
      text969.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text969.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text969.setAttribute('y', valueXIncrease);
      text969.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF969, 0.001);
const text970 = document.getElementById('text970');
text970.innerHTML = number[0];
function textF970() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text970.setAttribute('fill', newColor);
  const id = text970.x['animVal'][0].value+""+text970.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text970.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text970.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text970.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text970.setAttribute('x', valueXIncrease);
      text970.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text970.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text970.setAttribute('x', valueXIncrease);
      text970.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text970.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text970.setAttribute('y', valueXIncrease);
      text970.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text970.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text970.setAttribute('y', valueXIncrease);
      text970.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF970, 0.001);
const text971 = document.getElementById('text971');
text971.innerHTML = number[0];
function textF971() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text971.setAttribute('fill', newColor);
  const id = text971.x['animVal'][0].value+""+text971.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text971.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text971.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text971.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text971.setAttribute('x', valueXIncrease);
      text971.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text971.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text971.setAttribute('x', valueXIncrease);
      text971.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text971.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text971.setAttribute('y', valueXIncrease);
      text971.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text971.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text971.setAttribute('y', valueXIncrease);
      text971.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF971, 0.001);
const text972 = document.getElementById('text972');
text972.innerHTML = number[0];
function textF972() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text972.setAttribute('fill', newColor);
  const id = text972.x['animVal'][0].value+""+text972.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text972.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text972.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text972.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text972.setAttribute('x', valueXIncrease);
      text972.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text972.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text972.setAttribute('x', valueXIncrease);
      text972.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text972.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text972.setAttribute('y', valueXIncrease);
      text972.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text972.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text972.setAttribute('y', valueXIncrease);
      text972.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF972, 0.001);
const text973 = document.getElementById('text973');
text973.innerHTML = number[0];
function textF973() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text973.setAttribute('fill', newColor);
  const id = text973.x['animVal'][0].value+""+text973.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text973.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text973.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text973.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text973.setAttribute('x', valueXIncrease);
      text973.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text973.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text973.setAttribute('x', valueXIncrease);
      text973.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text973.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text973.setAttribute('y', valueXIncrease);
      text973.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text973.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text973.setAttribute('y', valueXIncrease);
      text973.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF973, 0.001);
const text974 = document.getElementById('text974');
text974.innerHTML = number[0];
function textF974() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text974.setAttribute('fill', newColor);
  const id = text974.x['animVal'][0].value+""+text974.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text974.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text974.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text974.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text974.setAttribute('x', valueXIncrease);
      text974.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text974.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text974.setAttribute('x', valueXIncrease);
      text974.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text974.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text974.setAttribute('y', valueXIncrease);
      text974.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text974.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text974.setAttribute('y', valueXIncrease);
      text974.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF974, 0.001);
const text975 = document.getElementById('text975');
text975.innerHTML = number[0];
function textF975() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text975.setAttribute('fill', newColor);
  const id = text975.x['animVal'][0].value+""+text975.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text975.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text975.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text975.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text975.setAttribute('x', valueXIncrease);
      text975.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text975.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text975.setAttribute('x', valueXIncrease);
      text975.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text975.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text975.setAttribute('y', valueXIncrease);
      text975.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text975.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text975.setAttribute('y', valueXIncrease);
      text975.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF975, 0.001);
const text976 = document.getElementById('text976');
text976.innerHTML = number[0];
function textF976() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text976.setAttribute('fill', newColor);
  const id = text976.x['animVal'][0].value+""+text976.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text976.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text976.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text976.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text976.setAttribute('x', valueXIncrease);
      text976.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text976.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text976.setAttribute('x', valueXIncrease);
      text976.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text976.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text976.setAttribute('y', valueXIncrease);
      text976.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text976.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text976.setAttribute('y', valueXIncrease);
      text976.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF976, 0.001);
const text977 = document.getElementById('text977');
text977.innerHTML = number[0];
function textF977() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text977.setAttribute('fill', newColor);
  const id = text977.x['animVal'][0].value+""+text977.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text977.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text977.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text977.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text977.setAttribute('x', valueXIncrease);
      text977.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text977.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text977.setAttribute('x', valueXIncrease);
      text977.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text977.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text977.setAttribute('y', valueXIncrease);
      text977.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text977.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text977.setAttribute('y', valueXIncrease);
      text977.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF977, 0.001);
const text978 = document.getElementById('text978');
text978.innerHTML = number[0];
function textF978() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text978.setAttribute('fill', newColor);
  const id = text978.x['animVal'][0].value+""+text978.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text978.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text978.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text978.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text978.setAttribute('x', valueXIncrease);
      text978.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text978.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text978.setAttribute('x', valueXIncrease);
      text978.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text978.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text978.setAttribute('y', valueXIncrease);
      text978.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text978.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text978.setAttribute('y', valueXIncrease);
      text978.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF978, 0.001);
const text979 = document.getElementById('text979');
text979.innerHTML = number[0];
function textF979() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text979.setAttribute('fill', newColor);
  const id = text979.x['animVal'][0].value+""+text979.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text979.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text979.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text979.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text979.setAttribute('x', valueXIncrease);
      text979.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text979.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text979.setAttribute('x', valueXIncrease);
      text979.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text979.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text979.setAttribute('y', valueXIncrease);
      text979.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text979.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text979.setAttribute('y', valueXIncrease);
      text979.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF979, 0.001);
const text980 = document.getElementById('text980');
text980.innerHTML = number[0];
function textF980() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text980.setAttribute('fill', newColor);
  const id = text980.x['animVal'][0].value+""+text980.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text980.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text980.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text980.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text980.setAttribute('x', valueXIncrease);
      text980.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text980.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text980.setAttribute('x', valueXIncrease);
      text980.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text980.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text980.setAttribute('y', valueXIncrease);
      text980.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text980.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text980.setAttribute('y', valueXIncrease);
      text980.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF980, 0.001);
const text981 = document.getElementById('text981');
text981.innerHTML = number[0];
function textF981() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text981.setAttribute('fill', newColor);
  const id = text981.x['animVal'][0].value+""+text981.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text981.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text981.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text981.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text981.setAttribute('x', valueXIncrease);
      text981.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text981.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text981.setAttribute('x', valueXIncrease);
      text981.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text981.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text981.setAttribute('y', valueXIncrease);
      text981.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text981.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text981.setAttribute('y', valueXIncrease);
      text981.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF981, 0.001);
const text982 = document.getElementById('text982');
text982.innerHTML = number[0];
function textF982() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text982.setAttribute('fill', newColor);
  const id = text982.x['animVal'][0].value+""+text982.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text982.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text982.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text982.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text982.setAttribute('x', valueXIncrease);
      text982.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text982.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text982.setAttribute('x', valueXIncrease);
      text982.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text982.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text982.setAttribute('y', valueXIncrease);
      text982.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text982.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text982.setAttribute('y', valueXIncrease);
      text982.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF982, 0.001);
const text983 = document.getElementById('text983');
text983.innerHTML = number[0];
function textF983() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text983.setAttribute('fill', newColor);
  const id = text983.x['animVal'][0].value+""+text983.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text983.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text983.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text983.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text983.setAttribute('x', valueXIncrease);
      text983.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text983.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text983.setAttribute('x', valueXIncrease);
      text983.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text983.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text983.setAttribute('y', valueXIncrease);
      text983.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text983.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text983.setAttribute('y', valueXIncrease);
      text983.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF983, 0.001);
const text984 = document.getElementById('text984');
text984.innerHTML = number[0];
function textF984() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text984.setAttribute('fill', newColor);
  const id = text984.x['animVal'][0].value+""+text984.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text984.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text984.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text984.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text984.setAttribute('x', valueXIncrease);
      text984.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text984.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text984.setAttribute('x', valueXIncrease);
      text984.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text984.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text984.setAttribute('y', valueXIncrease);
      text984.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text984.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text984.setAttribute('y', valueXIncrease);
      text984.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF984, 0.001);
const text985 = document.getElementById('text985');
text985.innerHTML = number[0];
function textF985() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text985.setAttribute('fill', newColor);
  const id = text985.x['animVal'][0].value+""+text985.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text985.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text985.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text985.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text985.setAttribute('x', valueXIncrease);
      text985.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text985.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text985.setAttribute('x', valueXIncrease);
      text985.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text985.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text985.setAttribute('y', valueXIncrease);
      text985.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text985.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text985.setAttribute('y', valueXIncrease);
      text985.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF985, 0.001);
const text986 = document.getElementById('text986');
text986.innerHTML = number[0];
function textF986() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text986.setAttribute('fill', newColor);
  const id = text986.x['animVal'][0].value+""+text986.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text986.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text986.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text986.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text986.setAttribute('x', valueXIncrease);
      text986.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text986.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text986.setAttribute('x', valueXIncrease);
      text986.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text986.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text986.setAttribute('y', valueXIncrease);
      text986.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text986.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text986.setAttribute('y', valueXIncrease);
      text986.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF986, 0.001);
const text987 = document.getElementById('text987');
text987.innerHTML = number[0];
function textF987() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text987.setAttribute('fill', newColor);
  const id = text987.x['animVal'][0].value+""+text987.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text987.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text987.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text987.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text987.setAttribute('x', valueXIncrease);
      text987.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text987.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text987.setAttribute('x', valueXIncrease);
      text987.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text987.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text987.setAttribute('y', valueXIncrease);
      text987.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text987.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text987.setAttribute('y', valueXIncrease);
      text987.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF987, 0.001);
const text988 = document.getElementById('text988');
text988.innerHTML = number[0];
function textF988() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text988.setAttribute('fill', newColor);
  const id = text988.x['animVal'][0].value+""+text988.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text988.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text988.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text988.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text988.setAttribute('x', valueXIncrease);
      text988.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text988.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text988.setAttribute('x', valueXIncrease);
      text988.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text988.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text988.setAttribute('y', valueXIncrease);
      text988.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text988.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text988.setAttribute('y', valueXIncrease);
      text988.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF988, 0.001);
const text989 = document.getElementById('text989');
text989.innerHTML = number[0];
function textF989() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text989.setAttribute('fill', newColor);
  const id = text989.x['animVal'][0].value+""+text989.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text989.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text989.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text989.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text989.setAttribute('x', valueXIncrease);
      text989.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text989.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text989.setAttribute('x', valueXIncrease);
      text989.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text989.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text989.setAttribute('y', valueXIncrease);
      text989.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text989.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text989.setAttribute('y', valueXIncrease);
      text989.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF989, 0.001);
const text990 = document.getElementById('text990');
text990.innerHTML = number[0];
function textF990() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text990.setAttribute('fill', newColor);
  const id = text990.x['animVal'][0].value+""+text990.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text990.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text990.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text990.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text990.setAttribute('x', valueXIncrease);
      text990.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text990.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text990.setAttribute('x', valueXIncrease);
      text990.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text990.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text990.setAttribute('y', valueXIncrease);
      text990.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text990.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text990.setAttribute('y', valueXIncrease);
      text990.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF990, 0.001);
const text991 = document.getElementById('text991');
text991.innerHTML = number[0];
function textF991() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text991.setAttribute('fill', newColor);
  const id = text991.x['animVal'][0].value+""+text991.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text991.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text991.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text991.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text991.setAttribute('x', valueXIncrease);
      text991.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text991.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text991.setAttribute('x', valueXIncrease);
      text991.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text991.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text991.setAttribute('y', valueXIncrease);
      text991.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text991.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text991.setAttribute('y', valueXIncrease);
      text991.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF991, 0.001);
const text992 = document.getElementById('text992');
text992.innerHTML = number[0];
function textF992() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text992.setAttribute('fill', newColor);
  const id = text992.x['animVal'][0].value+""+text992.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text992.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text992.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text992.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text992.setAttribute('x', valueXIncrease);
      text992.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text992.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text992.setAttribute('x', valueXIncrease);
      text992.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text992.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text992.setAttribute('y', valueXIncrease);
      text992.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text992.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text992.setAttribute('y', valueXIncrease);
      text992.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF992, 0.001);
const text993 = document.getElementById('text993');
text993.innerHTML = number[0];
function textF993() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text993.setAttribute('fill', newColor);
  const id = text993.x['animVal'][0].value+""+text993.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text993.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text993.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text993.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text993.setAttribute('x', valueXIncrease);
      text993.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text993.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text993.setAttribute('x', valueXIncrease);
      text993.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text993.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text993.setAttribute('y', valueXIncrease);
      text993.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text993.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text993.setAttribute('y', valueXIncrease);
      text993.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF993, 0.001);
const text994 = document.getElementById('text994');
text994.innerHTML = number[0];
function textF994() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text994.setAttribute('fill', newColor);
  const id = text994.x['animVal'][0].value+""+text994.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text994.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text994.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text994.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text994.setAttribute('x', valueXIncrease);
      text994.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text994.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text994.setAttribute('x', valueXIncrease);
      text994.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text994.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text994.setAttribute('y', valueXIncrease);
      text994.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text994.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text994.setAttribute('y', valueXIncrease);
      text994.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF994, 0.001);
const text995 = document.getElementById('text995');
text995.innerHTML = number[0];
function textF995() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text995.setAttribute('fill', newColor);
  const id = text995.x['animVal'][0].value+""+text995.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text995.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text995.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text995.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text995.setAttribute('x', valueXIncrease);
      text995.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text995.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text995.setAttribute('x', valueXIncrease);
      text995.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text995.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text995.setAttribute('y', valueXIncrease);
      text995.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text995.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text995.setAttribute('y', valueXIncrease);
      text995.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF995, 0.001);
const text996 = document.getElementById('text996');
text996.innerHTML = number[0];
function textF996() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text996.setAttribute('fill', newColor);
  const id = text996.x['animVal'][0].value+""+text996.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text996.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text996.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text996.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text996.setAttribute('x', valueXIncrease);
      text996.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text996.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text996.setAttribute('x', valueXIncrease);
      text996.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text996.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text996.setAttribute('y', valueXIncrease);
      text996.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text996.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text996.setAttribute('y', valueXIncrease);
      text996.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF996, 0.001);
const text997 = document.getElementById('text997');
text997.innerHTML = number[0];
function textF997() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text997.setAttribute('fill', newColor);
  const id = text997.x['animVal'][0].value+""+text997.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text997.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text997.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text997.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text997.setAttribute('x', valueXIncrease);
      text997.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text997.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text997.setAttribute('x', valueXIncrease);
      text997.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text997.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text997.setAttribute('y', valueXIncrease);
      text997.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text997.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text997.setAttribute('y', valueXIncrease);
      text997.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF997, 0.001);
const text998 = document.getElementById('text998');
text998.innerHTML = number[0];
function textF998() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text998.setAttribute('fill', newColor);
  const id = text998.x['animVal'][0].value+""+text998.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text998.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text998.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text998.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text998.setAttribute('x', valueXIncrease);
      text998.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text998.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text998.setAttribute('x', valueXIncrease);
      text998.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text998.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text998.setAttribute('y', valueXIncrease);
      text998.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text998.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text998.setAttribute('y', valueXIncrease);
      text998.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF998, 0.001);
const text999 = document.getElementById('text999');
text999.innerHTML = number[0];
function textF999() {
const svg = document.getElementById('center');
  let a = Math.random() < 0.5;
  let b = Math.random() > 0.5;
  const newColor = color[Math.floor((Math.random() * 10) % 5)];
  text999.setAttribute('fill', newColor);
  const id = text999.x['animVal'][0].value+""+text999.y['animVal'][0].value;
  if (!location1.get(id)) {
    const shadow = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    shadow.setAttribute('x', text999.x['animVal'][0].value);
    shadow.setAttribute('id', id);
    shadow.setAttribute('y', text999.y['animVal'][0].value);
    shadow.setAttribute('style', 'font-size:10px');
    shadow.setAttribute('fill', newColor);
    shadow.append(pointer);
    document.getElementById('center').append(shadow);
    location1.set(id, true);
  }else{
  svg.removeChild(document.getElementById(id));
            location1.delete(id);
  }
  if (a) {
    if (b) {
      let valueXIncrease = text999.x['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.width['animVal'].value)){
      text999.setAttribute('x', valueXIncrease);
      text999.innerHTML = number[0];
      }

    } else {
      let valueXIncrease = text999.x['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text999.setAttribute('x', valueXIncrease);
      text999.innerHTML = number[1];
      }

    }
  } else {
    if (b) {
      let valueXIncrease = text999.y['animVal'][0].value + 5;
      if (!(valueXIncrease >= svg.height['animVal'].value)){
      text999.setAttribute('y', valueXIncrease);
      text999.innerHTML = number[2];
      }

    } else {
      let valueXIncrease = text999.y['animVal'][0].value - 5;
      if (!(valueXIncrease <= 0)){
      text999.setAttribute('y', valueXIncrease);
      text999.innerHTML = number[3];
      }

    }
  }
}
setInterval(textF999, 0.001);
